Treat this as three separable stages so bugs in one don't hide in the next: (1) schema design — normalize the flat CSV into users/products/transactions by deduplicating on natural keys (UserID, ProductID) as you stream rows, inserting each user/product only once and always inserting one transactions row per CSV line with correct foreign keys; (2) indexing — after loading, add indexes that match your query's WHERE/JOIN columns (region on users, txn_date on transactions, and the join columns user_id/product_id) so the region+date filter and the joins aren't full table scans; (3) reporting — write the SQL aggregation (`SUM(price*quantity)` grouped by category, filtered to EU region and the Q3 2023 date range using string comparison since dates are stored as YYYY-MM-DD text, which sorts correctly) with `ORDER BY total DESC LIMIT 3`, then format each revenue value to exactly 2 decimal places when writing the CSV. Common pitfalls: using `TxnDate` as a float/int comparison instead of string range on ISO dates, forgetting the date range is inclusive on both ends, and letting Go's default float formatting produce inconsistent decimal places instead of explicit `%.2f`. Initialize the Go module and actually run the program so `sales.db` and `report.csv` exist on disk.