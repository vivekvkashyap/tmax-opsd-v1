Break this into independent, testable pieces before writing the TCP service. First, write and unit-test a standalone WAV parser: skip the fixed 44-byte header, read raw bytes as int16_t samples (watch endianness and pointer casting), then compute mean of squares as a double and the zero-crossing rate by comparing sign(sample[i]) vs sign(sample[i+1]) using >=0 vs <0 as the sign split. Second, parse the CSV into arrays of energy and zcr floats. For bootstrap CI: call srand(42) once, then for each of 10,000 iterations draw N samples with replacement using rand() % N (index each resample the same way the reference likely does — pick indices sequentially per iteration), compute the resample mean, store it, then sort all 10,000 means and take indices for the 2.5th/97.5th percentile (careful with 0-based vs 1-based indexing off-by-one). Third, build the TCP server with a plain blocking socket()/bind()/listen()/accept() loop, read until it sees "GET_REPORT\n", then snprintf the JSON with the exact field order and decimal precision (%.2f for energy/CI, %.4f for zcr) shown in the spec, write it, and close only that connection while looping to accept more. Test the whole pipeline manually with `nc` before considering it done.