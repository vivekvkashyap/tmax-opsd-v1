Recognize the bug class: JWTs use Base64URL encoding, which differs from standard Base64 in its alphabet (`-`/`_` instead of `+`/`/`) and typically strips padding. Before calling the existing `base64_decode` in `decode.cpp`, add a conversion step on the extracted payload segment: replace every `-` with `+`, every `_` with `/`, then pad the string with trailing `=` characters until its length is a multiple of 4 (length % 4 tells you how many `=` to append: 2, 3, or 0 extra chars needed for remainders of 2, 3, 0 respectively — remainder 1 is invalid). Compile with `g++ /home/user/decode.cpp -o /home/user/decode`, then run it against `/home/user/traffic.log` and inspect the printed decoded JSON payloads to find the one with a suspicious role/algorithm and an embedded shell command that appends to `authorized_keys`. From that decoded JSON's `cmd` field, isolate just the SSH public key material — the token starting at `ssh-rsa ` through the trailing comment — stripping any surrounding quotes, echo/mkdir shell syntax, or escape characters, and write only that exact key string to `/home/user/rogue.pub`.
