Implement the pipeline as discrete, order-sensitive pandas/numpy steps rather than one-shot cleaning, since the checker recomputes each stage exactly the same way. (1) `df.ffill().bfill()` — apply in that order, not the reverse, or the first-row NaN won't fill correctly. (2) Compute z-scores using `(df - df.mean()) / df.std(ddof=1)` (sample std, not population) per column, take `.abs() > 3.0`, flag any row where this holds for at least one column via `.any(axis=1)`, and capture the original 0-indexed row positions of flagged rows before dropping them — use the filled DataFrame's index, not a re-numbered one after dropping. (3) For correlations, use `.corr().abs()` on the cleaned data, only inspect the upper triangle (i<j) to avoid duplicate/self pairs, keep pairs with abs corr >= 0.90, sort each pair's two names alphabetically then sort the list of pairs. (4) Cast cleaned values to `float64`, use `np.cov(values, rowvar=False, ddof=1)` (variables as columns) and `np.linalg.det`, rounding to 4 decimals only at the very end for JSON output — don't round intermediate values. Write the JSON with exactly the three specified keys.
