Work bottom-up. First understand `/app/anonymizer` by probing it: run it with a sample email as `argv[1]` and observe stdout; run it with no args to see if it errors; confirm it's deterministic (same input -> same hash) by calling it twice — this is a black-box CLI hashing oracle, so your pipeline should shell out to it per email (or once per unique email, caching results, since repeated invocation is likely slow). For timestamp normalization, detect each line's format with a small set of `strptime` patterns (try `%b %d %H:%M:%S %Y`, `%Y/%m/%d %H:%M:%S`, etc. in order, catching parse failures) and reformat to `YYYY-MM-DDTHH:MM:SSZ` via `strftime`. Extract the JSON payload substring with a regex anchored on `payload=` through end of line (validate it parses with `json.loads`, don't just slice blindly). Anonymize each `user=<EMAIL>` by piping the email into the anonymizer and substituting the hash. Persist the cleaned rows sorted by ISO timestamp per anonymized-email group (e.g. into a lookup file/dir keyed by hash) so the serving script can fetch quickly. For `/home/user/serve_cleaned.sh`, use a `nc -lk` or `socat` loop on 127.0.0.1:8333 that parses the `FETCH <hash>\n` request line and streams back matching `TIMESTAMP | ANONYMIZED_EMAIL | JSON_STRING` lines in chronological order, looping to accept further connections; test it with a manual `nc` client round-trip before backgrounding it.