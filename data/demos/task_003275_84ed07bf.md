Diagnose before fixing: grep `/home/user/nginx/logs/error.log` for the specific 502 upstream-connect-failure lines (not every log line), extract just the timestamp fields with `awk`/`cut`, and take the last 5 in the order they appear in the file (chronological), writing them exactly as formatted in the log to `/home/user/incident_report.txt`. Compare the socket path currently referenced by `proxy_pass` in `nginx.conf` against the actual path the Python backend creates (`/home/user/app/run/backend.sock`) — the mismatch is the root cause — and edit the config so `proxy_pass` points at the real path, preserving the rest of the file. Write `/home/user/deploy.sh` to be safely re-runnable: create the backup directory if missing, copy (overwrite) the fixed config to the backup path, and re-verify the proxy_pass path programmatically (e.g. grep) rather than assuming it stays correct. For log rotation, implement the shift order carefully to avoid clobbering: move `.2`→`.3` before `.1`→`.2` before current→`.1` (i.e. process from the highest numbered file downward), then create a fresh empty `app.log`. Make the script executable and actually run it once, then inspect the resulting files to confirm the shift and backup worked as intended.
