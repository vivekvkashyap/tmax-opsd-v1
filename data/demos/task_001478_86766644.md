Compile the C file into a shared object with position-independent code, e.g. `gcc -shared -fPIC -o /home/user/libintegrator.so /home/user/integrator.c`, then load it from your optimization script via ctypes (or your chosen language's FFI), declaring the correct argument/return types (two doubles in, one double out) so values aren't truncated to int. Before optimizing, run a small convergence sweep over step_size values (e.g. try progressively smaller values like 0.5, 0.1, 0.05, 0.01...) and observe where the integrator's output stops blowing up or returning huge/non-finite results — pick the largest stable, accurate step_size from that sweep rather than guessing a tiny arbitrary one, since an unstable choice reproduces the original divergence. With a stable step_size fixed, define an MSE objective comparing model predictions `integrate(mu * x_i, step_size)` against the dataset's y_i, and minimize over mu starting from an initial guess of 1.0 using something like `scipy.optimize.minimize` (Nelder-Mead) or manual gradient descent. Round the converged mu to exactly 2 decimal places and write only that number to result.txt.