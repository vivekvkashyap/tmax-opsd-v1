First transcribe `/app/instructions.wav` (e.g. `whisper`, `speech_recognition`, or any available STT tool) and read the rules carefully — do not guess at the classification criteria. Then compare the dictated rule against the two corpora at `/app/corpus/clean/` and `/app/corpus/evil/` to confirm your understanding: inspect a couple of files from each with `cat`/`grep` and check what character patterns distinguish them. Implement `/home/user/filter.sh` so it strips the header line (any line starting with `>`) and validates only the sequence body against the exact rule you transcribed — a strict character-class regex check (e.g. with `grep -Ev` or `awk`) is more reliable than eyeballing. Test your script explicitly against every file in both corpora before finishing, since the pass criterion requires 100% correct classification on both sets — a single missed edge case (whitespace, digits, an ambiguous letter) will fail verification. Remember to `chmod +x /home/user/filter.sh` and never modify files under `/app/corpus/`.