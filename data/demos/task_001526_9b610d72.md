Walk the directory tree first with `os.walk('/home/user/app_configs')` collecting every `*.ini` path, then for each file: open it, acquire an exclusive lock via `fcntl.flock(fd, fcntl.LOCK_EX)` on the open file descriptor before reading, read the full original content, and check for the exact substring `[auth_v1]` — only act on files that actually contain it (files already on `[auth_v2]` or without any auth section must be left untouched and excluded from the backup). Before modifying, add the file's untouched original bytes to a zip via `zipfile.ZipFile('/home/user/pre_update_backup.zip', 'a')` using an `arcname` that is the path relative to `/home/user/app_configs` (compute with `os.path.relpath`), so the archive layout matches the source tree without the leading directory. Then produce the modified text via straightforward string replacement of `[auth_v1]` -> `[auth_v2]` and `token_expiry = 3600` -> `token_expiry = 86400`, truncate and rewrite the file at the same locked descriptor (seek to 0, write, truncate), then explicitly `fcntl.flock(fd, fcntl.LOCK_UN)` and close — always release the lock even on error paths (use try/finally). Manually inspect the resulting zip contents and the rewritten files afterward to confirm only the files that had `[auth_v1]` were touched.