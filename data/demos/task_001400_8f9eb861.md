Do the symlink first since it's a one-shot check: `ln -s /home/user/metrics_data /home/user/dash_app/current_metrics` (target then link name; verify direction with `readlink`, since a reversed symlink is a common mistake). For the supervisor script, structure it as: kill anything already bound to port 8080 (e.g. via `lsof`/`fuser` or a pattern-based `pkill`), launch the dashboard in the background and remember its PID, then loop forever with `sleep 1`, each iteration using `curl -s -o /dev/null -w "%{http_code}" --max-time <n> http://127.0.0.1:8080/health` so a timeout or connection failure is treated the same as a bad status code — check both curl's exit code and the returned status, since a failed connection won't produce "200" but also shouldn't be misread as some other numeric code. On any non-200/failure: kill the tracked PID, increment a counter variable that persists across loop iterations (not reset each time), relaunch the dashboard capturing its new PID, and append exactly `RESTART_EVENT | TOTAL_CRASHES: X` to the log with the running total. Test manually by running the script and watching the dashboard's built-in crash-after-N-requests behavior trigger a restart and log line before trusting it.