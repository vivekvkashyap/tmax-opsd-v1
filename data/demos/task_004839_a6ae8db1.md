Parse the CSV manually (no header) by splitting each line on the first 4 commas only, treating everything after the 4th comma as the Message field so embedded commas in text don't break parsing. For timestamp alignment, parse the `YYYY-MM-DDTHH:MM:SSZ` string and zero out the seconds field, keeping the `Z` suffix. For encoding: only transcode when `Encoding=="ISO-8859-1"` — convert each byte b to UTF-8 as one byte if b<0x80, else emit `0xC0|(b>>6)` then `0x80|(b&0x3F)`; leave UTF-8 rows untouched. Maintain a `std::deque<int>` (or similar) per locale, push the current latency, pop from the front once size exceeds 3, and average what remains — do NOT average over all history, only the last up-to-3 events for that specific locale, and update the window before computing the average for the current row. Round to 2 decimals with something like `round(avg*100)/100.0` and format explicitly with 2 decimal places when serializing. Write JSON Lines using a real JSON library (e.g. nlohmann-json) so escaping/formatting is correct, preserving input row order exactly.
