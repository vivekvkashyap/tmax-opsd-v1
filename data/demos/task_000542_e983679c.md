First learn the decoder's calling convention by experimentation: run `/app/sensor_decode` with a few hand-crafted hex strings (e.g. hex-encode a simple test CSV line yourself and pass it as the positional arg) and observe stdout to confirm it just hex-decodes to `timestamp,sensor_id,value` — use `strings`/`file` on the binary only to confirm behavior, not to hardcode assumptions. Build the HTTP service (Flask/FastAPI/http.server or similar) bound to `127.0.0.1:9042`, checking the exact `Authorization: Bearer sec-iot-pipeline-8821` header first and returning 401 on mismatch before any processing. For the pipeline: load `/home/user/data/historical_context.csv`, decode each new payload by shelling out to `/app/sensor_decode <hex>` and parsing its stdout, merge with history, then impute missing `value` fields via linear interpolation between the nearest valid neighbors sorted by timestamp (careful with edge rows at file start/end). Bucket all points into fixed 15-second tumbling windows aligned to absolute epoch time (not relative to the batch), compute per-bucket means, then a trailing 4-bucket rolling average per bucket. Flag anomalies only on buckets touched by the new batch, using `abs(mean - rolling_avg) / rolling_avg > 0.15`. Run the service in the background (e.g. via a process manager or `&`) so the verifier's HTTP client can reach it.