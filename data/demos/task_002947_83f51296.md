Reverse-engineer before you code. Run `strings /app/legacy_checker | grep -iE 'pattern|regex|password|AKIA|[0-9A-Za-z\\[\\]{}+*.]'` to surface any embedded regex-like literals, and cross-check with `objdump -d` or `gdb` (disassemble around string references) if `strings` alone is ambiguous, so you recover the exact two secret-detection patterns rather than guessing generic ones. For the time-series check, parse each `[YYYY-MM-DD HH:MM:SS]` timestamp, bucket by truncating to the hour, and count entries per bucket — flag "Evil" if any bucket's count exceeds 50 (strictly greater, not >=). Implement both checks in `/home/user/detector.cpp` using `<regex>` for the secret patterns and simple string/date parsing for the time buckets, returning exit code 1 as soon as either condition is met and 0 otherwise, with no stdout/stderr output on success paths. Compile with C++17 (`g++ -std=c++17 detector.cpp -o detector`) and validate against the provided `/home/user/samples/` logs, checking exit codes with `echo $?` for each, before trusting it against the hidden corpus.
