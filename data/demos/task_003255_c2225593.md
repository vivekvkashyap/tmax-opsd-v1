Break this into independent, individually-testable pieces before wiring them together. First, extract frames from `/app/build_capture.mp4` with ffmpeg (e.g. `ffmpeg -i /app/build_capture.mp4 frames/frame_%04d.png`, matching the video's actual frame rate so your frame numbering is 1-based and consistent with the video's true index), then run `tesseract` over each frame (or a subsampled set, tightening your search once you find the right region) to locate the exact frame containing "CORRUPTION_DETECTED: <module_name>" — record both the frame number and the module name exactly as OCR'd. Second, in `/home/user/organize.sh`, implement graph traversal over `deps.txt` (format `module: dep1 dep2 ...`) as a BFS/DFS from the corrupted module using a "visited" set to guard against cycles, collecting only the modules that DEPEND ON (downstream of) the corrupted one — trace the direction of the arrows carefully, since "downstream dependents" vs "upstream dependencies" are easy to invert. Sort and dedupe the result (`sort -u`), concatenate the strings with no separator, and sum ASCII codes character-by-character (e.g. via `awk` or a loop over `od`/`printf '%d'`). Finally, implement a minimal WebSocket server (RFC 6455 handshake + text frame parsing) in your Bash/Python/socat combo, listening on `0.0.0.0:8081`, that replies to a `GET_REPORT` payload with the exact JSON object using the discovered values — test it locally with a WebSocket client before finishing, and keep it backgrounded and running.