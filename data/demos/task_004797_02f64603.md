Parse `/home/user/sensor_data.csv` and first apply the row-level validity filter: drop any row where `temperature` is outside `[-20.0, 50.0]` or `humidity` is outside `[0.0, 100.0]` — do this filtering before any grouping so invalid rows never enter the rolling window or max calculations. Group the remaining valid rows by `sensor_id`, preserving chronological order within each group (the file is already time-sorted, but sort by timestamp per group to be safe). For each sensor, compute a strict rolling window of exactly 3 consecutive *valid* readings (windows of size 1 or 2 produce no output — only emit once you have 3+), rounding each SMA value to 1 decimal place; be careful that "3 valid readings" means readings that survived the earlier filter, not raw row indices, since dropped rows must not count toward the window. Separately compute per-sensor max humidity over the valid rows only, rounded to 1 decimal. Write both dictionaries, keyed by the actual sensor IDs found in the data, to `/home/user/etl_output.json` matching the exact schema.