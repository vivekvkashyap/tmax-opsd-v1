Start by transcribing `/app/voice_memo.wav` (e.g. `ffmpeg` to extract/convert audio, then a speech-to-text tool such as `whisper` or any available offline ASR) to recover the internal backend port and firewall instructions spoken by the admin — do not guess a port number. Update `/etc/nginx/sites-available/default`'s `proxy_pass` to point at `127.0.0.1:<recovered-port>` and reload/restart nginx. In `server.cpp`, add a request-body filter that runs before any processing: reject (return 403) payloads containing shell metacharacters (`;`, `|`, `` ` ``, `$(`, `&&`, etc.) or SQL injection markers (`'`, `--`, `DROP`, `UNION SELECT`, etc.), and accept everything else with 200 — validate your filter against every file in `/app/corpus/clean/` and `/app/corpus/evil/` before finalizing, since 100% correctness is required. Compile the server, bind it to `127.0.0.1:<recovered-port>` only, and wire it as a user systemd unit (`~/.config/systemd/user/backend.service`) with `Restart=always`, then `systemctl --user enable --now backend.service`. Finally lock down the backend port with `ufw`/`iptables` so only `127.0.0.1` can reach it while nginx's 8080 stays open, and confirm end-to-end with curl through port 8080.
