Work the phases in order and treat each as a build block for the next. (1) Extract frames from the 00:03-00:05 window with `ffmpeg -ss ... -to ... -vf fps=N frame_%03d.png`, then OCR each frame (e.g. `tesseract`) to recover the 16-char passphrase — don't guess it, read it off the frames. (2) Decrypt the log with `openssl enc -d -aes-256-cbc -pbkdf2 -pass pass:<recovered-passphrase> -in ... -out ...`, then grep the plaintext for a redirect parameter pointing to an external domain followed immediately by a request from the same IP under a real username — that IP is the attacker, that username is the hijacked session. (3) Find world-writable files with `find /home/user/web_root -type f -perm -o+w`. (4) Write findings.json exactly matching the required schema, then write a small script (Python http.server or similar) that binds 127.0.0.1:8080, checks the Authorization header equals `Bearer Incident-Resp-2024` on POST /report, and returns 200 with body `REPORT_RECEIVED`; run it in the background (e.g. with `nohup ... &`) so it stays up for the verifier.