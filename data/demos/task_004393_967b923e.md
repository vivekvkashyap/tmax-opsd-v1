First get the build green, then fill in the logic. Run `make` in `/home/user/ws_filter` and read the linker error carefully — a C++ project split across `main.cpp`, `policy.cpp`, and a separate `semver.cpp`/`semver.h` will fail to link if the Makefile's target rule only lists a subset of the required object files; check which `.o` files the `ws_filter` target depends on and add the missing one plus its compile rule if absent, without changing the compiler. Once `make` succeeds, open `policy.cpp` and implement `evaluate_policy`: tokenize the expression string (format `V <op> <version>`) by whitespace to pull out the operator (which may be one or two characters — check `<=`/`>=` before falling back to single-char `<`/`>`) and the target version, then delegate the actual comparison to the already-implemented `compare_semver` from `semver.h` rather than reimplementing version comparison yourself. After rebuilding, run `./ws_filter ws_requests.txt > /home/user/allowed.log` and manually verify each line of the input file's expected true/false outcome against your logic before trusting the file.