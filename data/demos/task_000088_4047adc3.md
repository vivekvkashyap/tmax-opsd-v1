In Go, use `archive/tar` over `compress/gzip` to stream the archive, but validate before you write anything: either do a first pass over all headers checking each `hdr.Name` for `..` segments (split on `/` and check each component) or a leading `/`, and only begin extracting after confirming the whole archive is clean — or extract into a temporary directory first and only move it into the real output directory (or delete the temp dir) once validation passes, so a rejected archive never leaves partial files behind. On any violation, print `MALICIOUS ARCHIVE`, exit non-zero, and make sure the output directory is empty (don't just stop mid-extraction). Use `tesseract` OCR on `/app/target_format.png` to recover the exact rename pattern string, then apply it programmatically (e.g. `fmt.Sprintf`) to each extracted `.log` file's base name with its extension stripped — don't hardcode a guessed format. Test against both a clean archive and a hand-crafted malicious one (containing `../` or an absolute path entry) yourself before considering this done, since grading uses full clean/evil corpora.
