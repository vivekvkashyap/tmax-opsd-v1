First get the build working, then fix correctness by testing against known cases, and finally fuzz-test broadly to catch edge cases you'd otherwise miss. Inspect the Makefile for an invalid `-I` include path pointing at a nonexistent directory and correct it (e.g. to `-I.` or the actual source include dir) so `make` succeeds. Then rewrite `normalize_doc_path` as a proper path canonicalizer: split the input on `/`, walk the components pushing onto a stack, skip empty components (handles `//`) and `.` components, and pop the stack on `..` (deciding carefully whether to allow popping past the root or leave a leading `..` for relative paths outside the root — infer the exact semantics by testing simple cases like `a/b/../c` and `docs/v1//../v2/./api.md` by hand, matching standard POSIX path-canonicalization conventions). Preserve trailing-slash and leading-slash handling consistently. Since your output will be checked for exact equivalence against a reference implementation under thousands of random fuzz inputs (varied lengths, mixes of letters, digits, `/`, and `.`), don't rely on a handful of manual test cases — write your own local property tests covering deep nesting, excess `..` beyond root, empty segments, and paths with no separators, and fix any inconsistent behavior before finishing.
