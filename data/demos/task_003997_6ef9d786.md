Build each layer and test it standalone before chaining them. Compile with `gcc -o /home/user/bin/interactive_daemon /home/user/src/interactive_daemon.c`. Add `export DAEMON_ENV=hardened` to `/home/user/.bashrc`. For the expect script, use `spawn /home/user/bin/interactive_daemon`, `expect "Enter Hardening PIN: "`, `send "7924\r"`, then keep the process alive and passing output through with `interact` (since the daemon loops forever printing status, `interact` will stream stdout without exiting) — test it manually first (`expect -d auto_start.exp`) to confirm the PIN prompt and acceptance message appear. The wrapper `launch.sh` must `source ~/.bashrc` (or explicitly `export DAEMON_ENV=hardened`) before invoking the expect script so the daemon's env check passes, and redirect both stdout/stderr of the expect invocation to `/home/user/daemon_logs/app.log` (e.g. `... >> logfile 2>&1`). Write the logrotate config with `daily`, `rotate 4`, `compress`, `missingok`, `notifempty` directives targeting the log path, and validate syntax with `logrotate -d config_file` (dry run, checks parsing without executing). Finally launch with `nohup ... &`, sleep a few seconds, then grep the log file to confirm both expected daemon messages appear.