First fix the vendored build before writing any pipeline code: inspect `/app/libstatboot-1.2/Makefile`'s `CFLAGS` for a bogus/unsupported `-Werror=...` flag causing gcc to fail, remove or correct it, then run `make` and confirm `libstatboot.so` is actually produced before proceeding — don't assume the header's declared signature is correct without checking the library's exported symbol too. Write `etl_pipeline.c` to read the CSV line by line, skip rows with an empty temperature field, and skip rows outside `[-50.0, 100.0]`, accumulating the surviving values into a dynamically-sized array. Declare `compute_bootstrap_mean` matching the header, compile with `-L. -lstatboot` (or full path) and the appropriate include path, then run the resulting binary with `LD_LIBRARY_PATH=/app/libstatboot-1.2:$LD_LIBRARY_PATH` (or copy/point to wherever the built `.so` lives) so the dynamic linker finds it at runtime — a common failure mode here is compiling fine but crashing or erroring at execution due to the linker not finding the shared library. Format the result to exactly 4 decimal places when writing `/home/user/bootstrap_result.txt`, and since the metric is stochastic, running it once and checking the value is roughly sane (near the dataset's true mean) is a reasonable sanity check before finishing.