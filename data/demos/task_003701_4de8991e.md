Work in three phases. First, read app.py fully — don't just grep for the reflected parameter. Find where user input is echoed unescaped into HTML (the injection point), then separately scan every other route for a place that reflects a request parameter back as `application/javascript` content — a JSONP-style "callback echo" is a classic same-origin gadget that satisfies a `script-src 'self'` CSP, because the browser loads it via a `<script src="...">` tag pointing at your own origin. Chain the two: inject a `<script src="/that-endpoint?callback=...">` tag through the XSS sink, and make the callback body itself be the code that reads `document.cookie` and sends it via `fetch()` (or `new Image().src`) to the one origin `connect-src` permits — build the exfil URL by concatenating the cookie value as a query parameter exactly as specified. Second, generate the TLS pair with `openssl req -x509 -newkey rsa:2048 -nodes -keyout key.pem -out cert.pem -days 365 -subj '/CN=localhost'` (unencrypted key = `-nodes`). Third, write the listener with Python's `http.server` + `ssl.SSLContext.wrap_socket` (or `ssl.wrap_socket`), parse the query string with `urllib.parse.urlparse`/`parse_qs` to pull the `data` param, append it to the log file, and respond 200. Test locally with `curl -k` before finishing.
