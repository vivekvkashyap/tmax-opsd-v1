Work top-down through the chain: config extraction, C fix, wrapper script, and live verification. First, grep `/app/provision_events.log` for lines matching `[ROUTING_UPDATE] NEW_UPSTREAM_PORT=<n>` (use `grep -oP` or similar), take the LAST match since the log may contain multiple/noisy entries, and write only the bare integer (no brackets, no trailing newline issues) to `/home/user/upstream.conf`. Next, open `/app/router.c` and find the hardcoded `listen_port = 80` (or similar literal); replace it with a lookup via `getenv("LISTEN_PORT")`, parsing it with `atoi` if present, else defaulting to 8080 — keep the logic that reads the upstream target port from `/home/user/upstream.conf` intact. Compile with `gcc /app/router.c -o /home/user/router`. Then write `/home/user/start_router.sh` that exports `LISTEN_PORT=8080`, launches `/home/user/router` in the background redirecting stdout/stderr to `/home/user/router.log`, and separately backgrounds a loop (`while true; do sleep 10; > /home/user/router.log; done &`) to keep the log from growing unbounded. Run the script, then confirm end-to-end with `curl -v http://127.0.0.1:8080/health` and check the backend on the extracted upstream port is actually reachable — verify with `ss -ltnp` or `curl` before declaring done, since the check hits the proxy over real HTTP.
