Split this into the sanitizer and the video pipeline, and validate each against the provided `/app/sample_data/` before trusting it on hidden data. For the sanitizer, don't attempt full SVD/Cholesky — implement randomized power iteration (repeated matrix-vector products with a random starting vector, normalizing each step) to estimate the largest eigenvalue, and a shifted/inverse-power variant or Hutchinson-style trace estimation to gauge the smallest, then derive an approximate condition number from many Monte Carlo trials averaged together; parallelize the trials or the matvecs with OpenMP pragmas (`#pragma omp parallel for`) and compile with `-fopenmp`. Pick a stability threshold empirically by testing against the sample clean/evil matrices in `/app/sample_data/` until your exit-code logic (0=clean, 1=evil) cleanly separates them, since the hidden corpus will use a similarly separated distribution. For the video half, use `ffmpeg` (or OpenCV) to extract frames from `/app/sensor_feed.mp4` into grayscale images, then convert each 100x100 pixel grid into the `N`-then-rows text format by mapping pixel intensity 0-255 linearly to 0.0-1.0, run your compiled `/home/user/sanitizer` on each generated file, tally exit-code-0 results, and write that count to `/home/user/clean_frame_count.txt`. Watch for integer overflow when reading large files and off-by-one errors in frame/row counting.