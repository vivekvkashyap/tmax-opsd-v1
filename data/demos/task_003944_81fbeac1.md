Write a C program that opens `/home/user/raw_observations.csv`, skips the header line, and parses each `id,a,b` row (e.g. with `fscanf`/`strtok`+`atof`, being careful with the comma delimiters). For each row implement Newton-Raphson on f(x) = x - a*sin(x) - b: derivative f'(x) = 1 - a*cos(x), update x_{n+1} = x_n - f(x_n)/f'(x_n), starting from x_0 = b exactly as specified. Loop until `fabs(f(x)) < 1e-6`, guarding against division by a near-zero derivative and capping iterations (e.g. 100) to avoid infinite loops on edge cases. Use `double` precision throughout — `float` will not hit the 1e-6 tolerance reliably. Write output rows with `fprintf(f, "%d,%.*f,%.*f,%.6f\n", ...)` preserving `a` and `b` with enough precision to round-trip the original values (or just echo the strings you read in), no header row. Compile with `gcc generate_labels.c -lm -o generate_labels` (the `-lm` link flag is required for `sin`/`cos`), then run the binary so `/home/user/training_data.csv` is actually produced — verify it manually with a couple of hand Newton-Raphson iterations before trusting the output.