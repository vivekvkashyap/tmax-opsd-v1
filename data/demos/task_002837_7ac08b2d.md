Work top-down through the three layers, verifying each before moving to the next. First, recover the leaked secret by scanning the simulated `/proc` dump for `cmdline` files (`find /home/user/forensics/proc_dump -name cmdline`), reading their null-byte-separated arguments (`tr '\0' ' '`), and looking for a `--secret-key=...`-style flag — that value is the repeating XOR key. Second, build the classifier: base64-decode each token, XOR-decrypt it against the recovered key (repeating the key over the ciphertext length), parse the resulting JSON, and classify by the `role` claim — `"user"` is CLEAN, `"admin"` is EVIL; test against both corpus files before assuming correctness. Third, wire up the live defense: write a small Flask/HTTP service on port 8080 that extracts the `auth_token` cookie, runs it through the same decrypt+classify logic inline (don't shell out per-request if avoidable), and returns 200 for CLEAN / 401 otherwise; then add an `auth_request /auth-check;` internal location block to nginx.conf pointing at your service, applied to the proxied `/api/data` location on port 8000, and start backend, auth service, and nginx together before testing with curl and both a clean and evil cookie.
