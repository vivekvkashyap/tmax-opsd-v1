Start by reading the vendored source at `/app/bash-webhook-1.0` carefully to locate both bugs before editing anything. In the Makefile, the install prefix is hardcoded to a root-owned path — change the `PREFIX` variable (or override it on the `make install` command line) to `/home/user/.local` so `mkdir -p`/`cp` succeed without root, then confirm the binary lands at `/home/user/.local/bin/webhook-server` and is on your PATH or invoked by full path. In `webhook-server`, the request-parsing code assigns the method into `HTTP_METHOD` but the routing check tests `$REQUEST_METHOD` — a classic variable-name mismatch that makes every POST look like a non-POST; grep for both variable names to confirm which one is set vs. read, then make the check reference the variable that is actually assigned. After fixing and installing, create `/home/user/logs/deploy.log` and the parent dir with the exact permission bits requested (750 on the dir, 640 on the file) using `chmod` — don't rely on the default umask. Write `provision.sh` to append the exact log line format using `date -I` for the date and `$1` for the project name, make it executable, then launch `webhook-server 8080 /deploy /home/user/provision.sh` in the background (`&` or `nohup`) and verify manually with a raw POST (e.g. via `curl` or `nc`) before leaving it running.
