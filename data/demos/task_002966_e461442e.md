Structure this as a Rust pipeline with clearly separated stages so each is independently testable: (1) read and parse each pipe-delimited line, extracting the 6-char hash from `raw_log` with a regex like `hash=\[([a-zA-Z0-9]{6})\]`; (2) group rows by `server_id`, sort each group by timestamp ascending, then fill missing `cpu_limit` values via linear interpolation using the nearest preceding and succeeding non-missing values (the guarantee that first/last rows are never missing means every gap has valid endpoints on both sides — track the last-seen valid (timestamp, value) while scanning forward, and pre-scan forward from each gap to find the next valid one, or do a two-pass approach: fill forward-known and backward-known arrays then interpolate); (3) walk each server's chronologically sorted, gap-filled rows and keep a row as a changepoint only if it's the first for that server or its rounded-to-2-decimals cpu_limit or hash differs from the last kept changepoint — round consistently before comparing to avoid float noise causing spurious changepoints; (4) write CSV sorted by server_id then timestamp with cpu_limit formatted to exactly 2 decimals (use format strings, not just round, since 10.0 must print as "10.00"). Test incrementally: verify hash extraction, then interpolation on synthetic gaps, then dedup logic, before wiring the full DAG together.