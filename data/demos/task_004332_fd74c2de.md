Get the WAV parsing exact before optimizing the search. In `telemetry_analyzer.c`, read the header to confirm/derive `sample_rate` and bit depth from the `fmt ` chunk rather than assuming a fixed 44-byte offset blindly — real WAV files can have extra chunks before `data`, so search for the `data` subchunk ID and use its declared offset/size as the start of PCM samples, falling back to 44 only if you don't want to parse chunks properly (the task says "usually 44 bytes," so a fixed offset is acceptable but riskier). Read all int16 samples into memory, then use a sliding-window sum of `abs(sample)` over `sample_rate`-length windows: compute the sum for the first window, then slide one sample at a time by subtracting the sample leaving and adding the sample entering (O(n) total, not O(n·window) — recomputing each window's sum from scratch is a common inefficiency and can also introduce mismatched boundary bugs). Track the window start index with the maximum sum, convert to seconds as `start_index / sample_rate` as a float. In `operator.sh`, capture that float, write it verbatim to `peak_time.txt`, and use a numeric comparison (`awk`/`bc`, not string `>`) against 5.0 before editing the deployment YAML with `sed`. The fstab line must have exactly six space-separated fields: device, mountpoint, fstype (`none` for a bind mount), options (`bind,ro`), dump, pass.
