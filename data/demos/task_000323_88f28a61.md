Fix the Nginx `proxy_pass` port to point at 8080 first and confirm with `nginx -t -c ...` that the config is syntactically valid before starting it. Create `/home/user/app_logs` and symlink `/home/user/app/logs -> /home/user/app_logs` before the backend ever runs, since the C++ binary opens `logs/startup.log` relative to its working directory and will fail immediately if that path doesn't resolve. Write the `post-receive` hook so it's idempotent: use `GIT_WORK_TREE=/home/user/app GIT_DIR=/home/user/repo.git git checkout -f` to deploy, compile with `g++`, then gracefully stop any previously running backend (track its PID in a file, or `pkill` by exact process name) before starting the new one. Inside the hook, explicitly `export PATH=/usr/bin:/bin:...` and `export TZ=UTC` before launching the backend in the background — the C++ code shells out to `date`, and hooks run with a minimal/inherited PATH that may not include it. Deploy by cloning the bare repo, fixing the C++ source if it doesn't compile as-is, committing, and pushing to trigger the hook; then start Nginx and curl port 8000 to confirm.
