Treat `/app/legacy_checker` as a black box to reverse-engineer, not something to shell out to from your Rust binary (the task wants your own native implementation). Run `file`, `strings`, and `ltrace`/`strace` on it against a handful of crafted test files (varying extension and first bytes) to infer its corruption rule; if it looks UPX-packed, try `upx -d` on a copy to get cleaner strings/disassembly. Confirm your hypothesis empirically by testing several inputs against the real binary and checking exit codes before hardcoding the check in Rust — e.g. read a file's first bytes and compare against a magic-number pattern only when the extension matches, since it's likely both a byte-signature check and an extension check combined. Separately, for symlink-loop detection during directory traversal, don't do naive recursion — track visited inodes/canonicalized paths (or bound recursion depth) using `std::fs::symlink_metadata` to detect cycles without following them into infinite loops, and treat any detected cycle as evil. Scaffold with `cargo new /home/user/dataset_filter`, walk the directory tree yourself (or use the `walkdir` crate carefully with loop protection), exit 1 on first evil trait found, exit 0 only if the whole tree is clean. Build release and test against both `/app/corpus/clean/` and `/app/corpus/evil/` before finalizing.