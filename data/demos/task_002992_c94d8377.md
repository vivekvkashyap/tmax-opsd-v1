Reverse-engineer `/app/legacy_etl`'s exact transformation by feeding it small, hand-crafted CSVs on stdin and inspecting the output rather than assuming the algorithm: vary timestamps to find the alignment rule (likely flooring to a fixed interval boundary — test minute/15-min boundaries), vary user_ids to check if masked_user_id is a straightforward hash (try common ones like MD5/SHA1 of the raw id and compare), and feed a long monotonic/known value sequence to deduce how anomaly_score is computed (test whether it's a rolling window z-score, and empirically find the window size and how it handles the first few rows before the window fills). Once you've confirmed a hypothesis matches the oracle on several test batches (not just one), reimplement it as a vectorized solution — pandas `rolling(window=N, min_periods=1).mean()`/`.std()` for the anomaly score, `dt.floor('15min')` or similar for alignment, and `hashlib.md5` for masking — since vectorized/batch computation is what will give you the required 5x speedup over the legacy binary's slow per-row processing. Wrap it in `/home/user/fast_etl` (a shebang script or compiled binary) that reads all of stdin, processes in bulk, and writes CSV to stdout with no header, matching column order exactly. Validate correctness (low MSE, exact time/id match) and timing against `/app/legacy_etl` yourself on a large synthetic CSV before considering it done.