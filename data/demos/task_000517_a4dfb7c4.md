Break this into extraction, math, then networking, verifying each stage before moving on. First run `ffmpeg -i /app/sensor_feed.mp4 /home/user/frames/frame_%04d.png` (or similar) to dump frames in order, and write a small C++ image-loading routine (e.g. using `stb_image.h` or decoding PNG via a library available on the system) that computes per-pixel grayscale `0.299R+0.587G+0.114B` and averages over all pixels per frame — index frames starting at 0 to match the frame-index convention used by the rest of the task, adjusting for ffmpeg's 1-based file numbering. Implement the Gaussian PDF as a small helper function `pdf(x, mu, sigma)` and compute the posterior via Bayes' rule: `P(Active|x) = prior*pdf(x,110,30) / (prior*pdf(x,110,30) + (1-prior)*pdf(x,0,2))`; test this manually on x=0 and a typical mid-range intensity to sanity check it saturates toward 0 and 1 correctly at the extremes. Precompute and cache all per-frame probabilities at startup so the server can answer instantly. For the TCP server, use raw sockets (`socket`/`bind`/`listen`/`accept`) reading newline-terminated commands per connection, parsing `FRAME <N>` and `COUNT_INACTIVE`, replying with the exact `PROB: %.4f\n` / `INACTIVE: %d\n` formats, and launch it backgrounded (e.g. `./server &`) so it's still listening when you finish.
