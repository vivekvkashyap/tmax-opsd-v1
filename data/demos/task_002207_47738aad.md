Work through the four parts in order since later parts depend on earlier analysis. Part 1: read `vuln_auth.cpp`, notice the admin hash is a literal constant baked directly into the source rather than derived or stored securely at runtime — look this weakness pattern up in the standard CWE catalog (mitre.org / cwe.mitre.org, or your own knowledge) using its canonical name "Use of Hard-coded Credentials", and write the matching `CWE-XXX` identifier you find to the report file, formatted exactly as `CWE-XXX`. Part 2: extract the custom `custom_hash` function's exact arithmetic and the hardcoded target constant from the source, then write a small C++ brute-forcer that loops all 4-digit PINs (0000-9999, as integers 0-9999), applies the identical hash formula, and stops when it matches the target constant, writing the discovered PIN as plain text. Part 3: process `network_logs.txt` with `sed` or `awk` using a regex anchored on `PIN=` followed by exactly 4 digits, substituting the digits with `XXXX` while leaving the rest of each line untouched, and save every line (not just matches) to preserve exact log structure. Part 4: tally failed/attempted logins per IP from the same log (grouping on the IP token in each line) and find the one IP appearing more than 3 times; emit a one-line executable bash script with `iptables -A INPUT -s <that IP> -j DROP` and `chmod +x` it. Verify each output file's exact content matches the source data before finishing, since grading is byte-exact.
