Build this as a C++ program linking `libhiredis` and `nlohmann/json`, structured as a loop: `BLPOP raw_logs <timeout>` (or `LPOP` in a polling loop) with a running timestamp of the last successful pop — exit once more than 3 seconds elapse with no new item, rather than blocking forever. Track seen `log_id`s in a `std::unordered_set<std::string>` and skip any log whose id you've already inserted (first occurrence wins) — do this dedup check before any other processing so duplicates never even get normalized/written. For message normalization, lowercase the string, then replace every character that isn't `[a-z0-9]` with a space using a single pass, and finally collapse runs of spaces and trim ends (careful: do this after lowercasing/replacing, not before, or you'll miss uppercase non-alnum edge cases). Load `/home/user/service_map.csv` once at startup into a map for the `service_code -> service_name` join, defaulting to `"UNKNOWN"` on miss. For sampling, parse the first 4 hex characters of `log_id` with `strtol(..., 16)` and only keep INFO logs where that value `% 10 == 0`; always keep ERROR/WARN. Write each surviving record as one JSON line with keys in the exact order specified, then compile with the given `g++` command, run the worker, trigger `curl -X POST http://localhost:5000/start`, and let it exit on its own before checking output.
