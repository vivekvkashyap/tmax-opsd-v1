Part 1: extract frames with `ffmpeg -i /app/surveillance.mp4 -vf fps=1 /home/user/frames/frame_%03d.jpg`, then for each output file compute `sha256sum` (or Python `hashlib.sha256`) and build the manifest as a dict of filename to lowercase hex digest, writing it with `json.dump`. Part 2 is the harder piece: implement the binary parser precisely per the spec, reading raw bytes from `sys.stdin.buffer` and using `struct.unpack('>H', ...)` / `struct.unpack('>I', ...)` for the big-endian length fields — get the field order exactly right (magic, count, then per-entry: path-length, path, data-length, data). Critically, check for truncation *before* each read (i.e., verify enough bytes remain before slicing) rather than trying/catching after the fact, and check the truncation condition ahead of the path-traversal check so incomplete entries are always reported as errors, not misclassified. The path-traversal rule is: reject if the path starts with `/` or contains `../` anywhere as a substring (not just as a path component) — implement this exactly, then hash only the data bytes for accepted entries and print the specified formatted lines. Test against hand-crafted valid, malicious, and truncated byte streams before trusting it.
