Start by inspecting `/app/fastcovar-0.3/include/fastcovar.h` for the type used by the internal accumulator/scalar — a "silent truncation to integers" bug in a header almost always means a `typedef` (e.g. `typedef int scalar_t;`) that should be `double`; grep the headers for `typedef` and any `int` used in accumulation paths, fix it, then `make` inside that directory to produce `libfastcovar.a` and confirm the .a timestamp updated. For `process_data.c`: read stdin line by line, split each row on commas into exactly 3 fields, and for each field first check for the exact substring `NaN` (case-sensitive) before attempting `strtod`-style parsing — reject the row on either a literal `NaN`, a parse failure, or any value outside `[-1000.0, 1000.0]` inclusive bounds (use strict `<`/`>` per the spec so exactly ±1000.0 stays valid). Keep running counts of valid vs dropped rows and print the exact `Valid rows: <N>, Dropped rows: <M>` line to stderr after EOF. Store valid columns into three double arrays, call `compute_pearson_3x3`, and print each output row comma-separated with `%.4f`. Compile with `gcc -O2 process_data.c -I<fastcovar>/include -L<fastcovar>/lib -lfastcovar -lm -o process_data`, and sanity-test manually with a few crafted rows including edge cases (NaN, out-of-range, boundary values) before trusting it.
