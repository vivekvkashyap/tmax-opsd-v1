Extract the two numeric parameters (nucleotide-to-float mapping and window size) from `/app/note.png` with `tesseract`, and treat OCR output skeptically — cross-check the extracted numbers make sense (four distinct mapping values for A/C/G/T, one integer window size) and re-run OCR with different `--psm` modes or crop/threshold the image with `imagemagick` if the first pass is garbled. Read the genome string, then split into non-overlapping windows of exactly the extracted size, discarding a trailing partial window (use integer division `len(seq)//window_size` to know how many full windows exist). Map each character to its float value per the note, then for each window compute the power spectrum as `np.abs(np.fft.fft(window))**2` — critically, do not normalize the FFT and do not use `rfft` (which would produce the wrong length/values), since the spec calls for the full complex DFT magnitude-squared. Parallelize the per-window FFT computation across workers with `multiprocessing.Pool.map`, then average all window spectra element-wise with `np.mean(spectra, axis=0)` and save via `np.save('/home/user/avg_spectrum.npy', result)`. Sanity-check the output array's length equals the window size before finishing.