Work phase by phase and validate each before moving to the next, since later phases depend on earlier ones. First install `protoc` plus `protoc-gen-go`/`protoc-gen-go-grpc` as user-local binaries (e.g. via `go install ...@latest` into `$HOME/go/bin`, or download prebuilt `protoc` release and unpack locally) and add them to `PATH` for the session. Second, write `proto/security.proto` exactly matching the required package/go_package/service/RPC/message field names and numbers specified in the task — protoc-gen-go is strict about matching field tags to what `server_test.go` expects via the generated struct names (e.g. `Logs`, `MaliciousIps` — protoc's default Go naming converts snake_case fields to CamelCase, so name proto fields accordingly). Third, write the Makefile so `proto` invokes `protoc` with `--go_out`/`--go-grpc_out` paths targeting `go/pb/`, and `build`/`test` correctly depend on `proto`+`rules` running first (order matters — generated pb code and rules.json must exist before compiling/testing). Fourth, `go mod init security` inside `go/`, add grpc/protobuf deps via `go get`, and implement `server.go`: load `rules.json` once, then in `AnalyzeLogs` spin up a goroutine per log line (or a worker pool) sending matches over a channel, collect results, dedupe IPs with a set, and sort alphabetically before returning. Run `make test`, redirect output to `/home/user/test_report.txt`, then `make build`.