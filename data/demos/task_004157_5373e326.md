Work the three phases independently and verify each in isolation before moving on. For the video: use `ffmpeg -i /app/server_rack.mp4 -vf "select='...'"` or extract frames (e.g. to raw PPM/PNG via `ffmpeg ... frame_%04d.png`) and compute each frame's average luminance yourself (e.g. with Python/PIL or ImageMagick `identify -format "%[fx:mean]"`); flag frames whose mean exceeds the 95% threshold and write their 1-indexed frame numbers, sorted, one per line. For the database: first inspect `sqlite_master` and run `PRAGMA integrity_check` to locate the corrupted index definition on `events`, then `DROP INDEX` and recreate it correctly on `event_time` before writing any Go analytics code — a broken index will silently return wrong rows to your window-function query. In Go, use `ROW_NUMBER()`/window `SUM(...) OVER (PARTITION BY service_id ORDER BY event_time ROWS BETWEEN 2 PRECEDING AND CURRENT ROW)` for the rolling-3 sum, and export straight to CSV with the exact column order requested. For the sanitizer, build a real line-by-line parser (not just substring matching) that case-insensitively rejects DROP/DELETE/INSERT/UPDATE and any UNION SELECT while passing plain SELECT/WITH queries through unmodified — test it against your own crafted evil/clean examples before trusting it, since the hidden corpora will be much larger and stricter.