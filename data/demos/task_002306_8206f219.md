Write the script so every path (data dir, log dir, output file) is a hardcoded absolute string (or built from `os.path.abspath(__file__)`'s parent only if you truly need relativity) — never rely on `os.getcwd()` or a relative path, since that's exactly the bug you're fixing. For storage size, walk `/home/user/app_data/` recursively with `os.walk` and sum `os.path.getsize` for every file, including subdirectories — don't just `du` the top level. For process monitoring, read `/proc/*/cmdline` (splitting on null bytes) or parse `ps -eo args` and count lines/processes whose full command line contains exactly `background_worker.sh`; be careful not to count your own grep/ps invocation as a match (avoid a naive `ps aux | grep ...` without excluding the grep process itself). For the user check, read `/etc/passwd` directly and look for a line starting with `deploy_user:` rather than shelling out to `id` (which can behave differently under sudo/root). Write the JSON with the exact three keys, run the script once to produce `/home/user/logs/health.json`, and `chmod +x` it with the `#!/usr/bin/env python3` shebang.
