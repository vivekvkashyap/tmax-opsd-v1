Treat `/app/legacy_validator` purely as a black-box oracle to reverse-engineer, not something to inspect for a shortcut. Run it against every file in the clean and evil corpora, note pass/fail, and also use `xxd`/`strings`/`objdump` on the binary and on individual corpus files to hypothesize structural rules (e.g., a magic-byte header, a length-prefixed metadata block, forbidden substrings, or non-printable-byte checks in some region). Iterate: form a hypothesis, encode it into `fast_filter.sh`, then re-run it against the full corpus and diff its verdicts against the oracle's real exit codes until they match 100%, since partial pattern-matching (e.g., only checking magic bytes) will fail on corpus files crafted to break a naive rule. Once the filter is byte-exact against the oracle, use `find /home/user/incoming -type f -mtime -7` (or equivalent) to get recently modified files, run each through your filter, and write only the ones passing to `/home/user/clean_backups.log`, one absolute path per line, sorted with `sort`.
