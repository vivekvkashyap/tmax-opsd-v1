Build the pipeline as a subprocess wrapper, not a shell one-liner. Run `/app/log_emitter` with `subprocess.check_output` (or `Popen` + read stdout), then process line by line. For the corrupted unicode escapes, do NOT drop the lines: before calling `json.loads`, apply a regex substitution that neutralizes invalid `\u` escapes (e.g. replace any `\uXXXX` sequence that isn't 4 valid hex digits, or any trailing/truncated `\u` at end of string) with an empty string or literal char, so the JSON becomes parseable — inspect a raw sample of the emitter's output first to see the exact broken pattern before writing the regex. For masking, split `client_ip` on `.` and rewrite the last octet to `0`. Bucket by `(timestamp // 60) * 60`, track a `set()` of masked IPs per bucket for uniqueness, and tally counts of `/login`, `/data`, `/health` per bucket into a 3-vector. Compute Euclidean distance against the fixed baseline `[15, 45, 10]` with `math.sqrt(sum((a-b)**2 ...))`, round to 4 decimals, and write CSV rows sorted/keyed by integer bucket start timestamp with the exact header `bucket_start_timestamp,unique_ips,distance`.
