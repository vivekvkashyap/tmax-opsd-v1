Build the script as four distinct, testable stages rather than one monolithic block, since the grader checks each behavior independently via log greps. (1) Network wait: loop polling for `/home/user/network/routing.ready` with `sleep 1`, and on every iteration where the file is still absent, append the literal line `WAITING FOR NETWORK` to the log before sleeping — get the ordering right (check-then-log-then-sleep) so it logs at least twice while a background process creates the file after a delay. (2) Backup: before processing each `*.conf` manifest, `mkdir -p` the backups dir and `cp` the manifest to `<name>.<<(date +%s)>>.bak`. (3) Manifest parsing: read `KEY=VALUE` lines (e.g. with a `while IFS='=' read` loop or `source`-ing safely) to get VM_NAME/HOST_PORT/VM_PORT, then construct the qemu command string EXACTLY as specified — reproduce the flag order and spacing precisely since verification greps for the literal string. (4) Restart supervision: wrap the qemu invocation in a loop with an attempt counter capped at 4 total runs; check `$?` after each run, and on nonzero exit append `RESTARTING <VM_NAME>` to the log and retry, stopping immediately on exit 0 or after the 4th attempt. Test locally with a mock `qemu-system-x86_64` script that fails N times before succeeding to confirm your loop and counters behave correctly.