Use rdflib to load org_graph.ttl and run a SPARQL query with a property-path operator (`ex:manages+` or a variant that also includes Alice herself) to capture her entire transitive reporting subgraph — a single-hop query will miss indirect reports several levels down. Connect to sales.db with sqlite3 and compute the rolling average using a window function, e.g. `AVG(amount) OVER (PARTITION BY employee_name ORDER BY sale_date ROWS BETWEEN 2 PRECEDING AND CURRENT ROW)`, so each row gets the correct 3-row trailing average scoped per employee. In Python, for each subgraph member (including Alice), find their row with the maximum sale_date and read that specific row's rolling average — not an aggregate over all their rolling averages. Sum these per-employee latest values, round to 2 decimals, and write the JSON with subgraph_members sorted alphabetically and the exact key names specified.
