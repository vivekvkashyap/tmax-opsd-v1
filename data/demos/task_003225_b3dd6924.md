Build this as a clean pipeline: parse -> derive parameters -> integrate -> export -> plot, checking each stage's intermediate output before moving on. Use Biopython's `SeqIO.parse` (or manual FASTA parsing) to get each record's id and sequence, then compute GC fraction as count('G')+count('C') over total length (use the sequence as given, don't assume case), and derive `r = 0.5 * gc_fraction` per sequence. For the ODE, use `scipy.integrate.solve_ivp` (or `odeint`) with the logistic growth right-hand side `r*V*(1-V/K)`, K=1000, V0=1.0, evaluated at `t_eval=numpy.linspace(0, 50, 100)` exactly — matching the eval grid matters because the verifier compares point-by-point against the closed-form logistic solution. Write each strain's resulting 100-point V(t) array as its own dataset in an HDF5 file via `h5py.File(...).create_dataset(seq_id, data=v_array)`, naming datasets exactly after FASTA headers (no `>`). Separately generate one matplotlib figure plotting all series with axis labels "Time"/"Viral Load" and a legend of sequence IDs, saved to the given PNG path. Install missing packages (`h5py`, `scipy`, `matplotlib`, `biopython`) via pip first, then actually execute the script and confirm both output files exist with correct shapes before finishing.
