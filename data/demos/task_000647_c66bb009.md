Transcribe `/app/audio/voicemail.wav` first — install `ffmpeg` plus a speech-to-text engine (e.g. `pip install openai-whisper`, or `speech_recognition` with an offline recognizer) and run it on the audio to recover the spoken numeric threshold; read the number out of the transcript text itself rather than assuming a round value, since spoken numbers can be tricky to parse (e.g. "eight thousand five hundred" needs correct numeric conversion). Then write `/home/user/sanitiser.py` to accept `--input_dir` and `--output_file` args (use `argparse`), iterate over every `.npy` file in the directory with `numpy.load`, compute the condition number using `numpy.linalg.cond(matrix)` (which defaults to the 2-norm/L2 condition number — confirm this matches "using the L2 norm" in the task), and classify: "clean" if `cond < threshold`, "evil" if `cond >= threshold`, using the exact threshold you transcribed. Wrap file loading in try/except so one corrupt/unreadable file doesn't crash the whole run, and write the final `{filename: classification}` dict as JSON with `json.dump`. Test against both `/app/corpus/clean/` and `/app/corpus/evil/` directories before finishing to confirm 100% correct classification.