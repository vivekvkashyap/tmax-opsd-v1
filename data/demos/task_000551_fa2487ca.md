Break the script into three independently-testable pieces before combining them. To find the unencrypted SSH key, iterate the files in `ssh_keys/` and for each run `ssh-keygen -y -f <key> -P ""` (or attempt to read the key with an empty passphrase) — the one that succeeds without prompting/erroring is the unprotected key; then get its fingerprint with `ssh-keygen -lf <that_key>` and extract the `SHA256:...` token (the second whitespace-separated field). For certificate validation, build the CA bundle by concatenating `intermediate.pem` then `root.pem` (or vice versa — test both orders with plain `openssl verify` first until you get `OK` before wrapping in the sandbox) and use `openssl verify -CAfile bundle.pem leaf.pem`. Only after that command works unsandboxed, wrap it in `bwrap --unshare-all --ro-bind / / --tmpfs /tmp -- openssl verify -CAfile <bundle> <leaf>` inside the script, and confirm the sandboxed output is identical to the unsandboxed one (paths inside the sandbox mirror the host since `/` is bound). Write line 1 as `Vulnerable Key Fingerprint: <fingerprint>` and line 2 as the exact stdout line `openssl verify` prints (typically `<path>: OK`) to `/home/user/audit_log.txt`, then `chmod +x` the script and run it to produce the log.