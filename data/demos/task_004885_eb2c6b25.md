Write `/home/user/analyzer.cpp` as a direct, literal implementation of the spec rather than a general Markov-chain library. Parse `/home/user/transition_graph.txt` lines as `source target prob` into an adjacency map keyed by node name (use a `std::map<std::string,...>` so iteration order is alphabetical, which you'll need later for output). Build the row-stochastic transition matrix implicitly (or explicitly) over the *set of unique node names* seen in the file. Initialize π uniformly over all unique nodes, then repeatedly apply `π_new[j] = Σ_i π[i] * P(i→j)` for every node, tracking `max |π_new[k]-π[k]|` across all nodes each iteration, and stop once that max delta is strictly `< 1e-7` — don't use a fixed iteration count, since the convergence threshold is exact. Parse `/home/user/empirical.txt` into P_emp, then compute KL divergence `Σ P_emp[k] * ln(P_emp[k]/P_stat[k])` over the same alphabetically-sorted node set, using natural log (`std::log`, not log10). Write `/home/user/results.log` with each `Stationary <Node>: <value>` line (nodes sorted alphabetically) followed by `KL_Divergence: <value>`, all values formatted to exactly 4 decimal places (e.g. `std::fixed << std::setprecision(4)`). Compile exactly with `g++ -std=c++17 -O3` and run it to produce the file — don't hand-transcribe pre-computed numbers into the log.