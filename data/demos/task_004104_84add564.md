First recover the target node ID by actually transcribing the audio rather than guessing — use whatever speech-to-text tool is available (`whisper`/`whisper.cpp` on `/app/oncall_voicemail.wav`), listen for a phrase naming a "backup server node" plus a spoken number, and convert the spoken number to the `node_<N>` identifier format the JSON schema uses. If a transcription tool isn't available, fall back to inspecting the WAV in a spectrogram/manual listen. Then build `validate_schema.sh` as a thin wrapper that calls into a script (Python is easiest) doing two checks on the given JSON file: (a) the recovered target node string appears in the `"nodes"` array, and (b) the graph defined by `"edges"` is acyclic — implement this with a standard DFS/topological-sort cycle detector using three-color marking (white/gray/black) or Kahn's algorithm on in-degrees, since a naive "visited-once" DFS will miss cycles reachable via multiple paths. Exit 0 only if both checks pass, otherwise exit 1. Test your script against the sample corpus provided in the environment, including edge cases like self-loops and multi-node cycles, before trusting it against the hidden adversarial set. Make the script executable with `chmod +x`.