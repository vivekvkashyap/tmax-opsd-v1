Diagnose the 502 before touching anything else: check what `nginx.conf`'s `proxy_pass` target actually is versus where gunicorn will actually listen (unix socket path or TCP host:port) — the two must match exactly, so either point nginx at the socket/port gunicorn will bind, or launch gunicorn to bind where nginx expects (e.g. `gunicorn --bind unix:/path/to.sock main:app` or `--bind 127.0.0.1:PORT` matching the conf). Start nginx unprivileged with the exact flags given, and gunicorn separately, then confirm with `curl 127.0.0.1:8080` before debugging the math. For the amplitude bug, read `main.py` and compare its normalization/reduction against the correct definition: 16-bit PCM samples must be divided by 32768.0 (not an arbitrary constant) to land in [-1, 1], and "mean absolute amplitude" means `np.mean(np.abs(...))`, not `np.sum`. Fix both, restart the backend, and only then write `check.py` to POST `/app/test_audio.wav` as multipart form-data under field name `file` to `http://127.0.0.1:8080/process`, printing just the returned float with no extra text.
