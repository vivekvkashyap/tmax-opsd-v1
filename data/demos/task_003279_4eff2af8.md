Build this as a precision-sensitive C simulation. Parse each edge-list file into an N×N adjacency array (N = max node index + 1, since it isn't given explicitly), fill both `adj[u][v]` and `adj[v][u]`, then row-normalize by each row's weight sum for the stochastic transition matrix. Implement `P_{t+1} = P_t * M` as a matrix-vector product parallelized with `#pragma omp parallel for`, writing into a temporary buffer so you don't read-while-write mid-update. Iterate exactly 100 steps per graph. Afterward, add epsilon 1e-9 to every entry and renormalize so entries sum exactly to 1.0 — do this for both P_A and P_B separately before comparing. Compute `sum(P_A[i] * log(P_A[i]/P_B[i]))` with natural log, write the result to the output file formatted `%.6f`. Compile with `-fopenmp -lm` as instructed.
