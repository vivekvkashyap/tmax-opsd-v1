Build and test the pieces separately before assembling `compute_lu.sh`. First, use `h5dump -d /A matrix.h5` (and similarly for `/L`, `/U` in ref.h5) and inspect its text output format so you can parse out just the 9 numeric values per matrix with `awk`/`grep`/`sed` (h5dump's default output has header/footer lines and comma-separated rows in braces — strip those reliably, e.g. with `grep -oE` for numbers). Second, implement Doolittle LU decomposition (no pivoting) for a 3x3 in `awk`: L is lower-triangular with unit diagonal, U is upper-triangular, computed via the standard nested-loop formulas `U[i][j] = A[i][j] - sum(L[i][k]*U[k][j])` and `L[i][j] = (A[i][j] - sum(L[i][k]*U[k][j])) / U[j][j]`. Third, compute element-wise absolute differences between your L/U and the reference L/U extracted from ref.h5, and take the max (L-infinity norm) using `awk` or `bc` for floating point. Fourth, wrap the whole computation with `time` (or capture `date +%s.%N` before/after) to get wall-clock seconds, and format the final report exactly as specified, comparing your max error against `1e-5` to decide PASS/FAIL. Test each awk snippet standalone on the extracted numbers before wiring into the full script, since h5dump formatting quirks are the most likely source of parsing bugs.