Start by scanning `localhost` ports 8000-8500 (e.g. `nmap -p8000-8500 localhost` or a quick bash/python TCP-connect loop) to find the two live services and note which port speaks TLS vs plain HTTP. For the TLS one, use `openssl s_client -connect localhost:<port> </dev/null 2>/dev/null | openssl x509 -noout -subject` (or `openssl s_client ... -showcerts`) to pull the certificate subject line, then extract exactly the value of the `O=` field and write it verbatim to `/home/user/cert_flag.txt` — don't guess or reformat it, copy the exact string from the cert. For the HTTP Basic-Auth service, iterate the given `/home/user/wordlist.txt` and try `curl -u admin:<candidate> http://localhost:<port>/` for each word (a bash loop or Python `requests` loop checking for a non-401 status) until one succeeds; capture the response body text and save it exactly to `/home/user/auth_flag.txt`. Finally, write `/home/user/pentest_summary.json` with `https_port`, `http_port`, and `cracked_password` populated from what you discovered — do not hardcode assumed port numbers, use the values you actually observed during scanning and cracking.
