Work through the archive layers one at a time and verify at each stage before moving on: `tar xzf old_docs.tar.gz` to get internal_drafts.zip, then `unzip internal_drafts.zip` to get the .xrd files — inspect one decrypted file's bytes before trusting the rest. Write the C++ program to open each .xrd file in binary mode, XOR every byte with 0x2F, and write the result out; verify manually (e.g. `xxd` or just printing) that the decrypted text looks like readable sentences, since a wrong XOR key or text-mode file corruption is the most common failure here. After decrypting, do the literal string replacement of `[CONFIDENTIAL_DRAFT]` → `[PUBLIC_RELEASE]` (either inside the C++ program's output buffer or with a follow-up `sed`/text tool pass) — replace exact matches only. Create /home/user/published_docs/, and for each decrypted+replaced file derive the output name by extracting the numeric suffix from file_XX.xrd and writing section_XX.md (be careful the renaming logic generalizes rather than hardcoding three filenames, in case the file count differs). Finally list the resulting .md files' absolute paths, sort them alphabetically, and write them one per line to /home/user/migration_log.txt.
