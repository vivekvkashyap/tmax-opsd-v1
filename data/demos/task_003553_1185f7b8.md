Work the phases in strict order since each depends on the previous. First, crack the zip: use `zip2john /app/keys/new_creds.zip > hash.txt && john --wordlist=/app/wordlist.txt hash.txt` (or a simple bash loop trying each word with `unzip -P`) to recover the password, then extract the three cert/key files into `/app/keys/` and `chmod 0400` the two certs. For Phase 2, write `/app/validator.c` so it: parses the header JSON and rejects immediately if `alg` is `"none"` (before touching crypto); loads and validates the leaf cert against the root CA using OpenSSL's `X509_STORE`/`X509_verify_cert` chain-verification APIs (not just "does it parse") — this is the step naive attempts skip, accepting any leaf; verifies the RSA-SHA256 signature over `header.payload` using the leaf's public key; and calls `stat()` on the leaf cert path checking the mode bits equal exactly `0400`. Compile with `-lssl -lcrypto`, then iterate against every file in `/app/corpus/clean/` and `/app/corpus/evil/` until exit codes are 1 for evil, 0 for clean. Only then wire up Phase 3: fix nginx `location` blocks for `/auth` and `/api`, point `server.py` at `/app/validator`, and reload nginx before testing the end-to-end curl flow.
