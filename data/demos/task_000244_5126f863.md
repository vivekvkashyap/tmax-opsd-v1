Treat this as two independent deliverables. For the 502: don't guess — read `/home/user/app/nginx.conf` and check the `proxy_pass` target against the actual backend address (`127.0.0.1:9090`); a common cause of 502s is the upstream port in the config not matching the real listening service, so diff them carefully rather than assuming the config is otherwise fine. After editing, reload nginx with the exact invocation given (`-c ... -p ...`) rather than a bare `nginx -s reload`, since it's running from a non-default config/prefix path. Confirm with `curl -s http://127.0.0.1:8080/health` before moving on — also verify the backend process on 9090 is actually running and serving `OK` at `/health`; if not, start it. For `waf.sh`: think in terms of decoding + normalization, not a fixed string list. A naive `grep -F '../'` misses URL-encoded traversal (`%2e%2e%2f`, `%2e%2e/`, `..%2f`, mixed-case hex). URL-decode the input first (or check both encoded and decoded forms case-insensitively) before searching for `../`, so all encoding variants collapse to the same check; then print exactly `ACCEPT`/`REJECT` with the matching exit code. Test against several encoded/mixed variants yourself since a hidden corpus will probe exactly these edge cases.