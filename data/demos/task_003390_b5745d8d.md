Handle the two subtasks separately. First, inspect `/app/vendored/query-builder-1.0.0/pyproject.toml` — find the bogus build-system dependency that doesn't exist on PyPI, remove just that entry (keep the legitimate ones like setuptools), then install with `pip install -e /app/vendored/query-builder-1.0.0` and confirm `python3 -c "import query_builder"` succeeds before moving on. Second, for the sanitizer, don't just string-search the raw JSON text — parse it and specifically inspect fields that should be plain identifiers (table names, column names, aliases) for SQL-injection indicators: statement terminators (`;`), comment markers (`--`), and disallowed SQL keywords (`DROP`, `UNION`, or embedded SQL fragments) appearing where only an identifier is expected. A safe identifier should match a strict allow-list pattern (e.g. alphanumeric/underscore only); anything else in an identifier-typed field is suspicious. Print exactly `SAFE`/`UNSAFE` and use matching exit codes. Validate your classifier against both `/app/corpora/clean/` and `/app/corpora/evil/` directories directly, checking every file passes, before considering the task done — a substring blocklist alone is fragile, so combine it with structural identifier validation for robustness.
