Use Python's `tarfile` module opened in streaming read mode (`tarfile.open(path, 'r:gz')`) and iterate its members with `getmembers()`/`getmember()` — never call `.extractall()` or `.extract()` on untrusted entries, since that's exactly the zip-slip vulnerability you're guarding against. For each `TarInfo` member, classify it unsafe if `member.name` starts with `/` or if any path segment equals `..` (check with something like `PurePosixPath(member.name).parts` or splitting on `/` and testing each component, not just a naive substring search for `"../"` which can miss edge cases like a bare `..` as the whole name). For safe members, extract their file object via `tar.extractfile(member)` and write both the data and a matching `TarInfo` into a new output `tarfile.open(new_path, 'w:gz')`, accumulating `member.size` across all safe entries as you go. Name the output using the original archive's basename (minus `.tar.gz`) plus `_safe_<total_bytes>.tar.gz`, and only finalize/rename the file once you know the total safe byte count. Create `/home/user/processed/` if absent, then run your script against everything currently in `/home/user/incoming/`.
