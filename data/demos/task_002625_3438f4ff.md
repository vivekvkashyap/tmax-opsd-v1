Read `/home/user/simulate_markov.c` and locate the hardcoded `double dt = ...;` line — this is the bug, not the loop logic. Replace it with the maximum stable value implied by the CFL condition given in the code's own `D` and `dx`: `dt = (dx*dx) / (4.0 * D)`, computed from those variables (don't hand-copy a numeric literal from elsewhere; derive it from D and dx as declared in that file). Recompile with `h5cc /home/user/simulate_markov.c -o /home/user/simulate_markov`, ensuring `h5cc`/HDF5 dev headers are installed (`sudo apt-get install -y libhdf5-dev` or similar) if missing. Run the binary on `init.h5`/`out.h5`. Then compute the Total Variation distance between `out.h5`'s `P` dataset and `ref.h5`'s `P` dataset using h5py/numpy: load both 50x50 arrays, `0.5 * np.sum(np.abs(P_out - P_ref))`, and write that value formatted to exactly 4 decimal places (e.g. Python f-string `f"{tv:.4f}"`) to `/home/user/tv_distance.txt`. Verify your fix actually stops NaNs/negatives by inspecting `out.h5` values before computing TV.