Run `cargo build` first and read the borrow-checker errors carefully — graph/topological-sort code that fails to compile usually stems from trying to hold `&Node` references while also mutating the owning collection (e.g. a Vec/HashMap) during traversal, or a self-referential struct; fix this by restructuring to use indices/IDs into a central collection instead of raw references, or by cloning small owned data where needed, rather than fighting the borrow checker with `unsafe`. Implement `Serialize`/`Deserialize` (derive them) on your artifact struct so results write back to valid JSON. For the topological sort itself, do a standard DFS-postorder or Kahn's-algorithm traversal over `artifacts_graph.json`'s dependency edges, ensuring dependencies precede dependents in the output order. For the audio artifact, call an external transcription tool (e.g. a Python whisper/SpeechRecognition script) as a subprocess from Rust, or shell out to it, and place its returned text into the `transcription` field for the matching object — do not fabricate the text. Verify `/home/user/final_artifacts.json` is valid JSON, correctly ordered, and includes the transcription before finishing.