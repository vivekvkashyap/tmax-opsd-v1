Two independent pieces must both be exact: an OCR'd constant and a streaming timezone/rounding transform that will be fuzz-tested against a hidden oracle, so match the spec precisely rather than approximating. First, extract the "Peak Multiplier" value from `/app/dashboard_snapshot.png` using `tesseract` (or another OCR tool) on the image — don't guess the number, read it out, and sanity check it's a plausible decimal. Then write `/home/user/projector.py` to read stdin line-by-line (not all at once, so it scales), split each CSV line on commas, parse the UTC timestamp with `datetime.strptime(...,"%Y-%m-%dT%H:%M:%SZ")` then localize to UTC and convert to `Europe/Berlin` using `zoneinfo` or `pytz` (get the DST handling right — Berlin is UTC+1 or +2 depending on date), and format as `YYYY/MM/DD HH:MM:SS`. Multiply cpu_cores and memory_mb by the OCR'd multiplier and round to exactly 2 decimals using standard rounding (e.g. Python's `round()` or `f"{x:.2f}"`) — since it'll be diffed against an oracle doing the same, avoid introducing extra precision drift. Print only the required `[timestamp] service: CPU=..., MEM=...` line per input row, with zero extra stdout output, and make the script executable.
