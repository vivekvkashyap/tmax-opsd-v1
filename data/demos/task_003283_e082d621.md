Build the ring buffer as a fixed-capacity `Vec<f64>` plus a write-index and count, guarded by a `std::sync::Mutex` inside a `static` (e.g. `once_cell`-free pattern using `std::sync::OnceLock<Mutex<RingBuffer>>` or a `lazy_static`-style `static mut` wrapped in a Mutex) so it is safely shared across FFI calls without external crates. `init_metrics` should replace/reset the global state with a new buffer of the given capacity; `record_and_average` should push into the buffer, overwrite the oldest slot once full (classic circular index `idx = count % capacity`), and compute the mean over only the currently-filled slots (not the full allocated capacity) so early calls before the buffer fills give the correct partial average. Mark both functions `#[no_mangle] pub extern "C"` and compile with `rustc --crate-type=cdylib -o /home/user/libmetrics.so /home/user/metrics.rs`. When linking bench.c, pass `-L/home/user -lmetrics` and set `LD_LIBRARY_PATH=/home/user` (or use `-Wl,-rpath,/home/user`) before running, otherwise the dynamic linker won't find the .so at runtime.