Build and test each component independently before wiring them together. For ip_filter.cpp, read the ACL file into memory (handle missing/empty file gracefully — treat as "no match"), read the single CLI argument, do an exact string comparison per line (trim any trailing newline/whitespace from each ACL line first), then append the ALLOW/DENY line to the log with an ofstream opened in append mode, and return the matching exit code — compile with g++ to the exact bin path. For setup_env.sh, make every step idempotent: use `mkdir -p` for directories, always (re)write acl.conf's exact two lines with `>` (overwrite, not append) so repeated runs don't duplicate content, chmod 400 only after writing (since 400 blocks further writes), and use `touch`+chmod 644 for the log only if it doesn't already exist so you don't clobber existing log data. For rotate.sh, perform the shifts in reverse order (3 first would overwrite, so do current .2->.3, then .1->.2, then log->.1, each guarded with `[ -f ... ]` checks) to avoid clobbering data mid-shift, then recreate an empty 644 log. For test_filter.sh, capture each ip_filter call's exit code explicitly (`$?`) rather than relying on `&&/||` short-circuiting across two independent calls.