Build and test each phase independently before chaining them. Phase 1: `user_manager.py` should use `sqlite3`, `CREATE TABLE IF NOT EXISTS users (username TEXT, role TEXT)` on every run (idempotent init), and an `add` subcommand via `sys.argv` that inserts a row — run it three times for the three users. Phase 2: `api.py` should subclass `http.server.BaseHTTPRequestHandler`, override `do_GET`, match path `/users`, open a fresh sqlite connection per request (SQLite connections aren't safely shared across threads/processes), `SELECT username, role FROM users`, serialize to JSON with the exact key names, and set `Content-Type: application/json` before writing the body; read the port from `sys.argv[1]`. Phase 3 is the trickiest part: read/default the active port, compute the other port, `nohup python3 api.py <new_port> &` capturing `$!` as the new PID, then poll `curl -s -o /dev/null -w '%{http_code}' http://localhost:<new_port>/users` in a loop with short sleeps up to 5s total; on success kill the old PID (from `active_pid.txt`, checking it's actually still running first) and atomically rewrite both tracking files; on failure kill only the new instance and exit non-zero, leaving old state intact. Run deploy.sh twice as instructed, then curl the final active port into `deployment_test.json`.