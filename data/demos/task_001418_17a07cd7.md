Write `/home/user/process_backup.c` using the SQLite C API (`sqlite3_open`, `sqlite3_prepare_v2`, `sqlite3_bind_text`, `sqlite3_step`). Construct the SQL with the literal clause `SELECT ... FROM employees NOT INDEXED WHERE status = ?` — `NOT INDEXED` must appear right after the table name in the query text, and bind `"ACTIVE"` via `sqlite3_bind_text` rather than string-concatenating it in (both are explicitly required and checked). As you iterate result rows with `sqlite3_step`, accumulate a running sum of `salary` keyed by `region` in memory (e.g. a small fixed array of struct{region,total} pairs, or a simple linked list, since region cardinality is small) — don't try to do the aggregation in SQL, do it in C to mimic the NoSQL `$group` step as specified. After processing all rows, sort your region buckets alphabetically by region name (e.g. `qsort` with `strcmp`), and hand-format the output as a JSON array of `{"region": ..., "total_salary": ...}` objects, writing it to `/home/user/region_salaries.json`. Compile with `gcc process_backup.c -o /home/user/process_backup -lsqlite3`, run the resulting binary, and check the JSON output parses correctly and matches expected region totals before finishing.