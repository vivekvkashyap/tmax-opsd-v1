For extraction: find the running process by name (e.g. `pgrep -f audit_daemon` or `ps aux | grep audit_daemon`), get its PID, then read its full argument list from `/proc/<pid>/cmdline` (note this file is NUL-separated, so translate NULs to spaces/newlines, e.g. `tr '\0' '\n' < /proc/<pid>/cmdline`) to pull out the first argument exactly as it was passed. Write that value to `/home/user/leaked_secret.txt` with no trailing whitespace or extra newline beyond what's expected — use `printf '%s' "$token" > file` rather than `echo` if you want to avoid an extra newline, or check what the verifier expects.

For remediation: edit `/home/user/src/audit_daemon.c` so that at the very start of `main`, before anything else runs, you overwrite each byte of the `argv[1]` string in place with `*` while preserving its original length (use `strlen(argv[1])` to get the length first, then loop over `argv[1][i] = '*'` for each index) — do not reallocate or reassign the pointer, since `/proc/<pid>/cmdline` reflects the actual process memory backing `argv`. Compile with `gcc` to `/home/user/fixed_daemon` and test by launching it, then inspecting `/proc/<pid>/cmdline` yourself to confirm the redaction is visible.
