Break this into independent, verifiable stages. For the video: use `ffmpeg` to extract all 120 frames as images (e.g. `ffmpeg -i /app/system_monitor.mp4 frame_%03d.png`), then for each frame sample the pixel value at the top-left 10x10 block (e.g. with Python/PIL or ImageMagick) and classify it as white=1 or black=0 based on brightness; assemble the 120 bits in frame order into 15 bytes (8 bits per byte, frame 0 = MSB of byte 0) and convert to ASCII to recover the auth key — do not assume its value, derive it from the frames. For the archive: recursively extract `/app/legacy_configs.tar`, noticing it contains further nested `.tar.gz`/`.zip` layers — write a small extraction loop that keeps unpacking until only `.conf` files remain, placing them in `/home/user/extracted_configs/`; then use `sed`/regex substitution to replace `server_ip=192.168.1.X` with `server_ip=10.0.0.X` (keep X captured, don't hardcode a single X) and `mode=legacy` with `mode=modern` across all extracted files. For the C server, implement a minimal HTTP listener on 127.0.0.1:8080 handling `GET /manifest`: check the `X-Auth-Key` header against your recovered key (return 401 if missing/wrong), otherwise compute SHA-256 (e.g. via OpenSSL's libcrypto or a self-contained implementation) over each `.conf` file's current contents, sort by filename, and emit `filename:hex` lines. Start it in the background and test with `curl` before finishing.