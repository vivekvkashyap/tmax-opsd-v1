Fix the two infra issues independently before wiring the pipeline end to end. CMake: add `target_link_directories(num_service PRIVATE lib)` (or `link_directories`) so the linker finds libencode.so at build time, and to avoid needing LD_LIBRARY_PATH at runtime set an RPATH — either `set(CMAKE_INSTALL_RPATH "${CMAKE_CURRENT_SOURCE_DIR}/lib")` with `set(CMAKE_BUILD_WITH_INSTALL_RPATH TRUE)`, or apply `target_link_options(num_service PRIVATE -Wl,-rpath,'$ORIGIN/../lib')`; verify with `ldd` on the built binary that libencode.so resolves without exporting anything manually. Nginx: inside the `server { listen 8080; }` block add a `location /api { proxy_pass http://127.0.0.1:9000; }` block, leaving the rest of the config (temp paths, pid, etc., needed for non-root operation) untouched. After both fixes, run build_and_serve.sh in the background, start nginx with the fixed config, then send the exact POST body specified in the task to http://127.0.0.1:8080/api with curl, capture the raw response body (it's already the base64 string, no extra parsing needed), strip any trailing whitespace/newline, and write only that string to result.txt.