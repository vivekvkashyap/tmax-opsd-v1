Apply the provided patch to the test runner first with `patch < fix_runner.patch` (or `git apply`) rather than hand-editing — this is what corrects the mismatched start/end block markers so the runner's actual output uses the exact marker strings your parser expects. Run the patched `test_runner.sh`, redirecting stdout to `raw_benchmark.log`, and inspect the raw log yourself to see the real marker text and noise pattern before writing the parser. Build `/home/user/qa/parser.awk` as an explicit state machine: maintain a boolean/flag variable and a running sum plus the current component name; on a line matching the start-block marker, extract the component name (e.g. with `match()`/substr or split on `: `) and reset the sum, entering the "inside block" state; while inside the block, match lines beginning with the iteration prefix and add the numeric millisecond value (strip the `ms` suffix, e.g. with `gsub` or by using `+0` numeric coercion) to the running sum, ignoring anything else including noise lines; on the end-block marker, print `<component>_TOTAL:<sum>ms` and exit the block state. Run the awk script against `raw_benchmark.log`, redirect to `final_report.txt`, and diff your output against your own expectation before finishing — ensure no trailing blank lines or extra text.