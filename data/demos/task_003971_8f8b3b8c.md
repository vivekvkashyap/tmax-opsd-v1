Follow the spec's stages literally and match the reference numerically, since tolerances are tight. Use `scipy.fft.fft`/`ifft` with `fftfreq(n, d=1/1000)` to get true frequency values (not bin indices), and zero out coefficients where `abs(freq) > 50` (strictly greater, so exactly-50Hz bins survive) before inverting; take `np.real(...)` of the inverse. Compute the residual as `raw_signal - smoothed_signal` and fit with `scipy.stats.norm.fit` to get `(mu, sigma)`. For the stability loop, call `np.random.seed(42)` exactly once, generate the full `(50, 1000)` perturbation array in one call (not per-iteration reseeded/regenerated), then loop over the 50 rows, repeating the identical filter+fit pipeline for each perturbed signal, collecting sigmas. Use `np.std(sigmas, ddof=0)` for population std. Write outputs via `h5py.File(..., 'w')` creating datasets `smoothed_signal`, `noise_params` (as `[mu, sigma]`), and `stability_std` (scalar). Create `/home/user/results` first if absent. Sanity-check by re-running the script twice — the seeded stability step should be exactly reproducible.