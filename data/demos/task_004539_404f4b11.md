Model this as graph construction followed by classic cycle enumeration, not string manipulation. Parse the CSV and build two maps: `granted[resource_id] = tx_id` and a list of `(waiting_tx, resource_id)` pairs. Derive directed edges: for each waiting entry, if that resource has a granter, add edge `waiting_tx -> granted[resource_id]`. Then find all elementary (simple) cycles in this directed graph — a DFS-based approach tracking the current path and a visited/on-stack set works well; when you revisit a node currently on the stack, extract the cycle as the path segment from that node to the top. Be careful to dedupe cycles that are rotations of each other (e.g., discovering T1->T2->T3->T1 vs T2->T3->T1->T2 should count once) — normalize by sorting each cycle's elements before storing, and dedupe the resulting sorted lists. Build the JSON manually with `std::ostringstream`, applying both sort rules (alphabetical within each cycle, then outer array sorted by first element) exactly as specified. Compile with the exact `g++ -std=c++17` command given, run it against the CSV, and validate with `jq -c '.deadlocks | map(sort) | sort'` yourself before finishing.
