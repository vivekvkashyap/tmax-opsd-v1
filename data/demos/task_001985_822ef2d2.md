Work this as three independent, separately-testable stages. Phase 1: use ffmpeg to dump raw frames (e.g. `-pix_fmt gray -f rawvideo`) at the video's native 30fps, then compute per-frame mean luminance in a `multiprocessing.Pool` worker function; treat luminance exactly 0.0 as missing and impute with linear interpolation between the nearest non-missing frames (watch edge cases if the first/last frame is missing — extend from the nearest valid value). Write `frame_index,timestamp,luminance` with timestamp = frame_index/30. Phase 2: build `log_sanitizer.py` as a pure CLI classifier — parse the CSV defensively (catch non-numeric strings when converting `sensor_reading` to float rather than letting pandas silently NaN them, since non-numeric injection is itself an anomaly you must detect), then check each rule independently (>50.0 abs consecutive delta, any value <0, >5% missing/NaN, unparseable strings) and exit 1 if any trips, else exit 0. Test it yourself against varied synthetic CSVs covering each anomaly type before trusting it. Phase 3: only aggregate files your own sanitizer exits 0 on, merge on timestamp with `pd.merge_asof`-style tolerance (0.01s) as an outer join, bucket by `floor(timestamp/5)*5` into 5s windows, and round means to 2 decimals per window.