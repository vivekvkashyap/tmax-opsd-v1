Reverse-engineer the decoder empirically rather than disassembling blindly: feed `/app/payload_decoder` known inputs via stdin (e.g. `echo -n "<base64-of-known-text>" | /app/payload_decoder`) and compare outputs, or use `strace`/`ltrace` to watch what library calls it makes; try the hypothesis that it's base64-decode followed by a simple transform (XOR with a fixed byte is a very common CTF-style obfuscation) and confirm by round-tripping a string you control. Get the cert fingerprint with `openssl x509 -in /app/rogue_cert.pem -noout -fingerprint -sha256`, then strip colons and lowercase it (`tr -d ':' | tr 'A-F' 'a-f'`) — do this programmatically in your Rust tool too, don't hardcode the value since it varies per environment. Inspect a few sample JSON files in `/app/training_data/` to confirm the `data_payload` field decodes cleanly with your discovered algorithm and that evil samples contain `EXEC_C2:` plus the cert fingerprint substring, while clean ones don't. Build the Rust binary with `cargo new` at `/home/user/detector`, implement the decode+check+exit-code logic, handle missing/invalid JSON as CLEAN (don't panic), and build with `cargo build --release` so the binary lands at the required path. Test against the sample files in `/app/training_data/` before relying on the hidden corpus.
