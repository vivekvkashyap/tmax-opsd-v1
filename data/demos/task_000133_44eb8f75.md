Build the archive first, then chunk it, then swap in symlinks last (order matters — don't delete the original logs until the archive is safely built and chunked, since tar needs to read them). Use Python's `tarfile` module in `w:gz` mode writing into an in-memory `io.BytesIO` or a temp file, adding each `.log` file found via `glob`/`os.listdir`. Read the resulting bytes and slice into 1,048,576-byte chunks with simple index arithmetic (`data[i:i+CHUNK_SIZE]`), naming them `chunk_%03d.dat` by enumeration index. For atomic writes: write each chunk's bytes to a sibling file like `chunk_000.dat.tmp` in the same target directory (same filesystem is required for atomicity), `flush()`+`os.fsync()` the file descriptor, close it, then `os.rename(tmp_path, final_path)` — `os.rename` is the atomic step, not the write. Only after all chunks are safely renamed, iterate the original log files, `os.remove()` each, then `os.symlink('/home/user/data/offline_placeholder.txt', original_path)` to recreate each name as a symlink. Test reconstruction yourself with `cat chunk_*.dat | tar -tzf -` before finishing.