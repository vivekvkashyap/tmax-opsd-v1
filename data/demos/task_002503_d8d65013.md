Work through the three evidence sources independently before writing any code. (1) Use `ffmpeg` to extract frames from `/app/ci_screen.mp4` (e.g. `ffmpeg -i ci_screen.mp4 out%04d.png`), then look near the end of the sequence for a frame showing a linker/runtime error naming a missing shared library (an "error while loading shared libraries" style message) — OCR it with `tesseract` if needed and record the exact library filename. (2) Recover the corrupted `/app/build_state/cache.db` using its WAL file — copy both files somewhere and run a checkpoint (e.g. open with `sqlite3` and run `PRAGMA wal_checkpoint(FULL);`, or just query directly since SQLite auto-recovers from WAL on open), then `SELECT` the last row from `processed_files` ordered by its rowid/timestamp to find the last asset processed before the crash. (3) Search `/app/build_state/core.dump` for the malicious macro string bounded by `[[[MACRO` ... `]]]` using `strings core.dump | grep` or `grep -a`, and use that exact payload string (not a prefix) as your filter signature. Build `/home/user/filter_assets.sh` to `grep -l` (or equivalent) each `.asset` file in the input dir for that exact payload, copying only non-matching files to the output dir, then `chmod +x` it. Write the two-line report with the library name and last-processed asset filename, in that order, no extra content.