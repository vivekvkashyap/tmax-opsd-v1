Handle this as a linear pipeline where each stage's correctness depends on getting the encodings right. In `process.py`, open each locale file with its exact stated legacy encoding (`ja.txt` as `shift_jis`, `de.txt` as `iso-8859-1`, `ru.txt` as `koi8-r`) using Python's `open(..., encoding=...)`, read the two lines as title/content, then write the substituted template out with `open(..., encoding='utf-8')` explicitly so the HTML files are true UTF-8 regardless of the source encoding. For `metadata.csv`, compute lengths with Python's `len()` on the decoded Unicode strings (this naturally counts characters, not bytes — don't measure `len(text.encode())`), and be careful about trailing newlines when reading lines (strip them before counting). In `pipeline.sh`, run the Python script first, then use `sqlite3 locales.db` with `.mode csv` and `.import` (skipping the header row, e.g. import to a staging setup or use `--skip 1` if your sqlite3 version supports it) to bulk-load `metadata.csv` into a `metadata` table with the specified column types; chmod the script executable. Finally write the cron line as `0 2 * * * /home/user/pipeline.sh` in standard 5-field crontab syntax.