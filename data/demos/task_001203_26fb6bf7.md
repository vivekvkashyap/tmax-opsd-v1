Locate the bug precisely before touching anything: the nested loop over tables and foreign keys prints a relationship for every (table, fk) pair regardless of whether they're actually related — add the missing condition that only prints when `fks[j].source_id == tables[i].id`, turning the implicit cross join into a proper inner join. Recompile with `gcc graph_mapper.c -o graph_mapper` and run it, redirecting stdout to the output file, then pipe through `sort` (or use `sort` on the file afterward) since the output must be alphabetically sorted — don't rely on insertion order matching sort order. For the aggregation step, parse the corrected output file's lines to extract the `Backup_<id>` at the start of each line (e.g. with `grep -oP` or awk field splitting on `_` and `)`), count occurrences per backup ID, and write a CSV with header `backup_id,fk_count` where rows are sorted numerically ascending by backup_id (numeric sort, not lexical, if IDs could have different digit counts). Verify your counts by manually cross-checking a couple of backup IDs against the tables/fks input files before finalizing.