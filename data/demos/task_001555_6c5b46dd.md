Write `/home/user/clean_data.py` as a straightforward pandas pipeline, but be careful about the order of operations. Use `glob` to load and `pd.concat` every CSV under `/home/user/telemetry_data/`. Apply the two cleaning filters first — drop rows with `latency_ms < 0` and drop rows where `event_type` is null or `== ''` — before computing the `payload_size` median, since the median must be the median of the surviving valid values (compute it with `.median(skipna=True)` and `.fillna()` only that column). Then derive `is_high_latency` as `(latency_ms > 1000).astype(int)`, strictly greater-than, not >=. Group by `user_id` and aggregate: `event_count` via `.size()` or `.count()`, `avg_latency` via `.mean()` cast to float, `high_latency_count` via `.sum()` on the new column. Reset the index, reorder/rename columns to exactly `user_id,event_count,avg_latency,high_latency_count`, sort by `user_id` ascending, and write with `to_csv(..., index=False)`. Run the script afterward so `/home/user/cleaned_summary.csv` is actually produced, then spot-check a couple of rows by hand against the raw CSVs to catch any filter-order or off-by-one mistakes.