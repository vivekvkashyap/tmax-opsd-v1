Write `/home/user/analyze_pipeline.sh` as a single pass over `/home/user/processed/data.csv`: skip the header, then for each row extract the `user_id` field (second CSV column) and classify it as corrupted if it contains a literal `.` or the substring `NaN`; tally total rows and corrupted rows (e.g. with `awk -F,` or a `while IFS=, read` loop, being careful to actually skip line 1). Compute `P(Corrupt) = corrupted/total` as a raw ratio you'll feed into `bc`. Then apply Bayes' theorem symbolically: `P(Fraud|Corrupt) = P(Corrupt|Fraud) * P(Fraud) / P(Corrupt)`, using the given priors `P(Fraud)=0.05` and `P(Corrupt|Fraud)=0.80`. Perform the actual division in `bc` with `scale=4` set before the division (note `bc` truncates rather than rounds, and scale must be set on the calculating expression, not just declared and ignored). Write only the resulting 4-decimal number, no extra text or trailing newline artifacts beyond what's expected, to `/home/user/posterior.txt`.
