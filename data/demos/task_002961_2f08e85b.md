Work through the three issues in dependency order. First inspect `Makefile` in `/app/fin_calc-1.2.0` and `requirements.txt`/`pip install` output to find the broken install step (typo'd package names are common) and fix it so `make install` succeeds. Second, use `git log` and `git bisect` (or manually diff commits) across the 200-commit history to locate exactly where the `/variance` endpoint's arithmetic switched from Python's `decimal.Decimal` to native `float` — search commit messages/diffs for `app.py` changes touching variance/mean calculations, then revert that logic in the current `app.py` to use `Decimal` consistently for all sums, divisions, and exponents (mixing float and Decimal will raise TypeErrors, so convert all inputs via `Decimal(str(x))` up front). Third, wrap the numeric parsing of the input array in a try/except catching `InvalidOperation`/`ValueError`, returning `jsonify({"error": "corrupted input"}), 400` on failure instead of letting it fall through to a 500. Keep the existing `X-Auth-Token: super-secret-token-99` check intact, bind the Flask app to `127.0.0.1:8080`, and leave it running in the background for the test suite to hit.