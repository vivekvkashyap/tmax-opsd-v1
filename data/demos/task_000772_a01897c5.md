Work the ticket in order. First, recover the secret: run `git -C /app/auth-service log --all -p` (or `git log -p -- src/main.rs`) and grep the full diff history for `API_KEY`/`SECRET` constants — an older commit still has the plaintext key even if HEAD removed it; write it into `/app/auth-service/.env` as `SECRET_API_KEY=<recovered_value>`. Second, open `src/main.rs` and locate every `.unwrap()` on the base64-decode call, the `String::from_utf8(...)` conversion, and the JSON extraction of `client_id`. Replace the base64 unwrap with a match/`?` that returns a 400 response on `Err`, replace missing/null `client_id` handling with an explicit check returning 400, and swap `String::from_utf8(bytes).unwrap()` for `String::from_utf8_lossy(&bytes)` so invalid UTF-8 becomes the replacement character instead of panicking. Also fix the base64 decoder to accept unpadded input (e.g. use `base64::engine::general_purpose::STANDARD_NO_PAD` or a permissive decode path) rather than erroring on it. Then start `redis-server --port 6380 --daemonize yes`, `cargo build --release`, and launch the binary bound to 127.0.0.1:8080 in the background, confirming it reads `.env` at startup.