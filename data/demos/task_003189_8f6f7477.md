Two phases, do them in order since phase 2 needs phase 1's derived key. For the video, extract frames around the terminal window with `ffmpeg -i /app/incident_record.mp4 -vf fps=2 frames/%03d.png` (or similar sampling), then visually/OCR-inspect the frames showing a terminal to find the `curl` command and its `redirect_uri=` value — read the actual characters carefully, don't assume. Take the first 6 characters of that recovered value, build the password as `DevSecOps_<those 6 chars>`, and run the exact `openssl enc -d -aes-256-cbc -pbkdf2 ...` command given, with `-pass pass:<derived_password>`, to produce `/home/user/policy.txt`. Read that decrypted policy text to confirm/derive the exact rule wording and precedence order before writing the script. For the bash script, use `[[ "$1" == *pattern* ]]` glob matching (not regex/eval) in strict if/elif precedence order matching the policy, since correctness under adversarial fuzzing with special characters (backslashes, newlines, quotes) depends on always quoting `"$1"` and avoiding word-splitting or command substitution of user input. Test it manually against a few of the documented example inputs before considering it done.