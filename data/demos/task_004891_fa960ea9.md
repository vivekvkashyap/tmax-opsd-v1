Build an HTTP service on `127.0.0.1:8000` (Flask or `http.server`) with `POST /extract`. Reject any request whose header is not exactly `Authorization: Bearer graph-token-999` with HTTP 401 before doing any work. Parse `{"num_nodes": N, "edges": [[u,v],...]}` into a symmetric adjacency matrix. Regularize so the Laplacian is non-singular: run 5000 random walks, each length 10 from a random start node, and for each walk add a weight-`0.01` edge between its start and end node — this connects the disconnected components that would otherwise crash the oracle. The walk simulation MUST use `multiprocessing` with at least 4 processes (e.g. `Pool` mapping chunks of walks); each worker returns its edges/end-node counts which you merge. Normalize the adjacency matrix, then write it as an N-row, N-column headerless comma-separated CSV to a unique `tempfile` path and run `/app/matrix_oracle <csv>` via `subprocess`, parsing the comma-separated floats from stdout as the embedding. Compute TV distance as `0.5 * sum(|freq_i - 1/N|)` where `freq_i` is the fraction of walks ending at node `i`. Respond HTTP 200 `{"embedding": [...], "tv_distance": <float>}`. Keep the server running in the foreground/background so it stays queryable.
