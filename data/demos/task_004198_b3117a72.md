Do the cleaning in bash/awk as a pipeline you can test row-by-row against `/app/detections.csv`: first filter out rows where frame_id or object_id is empty, "NaN", or "NA" (e.g. with `awk -F, 'NR==1 || ($1!="" && $1!="NaN" && $2!="" && $2!="NaN")'`), then strip the trailing `.0` from float-formatted ints (careful: use something robust like `awk` printf `%d` conversion or string truncation on the decimal point, not a fragile regex), then compute `area = w*h` and emit the header `frame_id,object_id,x,y,w,h,area`. Verify the cleaned CSV matches your own hand-check of the sample rows before moving on. For frame extraction, use ffmpeg with `-vf fps=1` against `/app/traffic.mp4`, count the resulting frame files, and append `Total Frames: <count>` to the log — don't hardcode a count, derive it from what ffmpeg actually produced. For the API, write a small Python HTTP server (stdlib `http.server` or Flask) that on startup loads the cleaned CSV, computes `/area` by summing the `area` column filtered by `object_id` (compare as integers), and gates `/secure_stats` on an exact `Authorization: Bearer ds_token_2024` header match, returning 401 otherwise. Run the server backgrounded and curl-test both endpoints yourself before finishing.