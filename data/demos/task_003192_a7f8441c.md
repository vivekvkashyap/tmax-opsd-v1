Treat this as fix-the-library-then-build-a-consumer. First get `/app/vendored/libsec-jwt/` compiling: run `make` in that directory, read the error, and check the `Makefile`'s compiler flags — an overly old C++ standard flag (e.g. `-std=c++98`) will choke on modern C++11+ syntax used in the source; bump it to a modern standard like `-std=c++17`. Second, audit the validation source (likely `src/validator.cpp`) specifically for the classic JWT "alg=none" vulnerability: look for a conditional that short-circuits signature verification when the algorithm header equals `"none"` (e.g. `if (alg == "none" || verify_hmac(...)) return true;`). Remove that bypass entirely and require the algorithm to strictly equal `HS256`, only returning true if the HMAC signature actually verifies against the provided secret. Third, write `/home/user/policy_enforcer.cpp` that takes the secret as argv[1], reads `Bearer <token>` lines from stdin, calls into the fixed library to validate signature/algorithm, then applies the business policy (not expired vs. the fixed timestamp 1700000000, `role` claim must be `admin`) and prints `PASS`/`BLOCK` per line — any parse/signature/expiry/role failure is `BLOCK`. Compile and link against the fixed shared/static library to produce `/home/user/build/policy_check`, and test manually with hand-crafted `alg=none` and forged-signature tokens to confirm they're blocked before considering it done.
