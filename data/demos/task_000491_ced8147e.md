Work through this pipeline stage by stage, verifying each before moving to the next. Compile `generator.cpp` with `g++ -O3` exactly as given — don't modify its RNG logic, since downstream determinism depends on it. For `bootstrap.cpp`, implement the resampling procedure literally as specified rather than approximating: read all doubles from stdin into a vector, seed `std::mt19937` with 42 once, then for each of 1000 resamples draw `count` indices via `std::uniform_int_distribution<size_t> dist(0, count-1)` calling the *same* generator instance sequentially (not re-seeding per resample), average the drawn values, store the 1000 means, sort them, and take indices 24 and 974 as the CI bounds — get these exact index/seed/distribution details right since the verifier recompiles its own reference bootstrap and expects your printed width to match precisely, so mismatched RNG usage order will produce different draws even with the same seed. Test your compiled `bootstrap` on a small hand-fed input first to sanity check the printed width is reasonable. Then loop N over 100, 500, 1000, 5000, 10000 in that fixed order, piping `generator N | bootstrap`, and stop at the first N where the printed width is strictly less than 0.12 — don't skip ahead or test out of order. Finally regenerate training data at that N and write the log line in the exact `N width` format.
