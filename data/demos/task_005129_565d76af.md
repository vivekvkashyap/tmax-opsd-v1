Split this into independently-verifiable layers and test bottom-up. In semver.c, parse each `X.Y.Z` string by splitting on `.` (`sscanf(s, "%d.%d.%d", &maj,&min,&patch)` is sufficient since no pre-release/build metadata is required) and implement `get_latest_version` by comparing major, then minor, then patch numerically (not lexicographically — this matters because e.g. "2.10.0" must beat "2.2.0" despite "10" sorting before "2" as a string). Compile it into a shared library via a Makefile target using `gcc -O3 -shared -fPIC -o libsemver.so semver.c`, and sanity-check the .so loads with a quick `ctypes.CDLL('./libsemver.so')` in a Python REPL before wiring up the server. In resolver_api.py, use `ctypes` to declare the function's argument/return types explicitly (an array of `c_char_p` and a `c_int` count, returning `c_char_p`) since ctypes won't infer this correctly by default. Build the HTTP server with `http.server.BaseHTTPRequestHandler`/`HTTPServer`, implement `do_POST` to read `Content-Length` bytes, parse JSON, call into the C function, and return `{"latest": ...}` as JSON. Write the exact readiness log line to pipeline.log only once the server socket is actually bound and about to serve, then run it as a background/daemon process so it stays up for testing.
