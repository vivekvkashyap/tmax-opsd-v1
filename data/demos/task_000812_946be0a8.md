Load the CSV with `csv.DictReader` (or pandas), keeping `weight` and `timestamp` as numeric types (not strings, else sort order breaks on lexicographic comparison). Build the deterministic evaluation order with a single sort key tuple: `(-weight, -timestamp, tx_id)` — descending weight, descending timestamp as tiebreak, then ascending tx_id as final tiebreak. Start with an empty `networkx.DiGraph()`. Iterate the sorted edges one at a time; for each, add the edge, then check acyclicity — the reliable way is to add the edge and call `nx.is_directed_acyclic_graph(G)` (or catch a cycle via `nx.find_cycle`), and if it's no longer a DAG, remove that specific edge (`G.remove_edge`) and move on. Do NOT batch-add all edges and then strip cycles afterward — the order in which edges are rejected matters and must follow the greedy sequential rule exactly. Track accepted edges' tx_id and weight as you go. At the end, sum weights of accepted edges and alphabetically sort the accepted tx_ids before writing the final JSON with keys `total_weight` and `accepted_tx_ids` to the specified output path.
