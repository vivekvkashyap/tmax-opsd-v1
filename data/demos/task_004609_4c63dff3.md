To recover the C2 token from `/home/user/investigation/c2_agent.pyc` without source, load and disassemble the code object (e.g. `marshal.loads` on the bytecode after the header, then `dis.dis`) and look at the constants table / `LOAD_CONST` instructions for the hardcoded string literal returned by the auth function. Use that exact token as a Bearer token: `GET http://127.0.0.1:8080/evidence` with header `Authorization: Bearer <token>`, and parse the JSON response for `secret_flag`. For the CSP analysis, iterate `/home/user/investigation/traffic.json` and compare each entry's `original_csp` against `modified_csp`: flag a domain as compromised if `unsafe-inline` was added into `script-src` that wasn't there before, OR if the `script-src` directive is present in the original but missing entirely from the modified CSP — don't just diff the raw strings, parse out the `script-src` piece specifically since unrelated directive changes shouldn't count. Assemble `/home/user/evidence_report.json` with the exact three keys requested, sorting `compromised_domains` alphabetically.
