Work in two clearly separated phases. First, sequentially (or concurrently, it's cheap) walk `/home/user/backups/node_*`, read each `etc/ssh/sshd_config`, and rewrite the `PubkeyAuthentication no` line to `PubkeyAuthentication yes` (in-place edit, e.g. read lines, replace, write back). Second, launch the 10 calls to `/app/img_builder <input_dir> <output_file>` concurrently using goroutines plus a `sync.WaitGroup` (or a worker pool), each invoked via `exec.Command` — since the tool sleeps ~1.5s per call, running them sequentially would take ~15s and blow the 3s budget, while running them in parallel keeps total wall-clock near 1.5-2s. For each `exec.Command`, explicitly set `cmd.Env` to include `TZ=UTC` and `LC_ALL=C` (don't just rely on the parent shell's env — you must construct the full env slice, typically `append(os.Environ(), "TZ=UTC", "LC_ALL=C")`). Collect errors from each goroutine and fail loudly if any fail. Make sure `/home/user/images/` exists before invoking the builder, and name outputs `node_<i>.qcow2` matching each backup dir's index.
