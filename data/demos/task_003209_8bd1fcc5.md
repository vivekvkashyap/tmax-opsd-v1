Work through the four parts sequentially, verifying each with a direct check before moving to the next. (1) Launch QEMU: `qemu-system-x86_64 -m 128 -vnc 127.0.0.1:5 -daemonize -pidfile /home/user/qemu.pid` — confirm with `cat /home/user/qemu.pid` and `ps -p $(cat ...)`, and check the VNC port with `ss`/`netstat` or `nc -zv 127.0.0.1 5905`. (2) Write `analyzer.cpp` to open `/home/user/qemu.pid`, read the pid, open `/proc/<pid>/statm`, parse the first whitespace-separated field (vmem in pages), and write `{"vmem_pages": <n>}` (no extra whitespace/newlines that would break the regex check) to the path in `getenv("METRICS_FILE")` — compile with `g++`, append the export line to `~/.bash_profile`, then run it with `METRICS_FILE=/home/user/metrics/current.json ./analyzer` to actually produce the file. (3) Generate certs with `openssl req -x509 -newkey rsa:2048 -nodes -days 365 -keyout key.pem -out cert.pem`, then write an unprivileged nginx.conf that redirects all temp/pid/log paths into `/home/user/nginx_temp/`, listens `8443 ssl`, serves `/home/user/metrics/` as root, and start with `nginx -c /home/user/nginx.conf`; test with `curl -k https://127.0.0.1:8443/current.json`. (4) Write `backup.sh` doing `tar -czf /home/user/backups/metrics_backup.tar.gz -C /home/user/metrics current.json` and run it once. Create every target directory before writing into it.