First probe the binary manually (`/app/stellar_core_sim 1.0` a few times) to learn its CLI and stdout format before scripting anything, then extract the numeric value with `grep -oP` or `awk`. In `analyze_core.sh`: loop the radius from 0.1 to 10.0 in 0.1 steps (generate the sequence with `seq` or an `awk` loop to avoid bash floating-point issues), and at each radius call the binary 50 times, capturing all readings into per-point sample files/arrays. Do all arithmetic with `bc -l` or `awk` (required — no Python/R/C). Compute the mean density per radius, then numerically integrate `4*pi*r^2*density` over the grid with the trapezoidal rule (remember density is effectively 0 at r=0, which isn't sampled). For the bootstrap CI: repeat ≥500 times — each iteration resample-with-replacement from the 50 raw observations per radius (use `$RANDOM` for index selection), recompute the per-point mean, re-integrate to get one bootstrap mass estimate, and collect all 500+ masses. Sort them numerically (`sort -n`) and pick the values at the 5th and 95th percentile ranks for CI_LOWER/CI_UPPER. Print exactly the three required lines.
