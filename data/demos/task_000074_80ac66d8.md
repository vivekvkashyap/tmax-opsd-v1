Write a small binary parser in Python using the `struct` module: open each file, read and validate the 4-byte `CONF` magic plus 1-byte version, then read the 2-byte little-endian unsigned short record count, and loop that many times reading key-length (1 byte), the key string, a 1-byte value type, then either a 4-byte little-endian signed int (`<i`) or a 2-byte length-prefixed ASCII string, returning a dict per file. Parse both `config_v1.bin` and `config_v2.bin` into two dicts, then diff them: iterate the union of keys — if a key is only in v1 it's `"removed"` (new_value null), only in v2 it's `"added"` (old_value null), present in both with different values it's `"modified"`, and skip keys with identical values in both. Watch out for the common bug of comparing values with mismatched types (e.g. int vs string) causing false "modified" entries, and for reading the key/string bytes with the wrong length variable. Write the diff dict to `/home/user/diff.json` with `json.dump`, using only stdlib.