Scaffold with `cargo new migrator` in `/home/user`, then add a JSON dependency (e.g. `serde`/`serde_json` in Cargo.toml) so you parse the array robustly rather than hand-rolling a parser. In `main.rs`: read and parse `/home/user/migration_data/legacy_services.json`, filter entries where `status == "migrated"`, and for each write the two required lines using the exact formats given in the task (`ip route add <ip>/32 via 10.99.0.1` and `<nfs_export> /home/user/mnt/<service_name> nfs defaults,user,noauto 0 0`) preserving JSON array order. Since the script must be idempotent/overwrite-on-rerun, open both output files with truncate (`File::create`, not append) each run, write the shebang line first for `apply_routes.sh`, then `set_permissions` to add the executable bit (mode 0o755) after writing. Build with `cargo build --release`, run the binary once manually to populate `/home/user/output/`, then install a cron entry with `(crontab -l 2>/dev/null; echo "*/5 * * * * /home/user/migrator/target/release/migrator") | crontab -` and dump it via `crontab -l > /home/user/output/crontab_dump.txt`.
