Build the server around a directory walker that tracks visited real paths to break symlink cycles: when you encounter a symlink, resolve it with `os.path.realpath`, check it against a `visited` set for the CURRENT recursion branch (pass a copy or remove-on-backtrack, not a single global set that would wrongly block sibling branches), and skip recursing into it if already visited — but still include legitimate symlinked files (like the one pointing outside media_assets) by hashing them once. Compute SHA-256 with `hashlib.sha256()` read in chunks for each regular file, keyed by its path relative to /app/media_assets. For auth, check every request's `Authorization` header equals exactly `Bearer backup_token_2024` before doing any work, returning 401 otherwise. For /thumbnail, avoid temp files: spawn ffmpeg with `-ss 5 -i /app/media_assets/surveillance.mp4 -frames:v 1 -f image2pipe -vcodec png -` via `subprocess.Popen` with stdout=PIPE, read the PNG bytes directly from the pipe, and stream them back with `Content-Type: image/png`. Use `http.server.ThreadingHTTPServer` (or Flask) so /manifest's directory walk doesn't block /thumbnail requests, launch it backgrounded (nohup/&), and manually curl both endpoints — with and without the auth header — to confirm behavior before finishing.