Fix the routing chain in order, testing each hop before moving to the next. First inspect `/home/user/nginx.conf` to find the wrong `proxy_pass` target and change it to `http://127.0.0.1:8000` (your WAF's port) — leave Flask's own port untouched since the WAF, not nginx, talks to it directly. Write `waf.py` as a simple HTTP server/reverse proxy (e.g. `http.server` + `urllib`/`requests`, or a small asyncio server) that forwards method/headers/body to `127.0.0.1:5000` and relays the response back, plus a `/status` route returning "WAF OK". Before adding filtering logic, read every file in `/app/corpus/clean/` and `/app/corpus/evil/` to find the actual discriminating pattern (e.g. suspicious substrings/structure present only in evil payloads) rather than assuming what's malicious, encode that check in the WAF, and log rejected payload signatures to waf_rejects.log. Add an nginx `location /health { proxy_pass http://127.0.0.1:8000/status; }` block, then reload with `nginx -s reload` (not restart) so existing connections aren't dropped. Finally, write monitor_quota.sh to `stat -c%s` (or `wc -c`) the log file, and if size > 1024, truncate it (`: > file` or `truncate -s 0`) and print the exact required message. Start waf.py in the background and verify each endpoint with curl before finishing.