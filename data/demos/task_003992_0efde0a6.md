Two separate diagnostic tasks — a numerical-optimization bug and a binary reverse-engineering puzzle — solve them independently. For the finite-difference bug: run `gradient_descent.py` as-is and observe it diverge; the two suspects named in the task ("epsilon" and "learning rate") should be inspected directly in the source — an epsilon far smaller than the cost function's own precision causes catastrophic cancellation in `(c_x_plus - c_x)/epsilon` (subtracting two nearly-equal floats and dividing by a tiny number amplifies noise), and a learning rate that's too large relative to the gradient scale causes overshoot/divergence; increase epsilon to a value appropriate for the cost function's output precision (test empirically — print the gradient at a few points and check it's stable) and shrink the learning rate until iterating actually converges monotonically, then write x rounded to 4 decimals to optimal_x.txt. For the backdoor: don't guess — use `strings`, `objdump -d`, or `gdb` on the `cost_function` binary to find the comparison against a hardcoded magic constant and the associated printed token, then invoke `./cost_function <magic>` yourself to capture its real stdout and save that output to secret.txt.
