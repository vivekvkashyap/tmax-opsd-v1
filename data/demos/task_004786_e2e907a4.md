Part 1 (extraction): extract frames of `/app/dashboard.mp4` (`ffmpeg -i dashboard.mp4 frame_%04d.png`), read the (0,0) pixel's R,G,B per frame (e.g. ImageMagick `convert frame.png -format "%[pixel:p{0,0}]" info:`), decode each channel back to its ASCII character (R→1st, G→2nd, B→3rd), and concatenate across frames in filename order into one raw text stream. Part 2 (Rust `/home/user/tracker`): parse each line strictly as 4 whitespace-separated fields, discarding malformed ones (bad `u64` timestamp, non-alphanumeric/underscore key); group events by timestamp preserving order, collapse to the *last* event per key at that timestamp (a map overwritten in arrival order does this), then apply SET/UPDATE/DELETE against the running state; emit a line only if state actually changed that timestamp, with sorted keys and the `EMPTY` case. Build with `cargo build --release`, pipe the extraction output into the binary, save stdout to `/home/user/final_state.log`. Since correctness is fuzz-tested bit-exactly, focus on exactly replicating the dedup-then-apply order and the "emit only on change" rule.
