Start by reading the disassembly file carefully with `dis`-style opcode reading: trace LOAD_CONST instructions to find the literal secret string, and follow the sequence of BINARY_ADD / string concatenation and CALL instructions (hashlib.sha256, .hexdigest, base64.b64encode) to reconstruct the exact token construction order — the order in which secret, user, and CSP are concatenated matters for reproducing the same hash, and whether base64 encoding is applied to the raw fields before joining with delimiters. Verify your reconstructed algorithm by running it against the given sample token: decode/recompute the checksum yourself and confirm it matches what's in `/home/user/app/sample_token.txt` — if it doesn't match, your reconstructed order or hash input is wrong, so re-check the disassembly rather than guessing. Once the generator function is verified against the sample, reuse the exact same function to forge a token for `admin-pipeline` with the specified compliant CSP. For CSP enforcement, do a case-sensitive substring check for `unsafe-inline`/`unsafe-eval` specifically within the `script-src` directive (split the CSP on `;`, find the `script-src` segment, then check for those keywords within it) rather than checking the whole CSP string, since other directives could contain similar-looking text. Write the four required fields to the JSON exactly as specified.