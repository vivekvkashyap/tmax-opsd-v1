Build and test each layer standalone before wiring them together with deploy.py. First write `edge_app/app.py` reading `PORT` and `SENSOR_ID` from `os.environ`, serving `/health` with the exact JSON shape via Flask/FastAPI or the stdlib `http.server` — run one instance manually with env vars set and `curl` it to confirm the response before automating. Second, write `haproxy.cfg` with a `frontend` bound to `*:9000` forwarding to a `backend` listing three `server` lines at 127.0.0.1:9001/9002/9003 with `balance roundrobin`; keep it in user space by omitting `user`/`group` directives, not backgrounding as a system daemon, and setting `pidfile /home/user/haproxy.pid`, and validate the config with `haproxy -c -f haproxy.cfg` before scripting around it. Third, write `deploy.py`: kill any stale PIDs from a previous run (e.g. by reading an old pids.log or matching process command lines), launch the three app instances with `subprocess.Popen` passing the right env per instance, launch haproxy with `-f haproxy.cfg -D` (or backgrounded another way) capturing its PID, poll each `/health` endpoint (direct ports first, then through 9000) until all respond before writing `pids.log`, then write the four `name:pid` lines in the required order. Run `deploy.py` and curl port 9000 repeatedly yourself to confirm round-robin across all three sensor IDs before finishing.
