Break this into independent pieces and verify each before wiring them together. (1) Frame extraction: use a video library (e.g. OpenCV in Python, or ffmpeg piping raw frames) to read each frame of `/app/experiment.mp4`, convert to grayscale, and compute the mean pixel value — write `frame_index,intensity` rows to `/home/user/raw_signal.csv`. (2) Cleaning: compute mean and *sample* standard deviation (ddof=1, not population) of the intensity column first, then drop rows where `abs(intensity - mean) > 3*std`, keeping the remaining rows in their original frame order (do not renumber frame_index). Apply the 5-point right-aligned moving average over the surviving sequence, using however many points are available for the first four positions — implement this as a simple sliding window over the filtered list, not the original unfiltered series. Write output as JSON list matching the exact schema given. (3) Wrap steps 1-2 in `pipeline.sh`, emitting the four exact log lines in order via `echo >> /home/user/pipeline.log`. (4) Build a minimal HTTP server (Python `http.server`/Flask/similar) enforcing the Bearer token on every route before returning data, and run it detached (e.g. `nohup ... &`) so it stays up after your turn ends.