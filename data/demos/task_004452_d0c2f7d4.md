First run OCR (e.g. `tesseract /app/deploy_info.png stdout`) to recover the exact MIN_VERSION and WS_PORT values — do not guess them. Assemble the shared library from the provided `.s` file with something like `gcc -shared -fPIC -o /app/lib/libasm_checker.so /app/src/asm_checker.s` (or `as` then `ld -shared`), then fix `/app/CMakeLists.txt` to add `link_directories(/app/lib)` (or full path) and `target_link_libraries(config_filter asm_checker)`, plus set rpath or export `LD_LIBRARY_PATH` so the built binary can find the `.so` at runtime. In `main.cpp`, declare `extern "C" int verify_magic(int);`, use `nlohmann::json` to parse the config file, implement a semver parser/comparator (split on `.` and `-`, compare major.minor.patch numerically, then handle pre-release suffixes) against the OCR'd MIN_VERSION, validate `ws_endpoint` with a literal prefix check against `ws://secure.local:` plus a suffix/port check against the OCR'd WS_PORT, and call `verify_magic()` on the `magic_code` field. Return 0 only if all three checks pass, else 1. Test against both `/app/clean/` and `/app/evil/` before considering it done, since hidden corpora follow identical rules.