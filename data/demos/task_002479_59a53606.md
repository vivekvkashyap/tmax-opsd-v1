Reverse-engineer `/home/user/filter_engine` methodically before writing any decoder: run `strings /home/user/filter_engine` to look for a suspicious 8-hex-character literal used as a comparison target near the start of packet processing (that's your magic signature), then use `objdump -d` to trace the function that handles matching packets — look for a loop doing byte-by-byte XOR against a constant (a single repeated immediate value in the disassembly is a strong signal of a single-byte XOR key). Confirm your hypothesis by testing it against a couple of lines in `/home/user/capture.hex` that start with the discovered signature, checking whether XOR-decrypting the remaining hex bytes with the candidate key yields printable ASCII. Once confirmed, write `decoder.c` to: read `capture.hex` line by line, skip any line whose first 8 hex chars don't match the magic signature, parse the remaining hex pairs into bytes, XOR each with the recovered key, and print the resulting ASCII characters. Compile with `gcc decoder.c -o decoder`, run `./decoder > /home/user/secret.txt`, and manually verify the output is a coherent readable message (not garbage) before finishing — garbled output means the key or signature was misidentified.