Implement PCA from scratch in `/home/user/prepare_data.py` step by step, matching each spec detail exactly since the grader compares numerically: (1) `pd.read_csv` the input; (2) bootstrap with `df.sample(n=10000, replace=True, random_state=42)` — use this exact call so the resampled rows are reproducible; (3) standardize using population std (`ddof=0`), not sample std — this is a common off-by-one mistake; (4) build the covariance matrix with `np.cov(data, rowvar=False, bias=True)` (bias=True again avoids the ddof mismatch); (5) get eigenvalues/eigenvectors via `np.linalg.eigh` (symmetric-matrix solver, keeps eigenvectors real and consistently ordered by ascending eigenvalue) then reverse-sort by eigenvalue descending; (6) take the first two sorted eigenvector columns and project the standardized data onto them via matrix multiplication; (7) round to 4 decimals and write a CSV with header `PC1,PC2` and exactly 10000 rows. Note eigenvector sign is arbitrary/implementation-dependent — don't try to force a particular sign, the grader tolerates flips. Run the script afterward and sanity check the output shape and column names.
