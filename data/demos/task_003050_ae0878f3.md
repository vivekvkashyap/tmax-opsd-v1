Build the Go pipeline in layers and test each before combining. First get JSONL decoding right: read `/home/user/telemetry.jsonl` with `bufio.Scanner` line-by-line (don't load the whole file), and for each line call `json.Unmarshal` into a struct — on error, increment a skipped-line counter and `continue` rather than aborting, since some lines contain deliberately malformed `\u` escapes that will fail to parse; verify this by counting how many lines your program skips versus total lines. Next filter to `locale == "es-MX"` only, and implement the rolling average as a sliding window over the last 5 valid es-MX `latency_ms` values (use a slice/queue, average whatever's currently in it — 1 to 5 elements — so early records average over fewer values); test this function in isolation against a small handcrafted sequence to confirm the windowing logic (especially whether the window is inclusive of the current record) before trusting it on the full file. Then set up `/home/user/telemetry.db` with `go mod init`, add a SQLite driver, create the `es_mx_stats` table with the exact schema given, and bulk-insert all processed rows inside a single `db.Begin()`/`tx.Commit()` transaction using a prepared statement. After running, spot-check results with `sqlite3 /home/user/telemetry.db "SELECT * FROM es_mx_stats;"` to confirm row count and values look sane relative to the input file.