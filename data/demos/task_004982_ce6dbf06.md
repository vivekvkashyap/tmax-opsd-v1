Start by extracting the hardcoded token from the ELF binary yourself using standard static analysis tools rather than guessing — `strings /home/user/legacy_worker | grep -i token` (or similar filtering) will typically surface embedded ASCII secrets in an unstripped binary; cross-check with `objdump -s`/`nm` if `strings` output is noisy. Once you have the legacy token, write `secure_proxy.py` using only stdlib (`http.server`, `subprocess`, `urllib.request` or raw `socket`): launch the legacy worker as a background subprocess bound to port 8080, then implement your own handler on port 9090 that inspects the incoming `Authorization` header, compares it exactly to the new rotated token, returns 403 immediately with no forwarding on mismatch, and on success builds a new outbound request to `127.0.0.1:8080` with the `Authorization` header replaced by the legacy token you extracted (don't just pass the client's header through). Keep the legacy worker process handle alive (don't let it get garbage-collected/reaped). Finally, manually test both the valid and invalid token paths with `curl -i` against port 9090 to observe the real status codes, and write those observed codes plus the extracted token into `audit_report.txt` in the exact required format.
