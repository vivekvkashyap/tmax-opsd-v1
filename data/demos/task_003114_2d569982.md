Bring the pipeline up service-by-service, verifying each hop before moving to the next, rather than trying to run everything at once.

1. Start `redis-server` locally (default port 6379) and confirm it's reachable with `redis-cli ping`.
2. In `/home/user/app/data_ingest/index.js`, find the misconfigured Redis connection URL and correct it to point at the local Redis instance/port; then run the ingestor once to push documents into the `doc_queue` list — verify with `redis-cli llen doc_queue`.
3. In `/home/user/app/scoring_service/main.py`, change the hardcoded port from 3000 to 8000, then launch it (e.g. `uvicorn`) and confirm `curl http://localhost:8000/process_next` returns a document.
4. Write a small client loop (Python or Node) that calls `GET /process_next` repeatedly until the queue is exhausted (empty/error response), collecting every `{id, tokens, score}` object into memory.
5. From the collected pool, perform sampling WITH replacement (bootstrap) to build exactly 1000 samples. Since a uniform-random bootstrap may not hit the required mean-score threshold, bias your sampling toward higher-scoring documents (e.g. weight by score, or oversample from the top-scoring subset) until the resulting mean satisfies the required minimum — then write one JSON object per line to `/home/user/final_dataset.jsonl`.
