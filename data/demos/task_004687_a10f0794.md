Get each pipeline stage numerically correct and verify it standalone before chaining to the next. In `integrator.c`, read all 500 floats from `observations.txt`, reshape logically into 100 rows of 5 (row-major, first 5 values = row 0, etc.) and compute each row's mean as that trajectory's initial condition — print/sanity-check a few of these means before integrating. Replace the buggy adaptive-step Euler loop with a plain fixed-step loop: for each of the 100 initial conditions, run exactly 1000 iterations of `y += dt * (-y*y)` with `dt = 0.001` starting at `t=0`, ending at `t=1.0` — do not let `dt` change. After integration, the derivative at t=1.0 is directly `-y*y` (from the governing ODE, not a finite-difference estimate), so compute it analytically from the final `y`. Write the 100 derivative values, one per line, to `/home/user/derivatives.txt`; compile and actually run the C program to produce real output before writing the Python side. In `calc_ci.py`, load these values into a 1D numpy array and call `scipy.stats.bootstrap((data,), np.mean, confidence_level=0.95, n_resamples=9999, method='percentile', random_state=np.random.default_rng(42))` exactly as specified (argument names and seed matter for exact reproducibility), then write `[lower, upper]` to `/home/user/ci.txt` rounded to 4 decimals in that exact bracket format.
