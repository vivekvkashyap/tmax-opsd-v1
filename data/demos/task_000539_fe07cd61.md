Do this as a shell pipeline driven by metadata.csv, processed line by line. For each row, split on commas to get the relative path, os_target, and version; compute `sha256sum` on the source file under /home/user/incoming/ to get its hash; determine the file's extension by stripping everything up to (and including) the last dot, but watch for multi-part extensions like `.tar.gz` — use something like `basename` plus a case/pattern match (or check if the filename ends in `.tar.gz` specially) rather than naively taking only the last `.ext`, since a naive approach would produce `.gz` instead of `.tar.gz` for that file. Copy (never move) the source into artifact_pool/ renamed to `<hash>.<ext>`. Create `curated_repo/<os_target>/<version>/` and inside it `ln -s` an **absolute** path to the pool file, named `artifact.<ext>`. While processing, either accumulate manifest lines in a variable/temp file (or use process substitution) and pipe the whole set through `sort` before writing to manifest.txt, since lines must be sorted alphabetically by the symlink path column, not by processing order. Double check each manifest line's exact field order and pipe-delimiter format against the spec.