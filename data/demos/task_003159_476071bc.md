Handle the three deliverables independently. (1) OCR `/app/ticket_8819_screenshot.png` (e.g. `tesseract`) to recover the Batch ID string printed in the screenshot, then write exactly that recovered string to `/home/user/batch_id_recovery.log` — don't guess the format. (2) In `ingest.py`, replace the `os.system(f"file {filename} ...")` shell call with a non-shell approach (e.g. `subprocess.run(["file", filename])`, a list of args, no shell=True) so filenames with spaces/special characters can't break or inject into the command; fix the SQLite race by using parameterized queries (`?` placeholders, never f-string interpolation into SQL) and either a single writer connection/lock, WAL mode, or a retry-with-backoff on `sqlite3.OperationalError` instead of silently swallowing it — verify by running concurrent ingests against a directory with tricky filenames and confirming the DB row count matches file count with no exceptions. (3) For `validate.py`, use a strict whitelist regex anchored on both ends (e.g. `^[A-Za-z0-9_-]+\.(png|jpg|jpeg)$`) rather than a blacklist of "bad" characters — blacklists miss adversarial cases like `..`, shell metacharacters, or unusual extensions — and print exactly `SAFE`/`UNSAFE` with the specified exit codes.