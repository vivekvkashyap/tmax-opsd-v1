Compile `sim.c` with `gcc -O2 -o sim sim.c` and run it first so `baseline.txt` exists as ground truth before writing any Python. Read `sim.c` carefully to extract the exact per-step update rule (initial state, coefficients, order of operations) and the exact layout of `noise.bin` (10,000 trajectories × 1000 steps, row-major, IEEE754 doubles) — mismatching the noise indexing/reshape is the most common way to silently diverge from baseline. In Python, load noise with `np.fromfile(..., dtype=np.float64).reshape(10000, 1000)`, then implement the identical recurrence twice: once keeping everything in float64, once casting the noise array (and the running state) to `np.float32` before the loop so precision loss accumulates naturally rather than being computed in float64 and cast at the end. You can vectorize across the 10,000-trajectory axis with NumPy array ops as long as each scalar float32/float64 update matches the C arithmetic exactly (same operator order, no algebraic rearrangement that changes rounding). Write both float64 and float32 final-position vectors to their respective output files. Then write a separate small script that loads `baseline.txt`, `py_float64.txt`, `py_float32.txt` and computes `scipy.stats.wasserstein_distance` and `scipy.stats.ks_2samp(...).statistic` for each pair, dumping all four values as plain Python floats into `metrics.json` in the exact required key format.
