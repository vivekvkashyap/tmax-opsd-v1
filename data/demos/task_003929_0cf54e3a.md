Handle the two regressions separately and in order. First get `/app/legacy_oracle` runnable: it's a stripped binary, so extract its embedded license key with `strings /app/legacy_oracle | grep '^KEY_'` (a 16-char string starting `KEY_`), export it as `ORACLE_KEY`, and sanity-check by piping a sample number through the oracle. For the infinite loop, use `git log` in `/home/user/sensor_pipeline` to inspect the commit history of `main.cpp`; you'll find a later commit changed a `while (i != 50)` counting loop so that a branch (`val < 0`) resets state without incrementing the loop counter — the fix is to always advance the counter each iteration regardless of that branch, or otherwise guarantee termination. For the precision loss, `git diff` or `git show` the commit that switched the computation from `double` to `float` (and truncated the pi constant) — the fix is to use `double` throughout and the full-precision constant `3.141592653589793`, not necessarily reverting the whole commit if later commits depend on it. Rebuild with the loop fix applied on top of the precision fix, and compile the final binary to exactly `/home/user/sensor_pipeline/pipeline_fixed`. Test it directly against the oracle on sample input, including the previously-hanging `sample_input.txt`, before considering it done.
