Diagnose the Makefile failure by attempting `make` first and reading the linker error — building a `.so` from object files requires both compiling the objects as position-independent code and linking with the shared-object flag; a Makefile missing either will produce a non-loadable or non-PIC library. Fix both the compile rule (add `-fPIC`) and the link rule (add `-shared`), then rerun `make` and confirm `file libchecksum.so` reports a shared object. Compile the `.proto` with `protoc --python_out=. message.proto` and confirm the generated `_pb2.py` module imports cleanly before writing test code. In the Python test script, build the `Record`, call `.SerializeToString()` to get the exact wire-format bytes (don't manually construct the field bytes yourself), load the shared library via `ctypes.CDLL('./libchecksum.so')`, and explicitly set `argtypes = [ctypes.c_char_p, ctypes.c_int]` and `restype = ctypes.c_uint32` before calling — omitting `restype` causes ctypes to truncate/misinterpret the 32-bit unsigned return value on 64-bit systems, a very common bug here. Pass the serialized bytes and their length, then write the returned integer as a string to the output file.