Start by diagnosing the build failure rather than reinstalling: run `make` in `/app/pigz-2.8` and read the linker error — it will show undefined references to zlib symbols (`deflate`, `inflate`), pointing to a missing `-lz` in the `Makefile`'s `LIBS`/link-flags variable. Fix that one line, rebuild, confirm `pigz --version` works, then copy the binary to `/home/user/bin/` (create the dir first). For the archival script, source `/home/user/archive_config.conf` as shell variables, then build a streaming pipeline with no intermediate files on disk: loop over `*.log` in `SOURCE_DIR`, pipe each through `awk`/`sed` to drop lines matching either exclude pattern (escape the regex, e.g. `\[DEBUG\]`), and feed the filtered streams into a `tar` process whose stdout is piped directly into `pigz` with the configured compression level, writing to `TARGET_FILE`. A common approach is using named pipes or `tar`'s ability to read file contents from a filter via `--to-stdout`/process substitution per file, then re-tarring the filtered output. After running, check the archive's byte size against the threshold and spot-check that DEBUG/TRACE lines are gone by decompressing a sample.
