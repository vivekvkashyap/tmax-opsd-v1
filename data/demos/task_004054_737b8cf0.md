This is a three-stage task; handle each independently and verify before moving on. Stage 1: start the stack with `/app/start.sh`, then edit `/app/nginx/nginx.conf` to add a `deny 192.168.200.0/24;` directive inside the relevant `server`/`location` block (place it before any `allow`/`proxy_pass` so it actually takes effect), then reload nginx (`nginx -s reload` or restart) so the config is live — check the config file literally contains that exact deny line. Stage 2: the target cookie name lives in a non-standard ELF section named `.tgt_cookie` on a stripped binary — use `objdump -s -j .tgt_cookie /app/exfiltrator_bin` or `readelf -x .tgt_cookie` to dump that section's raw bytes and read off the ASCII string directly (strings/grep across the whole binary may miss section boundaries or pick up noise); do not guess a cookie name. Stage 3: write the C redactor to process stdin line-by-line (or as a byte stream) looking for lines beginning with `Cookie: ` or `Set-Cookie: ` followed by `<cookiename>=`, replacing only the value up to the first `;` or `\r` with `REDACTED`, and echoing everything else byte-for-byte unchanged, including partial/malformed lines and missing trailing newlines. Since a fuzz-equivalence check will hammer edge cases (empty input, no cookie present, cookie at buffer boundary, multiple cookies), avoid fixed-size buffer assumptions and test manually against constructed edge-case inputs before considering it done.