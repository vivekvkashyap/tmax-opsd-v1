Start by trying to build `/app/tinyml` as-is to see the actual compiler errors — they'll point at C++17-only constructs (e.g. `std::optional`, structured bindings) being compiled under an older `-std` flag in the Makefile. Fix it by editing the `CXXFLAGS` line in the Makefile to request a C++17-compatible standard, then `make` to produce `libtinyml.a`; don't touch the library source itself. For the server, use the vendored single-header `httplib.h` and `json.hpp` to implement a `POST /infer` handler that first checks the `Authorization: Bearer <token>` header exactly and returns 401 if missing/wrong before touching the body. In the cleaning pipeline, compute mean/stddev over only the non-sentinel (-9999.0) values, impute missing values with that mean, then clip every value (including newly imputed ones) to `mean ± 2*stddev` using min/max clamps — apply clipping after imputation, not before. Call `tinyml::dot_product` from the fixed static library against the fixed weight vector for the prediction, and return both the cleaned vector and prediction as JSON. Write `/home/user/run_server.sh` to compile `server.cpp` with `-I` paths for tinyml/httplib/json headers, statically link `libtinyml.a`, and launch the binary in the background (e.g. with `&` or `nohup`) so it's still listening when the script returns.