This is a classic train/test leakage bug: any `fit` or `fit_transform` called on test data lets the transformer "see" test statistics, inflating apparent performance. Read `/home/user/model_pipeline.py` top to bottom and specifically check every preprocessing step (scaler, PCA, etc.) — for each, confirm the transformer is `fit` (or `fit_transform`) only once, on the training set, and every subsequent application to test data uses `transform` only, never `fit_transform` or a fresh `fit`. Fix each violation in place without touching random seeds, hyperparameters, or the train/test split ratio. Then run the script, capture the printed accuracy and ROC AUC, and write them into `/home/user/results.json` with exactly the keys `accuracy` and `roc_auc` as floats (you may need to add JSON-writing code to the script if it currently only prints). Re-run the fixed script yourself to confirm the JSON file is produced and the numbers look plausible (leakage bugs typically make metrics look worse or erratic once removed, not better — validate what "fixed" behavior should look like rather than assuming).
