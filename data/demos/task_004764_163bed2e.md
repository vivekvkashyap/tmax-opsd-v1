Start by extracting the ground-truth rules from the image rather than guessing them: run `tesseract /app/rules.png stdout` (install via apt if missing) and read the OCR'd text carefully — it lists restricted table names and forbidden function names under clearly labeled headings. Parse those two lists out programmatically (or by careful reading) rather than hardcoding assumptions about what a "typical" security memo would forbid, since OCR output can have quirks you need to handle (e.g. stray whitespace/case).

Then write `/home/user/sanitizer.sh` so it: reads the CSV argument, skips the header line, and for every remaining `query_text` field checks — case-insensitively — whether it contains any of the restricted table names or forbidden function names extracted from the image. Use something robust like lowercasing both the query and each term and doing substring/word-boundary matching, not a fragile single case-sensitive grep. If any row matches, print `REJECT` and exit; only after scanning every row with no matches print `ACCEPT`. `chmod +x` the script. Validate against several CSVs you construct yourself with mixed casing and edge cases before trusting it against the hidden corpus.