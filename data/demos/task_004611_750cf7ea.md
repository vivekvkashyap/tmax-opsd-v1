Generate the diff with `diff -u project_v1/.../main.rs project_v2/.../main.rs`, then count added/deleted lines by grepping lines that start with a single `+` or `-` while explicitly excluding the `+++`/`---` header lines (e.g. `grep -c '^+[^+]'` and `grep -c '^-[^-]'`, or filter in awk/python). Plug the counts into the exact formula given: R = (A*2) + (D*3) + ((A*D) mod 7); compute this with `bc`, `awk`, or python to avoid shell-arithmetic mistakes. For the API spec, parse `api_specs.txt` line by line: lines starting with `REST` contribute the path (second token after the HTTP method) in file order, lines starting with `GRAPHQL type` contribute the type name in file order — use `grep`/`awk`/`sed` or a short python script rather than hand-transcribing, so ordering and exact strings are preserved. Finally assemble the JSON with `jq -n` (or python's `json.dump`) using the computed integer and the two ordered arrays, and write it to `/home/user/release_manifest.json`. Double-check the diff excludes header lines — that's the most common source of an off-by-N risk score.