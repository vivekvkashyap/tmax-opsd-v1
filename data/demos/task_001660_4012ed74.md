Build this bottom-up and verify each layer in isolation before wiring them together. First compile the C fingerprint function with `gcc -shared -fPIC -o /app/lib/libfingerprint.so /app/src/fingerprint.c` and write a tiny standalone test that loads it via your language's FFI mechanism (ctypes in Python, cgo/purego in Go, etc.) and calls it on a known byte array to confirm the checksum logic (running sum mod 99991) actually matches before touching the API. Second, separately verify you can extract PCM samples from the WAV file (skip the RIFF/fmt header, read the data chunk) and feed those raw bytes to the FFI call. Third, get transcription working standalone with an offline tool (e.g. `ffmpeg` to resample to 16kHz mono + pocketsphinx, or a local whisper model) against the sample file, since network-based STT won't be available. Only then assemble the HTTP server: enforce the exact token check returning 401 on mismatch, and make sure the server is started as a background/detached process so it stays listening. Write the property-based test with random-length byte arrays asserting the FFI call never crashes and the result stays in [0, 99990], matching the modulo bound in the C code.
