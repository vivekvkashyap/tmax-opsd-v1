Build this as a single awk-centric pipeline rather than multiple scripting languages. First strip the header (`tail -n +2`) and filter out any row where any of the X/Y/Z fields is empty or literally `NA` — do this with `awk -F,` field checks (`$2==""||$2=="NA"||...`) rather than a blunt grep, since grep for "NA" could accidentally match valid numeric substrings. For each surviving row compute the Manhattan distance to (12,19,28) using `awk`'s absolute-value idiom (e.g. a helper function or `($2-12<0?12-$2:$2-12)` for each axis, summed). Emit `distance,ID` (or `distance ID`) pairs, then pipe through `sort -t, -k1,1n -k2,2n` so ties on distance break by ascending numeric ID — using plain `sort -n` on distance only won't guarantee the ID tiebreak, which is the main way naive attempts diverge from the expected order. Take the top 3 with `head -n 3`, extract just the ID field with `cut`/`awk`, and redirect one-per-line into `/home/user/recommendations.txt`. Verify by manually recomputing distances for a couple of rows.