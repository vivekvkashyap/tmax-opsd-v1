This is a classic train/test data-leakage bug: locate the loop(s) in pipeline.c that compute `min_f1/max_f1/min_f2/max_f2` and check their loop bounds — the fix is to compute those min/max values by iterating only over the Reference rows (indices `0` to `NUM_REF-1`), not the full `TOTAL_ROWS` range, since the scaler must never see Query-set values during fitting. Keep the separate "apply scaling" loop iterating over all rows (Reference and Query alike get transformed using the Reference-derived min/max) — only the *fitting* step needs restricting, not the *transform* step. Leave the nearest-neighbor distance computation and output formatting untouched since those aren't the bug. After fixing, compile with `gcc -o pipeline pipeline.c -lm` and run it so it reads `/home/user/experiments.csv` and writes `/home/user/results.csv` in the existing `QueryID,ClosestReferenceID,Distance` format with 4 decimal places — verify your fix changed the output (compare against the buggy version's results) as a sanity check that the leakage was actually removed.
