Follow the pipeline steps in the exact order specified, since seed placement and sample order affect reproducibility and the verifier checks the R^2 numerically. Load data.csv, drop NaN rows with `dropna()`, and cast f1-f4 to float64. Set `np.random.seed(42)` first, then take the bootstrap training sample with `df.sample(frac=1.0, replace=True, random_state=42)` on the *cleaned* dataframe — the OOB test set is whatever cleaned-dataframe rows' index did not appear in the bootstrap sample (`df.drop(train_df.index)`, relying on pandas' duplicate-aware index handling, not a naive set difference that would double count). Build a scikit-learn `Pipeline([('scaler', StandardScaler()), ('model', LinearRegression())])` and call `.fit()` only on the training split so the scaler's mean/std come solely from training data, never fit on the full dataset first. Compute R^2 via `r2_score` on OOB predictions. For the benchmark, call `pipeline.predict(X_oob)` 100 times in a loop, timing each call, and average the per-call wall-clock time in milliseconds. Write both numbers as floats to results.json with exactly the two specified keys.