Design the script around absolute paths from the start — hardcode `/home/user/app_data/network_logs` and `/home/user/current_logs` as constants rather than relying on `os.getcwd()` or relative paths, since the verifier will run it from `/tmp`. For directory creation use `os.makedirs(path, exist_ok=True)`. For the symlink, write idempotent logic that inspects the target path's current state before acting: if `os.path.islink(link)` and `os.readlink(link)` already equals the target, do nothing; if it exists as a broken symlink, wrong symlink, or a regular file/directory, remove it first (`os.remove` or `os.unlink` — `lexists` catches broken links that `exists` misses) then recreate with `os.symlink(target, link)`. For MTU data, invoke `subprocess.run(['ip', '-j', 'link', 'show'], capture_output=True, text=True)` and parse the JSON array it returns, extracting `ifname` and `mtu` per interface into a dict (avoid text-parsing `ip link` output by hand — the `-j` flag gives structured JSON that matches exactly what an independent verifier would compute). Write that dict to `mtu.json` via `json.dump`, then actually execute the script once to prove it works before finishing.