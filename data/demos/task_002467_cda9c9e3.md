Chain three distinct sub-skills in order and confirm each output before proceeding. First, extract the SHA-256 hash from the `.rodata` section: run `objdump -s -j .rodata dropper.elf` (or `strings dropper.elf | grep -E '^[a-f0-9]{64}$'`) to pull out the 64-character hex string — don't assume its exact position, search for a string matching the hash length/charset. Second, write `/home/user/crack.py` to brute-force it: since you know the passphrase is a 4-digit number followed by `admin`, iterate all 10000 candidates (`f"{i:04d}admin"`), hash each with `hashlib.sha256`, and compare hex digests to the extracted hash — this keyspace is tiny so brute force is instant. Write the matching plaintext to `/home/user/passphrase.txt`. Third, use that passphrase to decrypt the SSH key: `cp id_rsa decrypted_id_rsa; ssh-keygen -p -f /home/user/decrypted_id_rsa -P "<recovered-passphrase>" -N ""` removes the passphrase in place, producing an unencrypted key. Finally `chmod 600 /home/user/decrypted_id_rsa` and verify with `ssh-keygen -y -f decrypted_id_rsa` (should succeed with no prompt) and `grep ENCRYPTED decrypted_id_rsa` (should find nothing).