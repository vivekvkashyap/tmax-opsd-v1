Build the pipeline as discrete, testable stages inside `/home/user/pipeline.py`. First extract exactly one frame per second with `ffmpeg -i /app/server_logs.mp4 -vf fps=1 frame_%03d.png` (or similar) via `subprocess`, and verify you got 60 frames before moving on. Second, OCR each frame with `pytesseract`/`cv2` and pull out the CPU value: don't just regex the raw OCR text — normalize common OCR confusions first (uppercase `O`→`0`, strip stray spaces, tolerate a missing/garbled `%`) then parse the float, e.g. with a regex like `CPU_USAGE:\s*([\d.O]+)\s*%?` followed by character substitution before `float()`. If a frame's OCR totally fails, don't crash the pipeline — fall back to interpolating from neighboring seconds so you still cover all 60 rows (missing rows count against you). Third, load the per-second series into a pandas Series/DataFrame and compute a trailing rolling mean with `rolling(window=5, min_periods=1).mean()`, which naturally handles the partial-window average for the first 4 seconds. Write the two-column CSV with exact header `second,smoothed_cpu`, then rerun to sanity-check row count and value ranges before finishing.
