Build the pipeline as separate, testable stages driven by one `pipeline.sh` wrapper. First write a join step (Python with `csv`/`pandas` is simplest): read both source CSVs, rename `req_id`->`id` and `review_body`->`text`, concatenate, drop date/timestamp columns, then inner-join on `id`==`item_id` against meta.csv so rows without a matching category are naturally dropped. Next write the tokenizer exactly as specified and in order: lowercase first, then strip non-alphanumeric-non-space chars (regex like `[^a-z0-9 ]`), then split on whitespace, then filter tokens with length < 3 — get this order right or counts will be off (e.g. punctuation attached to short words changes lengths). Compute `score = (token_count * len(category_code)) % 10` per row, sum for `sum_scores`, and count rows for `total_rows`. For "benchmark 100 times", wrap the scoring computation in a loop that runs it 100 times (timing/perf doesn't affect the JSON), but write `sum_scores`/`total_rows` from a single clean pass, not from the loop. Have `pipeline.sh` call your Python script(s) in sequence and finish by writing `/home/user/report.json` with keys `total_rows`, `sum_scores`, `benchmark_runs: 100`. Make the script executable with `chmod +x`.