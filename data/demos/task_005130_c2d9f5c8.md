Work through the four sub-problems in dependency order since later steps need earlier outputs. First, run OCR on the diagram: `tesseract /app/pipeline_specs.png stdout` (or similar), then grep the output for the line matching `TOKEN: <value>` and capture just the value — don't hardcode or guess it. Second, fix the Makefile's link step: a shared library built from C code that calls `pow`/`sqrt` needs `-lm` on the final link command (`gcc -shared -o libresolver.so resolve.o -lm`), and note that `-lm` must appear after the object files on the command line for the linker to resolve the symbols correctly — verify with `make -C /app/dep_resolver/` succeeding cleanly. Third, write resolver.proto with exactly the specified `syntax = "proto3"`, `package ci;`, message fields with the given field numbers, and the `DependencyResolver` service/RPC signature — field names and numbers matter for wire compatibility with the pre-built server.py. Fourth, in run_service.sh, generate stubs with `python3 -m grpc_tools.protoc -I/app/proto --python_out=/app --grpc_python_out=/app /app/proto/resolver.proto`, export the OCR'd token as `GRPC_AUTH_TOKEN`, run `make -C /app/dep_resolver/`, then launch `python3 /app/server.py &` after making the script executable. Test the full chain with a gRPC client call before considering it done.
