Fix the Makefile first: the `release` target is compiling `crunch.c` into an ordinary executable instead of a position-independent shared object, so add the missing `-shared -fPIC` flags alongside `-O3` (mirroring how the working `debug` target is built, just with `-O3` instead of `-g`) so `libcrunch.so` is a valid `.so` that `file` reports as "shared object". Then inspect `process_array` in `crunch.c` for a `malloc` whose returned pointer is never freed before the function returns — add the matching `free()` call right after its last use, being careful not to free memory the caller still needs (e.g. don't free `data`, only the locally-allocated temp buffer). Rebuild with `make release`, then validate with `valgrind --leak-check=full python3 test_run.py` and confirm it reports "definitely lost: 0 bytes" — don't just trust that the code looks fixed, actually run valgrind since the test loops 50000 times specifically to surface leaks. Only after both checks pass, write `/home/user/release_info.txt` with the two exact required lines.