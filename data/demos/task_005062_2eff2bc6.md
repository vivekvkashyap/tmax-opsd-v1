Work bottom-up: first get `libfilter.so` building (`gcc -shared -fPIC -o /home/user/libfilter.so src/filter.c`) and confirm `ctypes.CDLL` can load it and call `is_valid_ip` with a C-encoded string (`.encode()`) before writing the rest. For `parse_routes.py`, implement a true line-by-line state machine (state OUTSIDE vs INSIDE_ROUTE): on a line matching `<route PATH>` capture the path and switch state, on subsequent lines inside a route parse `key value` pairs into a dict (cast numeric-looking values like `limit` to int), and on `</route>` push the dict and return to OUTSIDE — this handles multiple route blocks robustly, unlike naive regex-over-whole-file. For `rate_limit.py`, parse each log line's `(ip, epoch, path)`, skip lines where `is_valid_ip` returns 0, then for each (ip, path) group its sorted timestamps and slide a window: for every request, count how many requests (including itself) fall within `[t, t+60)`; if that count exceeds the path's `limit` at any point, ban the IP. Write sorted unique banned IPs to `banned_ips.txt`. Wire `build`, `run`, `bench` (using `/usr/bin/time -v`), and `all` targets into the Makefile as specified.
