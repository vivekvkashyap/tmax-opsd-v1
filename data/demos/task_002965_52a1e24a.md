Start the mock server first (`python3 /home/user/mock_api.py &`) so you can hit it live while iterating on the script. Build `/home/user/state_tester.sh` as an explicit state machine using `curl` + `jq`: GET `/api/start`, extract `session_id`/`challenge` with `jq -r`. For AUTH, reverse the challenge string in bash (e.g. `echo "$challenge" | rev`) and POST it as JSON with `Content-Type: application/json` to `/api/auth`, extracting the returned `token`. For FETCH, GET `/api/data` with header `Authorization: Bearer $token`, then sum the `value` field across the `items` array — `jq '[.items[].value] | add'` is the reliable way rather than manual bash arithmetic on parsed strings. For SUBMIT, POST `{"session_id":..., "total_value": <sum>}` (as a raw number, not a string, since the server likely checks numeric equality) and extract the `flag` from the response, printing only that flag to stdout. Chain these states sequentially since each depends on the prior response rather than writing a generic loop. Finally add a `test-api` target to `/home/user/Makefile` that runs the script and redirects stdout to `/home/user/ci_output.log`, then execute `make test-api` to produce the log.