Build this bottom-up and test each layer before wiring the next: (1) Write `compat.proto` with `syntax = "proto3";`, the `CompatibilityDaemon` service, `CheckVersion` RPC, and the exact message field names/numbers specified in the task — field numbers matter for wire compatibility. (2) Implement `evaluate_version` by splitting the version string on `.` into three integers and comparing them as a tuple (major, minor, patch) — do not compare version strings lexicographically. Parse the constraint by splitting on whitespace into pairs of (operator, version); support one or two conditions ANDed together; implement `>=`, `<=`, `==` as tuple comparisons yourself, no external semver library. (3) In `main.py`, generate the pb2 modules via `python -m grpc_tools.protoc` with `--python_out` and `--grpc_python_out` pointed at the `server` directory, implement the servicer by looking up the hardcoded per-component constraint dict and calling `evaluate_version`, defaulting unknown components to `is_compatible=False`. (4) In `build_and_test.sh`, chain each step and check exit codes; after starting the server backgrounded, sleep briefly or poll before running the ad-hoc gRPC client so it doesn't race the server startup, then kill the server PID at the end. Test the three given (component, version) cases against the log output before finishing.