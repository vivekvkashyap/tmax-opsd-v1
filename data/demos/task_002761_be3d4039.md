OCR `/app/system_diagram.png` with `tesseract` to recover the exact maximum nesting depth limit written on the diagram — don't hardcode a guessed number, parse it out of the image text (watch for OCR misreads of digits) and use that value as your threshold in the script. Implement the depth/balance check with a simple counter scan over the file's bytes: increment on `{`, decrement on `}`, track the running max of the counter, and flag "unsafe" if the counter ever goes negative (an unmatched `}`) or ends non-zero (unmatched `{`), or if the running max exceeds the extracted limit. Treat any non-brace characters as irrelevant unless the task/corpus implies otherwise. Exit code 0 for safe, 1 for unsafe, and make sure the script takes the file path as `sys.argv[1]`. Before considering it done, test it locally against a few balanced-but-deep and unbalanced strings you construct yourself to confirm both failure modes (imbalance and over-depth) are caught, since the hidden grading corpus will specifically probe both.
