Treat this as three separable subsystems; verify each in isolation before wiring them together. For QR extraction, use `ffmpeg -i /app/ui_test_recording.mp4 -vf fps=<N> frame_%04d.png` to dump frames, then loop decoding each with `pyzbar.pyzbar.decode(Image.open(path))` until you find a non-empty `ROOT:<module_name>` match — scan the whole set, don't assume a fixed frame number. For the hash translation, replicate legacy_hash.js's exact bit operations: JS's `>>> 0` forces unsigned 32-bit wraparound, so mask every intermediate result with `& 0xFFFFFFFF` in Python (Python ints don't overflow natively, so this masking is essential), then format as lowercase hex zero-padded to 8 chars. For the verifier, do a DFS from the root module collecting reachable nodes, using a visiting/visited-color technique to detect cycles specifically within the *reachable* subgraph — unreachable nodes shouldn't affect detection. Only after building the sorted, comma-joined reachable module list should you hash it and compare to expected_hash; test against both corpora before finishing.
