Diagnose this as a numerical-stability bug before touching supervisor config. The naive one-pass variance formula (sum of squares minus square of sum, divided by n) loses precision catastrophically when values are large (~1e9) because you're subtracting two huge nearly-equal numbers — switch to Welford's online algorithm instead: maintain running `mean` and `M2` (sum of squared differences), updating per new value `x` as `delta = x - mean; mean += delta/n; M2 += delta * (x - mean)` (note: the second delta uses the *updated* mean). Handle the first line explicitly — with n=1, delta = x - 0, mean becomes x, and M2 becomes 0; don't special-case a division that isn't actually needed once you use this formula correctly. Format `mean` and `M2` to exactly 4 decimal places with awk's `printf`. Test your script directly against a range of synthetic large-integer sequences piped via stdin before wiring it back into the pipeline, since an equivalence check against a reference implementation will be run — match its output bit-for-bit, not just numerically close. Only after the script is verified standalone should you fix the supervisor config and restart services with `supervisorctl restart`, then tail `alerts.log` to confirm data is flowing end-to-end.
