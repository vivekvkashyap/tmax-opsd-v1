Build the existing Rust project at `/home/user/sim` in release mode (`cargo build --release`) and run the resulting binary 1000 times, capturing each single float printed to stdout into `/home/user/results.txt` (one per line) — note the simulation intentionally uses parallel reduction, so outputs will vary slightly run to run; that's expected, don't try to "fix" it. Then create a new Rust project at `/home/user/analyzer` that reads `results.txt` and computes three things carefully: (1) the robust mean via root-finding on `sum(tanh(x_i - mu)) = 0` using bisection/Newton-Raphson bracketed by [min(x), max(x)], iterating to convergence at 1e-5; (2) a 95% bootstrap CI on the plain sample mean via the percentile method with exactly 10,000 resamples using the `rand` crate; (3) a Gaussian KDE with bandwidth h=0.01 evaluated precisely at the robust mean computed in step 1 (not at the sample mean). Write all four numbers as `f64` into `/home/user/analysis.json` with the exact keys specified. The grading recomputes these same statistics independently from your actual `results.txt`, so correctness of the root-finder and KDE formula matters more than matching any specific numeric target.
