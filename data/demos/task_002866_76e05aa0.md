Install `libhdf5-dev`, `libfftw3-dev`, and `libeigen3-dev` via apt before writing code. Structure the C++ pipeline in clear stages: (1) use the HDF5 C++ or C API to read the `/dna_signals` dataset into a 10x1024 buffer; (2) for each row, run FFTW's `fftw_plan_dft_r2c_1d` (or complex-to-complex) to get the DFT, then compute magnitude `sqrt(re*re+im*im)` per frequency bin — be careful with FFTW's real-to-complex output layout, which only gives N/2+1 complex bins, not N; if you need a full N-length magnitude spectrum matching a plain `scipy.fft.fft`, use a complex-to-complex transform (real input, zero imaginary part) instead so you get all 1024 bins directly comparable to the verifier's `scipy.fft.fft`. (3) Load the 10x1024 magnitude matrix into an Eigen `MatrixXd` and run `Eigen::JacobiSVD` (or BDCSVD) with `ComputeThinU | ComputeThinV` to get singular values and V; the first right singular vector is column 0 of V (equivalent to row 0 of Vh in numpy). (4) Write `/singular_values` and `/principal_spectrum` back with HDF5, as plain double arrays. Note SVD sign is arbitrary — that's fine, the verifier allows a sign flip. Provide a CMakeLists.txt linking hdf5, fftw3, and Eigen3::Eigen, build, and run the executable to actually generate results.h5 before finishing.