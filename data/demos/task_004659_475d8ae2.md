Write the C program to first load both CSVs into arrays keyed by exp_id (parse with `strtok`/`sscanf` on commas, being careful with the header line), then perform the join by iterating one array and doing a linear or simple lookup search against the other on matching `exp_id` (only emit a row for IDs present in both files, matching the join semantics implied by "joining ... using exp_id as the key"). For each joined record, apply validation checks in order — learning_rate > 0, epochs >= 1, initial_loss >= 0, final_loss >= 0 — and mark Invalid if any fails, otherwise compute `predicted_loss = initial_loss / epochs` as a double and compare `final_loss < predicted_loss` for Success vs Failure, watching out for epochs==0 which must be caught by the epochs>=1 validation rule before you ever divide. Sort the output records by numeric exp_id ascending (e.g. with `qsort` on the parsed integer, not a string sort) before writing `/home/user/report.csv` with header `exp_id,status` and one row per experiment. Compile with `gcc -std=c11 /home/user/evaluate_experiments.c -o /home/user/evaluate_experiments` (or similar) and run it to produce the final CSV; check the exact output against your hand-traced logic for each row before finishing.