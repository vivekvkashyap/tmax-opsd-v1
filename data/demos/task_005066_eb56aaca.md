Build this as a minimal raw-socket HTTP server in C — you don't need a framework, just `socket()`/`bind()`/`listen()`/`accept()` on 127.0.0.1:8080, a loop that reads the request line, checks it starts with `GET /remediate`, and writes back a `HTTP/1.1 200 OK` response with the right `Content-Length` and body. For the remediation logic triggered per-request: open `/proc`, iterate numeric-named entries, for each read `/proc/<pid>/cmdline` (note: fields inside are NUL-separated, not space-separated, so read the whole buffer and split/scan on `\0` rather than treating it as a normal C string terminated at the first byte) and check if it contains `rogue_actor.py`; once found, locate the `--stolen-auth-token=` substring within the cmdline buffer and take everything after `=` up to the next NUL as the token. Then open a second TCP connection to 127.0.0.1:9000, send exactly `REVOKE_TOKEN <token>\n`, read the full response until the socket closes or a newline, and relay that exact string back as the HTTP body. Handle the process-not-found and connect-failure cases without crashing so the server keeps serving. Start the scenario script first, then compile and run your server in the background before the verifier queries it.