Write a Python script using pandas: read `/home/user/raw_data.csv` with `pd.read_csv(path, encoding='cp1252')` — the source is legacy Windows-encoded, so reading as utf-8 will corrupt or crash on special characters like curly apostrophes (a common naive mistake). Coerce `sensor_value` to numeric with `pd.to_numeric(..., errors='coerce')`, then drop rows where it's NaN or outside `[0.0, 100.0]` inclusive. Normalize `comment` with `.str.lower()`, then a regex `re.sub(r'\s+', ' ', text).strip()` (or `.str.replace(r'\s+', ' ', regex=True).str.strip()`) to collapse whitespace runs and trim. Compute `rolling_avg` from the *validated* rows only, in their filtered order, using `df['sensor_value'].rolling(window=3, min_periods=1).mean().round(2)` — do this after dropping invalid rows so the window only sees valid data, not before. Select and order columns exactly as `timestamp, comment, sensor_value, rolling_avg`, then write with `df.to_csv('/home/user/processed_data.csv', index=False, encoding='utf-8')`. Double-check row order is preserved and no extra index column leaks into the output.