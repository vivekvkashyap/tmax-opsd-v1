Use pandas (or plain csv module) but be deliberate about encoding at every I/O boundary: open/read `raw_transactions.csv` with `encoding='iso-8859-1'` (never default/utf-8, which will corrupt accented characters), and write the output explicitly with `encoding='utf-8'`. Deduplicate on `transaction_id` keeping the first occurrence (`df.drop_duplicates(subset='transaction_id', keep='first')`), and compute raw/unique/duplicate counts from the actual row counts before and after — don't hardcode them. For masking, write a small helper that splits each email on `@`, takes the local part's first character, appends exactly `***` (not padding to original length), then re-appends `@domain` — apply it to every row's email column, being careful with edge cases like a single-character local part. Write the cleaned CSV with `index=False` so no row-index column leaks in. Finally, generate `summary.txt` using the literal template given in the task, substituting only the three computed integers, with no extra whitespace or lines. Run the script and inspect both output files afterward to confirm formatting matches exactly.
