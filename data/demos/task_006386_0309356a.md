Set up the Rust project with `regex`, `serde`, and `serde_json`, then build the pipeline in clear stages. Write a regex that anchors on the literal structure `{LANG_CODE} <KEY_NAME> : "VALUE"` (tolerating the leading `[TIMESTAMP]` and surrounding log noise), using capture groups for the three fields, and skip any line that doesn't match — don't try to parse every line as structured data. For decoding, the extracted VALUE contains raw `\uXXXX` escape sequences as literal text (not real Rust escapes), so scan for the `\uXXXX` pattern with a regex or manual parser, parse the 4 hex digits, and convert the resulting code point to a `char`/UTF-8 (watch out for surrogate-pair cases if any appear, though this dataset likely stays in the BMP). Group parsed entries into a `BTreeMap<String, BTreeMap<String, String>>` keyed by `LANG_CODE` then `KEY_NAME` — using `BTreeMap` instead of `HashMap` gives you alphabetical key ordering for free in both dimensions, satisfying the sort requirement without a manual sort step. Serialize with `serde_json::to_string_pretty` (2-space indent is its default) and write to `/home/user/loc_clean.json`, then compile and actually run the program rather than just writing the source.