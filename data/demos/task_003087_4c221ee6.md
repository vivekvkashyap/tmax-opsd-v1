Work through the three objectives independently, then assemble the report. (1) For the tampered binary, compute `md5sum` on every file in `/home/user/evidence/bin/` and compare each against the corresponding line in `/home/user/evidence/checksums.md5`; the one file whose computed hash disagrees with the manifest is the tampered one — report just its filename. (2) For the cookie, grep `/home/user/evidence/access.log` for the line containing the `/exfiltrate` request, then parse the `Cookie:` header field in that line's log entry for the `session_id=<value>` pair and extract only the value portion. (3) For the PIN, read `lock.go` to understand the exact hashing scheme (salt concatenated with a 4-digit numeric string, then MD5), then write a Go program that loops over all 10000 candidates from "0000" to "9999" (zero-padded), computes `md5(salt+pin)` for each with `crypto/md5`, and compares the hex digest against the contents of `secret.hash` until it matches. Write the three findings to `/home/user/forensics_report.txt` in the exact order: tampered binary name, cookie value, cracked PIN — one per line, no extra text.