Compile the C parser first (`gcc -O2 -o /home/user/parser /home/user/parser.c`) and sanity-test it standalone on `data.fasta` to confirm the output binary file size matches the expected float count (4 bytes per base, skipping header/newlines) before touching the Python script. Update `analyze.py` to invoke the compiled parser via `subprocess.run([...])` with the FASTA path and a chosen output binary path as its two arguments, then load the result with `numpy.fromfile(path, dtype=np.float32)` — using the wrong dtype (e.g. float64) will silently corrupt the array length and FFT. Keep the FFT logic itself unchanged: `np.fft.fft`, take `np.abs`, zero out index 0 (DC component) before finding `argmax`, and read the magnitude at that index. Round the magnitude to 1 decimal place as specified and write `{"peak_index": int, "peak_magnitude": float}` to `/home/user/result.json` via `json.dump`. Run the updated script end-to-end and confirm the JSON file is produced with the right key names and types before finishing.