First determine W by analyzing `/app/sim_video.mp4`: extract frames (e.g. with `ffmpeg -i sim_video.mp4 frame_%04d.png`), then for each frame compute the average pixel intensity (e.g. via a quick Python/PIL/numpy script or `ffmpeg`'s signalstats filter) and count how many frames are essentially solid white (avg intensity > 250) — don't rely on eyeballing; script the count so it's exact. That count is W, giving your upper distance bound `W + 0.5`. Then write `/home/user/filter_corpus.c` in C: for each `ATOM` line matching atom name `CA`, parse the x/y/z coordinates from the fixed PDB columns 31-54 (columns are fixed-width, not whitespace-delimited, so slice by character position rather than naive tokenizing — some PDB coordinate fields can run together without spaces). Collect CA coordinates in file order, compute Euclidean distance between each consecutive pair, and check every such distance is strictly greater than 2.5 and strictly less than `W+0.5`; if any distance fails, or if zero CA atoms are found, exit non-zero, otherwise exit 0. Compile with `gcc` to `/home/user/filter_corpus` and sanity-test it against a couple of hand-crafted clean/evil PDB snippets before considering it done.