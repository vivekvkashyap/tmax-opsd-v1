Start by opening `/home/user/system_graph.db` with the `sqlite3` CLI and running `.tables` and `.schema <table>` on each table to discover the actual table/column names — don't assume names like "nodes"/"edges"; inspect what's really there. Once you find the entity table and the relationship table, run a quick `SELECT DISTINCT` on whatever column holds the relationship/edge type to confirm which literal value corresponds to "DEPENDS_ON" (there may be other unrelated relation types mixed in as noise — exclude those). Write `/home/user/analyze_graph.cpp` using the `sqlite3` C API (`sqlite3_open`, `sqlite3_prepare_v2`, `sqlite3_step`) to run a query that counts, per source entity, the number of distinct destination entities connected via that specific relation type (out-degree). Sort the results by out-degree descending, and for ties sort those tied entity names alphabetically ascending — get this tie-break rule exactly right since it changes which entities land in the top 3. Compile with `g++ -lsqlite3`, run the binary, and write only the top 3 entity names (one per line, no extra text) to `/home/user/result.txt`.