Implement this as straightforward, careful CSV parsing in pure Rust std — no crates. Read the file with `std::fs::read_to_string`, skip the header line, split each remaining line on commas, and parse `model_score` as `f64` and `is_correct` as an integer. Use an inclusive-lower/exclusive-upper filter (`score >= 0.70 && score < 0.80`) — be careful with floating-point comparisons on boundary values like exactly 0.70 or 0.799, since naive string-to-float parsing can introduce tiny epsilon errors; parsing standard decimal literals with `str::parse::<f64>()` is safe here, just don't round before filtering. Count N (rows in bin) and K (rows with is_correct==1) via simple accumulation. Apply the given closed-form Bayesian formula `(1+K)/(N+2)` directly — do not approximate via simulation. Format the mean to exactly 4 decimal places using Rust's formatting (`format!("{:.4}", mean)`), which rounds half-to-even by default — verify this matches expectations on your own test cases before trusting it blindly. Compile with `rustc validate.rs -o validate` (or `cargo` if a project exists), run it, and inspect `/home/user/output.txt` to confirm the exact `N=...,K=...,PosteriorMean=...` format with no extra whitespace.
