Design the protobuf schema first exactly as specified (oneof payload with Metadata/Chunk, CRC32 per chunk, final sha256 response), since the client-streaming semantics drive everything else. Generate Python stubs with `python -m grpc_tools.protoc` before writing server logic. In the server's streaming RPC handler: consume the request iterator, require the very first message to carry `metadata` (check `request.WhichOneof('payload') == 'metadata'`), aborting with `INVALID_ARGUMENT` otherwise; for each subsequent chunk message, recompute `zlib.crc32(chunk.data) & 0xFFFFFFFF` and compare to the client-supplied `chunk.crc32`, aborting the whole RPC with `context.abort(grpc.StatusCode.DATA_LOSS, ...)` the instant a mismatch is found (don't keep processing after abort); append valid chunk bytes to a file opened in the storage directory named after `metadata.filename`. After the stream ends, reopen and hash the saved file with `hashlib.sha256` (don't accumulate the hash in memory from chunks alone — computing from the file on disk avoids subtle bugs) and return it in `UploadResponse`. Serve with `grpc.server` bound to `[::]:50051` and keep the process alive (e.g. `server.wait_for_termination()`) in the background. Write `client.py` as an independent sanity test — generate 5MB random data, chunk at 64KB, compute per-chunk CRC32, stream it, and confirm the returned SHA-256 matches a local `hashlib.sha256` of the same data before considering the server correct.