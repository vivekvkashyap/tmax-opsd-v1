Build this as a straight-line pandas/networkx/scipy pipeline rather than hand-rolling graph or stats math. Load trace.csv with pandas, then `groupby(['caller','callee'])['duration'].sum()` to get aggregated edge weights (this collapses repeated caller-callee pairs before graph construction, which matters since PageRank needs one weighted edge, not multiple parallel ones). Build a `networkx.DiGraph()`, adding each aggregated edge with its summed weight via `add_edge(caller, callee, weight=summed_duration)`. Run `nx.pagerank(G, alpha=0.85, weight='weight')` and take the node with `max(pr, key=pr.get)` as the bottleneck — do not use unweighted PageRank, since the task explicitly requires edge weights to factor into centrality. Then filter the *original* (non-aggregated) dataframe for rows where `callee == bottleneck` to recover the individual duration samples (not the summed weight), fit `scipy.stats.gaussian_kde(durations, bw_method='scott')` (which is also the library default), compute the plain mean of those durations, evaluate the KDE at that mean via `.evaluate([mean])[0]`, and write both results to bottleneck_analysis.txt in the exact two-line format with the density rounded to 4 decimals — no extra whitespace or trailing content.