Start by copying the remote CSV into the workspace, then inspect it (`head`/`pandas.read_csv`) to confirm the actual locale columns present rather than assuming which ones exist. Reshape with `pandas.melt(df, id_vars=['string_id'], var_name='locale', value_name='value')`, then write it out with `to_csv(..., index=False)` so no stray index column is added — verify the column order is exactly `string_id, locale, value`. For the XML generation, load the Jinja2 template from `/home/user/workspace/strings.xml.j2` with a `jinja2.Environment`/`FileSystemLoader` (or read+`Template()`), and for each distinct locale in the long-format data build the list of `{string_id, value}` dicts the template expects (check the template's variable name, e.g. `strings`, before rendering — a naive re-implementation of the template instead of using its actual loop variable will silently produce empty output). Create `output/values-<locale>/` with `os.makedirs(..., exist_ok=True)` before writing each `strings.xml`. Run the script and spot-check one generated XML file for well-formedness and correct translated text before finishing.
