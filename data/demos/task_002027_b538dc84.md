Build this bottom-up: first get `/home/user/run_inference.py` correct in isolation before wiring the orchestration script. It should take a directory argument, `json.load` architecture.json for input_dim/hidden_dim/output_dim (mainly for validation/shape-checking, since the weight arrays already encode shape), `np.load` weights.npz for W1/b1/W2/b2, load features.csv with `np.loadtxt(..., delimiter=',')`, then compute the forward pass exactly as specified: Z1 = X@W1 + b1, A1 = relu(Z1), Z2 = A1@W2 + b2, and print only the mean of Z2 across the 50 rows as a bare float (no extra text) — this printed value is what the shell script will capture. For `/home/user/organize.sh`, loop over each subdirectory in `/home/user/experiments/`, capture `python3 run_inference.py "$dir"` output via command substitution, insert a row into the SQLite results table (create table if not exists with `sqlite3 ... "CREATE TABLE IF NOT EXISTS..."` then an INSERT), and use a numeric comparison (bash arithmetic won't do floats — use `awk` or `bc` or python one-liner) to test `mean_score > 0.0` before `mv`-ing the directory into `organized_data/positive/` or `organized_data/negative/` (mkdir -p both first). Run the script and confirm the DB and moved directories via `sqlite3` queries.
