Two independent recoveries feed this task: (1) use `ffmpeg` to extract the first 10 frames of `/app/incident.mp4` as images (e.g. `ffmpeg -i incident.mp4 -vf "select=lt(n\,10)" -vsync 0 frame_%d.png`), then for each frame compute the average value of the Red channel as an integer (e.g. via Python/PIL: load pixel array, take mean of the R channel, cast to int) — sum these 10 integer averages and take mod 256 to get the 8-bit key; do not hardcode a guessed key, derive it from the actual extracted frames. (2) Read `encrypt.c`'s pseudocode carefully: `ciphertext[i] = (plaintext[i] ^ key) + (i % 256)`, done as byte arithmetic (implicitly mod 256 via unsigned char overflow). The decoder must invert this exactly: `plaintext[i] = (ciphertext[i] - (i % 256)) ^ key`, again using unsigned char arithmetic so subtraction wraps correctly mod 256. Write this in C, read a continuous hex string from stdin until EOF (no whitespace splitting assumptions — read the whole stream), convert hex pairs to bytes, apply the per-index inverse, and `fwrite` raw bytes to stdout with no added newline. Compile to exactly `/home/user/decode`. Since correctness is checked by fuzzing against a hidden oracle, stress-test your own binary against manually encrypted-then-decrypted round trips for various lengths including edges (empty, length 1, odd/even).
