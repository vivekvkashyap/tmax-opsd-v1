This task has three independent deliverables — sequence them: extraction, pathfinding, sanitiser. First, OCR `/app/legacy_schema.png` with `tesseract` (upscale if needed) to recover the root node identifier and the column-mapping convention (source/dest/weight) described in the diagram; use exactly what you recover, don't assume a default root node. Second, write a script that loads all CSVs from `/app/data/relational/` and JSONs from `/app/data/document/`, merges them into one in-memory directed weighted graph using the recovered column mapping, then runs a standard shortest-path algorithm (Dijkstra, since weights are likely non-negative — inspect the data first to confirm) from the recovered root node to `NODE_OMEGA`; write the path as a comma-separated string to `/home/user/shortest_path.txt`. Third, write a sanitiser that reads a backup file, detects and strips/rejects malicious loop-injection artifacts (self-referential edges or cycles that don't belong in legitimate topology), and prints cleaned output to stdout while exiting 1 only if the file is entirely malicious. Validate the sanitiser explicitly against both `/app/corpora/clean/` (must pass through, exit 0, no data loss) and `/app/corpora/evil/` (must filter or reject) before finishing — don't assume your loop-detection logic is correct without running it on both corpora.