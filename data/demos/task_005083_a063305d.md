Build this as a two-stage pipeline where sorting happens in bash before the C program ever sees the data, since the C imputation logic depends on records already being grouped by user_id then ordered by timestamp — use `sort -t, -k2,2 -k1,1n /home/user/server_logs.csv` (field 2 alphabetically for user_id, field 1 numerically for timestamp) piped into the compiled C binary. In the C program, read lines with something like `fgets`/`scanf` splitting on commas, mask the IP by parsing the first two dotted octets and printing `oct1.oct2.*.*`, and track a `last_valid[user_id]` style running value per user (a simple approach: since input is grouped by user, just keep one "current user" and "last seen response_time" pair that resets to `0` whenever the user_id field changes) — emit that carried-forward value whenever the response_time field is empty. In the bash script, compile with `gcc -O2`, redirect the C program's stdout to `/home/user/anonymized_logs.csv`, then do a second pass (awk associative arrays keyed by user_id summing values and counts) to compute per-user averages formatted with `printf "%.2f"`, sort by average descending with a numeric sort and tie-break user_id ascending, and take the top 3 into `/home/user/top_users.txt`. Make the script executable and actually run it end-to-end to produce both output files.
