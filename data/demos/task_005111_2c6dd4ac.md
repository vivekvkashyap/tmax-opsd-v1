Build and test the C classifier in isolation first, since it has its own corpus-based pass/fail criterion independent of the services. Read stdin fully (up to 4096 bytes) with `fread`/`fgets` into a buffer, then apply substring/pattern checks in order: reject if it contains `[*` (unbounded variable-length path syntax) or `*..` ; reject if it contains `CALL db.` ; reject if the query has a `MATCH` clause with multiple comma-separated parenthesized node patterns that aren't joined by a relationship arrow (`-[`/`->`/`<-`) and no `WHERE` — a simple heuristic is: within the MATCH clause, if you see `),` followed eventually by another `(` without any `-` or `>` between them, flag it. Print `CLEAN`/exit 0 or `EVIL`/exit 1 accordingly. Validate against the given example queries manually before trusting it on hidden corpora. Compile with `gcc -o /home/user/app/cypher_sanitizer cypher_sanitizer.c`. For Part 2, edit `nginx.conf`'s `/query` location block to `proxy_pass http://127.0.0.1:5000;` and set `GRAPH_DB_URL=tcp://127.0.0.1:7687` in `.env`, then restart via `start_services.sh` and confirm the full chain with `curl -X POST http://127.0.0.1:8080/query -d '{"query":"MATCH (n) RETURN n LIMIT 1"}'` returns 200.