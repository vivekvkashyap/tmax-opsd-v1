Structure the C program as three clearly separated phases (parse -> filter/project -> aggregate) since the two outputs use different filter scopes and mixing them up is the most common mistake: the Cypher export only includes active-status nodes/edges (weight>=10, both endpoints active), while the JSON aggregation sums ALL edges in the original graph regardless of the active filter. First fetch cJSON (`wget` the raw `cJSON.h`/`cJSON.c` from its GitHub repo into `/home/user/`) and compile it together with your program (`gcc graph_processor.c cJSON.c -o graph_processor`) so you can parse the `properties_json` column to check `"status"=="active"` per node. Parse both pipe-delimited files into arrays/structs, build a node lookup (by id) so edge filtering can check both endpoints' active status in O(1). For the Cypher output, sort node ids numerically before emitting `CREATE` lines, then sort qualifying edges by (src_id, tgt_id) numerically before emitting `MATCH...CREATE` lines, following the exact string format given (including quotes and property names). For aggregation, accumulate a running sum per distinct `rel_type` over every edge in the file (no active filter) and hand-serialize it as JSON (or use cJSON) to `edge_aggregation.json`. Compile and actually execute the binary so both output files exist on disk.