Read the CSV with Python's `csv` module, and for each row's `raw_payload`, first fix the literal backslash-u escapes before JSON-parsing: since the string contains literal `E` text rather than an actual unicode char, use `raw_payload.encode().decode('unicode_escape')` (or a regex substitution replacing `\uXXXX` sequences with `chr(int(XXXX,16))`) before calling `json.loads`. Group rows by date into a dict, then iterate the full date range from 2023-11-01 to 2023-11-05 with `datetime`/`timedelta` day-by-day so every day appears even if absent from the CSV (this is the "gap-filling" step — don't just print whatever unique dates exist in the file). For each event string, compute `difflib.SequenceMatcher(None, event, "SERVER_CRASH").ratio()`, take the max per day, and round with Python's `round(x, 2)` — watch out that `round()` can produce values like `1.0` rather than `1.00`; format explicitly if the template expects two decimals. Build each line from the two exact templates given and join with newlines into `/home/user/summary.txt`.
