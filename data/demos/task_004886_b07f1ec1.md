Work through the four objectives in order and verify each before moving on. First, `tar czf` the config directory to the exact backup path — do this before touching anything else. Second, fix the symlink: `rm` the existing broken link (don't try to edit it in place) and recreate it with `ln -s /home/user/app_env/run/backend.sock /home/user/app_env/sockets/upstream.sock`, using the absolute target path so it resolves regardless of cwd. Third, implement the storage check in backend.cpp: use `std::filesystem::space("/home/user")` which returns a `space_info` struct with `.available` giving bytes free (not `.capacity`, which is total size) — format the response as `"OK: " + std::to_string(info.available) + "\n"` exactly, matching the required response format precisely (including the trailing newline). Compile with `g++ -std=c++17 -o /home/user/app_env/bin/backend src/backend.cpp`. Fourth, launch the daemon in the background (`&` or `nohup`), give it a moment to bind the socket, then query with `echo -n "STATUS" | nc -U /home/user/app_env/sockets/upstream.sock` (note: send "STATUS" not "STATUS\n" as literal text depends on nc behavior — check nc appends newline or add one explicitly), and redirect/save the raw output to verify.log. Confirm the daemon is actually listening on backend.sock before querying, and check the socket path length fits in `sun_path`.