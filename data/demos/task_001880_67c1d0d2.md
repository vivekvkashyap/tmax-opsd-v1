Work through each requirement independently, then assemble the final report last so ordering/formatting mistakes don't corrupt earlier work.
1. Corrupted archives: use `find /home/user/backups/archives -name '*.tar.gz'`, then test each with `tar -tzf <file> >/dev/null 2>&1`; a non-zero exit means corrupted. Collect and sort those paths.
2. Multi-line log parsing: split `job_history.log` on `---` (e.g. `awk` with `RS="---"` or a Python loop), then within each record grep the `Job ID:` and `Status:` lines; keep the ID only when Status is FAILED. Don't assume fixed line positions — parse by label.
3. ELF detection: recursively walk `/home/user/backups/data` and use `file` (or check the `\x7fELF` magic bytes directly) rather than trusting file extensions, since ELF binaries here are named `.bin`/`.exe`/etc.
4. Encoding fix: for each `.txt` under `/home/user/backups/notes`, detect/assume ISO-8859-1 and convert in place with `iconv -f ISO-8859-1 -t UTF-8` (write to a temp file then move over the original), counting how many files you converted.
Finally write the report to `/home/user/backup_report.txt` matching the exact section headers and sort order specified, then verify with `file` that a converted note now reports UTF-8.
