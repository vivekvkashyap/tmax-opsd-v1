Build `/home/user/orchestrate.sh` as a sequential pipeline where each stage's output feeds the next, and compute values rather than assuming them. Count `N` with `grep -c '^>' /home/user/data/input.fasta`. For the Monte Carlo Pi estimate, use `N * 10000` as the iteration count computed from that live `N` (not a hardcoded number) — implement the sampling in `awk` (random x,y in [0,1], check `x*x+y*y <= 1`, ratio*4) or bash with `$RANDOM`, and write just the numeric estimate to `/home/user/data/pi_estimate.txt`. For PDB parsing, count Alpha Carbon atoms by matching lines starting with `ATOM` where the atom-name field (columns 13-16, i.e. characters 13-16 in a fixed-width PDB record) equals `CA` — use `awk`/`cut` on fixed columns rather than a loose grep for "CA" anywhere in the line, since other atom names or residue names can contain "CA" as a substring and produce false positives. Finally invoke `papermill /home/user/templates/report.ipynb /home/user/data/final_report.ipynb -p ca_count $C -p pi_estimate $PI` with the shell variables populated from the previous steps. Make the script executable and ensure it runs non-interactively end to end.
