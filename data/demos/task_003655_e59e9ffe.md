First get the plumbing correct before touching the math: check `/app/nanopore_pipeline/.env` for the Redis port the worker expects and confirm it matches the port your actual `redis-server` is listening on (start redis, then `redis-cli -p <port> ping`); separately, read `streamer.py` to see which port Flask actually binds (`app.run(port=...)`) and make sure `worker.sh`'s `curl` calls target that same port — fix any mismatch in both places. Only after `worker.sh` can successfully fetch a signal and reach Redis should you tackle accuracy: replace the naive fixed-step Riemann sum in `integrate.py` with an adaptive method — either `scipy.integrate.quad`/`simpson` with sufficient sampling, or implement adaptive step refinement that subdivides intervals where the signal changes sharply (based on local curvature/derivative) rather than using a uniform step. Run Redis, launch the Flask streamer in the background (`&` + confirm with `curl`), then run `worker.sh` end-to-end over all 100 events, redirecting `event_id,total_charge` pairs into `/home/user/results.csv`. Spot-check a few known peak shapes manually to sanity-check your integration before trusting the full run.
