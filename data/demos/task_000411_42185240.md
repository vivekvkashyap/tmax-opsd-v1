Split this into an extraction stage and a forging stage, and validate the extraction before moving on. For extraction, use `ffmpeg -i /app/target_ids.mp4 -vf fps=<rate> out/frame_%04d.png` to dump frames (pick a frame rate/dedup strategy that captures each distinct flashing ID at least once — since IDs match pattern `uid_[a-z0-9]{8}`, extract enough frames to cover all flashes, e.g. sample every frame or every few frames), then run `tesseract` on each frame image to OCR the text, collect the matches with a regex like `uid_[a-z0-9]{8}`, and deduplicate into a set — print the count and spot-check a few against the video visually/manually if possible to sanity-check OCR accuracy (tesseract can misread characters, so consider preprocessing frames: grayscale, threshold/binarize, and upscale before OCR to raise accuracy toward the 0.95 target). For forging, build a JWT with header `{"alg":"none","typ":"JWT"}` and payload `{"user_id": "<id>", "role": "admin"}`, base64url-encode each part (no padding) with `.` separators and a trailing `.` for the empty signature — write this construction as a small Python helper so it's applied identically to every extracted ID, one token per line in `/home/user/forged_tokens.txt`.
