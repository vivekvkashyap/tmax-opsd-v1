Treat this as a two-stage pipeline: pull relational data out of SQLite, then do the graph computation in Python before writing JSON. For degree, remember `friendships` stores each undirected edge only once — build an adjacency structure (e.g. a dict of sets, or an undirected graph via networkx) and add both directions per row so each endpoint's neighbor set grows, then take set sizes (not row counts) as degree so a user appearing in multiple friendship rows isn't overcounted and isolated users default to 0. For engagements, LEFT JOIN posts to engagements per user and SUM(likes+shares), defaulting missing/null sums to 0 for users with no posts. Merge the two computed maps with the users table, filter to degree >= 2, then sort in Python (not SQL) by total_engagements descending, breaking ties by user_id ascending — get the sort key and tie-break order exactly right since JSON output must match an exact expected structure. Write the result as a JSON list of dicts with keys in the given field names, then actually execute the script to produce `/home/user/influencer_metrics.json`.
