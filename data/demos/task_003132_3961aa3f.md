Build the pipeline in the order the spec describes, and reverse-engineer the legacy scanner's behavior empirically rather than guessing: run `/app/legacy_scanner` against known clean and evil sample files from `/app/corpus/`, and use `strings`/`file`/a hex viewer on the binary and on sample inputs to determine (a) the exact watermark byte sequence it flags and (b) how it handles alternate text encodings — since the task says the actor "obfuscates by changing character encoding," check whether the watermark also triggers when the file is UTF-16 (LE/BE) encoded, not just ASCII/UTF-8, and confirm by constructing your own tiny UTF-16-encoded test file. For the shell script: concatenate chunk files in sorted alphabetical order (`cat chunk_* ...` after `ls | sort`) into one blob, verify gzip integrity strictly with `gzip -t` (which will reject corrupted headers/checksums/trailing garbage — check its exit code, don't just check the tar step), then `tar -tzf`/`tar -xzf` to extract only if the integrity check passed. Scan every extracted text file for the watermark, converting each file's bytes with `iconv` from UTF-16LE/BE to UTF-8 (and also checking the raw bytes) before searching, so obfuscated encodings are still caught. Print exactly `CLEAN` or `EVIL` per the rules, make the script executable, and validate it against every sample in both `/app/corpus/clean/` and `/app/corpus/evil/` before considering it correct — a single misclassification on those known samples predicts failure on the larger hidden set.
