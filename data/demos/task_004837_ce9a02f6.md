Build the pipeline as clearly separated stages so each is easy to verify: (1) load the CSV with the `csv` module and validate each row against every stated rule (id integer, age integer > 0, income float >= 0, spending_score float in [0,100]) — wrap numeric parsing in try/except and drop the row on any failure or missing field, rather than assuming clean data; (2) after filtering, compute min/max for age, income, spending_score only over the surviving valid rows; (3) apply the same min-max formula to both the dataset and the fixed target profile (age=35, income=75000, spending_score=50) using the dataset's min/max, not separately-derived bounds; (4) compute Euclidean distance in the 3D normalized space between each valid row and the normalized target using `math.sqrt` over squared differences; (5) sort by ascending distance and take the top 3 ids. Print/inspect intermediate valid-row count and min/max values while developing to catch data-cleaning bugs early, then write only the final 3 comma-separated ids (most-similar first) to the output file with no trailing extras.
