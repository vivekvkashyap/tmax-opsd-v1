Treat the audio file as a spec document you must recover before writing any SQL. Use `ffmpeg -i /app/voicemail.wav -ar 16000 -ac 1 output.wav` to normalize it, then transcribe with an installed tool (`openai-whisper` or `SpeechRecognition`+`pocketsphinx`) — listen to or print the transcript and read it carefully rather than skimming, since it will name the specific table, grouping, and filter condition you must use in place of the corrupted `relationships` table. Before trusting the transcript, sanity-check it against the actual schema: run `sqlite3 /app/network.db ".schema"` to see what tables/columns really exist (e.g. an audit/event log table with source, target, event type, and timestamp columns), and confirm the transcript's instructions map onto real column names. Implement the script to query that table, group by (source, target), take only the most recent event per pair, and keep pairs whose latest status indicates the link is currently active — then output the sorted target IDs as a JSON array, and empty array for unknown/missing nodes. Test against several different node_ids and cross-check by hand-tracing the raw table rows for at least one node.