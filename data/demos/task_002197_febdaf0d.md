Tackle the three failures independently and confirm each is actually fixed before moving on. First, try `pip install -e /app/vendored/py-fast-distance` and read the linker error carefully — a C extension calling libm functions (sqrt/pow/etc.) that fails to link with "undefined reference to `sqrt`" almost always means the `Extension(...)` in setup.py is missing `libraries=["m"]`; add it and reinstall, then verify with a quick `python3 -c "import fast_distance; print(fast_distance.compute(...))"` before moving on. Second, open pipeline.py's SQL query and change it to an `INNER JOIN` between the sensor table and `calibration` on `sensor_id`, adding a `WHERE status = 'ACTIVE'` filter — an INNER JOIN (not LEFT JOIN) is what drops sensors lacking a calibration row, and the ACTIVE filter is required by the spec. Third, find the scoring line and change the `+` to `*` so `score = fast_distance.compute(lat, lon) * factor`. Make sure the script accepts exactly one CLI argument (the DB path), sorts output by sensor_id ascending, and prints one float per line rounded to 4 decimal places — test end-to-end against a small hand-made SQLite DB with a mix of ACTIVE/INACTIVE sensors and sensors with/without calibration rows to confirm the join and filter behave as expected, since your output will be fuzz-compared bit-for-bit against a hidden oracle.