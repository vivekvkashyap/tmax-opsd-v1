Start by empirically characterizing corruption rather than guessing thresholds: run `/app/sensor_decode` on several files in `/app/data/raw/`, inspect the resulting CSVs (`reading_id,temperature,humidity,pressure`), and look for physically impossible values — humidity outside [0,100], temperature below absolute zero (-273.15°C), or malformed/missing columns — since the decoder does no bounds checking and simply reflects garbage from corrupted binary input. Write `/home/user/filter.py` to parse the CSV (`csv` module), validate every row against these physical bounds plus basic well-formedness (correct column count, parseable floats), and exit 1 on any violation, exit 0 only if every row is clean — test it against a handful of clean and manually-corrupted CSVs you construct yourself before trusting it on the hidden corpora, since 100% accuracy is required. For the ETL/model script: iterate `.bin` files, decode each, run them through your `filter.py` as a subprocess check (or import its validation logic directly) to drop corrupted ones, merge surviving rows with `/app/data/metadata.csv` on `reading_id`, then bootstrap-resample the minority `status` class (e.g. `sklearn.utils.resample` with `replace=True` up to majority-class count) before training a `RandomForestClassifier`, and write `classification_report(...)` output to `/home/user/report.txt`.