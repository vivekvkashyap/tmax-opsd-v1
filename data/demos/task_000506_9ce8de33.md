Break this into four sequential Rust phases and validate each before moving to the next. Phase 1: shell out to `ffmpeg` to extract frames from `/app/stability.mp4` (e.g. as PNGs or raw pixel data), then for each frame locate the white square by scanning pixels for the bright region and recording its topmost/Y coordinate; scan all frames and pick the index `N` where that Y coordinate is maximal (lowest on screen) — don't assume a formula, actually measure it from decoded pixels. Phase 2: sequentially unzip `rev_0.zip` through `rev_<N>.zip` (skip revisions beyond N), untar each payload, and extract into `/home/user/extracted_configs/`, letting later revisions overwrite same-named files as a real incremental restore would. Phase 3: walk that directory recursively, and for any file ending in `.modX`, strip the suffix; if the stripped name already exists, append the `.modX` file's bytes onto the existing file's content and remove the `.modX` file, otherwise just rename it. Phase 4: read all remaining files in alphabetical order by absolute path, concatenate their bytes, and RLE-encode as `[count:u8][byte]` pairs, splitting any run longer than 255 into multiple pairs; write the result to `/home/user/final_state.dat`. Getting N exactly right in Phase 1 is what determines the final byte count, so verify your frame-detection logic against a couple of known frames before trusting it.
