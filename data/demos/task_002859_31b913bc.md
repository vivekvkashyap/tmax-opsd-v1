Start by reading the legacy Python 2 server at `/home/user/legacy/audio_graph_server.py` to understand its graph-resolution and audio-metric logic, then port it to a Python 3 CLI (argparse with `--audio` and `--graph`) rather than a websocket server. For graph traversal, implement a proper topological sort of the DAG by `deps`, and when multiple nodes are simultaneously ready (no unresolved deps), execute them in ascending order of their `id` string to guarantee determinism — this tie-breaking detail is easy to skip and will cause mismatches under fuzzing. Use a real audio library (`wave`, `numpy`, or `soundfile`) to compute `duration`, `peak`, `rms`, and `zcr` from `/app/interview.wav`, caching intermediate results per task id so downstream ops can reuse them. Output must be compact, deterministic JSON (`json.dumps(..., separators=(',',':'))` or similar) mapping task id to result, printed to stdout with no extra text/logging leaking in. Since correctness is judged by exact byte-for-byte comparison against a hidden equivalence check across many random DAGs, write your own varied test graphs locally and confirm output formatting (float precision/rounding) is stable and consistent across runs before considering it done.