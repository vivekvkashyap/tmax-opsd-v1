First extract the secret header from the image: run `tesseract /app/protocol_spec.png stdout` and read the printed text carefully — it embeds a phrase like "MAGIC_HEADER: <value>", so parse out the token after that label rather than assuming any particular string; the value has mixed case and punctuation, so copy it exactly as OCR reports it (re-run/inspect if the OCR looks garbled). Then write `/home/user/ws_normalizer.sh` as a POSIX bash script taking `$1` as the payload: prepend `"${HEADER}: "`, then transform the ORIGINAL input (not the already-prefixed string) by uppercasing (`tr '[:lower:]' '[:upper:]'` or bash `${var^^}`) and replacing spaces with underscores (`tr ' ' '_'` or `${var// /_}`), concatenating header+colon+space+transformed-payload with no extra newline logic beyond `echo`/`printf`. Since it's bit-exact fuzzed against a hidden reference across thousands of random alphanumeric+space strings, be careful with bash quoting of `$1` (always double-quote), locale-dependent case folding, and any trailing whitespace/newline differences between `echo` and `printf`. Make the script executable with `chmod +x`, then test it locally against a handful of manual example strings (including edge cases like empty string, all-uppercase, leading/trailing spaces) before considering it done.