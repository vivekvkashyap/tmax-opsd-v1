Start by installing requirements and launching the server (`/home/user/start_server.sh`), then read `/home/user/api/app.py` carefully: look for how it decodes and verifies JWTs manually rather than using a vetted library — specifically check whether the `alg` field from the token header is trusted and whether an `alg: none` (or case variants) path skips signature verification entirely. That is the classic "JWT algorithm confusion / signature-not-verified" flaw, which maps to a specific CWE for improper cryptographic signature verification. To exploit it, hand-craft a JWT yourself: base64url-encode a header `{"alg":"none","typ":"JWT"}` and a payload claiming `role: admin`, join them with dots and an empty signature segment, then send it as a Bearer token to `/admin/data` and read the secret from the JSON response. For log analysis, write a regex over `Authorization: Bearer <token>` headers in the log, decode each token's header segment, and flag IPs whose decoded `alg` matches the same none-style bypass (case-insensitively) — not just any failed/403 request. Finally build a patched `fixed_app.py` that hardcodes and enforces `alg == HS256`, verifies the signature, and rejects any other algorithm.