Implement the C program's pipeline stages in exactly the order specified, since later steps depend on earlier ones' output. Parse the CSV line by line (watch for the header row), and for `status_message` replace any byte with value >127 (cast to `unsigned char` before comparing, since plain `char` may be signed) with `'?'`. Apply the temperature/humidity range checks and drop rows outside them before deduplication. For deduplication, keep a growing list/hash of `(timestamp_ms, sensor_id)` pairs seen among rows that already passed validation, and skip any later row matching one of those pairs — do not count invalid rows as "seen". For the rolling average, maintain a small per-`sensor_id` history (e.g., a dynamic array or map keyed by sensor_id) of up to the last 3 valid temperatures and average whatever is present so far; update it only for rows that survive validation and dedup. Write the output with `%.1f` for temperature/humidity and `%.2f` for the rolling average, fields comma-joined with no trailing separator, matching the header exactly given in the task. Compile with `gcc` and run it, then diff `/home/user/cleaned_sensor_data.csv` line-by-line against your own hand-traced expectations from the input file to catch off-by-one errors in the rolling window or dedup logic before considering it done.