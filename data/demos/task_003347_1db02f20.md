Do not guess the sort order — determine it empirically. Generate a handful of small binary files with deliberately overlapping/tied `score` and `label` values (32-byte records: uint64 id, double score, char[16] label, matching the exact struct layout including any padding — verify record size assumptions with a minimal reader first), run the legacy oracle binary named in the task on them, and inspect the output byte-for-byte to infer the primary/secondary/tertiary sort keys and ascending/descending direction for each, rather than assuming a single obvious ordering. Once the comparator is nailed down, implement it in C++20 using a parallel algorithm — `std::sort` with `std::execution::par_unseq` (needs TBB linked) or explicit thread-partitioned merge sort — since a plain single-threaded `std::sort` won't clear the 1.5x speedup bar. Set up CMake to `find_package(TBB REQUIRED)` (or use OpenMP) and link it, placing the built binary at `/home/user/workspace/build/fast_sorter` as required. Validate correctness by diffing your binary's stdout against the oracle's on the same input file before worrying about speed, then benchmark on a large generated file.
