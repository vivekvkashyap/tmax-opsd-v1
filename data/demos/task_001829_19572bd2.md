Apply the patch first (`git apply` or `patch -p1` from the repo root), then run `cargo build` and read the compiler error carefully: `#[serde(borrow)]` on a struct holding `&str` fields fails because those slices borrow from a `String` that is dropped once the loading function returns. Fix this by making the config struct own its data — change borrowed `&'a str` fields to owned `String` and drop the `#[serde(borrow)]` attribute and the lifetime parameter — rather than trying to extend the borrow's lifetime. After it compiles, run `cargo test` and inspect the failing test in `src/routing.rs`; compare the patch's diff to the original path-matching logic to find the introduced logical error (e.g. a wrong path prefix or comparison) and correct it so all tests pass. For the passphrase, run the provided `/usr/local/bin/transcribe /app/voicemail.wav` tool and use its output (lowercased, punctuation stripped) as the header's required value in `proxy_config.json` — do not guess or hardcode a phrase. Build and start the proxy in the background, confirm it listens on 127.0.0.1:8080 with curl before writing READY to status.txt.