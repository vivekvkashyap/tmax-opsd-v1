First collect data by looping `for id in 1..50; do /app/hardware_sim $id >> raw_capture.txt; done` (or per-file), then strip the non-numeric log lines: use `awk`/`sed`/`grep` to keep only lines between the `--- BEGIN DATA ---` and `--- END DATA ---` markers (e.g. `awk '/BEGIN DATA/{f=1;next}/END DATA/{f=0}f'`), concatenating all 50 runs' 100-row blocks in workload order into `/home/user/raw_data.txt`. In Python, load with `numpy.loadtxt`, standardize each column with `(X - X.mean(axis=0)) / X.std(axis=0, ddof=0)` (guard against zero-std columns), run `numpy.linalg.svd(X_scaled, full_matrices=False)`, compute cumulative explained variance from `S**2`, and pick the smallest `k` where cumulative variance ≥ 0.95. Reconstruct with `U[:,:k] @ diag(S[:k]) @ Vt[:k,:]` and save via `numpy.savetxt(..., delimiter=',')` with no header/index — get this reconstruction formula and the variance threshold exactly right since grading is MSE-based against a reference. Also produce the PC1-vs-PC2 scatter plot (`U[:,0]*S[0]` vs `U[:,1]*S[1]`, or Vt-projected scores) saved as PNG via matplotlib.