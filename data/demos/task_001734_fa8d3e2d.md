Parse `/home/user/vm_data/manifest.json` (e.g. with `jq` or Python's `json` module) and select entries whose `connections` array is empty — don't rely on string matching, check the actual array length/emptiness. Collect the matching `disk_image` filenames and `vm_id`s. Build the tar archive with `tar -czf /home/user/cold_storage/orphaned_disks_backup.tar.gz -C /home/user/vm_data/disks <file1> <file2> ...` — the `-C` flag (change directory before adding) is the key to avoiding the parent directory structure ending up in the archive paths; a naive `tar -czf ... /home/user/vm_data/disks/file1` would embed the full path. Sort the disconnected `vm_id`s alphabetically and write one per line to `/home/user/finops_report.txt` with no extra formatting. Verify your work with `tar -tf` on the resulting archive to confirm only the bare filenames appear, not directory prefixes.