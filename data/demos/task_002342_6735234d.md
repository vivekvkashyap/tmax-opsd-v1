Set up the Cargo project first (`cargo new /home/user/pca_prep`) and add `nalgebra` (or implement the linear algebra by hand with plain Vec<f64> if you want to avoid dependency-fetch issues — either is fine as long as the math is exact). Parse the CSV with basic split-on-comma and `f64::parse`, building a row-major matrix; determine N (rows) and M (cols) from the data itself, don't hardcode them. Compute column means by summing each column and dividing by N, then subtract to center the data. Compute the sample covariance matrix as `X_c^T * X_c / (N-1)` — you only actually need its trace, which equals the sum over columns of (sum of squared centered values in that column)/(N-1), so you can compute it directly without materializing the full covariance matrix if you prefer a simpler, less error-prone implementation. Round the final trace to exactly 4 decimal places (careful with floating point rounding — e.g. format with `{:.4}` rather than manual rounding) and write the JSON with `rows`, `cols`, `total_variance` fields matching the exact schema. Run `cargo run` and confirm `/home/user/experiment_log.json` is populated before finishing.