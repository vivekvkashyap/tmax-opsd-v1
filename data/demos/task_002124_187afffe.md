This is a multi-stage chain — solve each stage before moving to the next, and don't skip straight to guessing the flag. Stage 1: since `auth_backend` runs only briefly, poll `/proc/*/cmdline` (or use `ps` in a tight loop, e.g. `while true; do ps aux | grep auth_backend; done` or a script reading `/proc/[pid]/cmdline` for the `--password <hash>` argument) fast enough to catch it before it exits — a 1-2s sleep loop matching the process's own interval should work. Stage 2: with the captured SHA256 hash, iterate `/home/user/wordlist.txt`, hash each candidate word with `sha256sum`/`hashlib.sha256`, and compare to the captured hash to recover the plaintext password. Stage 3: POST `{"username":"admin","password":"<cracked>"}` as JSON to `http://127.0.0.1:5000/login` and capture the returned `auth_token`. Stage 4: send that token as `Authorization: Bearer <token>` to `/api/items?id=1`, then probe the `id` parameter for SQL injection (it's concatenated unsanitized into a query) using a UNION-based payload against the `id=` parameter to pull `flag` from `hidden_secrets` instead of the normal `items` columns — test incrementally (single quote, then UNION SELECT with matching column count) rather than guessing the full payload blind. Write the extracted flag exactly to `/home/user/solution.txt`.
