Build and test the math in isolation before writing the full script: use `awk` (which has native floating point and can call `sqrt()`) to compute accuracy as correct/n for each file, then the standard-error formula and the ±1.96·SE bounds, and use `printf "%.4f"` (in awk or a final formatting step) to round to exactly 4 decimals — avoid `bc` unless you configure sufficient scale, since default `bc` truncates rather than rounds. For the reproducibility check, compare columns 4 and 5 (pca1, pca2) row-by-row between the two CSVs after aligning by `id` (don't assume row order matches — join on id explicitly, e.g. with `join` after sorting, or an associative array in awk keyed by id); on the first mismatch, print exactly `Reproducibility Error` and `exit 1` immediately, short-circuiting the rest of the script. Only if all rows match do you compute accuracies and CI and write the three-line report to `/home/user/experiment_report.txt` in the exact specified format. Make the script executable and test it against the two given CSVs, checking your computed numbers by hand against the stated formula before trusting the script's output.
