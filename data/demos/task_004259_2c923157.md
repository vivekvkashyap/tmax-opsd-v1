In Go, read the CSV with `encoding/csv` (set `FieldsPerRecord` or check `len(record) == 4` explicitly per row), and validate each field independently: timestamp non-empty; `sensor_id` parses via `strconv.Atoi` and falls in `[1,3]`; temperature and pressure parse via `strconv.ParseFloat(s, 64)` — any parse error means the row is invalid and gets skipped, not just partially processed. Only rows passing all checks go into partitioned output: create `/home/user/etl_output/sensor_{id}/` per sensor with `os.MkdirAll`, and append each valid row as a JSON object (marshal a struct with matching field names/types, int for sensor_id, float64 for temp/pressure) to that partition's `valid.jsonl`, one JSON per line. Simultaneously accumulate temperature/pressure pairs across all valid rows (regardless of partition) into slices for the correlation calc. Compute Pearson correlation manually with the standard formula (sums of x, y, xy, x², y², n) using `math` package functions — don't approximate; then format with exactly 4 decimal places via `fmt.Sprintf("%.4f", corr)` and write that string (no extra whitespace/newline surprises) to `/home/user/metrics/correlation.txt`. Create both output directories before writing. Run the program and independently verify line counts per partition and the correlation file content match your own hand-check of the filtered rows.