Build this as three separable pieces and verify each before moving to the next. (1) Environment: `python3 -m venv /home/user/sim_env`, then use `/home/user/sim_env/bin/pip install numpy` — inside the bash script, invoke the venv's python directly via its absolute path (e.g. `/home/user/sim_env/bin/python3`) rather than relying on `source activate`, which is more robust when the script is later executed non-interactively. (2) Regression: for each `.txt` file under `/home/user/data/candidates/`, load reference.txt as X and the candidate as Y with numpy, then compute slope and intercept via `np.polyfit(x, y, 1)` (returns slope, intercept) — do not hand-roll the normal equations, it's error-prone. Compute score as `(slope-1)**2 + intercept**2`. (3) Aggregation: write results to the CSV with basenames only (strip directory), format floats with `%.6f`, and after processing all files pick the minimum-score row and write just its basename (no trailing content beyond a newline) to best_run.txt. Make the bash script executable with `chmod +x`, then actually run it once to confirm both output files are populated before considering the task done — an inline heredoc Python block invoked from bash is the cleanest way to embed the numpy logic.
