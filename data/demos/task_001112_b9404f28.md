Treat this as three independent sub-problems you can validate separately before combining. (1) CSV parsing: don't split naively on newlines — use a proper CSV reader (Python's `csv` module, or `pandas.read_csv`) that respects quoted fields so embedded newlines inside quoted `event_remarks` values don't fragment rows; load the parsed rows into a SQLite `measurements` table at `/home/user/sensors.db` and sanity-check the row count matches the number of logical records, not raw newlines. (2) Audio: transcribe `/app/calibration.wav` with a speech-to-text tool (e.g. `pip install openai-whisper` and run it, or another local STT), then parse the transcript text for sensor-name/offset pairs — watch for spoken number words like "plus"/"minus" and spelled-out digits that need converting to signed numeric offsets — and add each sensor's offset to every one of its `value` entries in the database (don't hardcode any offset; derive it from what the transcription actually says). (3) Aggregation: compute a 3-hour rolling average of corrected values per sensor_id ordered by timestamp (a SQL window function with a time-based frame, or an equivalent Python pass using pandas `rolling` on a time index), then format each result into `/home/user/rolling_report.txt` using exactly the given `[<timestamp>] Sensor <sensor_id> Rolling Avg: <value rounded to 2 decimals>` template, sorted chronologically then by sensor_id.