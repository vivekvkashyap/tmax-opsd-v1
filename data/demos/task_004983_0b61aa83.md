Work through this as a layered integration fix, verifying each layer independently before moving to the next. First run `/app/start_services.sh` and confirm Postgres/Redis are actually accepting connections (e.g. `pg_isready`, `redis-cli ping`). Fix `/home/user/nginx.conf` by adding a `proxy_pass http://127.0.0.1:8081;` inside the relevant `location` block, then start nginx with `-c` pointing at it and confirm it's listening on 8080. In `config.hpp`, hardcode the given Postgres/Redis connection parameters exactly. In `db_handler.cpp`, modify `execute_postgres_query` to build SQL ending in `ORDER BY id ASC LIMIT <limit> OFFSET <offset>` using the passed integers (parameterize or validate them as integers — don't string-interpolate untrusted values here since this is the pagination path, not the sanitizer). For the sanitizer, actually read every file under `/app/corpora/clean/` and `/app/corpora/evil/` to derive the real distinguishing patterns (common SQL/NoSQL injection signatures like `UNION SELECT`, `OR 1=1`, `DROP TABLE`, `; --`, `pg_sleep`, `$where`) rather than inventing your own; implement `is_safe_payload` as substring/regex checks that block those exact patterns while allowing every clean sample verbatim — test both corpora locally with `curl` against your running binary before declaring done. Compile with `make`, run `./query_router` in the background, and confirm end-to-end routing through nginx on 8080.
