First fix the vendored library build: inspect `/app/vendored/simple_csv_parser/Makefile` for a bad `CC=` assignment and correct it to a real compiler (or override with `make CC=gcc`), then run `make` and confirm `libcsv_parser.a` and the header are produced. Only after the library builds should you write `/home/user/graph_server.c`: parse `nodes.csv`/`edges.csv` with the fixed parser, build an adjacency list keyed by node id, and implement descendant lookup as a BFS/DFS traversal over children (watch out for cycles if any exist — track visited nodes). For the TCP server, use a standard `socket()/bind()/listen()/accept()` loop on `127.0.0.1:8080`, read line-delimited queries (buffer until `\n`), parse the `GET_DESCENDANTS <id>` command, collect all reachable descendant ids into a set, sort them lexicographically (string comparison, not numeric), and emit them comma-joined with a trailing `\n`, or `NONE\n` if empty/unknown id. Keep the accepted connection open in a loop for multiple queries rather than closing after one. Log every incoming query line to `/home/user/query_log.txt`. Compile with `gcc -I<parser_include_dir> graph_server.c -L<lib_dir> -lcsv_parser -o graph_server`, then launch it with `&`/`nohup` so it stays running in the background, and verify with a quick manual `nc` or Python socket test before finishing.
