The critical insight here is that bit-exact reproduction of a C Monte Carlo simulation requires using C's own PRNG, not Python's `random` module — implement the DFT/normalization/TVD math natively in Python (or with `numpy.fft`), but for the seeding-and-sampling step, load `libc.so.6` via `ctypes.CDLL("libc.so.6")` and call its `srand(seed)` and `rand()` functions directly so the exact same LCG sequence is produced as in the C binary. Compute the seed as `int(P[0] * 10000.0)` from the DC-bin normalized magnitude, call `srand()` with it, then call `rand()` 100,000 times taking `val % 64` each time to build the empirical distribution Q — be careful that `rand() % 64` in C matches `ctypes` `rand()` return type handling (glibc's `rand()` returns a C `int`; make sure you're not accidentally getting a different range). Compute TVD as `0.5 * sum(|P_i - Q_i|)` and print with exactly `%.6f` formatting via Python's `f"{tvd:.6f}"`. Validate iteratively: generate random 64-float inputs, run both `/app/signal_processor` and your script, and diff their outputs directly rather than assuming correctness — mismatches likely trace back to PRNG or seed-computation differences.