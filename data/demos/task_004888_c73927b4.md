Write the Go ETL as a straight-line per-line transform pipeline, testing timestamp parsing separately since it has two input formats. For each line: try `time.Parse(time.RFC3339, ts)` first, and if that fails, try Apache Common Log format with `time.Parse("02/Jan/2006:15:04:05 -0700", ts)`; convert the parsed time to UTC with `.UTC()` then format with `.Format(time.RFC3339)` — verify this produces the `...Z` suffix (Go's RFC3339 formatter does this automatically for UTC times, no manual string surgery needed). For IP masking, split on `.`, replace the last octet with `"0"`, rejoin — don't assume fixed-width formatting. For email masking, split on `@` and reconstruct as `"***@" + domain`, then drop the original field and add `masked_email` — use a struct with only the required output fields (or a map, deleting the old keys) to guarantee exact output schema. For URL path extraction, use `net/url.Parse` and take `.Path` only, discarding query/fragment/host. Classify `speed` with simple threshold comparisons matching the inclusive/exclusive boundaries stated (100 and 500 are boundary values — get the inclusive side right). Accumulate the four counters as you process each line and marshal `metrics.json` with `encoding/json` at the end. Run with `go run` or build first, and inspect the output files directly against the schema before considering it done.