Implement this in three clearly separated pieces and test each before combining. First, a CSV reader that parses `/home/user/dataset.csv` (skip header, parse floats). Second, a Newton-Raphson solver for f(y) = y^3 + p1*y - p2*x = 0 given fixed p1, p2, x: derivative f'(y) = 3y^2 + p1, update y -= f(y)/f'(y), starting at y=1.0, iterating up to 50 times or until |f(y)| < 1e-6 — verify this converges correctly on a hand-picked (x, p1, p2) triple before moving on. Third, gradient descent over (p1, p2) minimizing SSE across all dataset rows: at each of 1000 iterations, use central finite differences with h=1e-4 (perturb p1 by +h/-h, recompute SSE via the full Newton-Raphson solve for every row, same for p2) to get each partial derivative, then update p1 -= 0.005*grad_p1, p2 -= 0.005*grad_p2 — starting from p1=1.0, p2=1.0. This is O(iterations * rows * newton_steps) so keep the solver tight. After 1000 iterations, round p1, p2, and final SSE to 4 decimal places and write them to `/home/user/results.json` in the exact key order/format shown. Compile with `g++ -O3 -std=c++17` and run, then sanity-check the JSON values look like a converged fit (small SSE) before finishing.