Start by profiling, not guessing: run the existing parser against `/home/user/system.log` and time it, or isolate the regex in `parser.py` and test it against one of the malformed lines directly to confirm it's the source of catastrophic backtracking. The classic cause is a regex chaining multiple greedy `(.*)` groups separated by literal characters (e.g. `-`), which on ambiguous/malformed input causes exponential backtracking — fix by replacing the greedy `.*` groups with non-greedy `.*?` or, better, character classes that exclude the delimiter (e.g. `[^-]+`) so each group's match is unambiguous and matching stays linear. Re-time the fixed regex against the full log to confirm it's well under the 2-second budget before moving on. Then write the Dockerfile (standard `python:*-slim` base, copy the package, `ENTRYPOINT ["python", "main.py"]`), and the pipeline script that builds the image, runs it with the log file bind-mounted and stdout redirected to `dashboard.html`, starts `python -m http.server 8080` in the background in the output directory, and curls the served file to capture and append the status code to `deploy_status.txt`.