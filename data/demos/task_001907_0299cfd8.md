Start by running `tesseract /app/topology_rules.png -` to recover the exact rule text (replica counts, label keys/values, CIDR ranges) — don't guess these constants, read them off the OCR output and re-check for transcription errors (e.g. confirm digits and punctuation look sane) before hardcoding them into the Rust program. Scaffold the Rust project with `cargo new`, add `serde_yaml`/`serde` (and `walkdir` if useful) to `Cargo.toml`, and parse each YAML in `/home/user/input_manifests/` into a generic `serde_yaml::Value` (not a rigid struct) so you can mutate only the specific fields the rules touch without dropping unrelated fields — this is the most common failure mode (rewriting the whole doc loses metadata/annotations). Apply each extracted rule conditionally based on `kind`/`metadata.namespace` and write files with matching names to `/home/user/output_manifests/`. For `pipeline.sh`, compile with `cargo build --release`, check the exit status, and wrap execution in a loop (`while true; do ...; sleep 2; done`) that restarts the binary on non-zero exit. Validate iteratively against `/home/user/evaluate_manifests.py --metric-only` until you clear 0.95, inspecting mismatches to see which rule is misapplied.
