Break this into extraction, filtering, and serving stages, verifying each before moving on. First run `tesseract /app/auth_label.png stdout` (trim whitespace/newlines from the result) to recover the Bearer token — don't guess it. Next, recursively find `.dat` files under `/app/archive_data/` (`find ... -name '*.dat'`) and for each, check the first 4 bytes against hex `42 41 4B 01` using something precise like `xxd -p -l4 file` or Python's `open(f,'rb').read(4) == bytes.fromhex('42414B01')` — don't rely on file size or extension alone, since the given fixtures include near-matches with a different trailing byte. Cross-reference the valid filenames against `index.csv` (use `csv` module or `awk`) to pull `owner`/`timestamp`, then emit `/home/user/manifest.json` as an array sorted alphabetically by `filename` with exactly those three keys. For the server, write a small loop using `socat TCP-LISTEN:8080,fork,reuseaddr SYSTEM:'...'` or a `nc`-based accept loop that parses the request line and `Authorization` header, comparing it exactly to `Bearer <recovered-token>`, and returns a proper HTTP status line plus `Content-Type: application/json` header before the manifest body on success, or 401 otherwise; launch it backgrounded and confirm it stays up with `curl` before finishing.
