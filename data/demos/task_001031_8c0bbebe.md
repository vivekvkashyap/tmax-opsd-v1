Rewrite `/home/user/deploy/deploy.py` as a sequential, dependency-aware launcher rather than firing all three `subprocess.Popen` calls at once. For each worker in turn: start it with `subprocess.Popen(["python3", "/home/user/deploy/worker.py", "<id>"])`, then poll in a loop (e.g. `while True: sleep briefly`) checking BOTH conditions before moving to the next worker — (1) `os.path.exists(f"/home/user/deploy/w{id}.ready")` and (2) a real TCP connectivity check on `127.0.0.1:<port>` (e.g. `socket.create_connection` wrapped in try/except, not just checking if the port "looks" open). Only after both checks pass for worker N do you start worker N+1; a naive attempt that starts workers concurrently or only checks the file (not the socket) will race and cause worker.py's own dependency check to crash it. After all three workers are verified ready, write exactly `DEPLOYMENT SUCCESSFUL` (no trailing newline mismatch issues) to `/home/user/deploy/status.log` and let the script exit normally — the background worker processes must remain running (do not kill or wait() on them), since Popen's default behavior already keeps them alive after the parent exits.
