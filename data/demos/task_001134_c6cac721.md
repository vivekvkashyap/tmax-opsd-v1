First get `cJSON` building as a shared object: run `make` in `/app/cJSON`, read the actual linker error, and compare the shared-library target's link command against a normal cJSON Makefile — the fix is almost certainly restoring a missing `-shared` flag on the final link line (don't just add `-fPIC` blindly; confirm what the error actually says is missing). Once `libcjson.so` builds, write `process_sensors.c` to parse the JSON with `cJSON_Parse`, then recursively walk the tree: for a node, recurse into each element of its `children` array first, summing each child's returned subtree value, add the node's own `value`, and return that total — this is naturally a post-order traversal, so compute children before the parent. While traversing, if a node's subtree sum is strictly greater than 100.0, append `id,subtree_sum` (formatted to 2 decimals, e.g. with `%.2f`) to your output buffer/file. Compile with `-I/app/cJSON -L/app/cJSON -lcjson` and set `LD_LIBRARY_PATH=/app/cJSON` (or use rpath) when running so the dynamic loader finds `libcjson.so`. Double check floating point formatting matches exactly two decimal places, since the verifier does an exact numeric comparison per row.
