The key trap here is that naive line-oriented tools (grep, plain `while read line`) will misparse the CSV because `message` fields contain embedded literal newlines inside quoted fields — you must parse the file with a proper CSV-aware reader that respects quoting across newlines. Since Python/awk/etc. are allowed inside the bash script, use Python's `csv` module (or an equivalent record-aware CSV library) invoked from within `/home/user/process_logs.sh` to read the whole file as structured rows rather than lines. Filter rows to `log_level` in {ERROR, CRITICAL}, validate `timestamp` against a strict ISO-8601 regex with year bounded 2020-2024 (reject malformed and out-of-range dates), and uppercase `user_id`. For dedup, group by exact `message` text and keep only the row with the earliest `timestamp` per group (parse timestamps to compare chronologically, not lexicographically, though ISO-8601 sorts correctly as strings too). From the deduplicated set, separately sort ERROR rows and CRITICAL rows by timestamp, take the first 2 of each, and write header + the 2 ERROR rows + 2 CRITICAL rows to the output CSV using a proper CSV writer so embedded newlines are correctly quoted, not corrupted.
