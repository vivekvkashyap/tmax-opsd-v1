Build and validate each pipeline stage independently before chaining. In Go, stream the CSV with `encoding/csv`'s reader row-by-row (do not `ReadAll` into memory) while buffering only enough context to interpolate: track the last valid (start_ms, end_ms) and look ahead to the next valid row when you hit a `-1`, then linearly interpolate intermediate rows' timestamps based on their position between the two valid anchors, rounding to the nearest integer — handle edge cases where a run of `-1`s spans multiple rows. For text, strip HTML-like tags with a regex such as `<[^>]*>`, remove punctuation with `unicode.IsPunct` filtering, lowercase, and collapse whitespace with `strings.Fields`+`strings.Join`. Write the cleaned rows to `/home/user/clean_subs.csv`, then bulk-load into SQLite (`/home/user/subs.db`, table `subtitles`) via the `sqlite3` CLI's `.import` or a small loader, matching the exact schema given. Before trusting the score, manually inspect a sample of `clean_subs.csv` rows for correct interpolation and clean text, since `/app/eval_lqs` penalizes any non-monotonic timestamp, any residual `<` or `!` character, or any non-lowercase text — run `/app/eval_lqs /home/user/subs.db` and save its output to `/home/user/final_score.txt`.
