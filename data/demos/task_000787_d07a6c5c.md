Work through the three parts in order and verify each before moving on. First, append `export MAX_STORAGE_MB=40` (a literal export line) to the end of `/home/user/.bashrc` with a here-doc or `echo >>` — don't overwrite the file. Second, write `/home/user/monitor.sh` starting with `#!/bin/bash` and `source /home/user/.bashrc`, then compute size via `SIZE=$(du -sm /home/user/telemetry_data | cut -f1)` (or `awk '{print $1}'`) to extract just the integer. Use a strict `-gt` comparison against `$MAX_STORAGE_MB`: if greater, append the CRITICAL line; otherwise append the OK line — match the required message format exactly, including spacing and colon placement, substituting the real computed size and limit values (do not hardcode a number you haven't verified `du` actually reports). `chmod +x` the script. Third, run the script once (`/home/user/monitor.sh`) so `/home/user/health.log` gets exactly one appended line reflecting the real current directory size — check the file's actual size with `du -sm` yourself first so you know which branch (CRITICAL vs OK) should fire and can sanity-check your script's output before finalizing.
