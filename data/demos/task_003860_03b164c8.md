Structure this as one Rust binary with two sequential responsibilities. First, parse the manifest CSV with the `csv`+`serde` crates, and for each row use `std::fs::create_dir_all` to build the parent directory under `/app/restored/` before calling `std::fs::hard_link(blob_path, restored_path)` — hard_link fails if the parent directory doesn't exist or if the destination already exists, so create dirs first and handle/skip existing-file errors gracefully. Second, once links are made, walk `/app/restored/` (e.g. with `walkdir` or manual recursion) to find the `.wav` file, and use `hound` to read 16-bit PCM samples, normalizing each sample by dividing by 32768.0. Chunk samples into non-overlapping windows of `sample_rate * 0.1` samples, zero-pad the final short window, and compute RMS per window as `sqrt(mean(x_i^2))` — get the window-size math and rounding exactly right since correctness is judged by MSE against a reference. For atomic output, write the JSON array to a temp file (e.g. `rms_output.json.tmp`) in the same directory, then `std::fs::rename` it to `rms_output.json` — a direct write is not atomic and could leave a truncated file if interrupted.
