Work the three sub-problems independently and verify each in isolation before wiring them together. (1) Systemd ordering: in `systemd/quotalogger.service`'s `[Unit]` section, add both `After=mock-db.service` and `Requires=mock-db.service` — `After` alone only affects ordering, not the failure-propagation you likely also want, so include both. (2) Performance: open `quotalogger/parser.py` and find where `calculate_quotas` does repeated linear scans/membership checks against a list (or nested loops) to look up or aggregate by user ID — replace the list with a `dict`/`defaultdict` (or `set` for membership) so lookups are O(1), turning the O(N^2) scan into O(N); keep the exact same aggregation/output logic. Benchmark with `time python -c "from quotalogger.parser import calculate_quotas; calculate_quotas('/app/data/test_logs.txt')"` and confirm it's well under 1.0s. (3) Deployment: `pip install -e /app/quotalogger-2.1`, copy the fixed unit file to `~/.config/systemd/user/`, then `systemctl --user daemon-reload && systemctl --user enable --now quotalogger.service`. Finally write `/home/user/ci_check.sh` (chmod +x) that runs `quotalogger-cli /app/data/test_logs.txt`, pipes through `grep '^TOTAL_USAGE:'` then `awk` to isolate just the numeric field, and redirects that single value into `/home/user/ci_results.txt`.
