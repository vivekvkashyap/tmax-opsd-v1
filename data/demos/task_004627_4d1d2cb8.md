First get a WebSocket client working against `ws://localhost:9090` (install `websocat` via package manager, or fall back to a short Python script using the `websockets` library if installation is restricted) and capture the full stream of `PKG` lines to a local file for repeated offline analysis rather than re-querying each time. Parse each line's fields, base64-decode the content with `base64 -d` (or in your parsing language), hash it with `sha256sum`, and compare against the provided hash — discard any package where they don't match; do this comparison carefully since a single flipped hex character is enough to mark it tampered. Then model the remaining valid packages as a constraint problem: each package's dependency requirement (or `NONE`) must point to a version of another *valid* (untampered) package that you've also selected — work through it by hand/script, propagating which versions become impossible once tampered ones are excluded, until exactly one full combination of core-lib/web-framework/auth-plugin remains consistent. Finally `mkdir -p` the workflows directory and write the YAML template substituting only the resolved version strings, matching the given template's whitespace and structure exactly.