Write the C parser as a small state machine over each CSV line rather than trying a one-shot regex: split on the first two unquoted commas to get ID and Category, then treat everything between the surrounding double quotes as the raw Text field. For that field, do a single left-to-right pass: whenever you see a whitespace character (space/tab/newline), skip forward consuming the whole run and emit exactly one space (unless it's leading/trailing, which you drop), and whenever you see the literal 4-character sequence `\u` followed by 4 hex digits, parse the hex to a 16-bit codepoint and manually encode it as UTF-8 bytes (1-3 bytes depending on the codepoint range — no external library, just bit-shifting per the UTF-8 spec) instead of emitting the raw backslash sequence. Track a boolean "last output char was a space" to enforce whitespace collapsing across the leading/trailing trim too. Build with the exact `gcc -O3` command given, run it against `/home/user/data.csv`, and diff your generated `/home/user/output.tsv` against your own hand-worked expectations for each edge case (leading/trailing spaces, embedded unicode, multiple whitespace runs) before considering it done.