Three independent deliverables, tackle in order. (1) Go path fix: open `/home/user/backup.go` and change the relative `tar` output path (`./users.tar.gz`) to the absolute `/home/user/backups/users.tar.gz` in the `exec.Command` args — a relative path breaks under cron because cron's working directory differs from your interactive shell's; then run `/home/user/cron_run.sh` and confirm the tarball lands in the right place. (2) Expect automation: write `/home/user/test_restore.exp` using `spawn /home/user/restore.sh`, then `expect "Enter backup file path: "` (match the prompt text exactly, including trailing space/colon), `send "/home/user/backups/users.tar.gz\r"`, and `expect "Restore complete"`; run it with `expect` and confirm files appear under `/home/user/restored_data/`. (3) fstab line: write exactly one line to `/home/user/fstab_entry.txt` in standard 6-field fstab format — `UUID=<uuid> <mountpoint> <fstype> <options> <dump> <pass>` — using the UUID, mount point, filesystem, options, dump, and pass values given in the task, with fields separated by single spaces and no extra commentary.
