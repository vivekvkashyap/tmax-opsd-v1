Get the vendored library compiling first, since nothing else works without it. Open `/app/pcg-c/Makefile` and look closely at the `CFLAGS` line and PIC flag — a perturbed build usually drops a leading `-` (turning a flag into a bare word) or omits `-fPIC` needed for later static linking into your executable; fix exactly what's broken (don't rewrite the whole Makefile) and confirm `make` succeeds and produces `libpcg_random.a`/`.so` under `src/`. In your C program, read raw floats from stdin with `fread` in a loop until EOF (don't assume a fixed count; the spec gives 10 to 1,000,000). Implement Kahan summation carefully for the mean — track a running `sum` and compensation `c` in `double`, and reuse the identical routine for both the original data and every bootstrap resample so rounding behavior is consistent. Seed PCG32 with exactly `pcg32_srandom(42u, 54u)` once before the bootstrap loop, and for each of 1000 iterations draw `N` indices via `pcg32_boundedrand(N)` in a fixed nested loop order (outer: bootstrap iteration, inner: sample index) since a fuzz-equivalence checker will compare byte-for-byte against a reference for identical inputs — any deviation in call order changes the RNG stream. Sort the 1000 bootstrap means and read off indices 24 and 974 for the 95% CI, then print using the exact specified format string.