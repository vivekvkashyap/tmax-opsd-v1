Work top-down through the four independent sub-systems, verifying each before moving on. First, OCR the baseline version from `/app/ticket.png` (e.g. `tesseract ticket.png -`) and read off the exact minimum semver constraint text it states — treat that recovered string as your policy threshold, don't assume a value. Second, fix `CMakeLists.txt` by adding an `add_custom_command`/`add_custom_target` that invokes `go build -buildmode=c-shared -o libengine.so engine.go`, wiring it into the default build target so `make` produces `libengine.so`; in `engine.go`, implement a `//export`-tagged C-compatible function that accepts a version array and baseline string, spins up a goroutine per version to parse Major.Minor.Patch and compare against the baseline (goroutines writing results into a mutex-protected slice or channel, then joined via `sync.WaitGroup`), and returns which versions pass. Third, in `resolve.py`, load the `.so` via `ctypes.CDLL`, marshal stdin lines into C arrays, call the Go filter, then insert surviving package names into a Radix Trie you implement yourself (nodes storing common-prefix edge labels, splitting shared prefixes on insert) and print it pre-order with 2-space-per-depth indentation. Validate incrementally: build with `cmake .. && make` first, then test `resolve.py` on a handful of your own crafted `stdin` lines covering shared-prefix packages before trusting the pipeline end-to-end.
