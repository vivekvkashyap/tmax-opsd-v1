Handle the symlink-loop hazard explicitly rather than doing a naive recursive extract/copy, which will hang or blow up. Extract the outer `raw_data.tar` normally, read `rules.ini` to learn the valid doc extensions and the "Tools format" (binary type) values it specifies, then for the nested `docs_*.tar.gz` archives extract them with a mechanism that skips symlinks — e.g. list contents first (`tar -tvf`) and extract only non-symlink members (filter out lines starting with `l`), or extract normally then immediately `find <dir> -type l -delete` before any recursive traversal, so a later `find`/`cp` never follows a self-referential symlink into an infinite loop. Parse `backup.log` for its multi-line error blocks (each begins with a `... ERROR Incident ID: N` line followed by a traceback) and locate the block whose traceback contains `InfiniteSymlinkException`, extracting just that block's Incident ID number to `/home/user/incident_id.txt`. Use `find <extracted-dir> ! -type l -type f` filtered by the valid extensions to flatten-copy matching files into `/home/user/clean_docs/`, and use `file <path>` on candidate binaries to identify the one ELF executable per rules.ini, copying it to `build_tool`. Finally `tar -czf organized_docs.tar.gz -C clean_docs .` so paths are rooted correctly without a parent folder.
