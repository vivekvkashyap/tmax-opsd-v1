Since this will be fuzz-tested across many randomly generated inputs, fix the vendored package correctly rather than special-casing the sample data. First run `make install` inside `/app/bkp-graph-1.0.0/` to see it fail, then open the `Makefile` with a tool that shows whitespace (`cat -A` or `sed -n l`) — Make requires literal tab characters to indent recipe lines, so if the `install` target's body uses spaces, replace them with real tabs (many editors will auto-convert; use `sed -i 's/^    /\t/'` carefully, or retype the line with a literal tab). Second, read `src/get_chain.py`'s traversal logic and compare the key it reads against the actual JSONL schema described in the task (`parent` vs some other key it might currently reference) — fix the mismatched dict key so `node.get(...)` returns the real parent id instead of None. After both fixes, `make install` should place the executable and library where expected. Test manually with a handwritten JSONL file containing a short full→incremental→incremental chain and confirm the script prints exactly `id1,id2,id3\n` with no extra output, matching root-to-target order, before trusting it against the fuzzer's larger random trees.
