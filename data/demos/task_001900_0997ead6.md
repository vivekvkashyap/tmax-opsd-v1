Build this bottom-up and verify each layer before moving on. First create the symlink topology (`mkdir -p /app/raw_data/{us-east-1,eu-west-1,ap-south-1}`, then `ln -s` each into `/app/data_links/bucket_*`) and the `fstab.optimized` line — match the exact whitespace/field pattern requested (device, mountpoint, fstype, comma-separated options with no spaces, dump, pass) since the grader likely uses a regex. Treat `/app/legacy_pricer` as a black box: don't try to disassemble it — just shell out to it (`popen`/`fork+exec`) with the byte count as an argument and capture stdout; compute directory size recursively through the symlink (`du -sb` or manual walk) rather than hardcoding test data, since the grader replaces your dummy files. For the C++ service, use raw sockets (or a tiny multiplexed select/poll loop) to listen on both 8080 and 2525 simultaneously in one process — don't spawn separate servers requiring two ports if the harness only starts one binary. Parse the `Authorization` header exactly and return 401 on mismatch before doing any work. For SMTP, implement only the minimal state machine (HELO/MAIL FROM/RCPT TO/DATA/QUIT) and scan the DATA body for a `Subject:` line, appending it to `/app/alerts.log` only if it contains "ALERT". In `deploy.sh`, compile with `-lpthread` as needed, background the service with `&` and `disown`/`nohup` so it survives the script's exit, and end with `exit 0`.
