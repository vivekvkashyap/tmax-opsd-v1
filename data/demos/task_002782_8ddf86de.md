Fix the three described defects precisely, then prove it with a real regression test against the given payload.
1. In `/home/user/suspicious_service.py`, change the `struct.unpack` format from unsigned (`'<III'`) to signed little-endian (`'<iii'`) so negative values decode correctly.
2. Rewrite `compute_root` to cap the Newton-Raphson loop at 100 iterations (use a `for`/counted `while`), raising `ValueError("Convergence failed")` if it exits the loop without meeting the convergence tolerance — do not wrap the loop body in a broad `except BaseException` that swallows and spins forever; let `asyncio.CancelledError` propagate normally.
3. Write `/home/user/regression_test.py`: read and hex-decode `/home/user/payload.hex`, `import handle_payload` from the service module, run it inside `asyncio.run(...)`, wrap the call in a `try/except ValueError as e` and assert `str(e) == "Convergence failed"`; print `"PASS"` and `sys.exit(0)` on success, otherwise exit 1.
4. Run `python3 /home/user/regression_test.py` yourself and confirm it prints PASS with exit code 0 before finishing — this specific payload is crafted to decode (once parsed as signed ints) into parameters that never converge, exercising your new iteration cap.
