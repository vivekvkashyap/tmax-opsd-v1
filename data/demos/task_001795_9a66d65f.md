Treat this as a standard text-to-features-to-stats pipeline and be disciplined about determinism where it matters. Tokenize `text`: lowercase, strip non-alphanumeric/non-space chars (regex like `[^a-z0-9 ]`), split on whitespace; count token frequency across all rows and take the top 50 by count (break ties by alphabetical order to make the vocabulary deterministic, since the count-only order is otherwise ambiguous). Build a document-term count matrix restricted to that vocabulary, mean-center each column, then run PCA/SVD to get PC1 per row — use a standard library (`sklearn.decomposition.PCA` or `numpy.linalg.svd` on the centered matrix) rather than a hand-rolled eigen-solver, since sign/scale conventions matter for matching the checker. Run Welch's t-test (`scipy.stats.ttest_ind(..., equal_var=False)`) comparing PC1 for rows where category=="Physics" vs "Biology". For the bootstrap CI: filter to Biology rows only, set `random.seed(42)` (or `np.random.seed(42)`) immediately before the loop — not earlier — then resample `citations` with replacement 10,000 times, compute the mean each time, and take the 2.5th/97.5th percentiles of the resulting distribution. Round every reported float to 4 decimals and write the six specified keys to `/home/user/results.json`.
