Treat each of the four deliverables as an independent, checkable artifact and verify each against its exact spec text before moving on. For the pexpect script: `spawn` the legacy shell script, use `expect("Enter monitor password: ")` (match the literal prompt string), `sendline("alertadmin")`, then parse the subsequent output line for the `STATUS: <VALUE>` pattern with a regex capture group — `mkdir -p /home/user/app_logs` before opening the log file, and append (not overwrite) a single-line JSON object built with `json.dumps({"event": "service_check", "status": parsed_value})`. For the logrotate config, include every directive literally as its own line/keyword: `size 10k`, `rotate 4`, `compress`, `create 0644`, `missingok`, `notifempty`, wrapped in the block syntax referencing the log path. For the fstab line, write the six fields space-separated in the exact order (source, mountpoint, fstype, options, dump, pass) with no extra lines. For the iptables rule, use `iptables -t nat -A PREROUTING -i eth1 -p tcp --dport 80 -j DNAT --to-destination 192.168.1.100:8080` matching flag order/spacing conventions. Finally actually run `python3 /home/user/monitor.py` once so the log file is populated, then re-check its contents match the JSON shape required.
