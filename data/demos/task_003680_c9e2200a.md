First confirm MongoDB is reachable (`mongosh --eval "db.version()"` or start `mongod` in the background if it's not running yet), and install `pymongo` if missing. In `migrate.py`, read `customers`, `orders`, and `items` from `/home/user/sales.db` via `sqlite3`, then build the nested document structure in Python code (don't try to do the nesting in raw SQL) by grouping orders under each customer and items under each order using dictionaries keyed by id, converting SQL rows into the required nested JSON shape, and inserting into MongoDB's `analytics.customer_orders` collection (clear/drop the collection first so reruns are idempotent). For the top-3 aggregation, write a Mongo aggregation pipeline that unwinds orders then items (or computes the sum in a single `$project`/`$reduce` over nested arrays), multiplies price*quantity per item, sums across all orders/items per customer with `$group`, then `$sort` by `total_spent` descending with `name` ascending as the tiebreaker, and `$limit 3`. Write the result as a JSON array (list of `{name, region, total_spent}` dicts) to `/home/user/top_customers.json` using `json.dump`, making sure floats aren't left as Mongo Decimal128/ObjectId types that break JSON serialization.
