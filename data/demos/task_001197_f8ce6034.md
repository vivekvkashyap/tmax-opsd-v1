Do this as a classic awk pipeline, since Python/Perl/Ruby are disallowed. (1) Deduplicate by `DEDUPE:<hash>` using an awk associative array keyed on the extracted hash, printing/keeping a line only the first time its hash is seen (`if (!(hash in seen)) { seen[hash]=1; print }`). (2) Extract the timestamp, convert `HH:MM:SS` to total seconds, floor to the nearest 900-second (15-minute) boundary with integer arithmetic (`bucket_sec = int(total_sec/900)*900`), then reformat back to `HH:MM:SS` and rebuild the ISO string `YYYY-MM-DDTHH:MM:SSZ` — `date -u` or pure awk arithmetic both work, but avoid re-invoking `date` per line for performance if the log is large. (3) Split the `METRICS:` field on commas, then each entry on `=`, to reshape wide key=value pairs into one row per (bucket, service, metric, value) — this is the "long format" step. (4) Aggregate the max value per (bucket, service, metric) using a 3-key associative array (e.g. array indexed by `bucket SUBSEP service SUBSEP metric`), then emit sorted with `sort -t, -k1,1 -k2,2 -k3,3` before writing the CSV with header `bucket,service,metric,max_value` to `/home/user/metrics_summary.csv`. Wrap it all in `/home/user/process.sh` and `chmod +x` it.
