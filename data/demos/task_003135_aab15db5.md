Build the query in stages and test each with the `sqlite3` CLI or interactively before finalizing the script. First write a recursive CTE that anchors on `SELECT id FROM taxonomy WHERE name = 'Artificial Intelligence'` and recursively joins `taxonomy` on `parent_id` to pull in all descendant topic ids — verify it returns the correct set of ids by itself. Next, join that topic-id set to `papers` and compute a per-topic total via `SUM(citations) OVER (PARTITION BY topic_id)` (or a separate GROUP BY aggregate), and separately rank papers within each topic using `ROW_NUMBER() OVER (PARTITION BY topic_id ORDER BY citations DESC)`; filter to rank = 1 to get the top paper per topic. Combine these into one query (CTE for hierarchy, window functions for rank/total, final SELECT joining topic name, top paper title/citations, and topic total), order by topic name ascending, then use Python's `csv` module to write the header `TopicName,TopPaperTitle,TopPaperCitations,TopicTotalCitations` followed by the rows to `/home/user/top_research.csv`. Run the script and spot-check the output against the raw table contents manually to make sure aggregation and ranking logic is correct.
