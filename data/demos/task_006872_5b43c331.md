Work through the pipeline layer by layer instead of patching symptoms. First resolve `requirements.txt`: pin compatible versions (check with `pip install --dry-run` or just try installing and read the resolver error) rather than guessing. Second, for encoding: don't assume UTF-8 — open each file in binary mode first, detect a BOM or use `chardet`/`file` to identify UTF-16 files, then decode accordingly before parsing JSON lines; never silently skip a file. Third, the race condition comes from multiple threads mutating a shared `dict`/counter without synchronization — fix it with a lock around increments, or better, have each worker return its own local tally and merge results in the main thread after `join()` (avoids lock contention entirely). Rewriting in Go/Rust with per-goroutine maps merged at the end is also valid if you prefer another language. After the pipeline runs cleanly and deterministically (run it twice, confirm identical output.json), compute per-IP 500-error counts and compare distributions — the anomalous IP will stand out by an order of magnitude versus the rest. Verify your report's count against a raw grep-based recount of the correctly-decoded logs before finalizing.
