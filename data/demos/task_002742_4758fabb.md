Do the pipeline stages in dependency order and validate each before moving on. Extract frames with ffmpeg using `-r 1 -t 10 -s 320x240 -pix_fmt rgb24 -f rawvideo` piped to per-frame files (or `-vcodec rawvideo -f image2` with a numbered pattern) so each `frame_NN.raw` is exactly 320*240*3=230400 bytes of interleaved RGB with no container header — verify the byte count of one file with `wc -c` before writing the C code that consumes it, since a header-bearing format would silently break the fixed-offset pixel math downstream.

In `embed.c`, read each raw frame into a buffer, iterate all 320x240 pixels computing `gray = 0.299*R + 0.587*G + 0.114*B` as double, and accumulate into 16 grid-cell sums by mapping each pixel's (x,y) to a cell index via `(y/60)*4 + (x/80)` (cells are 80 wide x 60 tall, 4x4 grid) — get this index arithmetic right since it's the part most likely to be off-by-one. Divide each cell sum by 80*60 to get the average, round to 2 decimals when writing CSV rows `filename,v0,...,v15`.

For the TCP server, use a plain blocking `accept()`/`read()` loop per connection (no need for multiplexing since it's sequential per the spec), parse `SEARCH v0,...,v15\n` lines with `sscanf` or manual tokenizing, compute squared Euclidean distance against all 10 loaded embeddings, reply `MATCH <filename>\n`, and handle `QUIT\n` by closing that connection — start it in the background (`&`) and confirm with `netstat`/a manual `nc` test that it's actually listening on 127.0.0.1:9090 before finishing.