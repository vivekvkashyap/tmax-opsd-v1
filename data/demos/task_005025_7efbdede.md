First inspect `/app/libspecfact-1.2.0/Makefile` and diff its compile/link flags against what a shared library normally needs — the two likely gaps are a missing `-fPIC` in the compile flags (required for code going into a `.so`) and a missing `-lm` on the link line (needed if the factorization code calls libm math functions); add both and rebuild with `make` until `libspecfact.so` exists and loads cleanly via `ctypes.CDLL("/app/libspecfact-1.2.0/libspecfact.so")`. In `pipeline.py`, read the input file with `numpy.fromfile(path, dtype='<f8')` and reshape to (256,256) in row-major order, apply `np.fft.fft2` then `np.abs(...)`, add `1e-6` to every element, and be exact about the ctypes call: declare `argtypes`/`restype` on the function pointer, pass numpy arrays via `.ctypes.data_as(ctypes.POINTER(ctypes.c_double))`, and pre-allocate the W (256x4) and H (4x256) output buffers as contiguous float64 arrays before passing pointers — a common bug is passing non-contiguous or wrong-dtype arrays, which corrupts output silently. Quantize W with `np.clip(W*255.0, 0, 255).astype(np.uint8)` and write raw bytes with `.tofile()`. Also produce the required matplotlib line plot with legend and axis labels.
