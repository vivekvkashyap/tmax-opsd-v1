Tackle this in two independent halves and verify each before combining. For the vendored `bashttpd` server, locate the line that extracts the request URI from the raw HTTP request line (it uses a positional field extractor like `awk '{print ...}'`); check which field index is being pulled — a common injected bug swaps the path field for the HTTP-version field — and correct the index so the extracted value is the actual path, then set up its config to route `GET /stats` to a handler that serves file contents with a `Content-Type: application/json` header, and launch it in the background bound to 127.0.0.1:8080. For the data pipeline, write a small script that: dedupes exact-duplicate lines (e.g., `sort -u` preserving needed fields, or `awk '!seen[$0]++'`), parses each line's timestamp down to the minute, HTTP status code, and response time, groups by minute, and computes total requests, count of status>=400, and the average response time truncated (not rounded) to an integer. Emit strict, valid JSON sorted by minute to `/home/user/stats.json`, and confirm `curl http://127.0.0.1:8080/stats` returns exactly that file's bytes before finishing.
