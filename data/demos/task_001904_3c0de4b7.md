Parse the custom archive format precisely rather than assuming fixed offsets. In C++, open `archive.pack` in binary mode, read forward until you hit the first `\0` byte to isolate the JSON header text, then parse it with the provided `json.hpp` (nlohmann-style: `json::parse(header_string)`); the byte immediately after that `\0` marks the start of the binary payload region, and each file's `offset`/`size` in the JSON is relative to that point — seek to `payload_start + offset` and read exactly `size` bytes per file, writing each to `/home/user/project/extracted/<name>` (creating the directory first with e.g. `std::filesystem::create_directories`). Compile with a C++17-capable compiler for filesystem support. After running the extractor, compute checksums with `sha256sum extracted/* | awk '{split($2,a,"/"); print a[length(a)]","$1}'`-style processing (or manually strip the path) to build `manifest.csv` in the exact `filename,sha256hash` format with no path prefix. Finally bundle with `tar -czf release.tar.gz manifest.csv extracted/` from within `/home/user/project`. Verify byte-for-byte correctness by checking extracted file sizes and re-hashing before finishing.
