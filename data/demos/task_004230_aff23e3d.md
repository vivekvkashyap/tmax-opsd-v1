Two independent fixes plus a wrapper script. For the Go circular import in `/app/build_tool`, don't try to merge the packages — extract the shared type/interface that both `cmd` and `core` depend on into a small third package (or move the dependency direction so only one package imports the other), then `go build`; run the built tool to populate `/tmp/fixtures`. For the C extension, open `src/parser.c`'s `parse_manifest`, find where it reads a length field and calls `memcpy` without checking it against the remaining buffer size, and restore a bounds check (`if (offset + name_len > total_size) return NULL;`) before the copy — this is what prevents the out-of-bounds-read segfault on malformed/truncated input. Rebuild and `pip install .` the package. In `/home/user/process_artifacts.py`, read the file's bytes, call `artifact_parser.parse_manifest(data)` inside a try/except catching `ValueError`, and also explicitly check for a `None` return — both cases should print `["ERROR"]`; otherwise sort the returned names alphabetically and print them as a JSON array. Test locally with both well-formed and deliberately truncated/garbage binary inputs (not just one happy-path file) before considering it done, since the real grading will fuzz arbitrary byte strings.
