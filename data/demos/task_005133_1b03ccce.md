Build this in layers and test each independently before combining. First, the VidScript interpreter: use OpenCV (`cv2.VideoCapture('/app/video.mp4')`) and keep the capture object open across SEEK/LUM calls within one script execution — call `cap.set(cv2.CAP_PROP_POS_FRAMES, n)` then `cap.read()` for SEEK/LUM rather than reopening the file per instruction, since sequential seeks should reuse the handle for efficiency. Implement luminance with the exact given formula and note OpenCV frames are BGR, so map channels correctly. Parse each script into (command, args) tokens, validate every line against the known command set before execution and return 400 on any unrecognized token. Second, the rate limiter: implement a token bucket per `client_id` as an in-memory dict storing (tokens, last_refill_time); on each request, add `elapsed/20` tokens capped at max 3, refuse with 429 if tokens < 1 else consume one — don't reset the whole bucket on each check, incrementally refill so the 21-second regeneration test passes. Wire both into `/home/user/app.py` with FastAPI, then write `/home/user/tests/test_api.py` with pytest covering both parts, using TestClient or manual time manipulation to validate rate-limit edges.