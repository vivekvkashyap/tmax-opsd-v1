Work top-down: first recover the hidden formulas, then build the graph, then serve it. Run OCR on `/app/cost_formula.png` (e.g. `pytesseract.image_to_string`) and inspect the raw text output carefully before hardcoding any formula — whitespace/case errors in OCR output are the most common naive-attempt failure, so print and sanity-check what you extracted rather than assuming it parsed cleanly. Load `nodes.csv`/`edges.csv` into a `networkx.Graph` (undirected), compute each node's `base_cost` per its `node_type` using the OCR'd per-type formula, then compute each node's `neighborhood_cost` as the sum of `base_cost` over its direct graph neighbors only (not itself, not transitive). Build the HTTP server (Flask/FastAPI+uvicorn or stdlib http.server) bound to `0.0.0.0:5050` with a `GET /api/neighborhood/{node_id}` route that: checks the `Authorization: Bearer net-graph-secret` header exactly and returns 401/403 if missing or wrong, returns 404 for unknown node ids, and otherwise returns `{"node_id": ..., "neighborhood_cost": ...}` as JSON. Start the server, then only after it is actually accepting connections, touch `/tmp/server_ready` — creating it too early will make the verifier hit a closed port.
