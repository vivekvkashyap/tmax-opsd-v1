Stage 1: extract frames from /app/evidence/screen_capture.mp4 (e.g. `ffmpeg -i screen_capture.mp4 frame_%04d.png`) and scan them with OCR (tesseract) to locate the single frame containing a `KEY-{32_hex_chars}` string — don't assume which frame number, search all of them programmatically. Use the recovered full key string as the openssl password to decrypt the .enc archive (it was encrypted with AES-256-CBC and PBKDF2, so decrypt with `openssl enc -d -aes-256-cbc -pbkdf2 -pass pass:<recovered-key>`), then untar into /home/user/forensics/scripts/ to get evil/ and clean/ samples. Stage 2: build a Rust CLI at that exact target/release/detector path that reads every file in a given directory and classifies based on real content signals the attacker corpus exhibits — sandbox/container-evasion checks (e.g. tests for /.dockerenv, grep against /proc/1/cgroup for "docker", or permission checks gating payload execution) rather than hardcoding filenames or matching only the specific sample scripts, since a hidden adversarial corpus with different filenames/wording will be used to grade generalization. Print one `EVIL:`/`CLEAN:` line per file in the required format, and validate against both the provided evil/clean directories and edge-case variants you construct yourself before finishing.