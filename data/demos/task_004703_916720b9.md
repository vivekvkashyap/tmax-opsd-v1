Build the ETL script in /home/user/etl_pipeline.py as clearly separated, independently testable stages. Transcribe /app/audio_logs/dictation.wav with a real ASR model (e.g. `whisper`) rather than assuming content, since accuracy is graded as 1 - WER against a hidden baseline (threshold 0.85). Normalize by lowercasing, stripping to only `a-z0-9` and spaces (regex, not naive replace), and collapsing whitespace; write to /home/user/full_transcript.txt. For stratified sampling: group transcript words by length, compute each length-bucket's proportion of the full word count, multiply by 30 and round for initial per-length counts, then if rounding makes the total not equal 30, adjust counts on the most frequent buckets until it sums to exactly 30 — sample that many words per bucket and write the 30-word list as a JSON array to /home/user/stratified_sample.json. Finally, install a user crontab entry running the script at exactly 04:30 daily (`30 4 * * *`), and confirm with `crontab -l` since the grader checks for that exact schedule string.
