Treat this as four largely independent recovery tasks that feed into one final script. For the crashing helper, use `gdb <binary> <corefile> -ex bt -ex quit` (or `coredumpctl`/whatever core is produced) to get a backtrace, and check what environment variable or file path it's dereferencing near the crash frame — the fix is almost certainly exporting a missing env var (e.g. a data directory path) before the helper is invoked in `run_pipeline.sh`, not a code change to the helper itself. For the SQLite recovery, don't just delete files: copy the intact `-wal` file alongside the truncated `metrics.db` (and create/restore a minimal valid `-shm` if sqlite complains, or let sqlite regenerate it) and open the database normally — SQLite will replay the WAL into the main file automatically on connect; then run a checkpoint (`PRAGMA wal_checkpoint(TRUNCATE);`) to persist the recovered data before trusting further queries. For the WHERE clause, OCR `/app/schema_clue.png` with `tesseract` and use the extracted conditions verbatim (don't paraphrase field names or operators) to build the `AVG(duration) ... WHERE ...` query in `get_metric.sh`, printing only the bare numeric result via `sqlite3 -noheader -csv` or similar so stdout contains nothing but the float. Validate every step by re-querying the recovered DB manually before wiring it into the final script.