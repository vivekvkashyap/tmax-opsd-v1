Build `/home/user/process_backup` (a shell script is simplest) to exactly mirror the described spec, and test it against the given `/app/system_status.mp4` + `/app/sample_backup.tar.gz` pair before trusting it — but remember your program will be re-run against many other unseen archive/video pairs with varying encodings and frame counts, so avoid hardcoding anything from the sample. For frame counting, use `ffprobe -v error -select_streams v:0 -count_packets -show_entries stream=nb_read_packets -of csv=p=0 <video>`, falling back to `-count_frames -show_entries stream=nb_read_frames` if the first returns empty/N/A — both must be reliable since fuzz videos vary in duration/rate. Extract the tar.gz to a temp dir, read `encoding` from `meta.json` via `grep -o`/`jq` (don't assume a fixed key ordering), unzip `data.zip`, and list `.log` files sorted alphabetically. For each, convert with `iconv -f "$ENCODING" -t UTF-8` — do not assume UTF-8 or a single hardcoded source encoding, since the fuzzer varies it across UTF-16LE, ISO-8859-1, WINDOWS-1252, MACINTOSH, UTF-8. Print exactly `F` `#` characters (no trailing space) then a newline, then the concatenated converted contents with no extra newlines inserted between files. Clean up temp dirs and `chmod +x` the final script.