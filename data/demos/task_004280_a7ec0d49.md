Characterize `/app/telemetry_obfuscator` algebraically rather than guessing constants. Feed it 4 zero bytes and read the output as a little-endian uint32 to isolate `key + constant`; feed a couple of different all-same-byte 4-byte chunks and difference the results to separate the additive term from the XOR term, and to tell XOR-then-add from add-then-XOR (addition carries between bytes, XOR doesn't). Then specifically probe non-multiple-of-4 lengths (1,2,3,5,6,7 bytes) to find the padding rule for the trailing partial chunk — vary total length to see what the pad byte depends on (length of input vs. size of the remainder). Cross-check inferred constants with `objdump -d`/`gdb` on the stripped binary. Implement the confirmed transform and padding rule in `/home/user/py_obfuscator.py` as a pure stdin→stdout filter using `struct.pack`/`unpack` with `<I`, and self-test against the oracle across boundary lengths before trusting the fuzzer.
