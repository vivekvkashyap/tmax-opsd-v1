The root cause is that `#pragma omp atomic` lets threads add their partial sums to `total_density` in whatever order they finish, and floating-point addition is not associative, so the result varies run to run. Fix it by decoupling computation from reduction: allocate a `double weights[N]` array before the parallel region, have the `#pragma omp parallel for` loop write each thread's result to `weights[i]` (each index written by exactly one thread, so no atomic/critical needed there), then after the parallel region ends, run a plain sequential `for (i = 0; i < N; i++) total += weights[i];` loop strictly in ascending order — this guarantees the same floating-point rounding on every run regardless of thread scheduling or thread count. Keep the parallel loop only around the per-node `compute_node_weight` calls, not the summation. Compile with the exact flags given, run the binary multiple times, and confirm the output in `/home/user/reproducible_density.txt` is byte-identical across runs before considering the task done.