First decode the video: extract frames with ffmpeg (e.g. `ffmpeg -i /app/telemetry.mp4 frames/f%04d.png`, one frame per second), then for each frame classify it as black (bit 0) or white (bit 1) by sampling average pixel brightness against a midpoint threshold — read frames in order to preserve bit order. Concatenate the resulting bit string, split into three 12-bit big-endian unsigned chunks to get A, B, C. Then write a Go program that searches X,Y,Z in [1,30]^3 concurrently (spawn goroutines over a partition of the search space, e.g. by X, send candidate matches over a channel, collect the unique solution satisfying all three equations simultaneously) rather than a single serial triple loop, since the task explicitly asks for goroutines/channels. Once X,Y,Z are known, build the Go service: an HTTP handler on :8080 for `GET /versions` returning `{"python":"3.X","node":"Y","go":"1.Z"}` with those exact substituted values, and a separate TCP listener on :9000 that on receiving `BUILD\n` shells out to `/home/user/build.sh` (write that script to echo the versions and exit 0) and replies `SUCCESS\n`. Run both listeners concurrently in the same process (e.g. one in a goroutine) and leave it running in the background; verify both endpoints yourself with `curl` and `nc` before finishing.
