Treat this as black-box reverse engineering followed by careful reimplementation, not guesswork. Start with `strings`, `file`, and `strace -f -e trace=file,network` on the legacy binary while feeding it a small crafted baseline file and a few stdin JSON lines, to see which syscalls it issues (mmap, inotify_init/inotify_add_watch, read) and in what order. Build a minimal baseline config file yourself matching the documented `CFG1` header + [2-byte LE key len][key][4-byte LE value len][value] record format, then feed known inputs and diff the oracle's SHA256 outputs against hypotheses about serialization order (e.g. does it hash keys sorted alphabetically, or in insertion order?) — an ordered container like std::map is a natural implementation choice worth testing first since it sorts automatically. Verify the delimiter used between key and value, and whether a trailing newline/separator is appended per record. For file-watching, use `inotify_add_watch` with `IN_MODIFY` and rebuild state from scratch on trigger, discarding in-flight deltas. Validate edge cases (deleting a missing key, malformed JSON lines) by observing oracle behavior directly rather than assuming. Compile a standalone executable at the exact required path.