Tackle this in the three stated phases, verifying each before moving to the next. Phase 1: extract the outer tar.gz, identify which split-zip part is corrupted (likely fails `unzip -t` or `zip -FF`), strip the trailing `BADEOF` garbage with something like `sed 's/BADEOF$//' part.zip.corrupt > part.zip` (or `truncate` off the known trailer length), then combine split-zip parts (`zip -F`/`-FF` or `cat` depending on split scheme) and unzip into `/home/user/raw_docs/`. Phase 2: since you have no source for `/app/bin/wal_parser`, black-box it — run `strings` on the binary to look for usage hints, and try feeding it the WAL file both as an argument and via stdin redirection to see which it accepts; observe its raw output. The obfuscation is a simple symmetric cipher (e.g. XOR with a fixed byte) — recover the key by comparing known-plaintext expectations (Markdown starts with predictable characters like `#` or whitespace) against the corresponding output bytes, then implement that transform in `/home/user/decoder.go`. Append the standard header via a bash loop over files in `/home/user/processed_docs/`. Phase 3: implement both an HTTP handler and a gRPC service (define a small .proto, generate stubs) that each check for the `Authorization: Bearer TECH-WRITE-2024` header/metadata before serving decoded file contents, and leave the server running in the background listening on 8080 and 9090.