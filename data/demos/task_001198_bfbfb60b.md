First understand the legacy binary's algorithm behaviorally (feed it small/known inputs like `[8]`, `[-8]`, `[27]` and observe it converges to cube roots via Newton's method), since your C code must match it exactly, not merely "work." Fix in this order: (1) dependency — remove the `#include <math_utils/special.h>` and any related conflicting header/library reference, use only standard `<math.h>` and functions like `cbrt`/`pow`/`fabs`, and confirm it now builds cleanly with `gcc -pthread -O3 -lm` (no special include path needed). (2) race condition — the threads are all incrementing/reading a shared global index without synchronization; instead of a shared counter, pre-partition the input array into fixed, non-overlapping index ranges per thread (or guard the shared index with a mutex/atomic increment) so no two threads ever touch the same element. (3) convergence/sign bug — Newton's method on `x^3 - C` blows up or NaNs for negative `C`; extract the sign first, run the iteration on `fabs(C)`, cap iterations (match the oracle's cap), then reapply the sign to the result before printing. Format every output value with `%.6f` and validate by piping the same random float arrays into both `/app/legacy_transform` and your compiled `/home/user/new_transform`, diffing stdout until it's identical.
