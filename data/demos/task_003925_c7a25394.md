Build the script around `argparse` with `--tz` and `--locale` both optional (default `None`). After parsing, check if either value is still `None`; if so, fall back to `input("Enter timezone: ")` and `input("Enter locale: ")` for whichever is missing — match those prompt strings exactly since the verifier both feeds interactive stdin and checks literal argv usage. Use `datetime.datetime.utcnow().isoformat()` (or `datetime.now(timezone.utc)` formatted without a timezone suffix to match the example's plain ISO format) to compute `DEPLOY_TIME`. Ensure the `/home/user/config` directory exists via `os.makedirs(..., exist_ok=True)` before writing, and write exactly three lines in the given `APP_TZ=`, `APP_LOCALE=`, `DEPLOY_TIME=` order — overwrite (not append) the file each run so the interactive-mode retest in verification reflects only the latest invocation. Add a shebang line (`#!/usr/bin/env python3`) and `chmod +x` the file so `os.access(path, os.X_OK)` passes. Finally, invoke the script once with `--tz Asia/Tokyo --locale ja_JP.UTF-8` to produce the required populated config file as the deliverable state.
