First transcribe the audio file (e.g. with `whisper` or another local STT tool run over `/app/issue_report.wav`) rather than guessing at the anomaly condition — the exact statistical rule and its numeric thresholds are only in the recording, so listen/transcribe carefully and note the precise procedure and cutoff value described (a bootstrap resampling statistic compared against a confidence-interval-width threshold). Once you have the rule, design the Rust classifier: for each FASTA file, parse the sequence (concatenating all lines after the header), compute the empirical frequency distribution over the 20 standard amino acids, then repeatedly resample with replacement to build many synthetic sequences of the same length, computing a distance metric (e.g. total variation distance) between each resample's distribution and the uniform distribution across amino acids. From that collection of distances, compute the requested percentile-based confidence interval width, and compare it against the threshold from the transcript to decide EVIL vs CLEAN. Use crates like `rand` for sampling. Validate against both provided corpora before finalizing, since the classifier must be 100% correct on both, and print one `filename,STATUS` line per file to stdout in the exact required format.
