Preserve exact behavior while eliminating the algorithmic inefficiency — profile first to confirm where time goes before rewriting. In `canonicalize_request`, the URL-decoding loop rebuilds the string via repeated slicing inside a nested loop (O(n^2) with re-scanning from the start after every substitution) — replace it entirely with `urllib.parse.unquote(path)`, which handles `%XX` decoding correctly and in linear time; keep the subsequent `unicodedata.normalize('NFKC', ...)` call unchanged since that part is already correct. The query-parameter sorting is a manual bubble sort (O(n^2) comparisons/swaps) — replace it with Python's built-in `sorted()` or `list.sort()`, which is not only correct but far faster; confirm the join/`?`-prefix logic (return path unchanged when query is empty) stays identical to preserve public behavior. After refactoring, run `pytest /app/authproxy/tests/test_normalize.py` to confirm functional equivalence, then run `python /app/authproxy/benchmark.py` and check the printed runtime is comfortably under 0.1 seconds — if it isn't, look for any other O(n^2) or string-concatenation-in-a-loop pattern you may have missed rather than micro-optimizing the already-fixed parts.
