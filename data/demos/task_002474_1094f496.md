First diagnose the build failure in `/app/tinyexpr/Makefile`: the `CFLAGS` line is missing `-fPIC`, which is required to build position-independent code for a shared object — add `-fPIC` to CFLAGS, then link with `gcc -shared -o libtinyexpr.so tinyexpr.o -lm`. Verify the .so loads and exports `te_interp` (check with `nm -D libtinyexpr.so` or a quick ctypes smoke test) before writing the router. In `fast_router.py`, load the library via `ctypes.CDLL`, set `te_interp.restype = ctypes.c_double`, and for each URL parse query params (a, b, c) then build/interp the expression string read from `/home/user/rule.txt` with the numeric values substituted in — avoid re-parsing the rule file or re-loading the library per URL, do that once outside the loop. Since correctness must exactly match the baseline and the run must finish in under 1.5s for 200k URLs, minimize per-line Python overhead (avoid `urlparse`/`parse_qs` if a simpler manual split is faster, batch file I/O, avoid unnecessary string formatting). Validate by diffing your output against `baseline_results.txt` and timing the full run end-to-end before considering it done.