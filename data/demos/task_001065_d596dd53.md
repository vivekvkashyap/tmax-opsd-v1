Read `/home/user/vulnerable_auth.sh` carefully to find where it builds the redirect URL from an untrusted parameter without validating it against an allowlist — that unrestricted-redirect pattern maps to a well-known CWE category (look it up by the vulnerability class name, "open redirect", rather than guessing a number). For the password, script an offline crack: for each candidate in `/home/user/wordlist.txt`, compute its MD5 with `md5sum` (or a loop in bash/python) and compare against the hash hardcoded in the script until you find the match. For the payload, figure out exactly how the script decodes the redirect parameter (look at its unescaping logic character by character) and URL-encode your target URL (`http://malicious.example.com/login`) so that after that specific decoding step it lands on exactly that string — a generic percent-encoder may not be enough if the script's decode routine is nonstandard, so test it. Then write audit_report.json with the three findings, and a verify.sh that exports USER_ID/PASS_KEY/REDIR_URL with your cracked password and payload, invokes the vulnerable script, and redirects stdout to exploit_result.txt — chmod +x it and run it to produce real output.
