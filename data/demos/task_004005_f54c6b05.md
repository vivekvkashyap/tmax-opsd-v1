Work in stages rather than fixing everything blind. First compile `decoder.cpp` as-is and read the compiler errors (likely missing `<cstdint>`/`<cstring>` includes and type mismatches around `size_t`/pointer serialization) and add the needed headers. Next look hard at the struct-serialization pattern in both files: writing `reinterpret_cast<char*>(&chunk.data)` writes the raw pointer value (8 garbage bytes), not the pointed-to buffer contents — this is the encoder's core bug, so replace it with writing the actual buffer content (`out.write(chunk.data, chunk.size)`) after the size field. Fix the decoder symmetrically: after reading `size`, read exactly `size` bytes directly into a `std::vector<char>` sized to `size` (`in.read(buffer.data(), size)`) instead of reading a bogus pointer field and touching out-of-bounds memory (`buffer[size+1000]`) — remove that stray out-of-bounds write entirely, it's the segfault's direct cause. Compile both with `g++`, run encoder then decoder on `/app/suspicious_payload.wav`, and diff or spot-check byte-for-byte before writing `/home/user/recovered.wav`, since the verifier computes MSE between original and recovered PCM samples and expects near-zero error, meaning both the WAV header and every sample byte must survive the round trip untouched.