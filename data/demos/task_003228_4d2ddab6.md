Split this into forensic extraction and adversarial filtering, and validate each half against ground truth you derive yourself. For the video: use `ffmpeg` to extract frames (e.g. `-r` at a low rate or all frames) from `/app/upload_logs.mp4`, then OCR each frame (e.g. `tesseract`) to reconstruct the scrolling terminal log text; scan the recovered text lines for HTTP request paths containing `../`, extract the associated source IP from each such line, dedupe, and write one IP per line to `/home/user/blocked_ips.txt` — don't guess IPs, actually read them off the frames. For the filter: build `/home/user/upload_filter.py` to reject (exit 1) on three independent conditions — (a) path safety: normalize `target_path` and reject any `../`, `..\\`, absolute paths, or paths that resolve (e.g. via `os.path.normpath`/`posixpath`) outside the `uploads/` prefix; (b) certificate chain: use the `cryptography` library to load `client_cert_pem` and `intermediate_cert_pem`, verify the client cert's signature was made by the intermediate's public key, and verify the intermediate's signature was made by the Root CA at `/app/root_ca.pem`, rejecting on any verification failure; (c) reject dangerous extensions (`.sh`, `.py`, `.exe`, `.php`). Only exit 0 if all three checks pass, and produce zero stdout/stderr on success. Test both halves against the corpora yourself before finishing, since the grader fuzzes both objectives independently.
