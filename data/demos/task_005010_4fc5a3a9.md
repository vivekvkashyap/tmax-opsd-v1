Reverse-engineer `legacy_stats.pyc` with `dis`/`uncompyle`-style bytecode inspection or by just importing it and probing behavior (`python3 -c "import dis, legacy_stats; dis.dis(legacy_stats.calculate_metrics)"`) — the classic naive variance bug is computing `E[x^2] - (E[x])^2` in one pass, which goes slightly negative under floating-point rounding when values share a huge common offset and have tiny relative spread. To build `crash_input.txt`, pick several floats with a very large shared magnitude (e.g. all near 1e8+) but differing only in the last few significant digits, so the two large sums nearly cancel and floating point error pushes the naive variance below zero — verify it actually raises `ValueError` when run through `legacy_stats.calculate_metrics` before finalizing the file. For `fixed_stats.py`, implement the two-pass method directly (no `statistics` module): compute the mean first, then average of squared deviations `(x - mean)**2` from that mean, then `sqrt` — this is guaranteed non-negative in exact math and numerically far more stable. `run_trace.py` should just parse the CSV line, call your fixed function, and write the resulting stddev to `fixed_output.txt`; confirm the same crash_input data no longer errors and produces a sane positive value.
