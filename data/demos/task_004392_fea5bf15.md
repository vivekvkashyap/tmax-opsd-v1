Treat this as black-box reverse engineering: probe `/app/legacy_cleaner` with a battery of crafted stdin inputs before writing any replacement code. Start with simple cases — pure hex strings, pure non-hex text, mixed content, empty input, odd-length hex — and diff the outputs to isolate the transformation rules. Useful probes: feed only hex-digit characters and see if it pairs them up; feed an odd number of hex digits and see what happens to the trailing one; feed hex pairs that decode to control characters vs. printable characters to find where the printable/non-printable boundary lies; feed completely random bytes (including punctuation and non-ASCII) to confirm non-hex characters are simply stripped rather than causing errors. Also check whether a trailing newline is always appended, and what happens on fully empty or fully non-hex input. Once you've inferred the pipeline (filter → pair → decode → printable-range substitution → terminator), implement it independently in your chosen language, avoiding any subprocess call to the original binary, then re-run your own fuzz inputs against both programs and diff byte-for-byte to catch edge cases like odd trailing digits, case-insensitive hex letters, and boundary values near the printable range limits.