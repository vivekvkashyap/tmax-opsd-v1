Diagnose the SQL bug by inspecting the schema relationships first: the query joins employees, collaborations, and departments but never links `departments.id` to the first employee's `dept_id`, which is an implicit cross join producing one row per department for every real match. Fix it by adding `AND e1.dept_id = d.id` to the WHERE clause (or rewriting as explicit INNER JOINs), keeping the `?` parameter bound to the target department id unchanged. Then extend the script: build the list-of-dicts as before, run `jsonschema.validate(instance=data, schema=<loaded schema.json>)` to confirm conformance before writing, and `json.dump` the result to /home/user/report.json (use `json.dumps(..., indent=...)` or plain dump — the checker compares parsed JSON content, not formatting). Run the script and inspect report.json against the actual DB contents by hand (query the sqlite3 CLI directly for department 1's collaborations) to confirm the join no longer duplicates rows or pulls in employees from other departments.
