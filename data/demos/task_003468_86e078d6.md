Design the supervisor around a small per-instance state struct (pid, current version, crash count) and build it incrementally, testing each capability before adding the next. Start with basic fork/execvp of the 3 instances using `/home/user/mda.sh <id> <version>`, logging `EVENT START` on each spawn, and a `waitpid(-1, &status, 0)` loop in the parent that identifies which child exited via the returned pid, distinguishes a clean/expected termination from a crash by checking `WIFEXITED`/`WIFSIGNALED` against a flag you set right before intentionally sending SIGTERM (so you can tell "we asked it to stop" apart from "it crashed"), logs `CRASH` or `GRACEFUL_STOP` accordingly, and respawns on crash with the same version while incrementing that instance's crash counter. When an instance's crash counter hits 2, write the exact `alert_<id>.eml` content to `/home/user/mail/outbox/`. For SIGUSR1, don't do heavy work directly in the signal handler — set a flag/self-pipe and handle the rollout in the main loop: read `/home/user/next_version.txt`, then for each instance 1→3 sequentially, mark it as "expected stop", SIGTERM it, block until its waitpid confirms exit (logging GRACEFUL_STOP), then start it fresh with the new version (logging START) before moving to the next instance — this sequencing and the expected-stop flag are the parts naive attempts get wrong.