Use `csv.reader` (or `pandas.read_csv`) rather than naive line-splitting to read `/home/user/server_logs.csv` — proper CSV parsing already handles quoted fields with embedded newlines correctly, so the "dropped rows" bug is really about not using a real CSV parser. Group rows by `server_id` (e.g. `df.groupby('server_id')` or manual dict-of-lists), then dispatch each group's processing to a `multiprocessing.Pool`/`Process` worker function that: sorts the group by `timestamp` ascending; forward-fills missing `cpu_usage` (empty-string cells) using the previous row's imputed value, defaulting the very first missing value to `0.0`; then computes a rolling window of the current row plus up to 2 preceding *imputed* values (window shrinks to size 1 or 2 near the start) and rounds the mean to exactly 2 decimals. Collect each worker's per-row `(timestamp, server_id, rolling_cpu)` results back in the parent process, concatenate across servers, and sort the combined output first by `timestamp` ascending then `server_id` ascending before writing `/home/user/rolling_metrics.csv` with the exact header `timestamp,server_id,rolling_cpu`. Double-check rounding is applied after averaging (not on intermediate ffilled values) and that multiprocessing results are correctly reassembled in order, since worker completion order is not guaranteed.