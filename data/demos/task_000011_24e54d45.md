Structure the C++ program as clean separate passes: a FASTA parser, a stats/integration pass, and formatted output. When parsing, skip lines starting with '>' as headers and concatenate all subsequent characters (stripping only newlines) into each sequence's buffer until the next header or EOF, so multi-line sequences are handled correctly; then for each complete sequence count 'G'/'C' occurrences over total sequence length to get its GC-ratio. Sort the resulting ratios ascending with `std::sort`. For trapezoidal integration over evenly spaced x-nodes spanning [0,1], use the standard formula: `dx * (0.5*(y[0]+y[N-1]) + sum(y[1..N-2]))` — don't just average all points, that's a different (and wrong) quadrature rule. Compute mean by summing and dividing by N, and compute the *population* standard deviation (divide the sum-of-squared-deviations by N, not N-1) before taking sqrt. Compute the Z-statistic as `(mean - 0.5) / (stddev / sqrt(N))`. Format every output value with exactly 4 decimal places (e.g. `std::fixed << std::setprecision(4)`) matching the literal three-line output format specified, then compile and run to produce `stats.txt`.
