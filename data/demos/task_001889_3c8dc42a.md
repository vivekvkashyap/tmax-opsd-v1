Work the three broken layers independently, verifying each in isolation before wiring them together. For Nginx, edit `/home/user/app/nginx.conf` so the relevant `location` block sets `proxy_pass http://127.0.0.1:5000;` (matching the Flask app's actual bind port), then reload/restart nginx and confirm with `curl http://localhost:8080/...` that requests actually reach Flask instead of erroring — don't just "look right", test the round trip. For the C extension, read `fast_encode.c` carefully for classic Trie bugs: a `free()` on a pointer that was never initialized (declare-without-NULL-init before an early-exit path) and a missing `free()` inside the node-deletion/cleanup loop that leaks memory on every request — fix both, then rebuild the extension and sanity-check with a quick script that repeatedly calls the encode function while watching RSS (e.g. via `/proc/self/status` or `ps`) to confirm memory stays flat. For the tests, add `unittest.mock.patch` around the external data-fetching call in `tests/test_api.py` so tests don't depend on real backend state. Finally run `/home/user/app/load_test.py` yourself to confirm it reports throughput ≥500 req/s and memory growth <5MB over 10k requests before finishing, since that's the exact pass/fail metric.
