Set up the Rust project with `cargo new fit_model` in `/home/user/fit_model`, add the `hdf5` crate to `Cargo.toml` (note it needs the system HDF5 dev libraries/headers available, and may need the crate's feature flags or `HDF5_DIR` env var set if linking fails — check the build error message if `cargo build` fails to locate the library). Open the file with `hdf5::File::open`, read the `x` and `y` datasets at the root as `Vec<f64>` via `.read_1d::<f64>()` or equivalent, and confirm their lengths match before computing anything. Implement ordinary least squares from the standard closed-form formulas: slope m = (n*Σxy − Σx*Σy) / (n*Σx² − (Σx)²), intercept c = (Σy − m*Σx) / n — compute these sums yourself in Rust rather than hardcoding results. Round both to exactly 4 decimal places (Rust's `{:.4}` format specifier handles this correctly) and write them as `m,c` on a single line to `/home/user/fit_result.txt`. Before finalizing, sanity-check your formula against a tiny hand-computable example to catch sign or summation errors.
