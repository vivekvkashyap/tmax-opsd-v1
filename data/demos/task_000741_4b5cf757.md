Do the reorganization first with `mkdir -p` for the three subdirectories, then `mv` (not `cp`) each file to its target location so the original workspace file is gone afterward — the checker verifies both that new files exist and old ones don't.

For the state machine bug, read `http_parser.c` state-by-state: it tracks method (state 0), path (state 1), version/terminator (state 2). Compare each state's handling of ' ' transitions against how it appends characters — a common bug is a state that correctly detects the space delimiter but forgets to append non-delimiter characters to the target buffer's index before incrementing, leaving that field empty. Fix only the missing append logic; don't touch the header or test file.

For the Makefile, define `libparser.a` using `ar rcs` against an object file compiled with `-Iinclude` from `src/http_parser.c`; define `test_runner` compiling `tests/test_http_parser.c` with `-Iinclude`, linking against `libparser.a`; define `test` as depending on `test_runner`, running it and redirecting stdout with `>` to `test_summary.txt`; define `clean` removing all build artifacts including `test_summary.txt`. Run `make test` and check the summary file reads exactly the expected pass string.
