This is a black-box reverse-engineering task: you're given the exact byte-format spec for `.carc` files and told to match `/app/conf_reader`'s behavior bit-for-bit, so use the legacy binary as an oracle for testing your hypotheses (craft small hand-built `.carc` byte sequences with Python/`xxd`, run them through the binary, and diff against your Go program's output) rather than trying to infer edge cases purely from the spec text. Implement the parser in Go reading the header (`CARC` magic, 4-byte little-endian CRC32 over everything after the checksum field using `hash/crc32` with the IEEE polynomial), then walk entries sequentially by type byte, respecting 1-byte lengths for symlink targets/paths but a 2-byte little-endian length for file content — mixing these up is the easiest bug to introduce. For resolution, do a per-path DFS/loop following symlink chains with a `visited` set: unresolved targets become `<BROKEN>`, revisiting an already-visited node in the current chain becomes `<LOOP>`. Marshal the final path→content map with `encoding/json` (default formatting, no custom indentation) for exact byte match. For the atomic write, always write to `<output>.tmp` first and use `os.Rename` to move it into place — the verifier watches syscalls for this, so writing directly to the final path will fail even if content is correct.
