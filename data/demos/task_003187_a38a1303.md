Start with OCR: run `tesseract /app/legacy_metadata.png stdout` (or convert to a cleaner format first if the raw scan is noisy — try upscaling/thresholding with `convert`/`imagemagick` if the first OCR pass is garbled) and grep the output for the `CRITICAL_ANOMALY_THRESHOLD=` and `TARGET_SENSOR=` lines to recover their values programmatically — never hardcode a guessed value, always derive it from the actual OCR text. For the C++ pipeline, read the CSV once, group rows by sensor_id, and use `std::thread` (or a thread pool / `std::async`) to compute min/max/mean/stddev per sensor concurrently rather than serially. Compute anomalies as values outside `mean ± 3*stddev` per sensor, then flag sensors whose anomaly count exceeds the threshold you extracted. For similarity, align each sensor's per-timestamp values as one ordered vector (they share timestamps) and compute pairwise Euclidean distance from the target sensor's vector to every other sensor's vector, keeping the minimum. Serve both computed results from an HTTP server bound to `127.0.0.1:9090` with the two GET endpoints, rounding output floats to 2 decimals, and keep it running as a background/foreground process for the verifier to query.