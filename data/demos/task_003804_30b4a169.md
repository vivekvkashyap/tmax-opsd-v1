Handle the three phases independently and verify each before moving on. Phase 1: write `redact.sh` using `sed` or `grep -oE` with the pattern `AKIA[A-Z0-9]{16}` to match AWS key IDs precisely (exactly 16 trailing uppercase-alphanumeric chars, not "at least 16" or loose matching), replacing matches with `[REDACTED]` via `sed -E 's/AKIA[A-Z0-9]{16}/[REDACTED]/g'` into `app_redacted.log`. Phase 2: the mem.dmp is hex-encoded ciphertext for a single-byte XOR of a string starting with the known plaintext prefix `DB_PASS=`; decode the hex to bytes, then derive the XOR key by XORing the first ciphertext byte against the first known-plaintext byte `D` (0x44) — verify the key is consistent by checking it also recovers `B_PASS=` for the following bytes — then XOR the full byte stream with that single key to get the plaintext, and write everything after `DB_PASS=` to `recovered_pass.txt` with no trailing newline (`printf` not `echo`). Phase 3: write `run_secure.sh` using `env -i SECRET_KEY="$2" bash -c 'ulimit -n 50; exec /home/user/process.sh "$1"' _ "$1"` (or equivalent construction) so the child runs in a fully empty environment except `SECRET_KEY`, with the file-descriptor limit applied inside the same subshell that execs the target script; also create the dummy executable `process.sh` that echoes to `success.out` as specified, then run the full chain end-to-end to confirm it works.
