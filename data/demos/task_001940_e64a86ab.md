Start by identifying the real exported function name rather than guessing from the task description — run `nm -D /app/libevaluator.so` or `objdump -T /app/libevaluator.so` (stripped binaries still keep the dynamic symbol table for exported functions) and grep for a plausible evaluator symbol; don't assume a generic name like `evaluate`. Once you have the exact symbol and confirm its signature is `const char* -> double` by inspecting how it's called/tested, load it via `ctypes.CDLL` (or your chosen FFI), setting `.argtypes`/`.restype` explicitly to avoid truncation bugs with the double return value. Build the HTTP server around this single FFI call: parse the `expr` query parameter (remember to URL-decode it), call the function, and return the raw result as plain text on port 127.0.0.1:9090. For the file-organizing step, reuse the same FFI binding (or hit your own running API) to evaluate each config file's first line, treating exceptions/NaN as "low", and move files with `shutil.move` or `mv` into the correct target directory — create both output directories first. For the benchmark, time 10,000 native FFI calls with `time.perf_counter()` around a loop (not subprocess calls), and write the elapsed seconds as a plain float to `/home/user/benchmark.txt`.