Work phase by phase and verify each output before moving to the next, since later phases depend on earlier ones being exactly right. Parse `/home/user/project_root/logs/build.log` for lines matching `error:` or `fatal error:` and extract the `.cpp` filename each belongs to (the error lines reference the file by path/name near an `error:` marker) — grep with context or a small script, don't assume every mentioned file failed. Load `/home/user/project_root/config/registry.json`, drop the component entries whose filename matches one of the failed files, and write the JSON back preserving the rest of the structure (use `python3 -m json.tool` or `jq` to validate it's still well-formed). For phase 3, recursively find `.cpp` files under `src/` containing the exact literal `/* TODO: UPDATE_LICENSE */` and replace it in-place with `/* LICENSE_UPDATED_2024 */` (e.g. `grep -rl` piped to `sed -i`), leaving files with other/no license markers untouched. For the C++ generator, include `/home/user/libs/json.hpp`, parse the pruned registry, walk `src/` recursively (`std::filesystem::recursive_directory_iterator`) to resolve each remaining component's absolute path, sort entries by component name, and print CSV rows to stdout; compile with `-std=c++17` and redirect the run's output into `/home/user/workspace/final_manifest.csv`, then diff it against your own expectations from phases 1-2.