Do not try to disassemble or patch the stripped Go binary — instead treat it as a black box and do differential analysis on the two labeled corpora. Start by hex-dumping (`xxd`/`od -A x -t x1z`) a few files from `/app/corpus/clean/` and `/app/corpus/evil/` side by side to reverse-engineer the record framing described in the task (1-byte type, 2-byte little-endian length, then payload) — confirm this by checking that summing header+length repeatedly walks cleanly to EOF on clean files. Then diff the byte-level structure of evil vs clean files to isolate exactly which type/length combination only appears in evil ones, and confirm it's the discriminating feature by checking it never appears in any clean file. Implement `/home/user/wal_classifier.sh` to walk the TLV records of the given file (a small embedded Python or awk parser is easiest — read type byte, read 2-byte LE length, skip that many payload bytes, repeat) and exit 1 the instant you see the type/length signature you identified, else exit 0 after fully parsing. As a robustness fallback/cross-check, you can also validate your classifier by running `timeout 1s /app/wal_ingester <file>` and confirming hangs correlate with your static detection — but ship the fast static check as the primary mechanism since it must scale to a large hidden corpus.