In the Rust file, `get_diff` takes ownership of two `Vec<String>` and tries to return `Vec<&'a String>` referencing their elements — the borrow checker rejects this because the Vecs don't live long enough tied to that lifetime. Fix by changing the function to borrow instead of own: take `&[String]` (or `&Vec<String>`) parameters and return references borrowed from those slices, updating the call site in `main` to pass `&a_lines, &b_lines`. Verify with `cargo build` before moving on. For the C side, compile `main.c` with gcc as-is (it already sorts and merges to produce the symmetric difference) into the named executable. Run the C binary on the two dataset files, redirecting stdout to the required artifact path. To capture timing, wrap the C binary invocation with `/usr/bin/time -p <cmd> ... 2> perf.log` since `time -p` writes its real/user/sys lines to stderr, not stdout — don't accidentally overwrite or omit that redirection. Run the fixed Rust verifier via `cargo run --manifest-path ... --` on the same inputs, redirecting to its own output file. Finally diff the two output files to confirm they match exactly before considering the task done.
