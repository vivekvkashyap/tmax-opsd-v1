First inspect `/home/user/start_server.sh` and `/home/user/math_server.py` to see how the server currently launches, then add `--py3` argument parsing (e.g. `if [[ "$1" == "--py3" ]]; then python3 ...; else python2 ...; fi`), background it with `&`, and `sleep 2`. Before running, kill any stale server (`pkill -f math_server.py`) so you don't test against a leftover Python2 instance. For `/home/user/ws_client.sh`, loop over `/home/user/expressions.txt` line by line, send each via `websocat -1 ws://127.0.0.1:8888` (e.g. `echo "$expr" | websocat -1 ws://127.0.0.1:8888`), capture the response, and check if it is exactly the literal string `MIGRATION_ERROR` — if so, recompute the same expression locally with `echo "$expr" | bc -l` instead of using the server's answer. Format each line as `[Expression] = [Result]` (use the original expression text) and append to `/home/user/evaluation_results.log`, trimming trailing whitespace/newlines from both server and bc output with something like `tr -d '\n'` or `sed`. Run the full sequence — restart server with `--py3`, then run the client — and inspect the resulting log for correct formatting on every line, including cases that trigger the local `bc -l` fallback.
