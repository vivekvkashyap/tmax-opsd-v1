Fix the three bugs surgically rather than rewriting the package. In `math_utils.py`, the naive truncated Taylor series for e^(-x) blows up for large x due to catastrophic cancellation of large alternating terms — replace it with Python's built-in `math.exp(-x)`, which is numerically stable for all magnitudes. In `serializer.py`, the code unpacks only the first 4 bytes as a 32-bit uint (`'<I'`, `data[0:4]`) but the malware's keys are 64-bit — change the format string to `'<Q'` and the slice to `data[0:8]`. In `decoder.py`, the lock is acquired then released only on the happy path, so any exception in `process_internal` leaves it held forever under contention — wrap the critical section in `with lock:` (or try/finally) so it's always released. For the HTTP service, use Flask/FastAPI/http.server bound to `127.0.0.1:9090`, implement `POST /decode` that first checks the `Authorization: Bearer sec-research-agent-2024` header exactly (401 if missing/wrong), then base64-decodes the `payload` field, calls the now-fixed `decode_payload`, and returns `{"decoded_key": <int>, "status": "success"}`. Start it as a background process (not blocking your session) and only write `/tmp/server_ready.log` containing `READY` after confirming the socket is actually accepting connections (e.g. poll it).