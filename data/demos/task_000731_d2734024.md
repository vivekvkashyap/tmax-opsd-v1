This task has four independent parts — do them in order and verify each before moving on. (1) Nginx: edit the `upstream` block in `/home/user/edge_proxy/nginx.conf` to point at `unix:/home/user/run/app.sock` instead of the stale `/tmp/backend.sock` path. (2) logrotate: write `/home/user/logrotate.conf` targeting both log files (a glob or explicit paths) with `daily`, `rotate 7`, `compress`, and `missingok` stanzas — check syntax with `logrotate -d`. (3) cron: add `*/5 * * * * /home/user/monitor.sh` to the current user's crontab via `crontab -e` or `(crontab -l; echo "...") | crontab -`, then confirm with `crontab -l`. (4) The encoder is the hard part — treat `/app/legacy_encoder` as a black-box oracle: feed it varied (T,H,P,L) inputs via stdin and hexdump the stdout (`xxd`/`od -A x -t x1z`) to spot fixed magic bytes, a checksum byte, a flag byte, and little-endian 16-bit fields — vary one input at a time to isolate which output bytes each field controls, and test boundary values (0, 10000) to catch overflow/endianness details. Reimplement your deduced byte layout in C++, then fuzz-compare your binary against the oracle on many random inputs before finishing.