Handle the reusable C++ tool and the one-off transcription task as separate deliverables. For `/home/user/process_telemetry.cpp`: read stdin line by line, split on commas, and discard (with `WARNING: Invalid line` to stderr) any line that doesn't split into exactly 4 fields or whose first field fails strict integer parsing (e.g. `strtol` with full-string validation, not just atoi). For deduplication, hash the full raw line (or the tuple of its 4 string fields) into an `unordered_set<string>` and skip lines already seen, checking membership before further processing. For valid unique rows, emit exactly three `Timestamp,SensorName,Value` lines to stdout using the literal names `SensorAlpha`, `SensorBeta`, `SensorGamma` in that column order. Build this defensively since it will be fuzz-tested against many random CSV-like inputs for exact byte-level output matching, so test edge cases (empty lines, extra columns, non-numeric timestamps) yourself first. Separately, transcribe `/app/telemetry_log.wav` with `ffmpeg`/a speech-to-text tool, listening for repeated timestamp+sensor triples (the dictation includes deliberate repetition to exercise your dedup logic) — hand-convert what you hear into the 4-column CSV format, pipe it through your compiled binary, and save the result to `/home/user/final_telemetry.csv`.