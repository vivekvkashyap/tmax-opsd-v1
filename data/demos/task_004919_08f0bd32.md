Work service-by-service, verifying each layer independently before wiring the next: (1) edit `/home/user/app/nginx.conf` so it proxy_passes to `http://127.0.0.1:5000/`, start Redis, start the Celery worker (`celery -A worker worker`) and Flask app in the background, then start nginx pointed at that config, and confirm the chain with a raw curl through 8080 before touching the classifier. (2) For the stiffness classifier, inspect what makes `solve_ivp(method='RK45')` diverge in the worker code — typically extreme GC content or long tandem repeats create numerically stiff kinetics — then write `/home/user/app/classifier.py` to compute per-sequence features (GC%, longest tandem repeat run) from the FASTA using Biopython or manual parsing, and threshold them empirically by testing against both `/home/user/data/clean_sequences.fasta` and `/home/user/data/evil_sequences.fasta` until every ID classifies correctly, writing `{"id": "accept"|"reject"}` to `classifications.json`. (3) Build `orchestrate.ipynb` last, importing the classifier module and POSTing only "accept" sequences to `http://localhost:8080/simulate`, and validate it runs cleanly end-to-end with `papermill`.
