Implement this as two clearly separated passes so you don't conflate refinement depth with bootstrap seeding. Pass 1: build the initial 10 equal bins of 100 points each, compute population variance (sum of squared deviations from the mean divided by N, not N-1) per bin, and mark for splitting only bins with variance strictly > 0.5; split each marked bin into two equal halves by index range — do this splitting only once, never recursing into the sub-bins even if their own variance also exceeds 0.5 (a naive recursive implementation is the classic bug here). Collect the final bin list (mix of full bins and half-bins) in ascending order of start index. Pass 2: for each final bin independently, seed a fresh `rand.New(rand.NewSource(int64(Start)))` right before that bin's bootstrap loop (reseeding per-bin is essential for reproducibility — sharing one global RNG across bins will desync the sequence from the expected output), run exactly 1000 resample-with-replacement iterations using `.Intn(N)` to pick indices, compute each sample's mean, sort the 1000 means, and read off index 25 and 974 for the CI bounds. Write the TSV with the exact header and `%.4f` formatting, and drive both compile and run through the required Makefile targets.