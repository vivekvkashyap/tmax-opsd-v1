Parse `/home/user/primer.fasta` by skipping the `>`-header line and concatenating the sequence characters, then derive `L` (sequence length) and `GC%` by counting G/C bases over L — compute these from the actual file contents, don't assume a value. Follow the simulation spec exactly: seed with `numpy.random.seed(42)`, draw all 100,000 `[Na+]` samples in one `numpy.random.uniform` call (order of operations affects the RNG stream, so don't draw in a loop), apply the Tm formula elementwise, and add the 1e9 offset to get `shifted_Tm` as float64. For the two variance methods, implement Method A literally as written (`np.sum(x**2)`, `np.sum(x)`, plug into the naive formula) rather than using `np.var` — this is the whole point, since summing huge shifted values squared loses low-order bits to floating-point precision (catastrophic cancellation), while Method B subtracts the mean first and stays numerically stable. Expect Method A's result to look degenerate (near zero or nonsensical) and Method B's to be small and sane; that contrast is expected behavior, not a bug — write both, rounded to 4 decimals, to the CSV in the specified order.
