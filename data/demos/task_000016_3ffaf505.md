Structure `main.rs` as the classic ETL stages so each is easy to verify independently. Extract: use the `csv` crate's `ReaderBuilder::new().has_headers(false)` to read both source files into a common row struct (timestamp: String, sensor_id: String, metric_type: String, value: f64), reading source_a fully before source_b so ordering is deterministic. Validate: apply the per-`metric_type` inclusive range checks given in the task and reject any unrecognized `metric_type` outright — do this as a filter before hashing, not after. Deduplicate: for each surviving row compute `md5::compute(format!("{}{}{}", timestamp, sensor_id, metric_type))`, hex-encode it lowercase (the `hex` crate), and use an insertion-ordered check (a `HashSet<String>` of seen hashes, iterating rows in the order you read them) so only the first occurrence of a given hash is kept. Load: open `rusqlite::Connection` to `/home/user/data/measurements.db`, create the exact `metrics` table schema given, and insert inside a single transaction with a prepared statement for performance. Run `cargo build` then `cargo run` in `/home/user/etl_pipeline` and inspect the resulting DB with `sqlite3` to sanity-check row count and values before finishing.
