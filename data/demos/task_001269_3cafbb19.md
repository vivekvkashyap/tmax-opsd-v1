Handle the three log formats separately before merging: parse `gateway.log` timestamps as UNIX epoch ints/floats via `datetime.fromtimestamp(ts, tz=timezone.utc)`, `aggregator.log` via `datetime.fromisoformat` (strip/replace the trailing `Z`), and `db_writer.log` via `datetime.strptime(ts, "%Y/%m/%d %H:%M:%S")` (treat as UTC) — a naive line-based merge without converting all three to a common comparable format will sort incorrectly. Tag each parsed entry with its source filename, format all as `YYYY-MM-DDTHH:MM:SSZ`, sort the combined list by timestamp, and write `/home/user/unified_logs.txt` as `[ISO8601_TIMESTAMP] [SOURCE_FILE] MESSAGE` lines. For the anomaly: query `/home/user/data/sensors.db`'s `readings` table, group by `sensor_id`, and compute each group's standard deviation of `value` (Python's `statistics.stdev` — sample stdev, not population — matching how the grader likely computed it) to find the sensor with the highest stddev. In `/home/user/investigate.py`, find the log line containing "FATAL" for `crash_time`, then write `/home/user/report.json` with `crash_time`, `anomalous_sensor`, and `highest_stddev` (rounded to 2 decimals).
