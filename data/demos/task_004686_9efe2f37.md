Split this into extraction, loading, and a single analytic SQL query, verifying each stage before moving on. Extract the subtitle stream with `ffmpeg -i /app/network_traffic.mp4 -map 0:s:0 subs.srt` and parse each cue's text against the fixed pattern `Route: (\S+) -> (\S+) \| Bytes: (\d+)` — inspect the raw extracted subtitle text yourself first to confirm the exact format before writing the parser. Load into SQLite table `edges(source, target, bytes)`, aggregating (summing) bytes when the same source-target pair repeats. For `/home/user/query_network.py`, write one SQL query using a recursive CTE that starts from the given node at level 0, walks `source -> target` edges forward computing each descendant's minimum level, then a window/aggregate step that, per level, takes the max `bytes` of edges landing on nodes at that level, and separately sums bytes over all edges reachable in the downstream subgraph for `total_descendant_bytes`. Test incrementally: first confirm the DB content with a manual `sqlite3` query, then test your Python script's JSON output against several node IDs (including leaf nodes and nodes not present, which should yield an empty/zero result) before considering it done — the grader fuzzes many node IDs against a hidden reference.
