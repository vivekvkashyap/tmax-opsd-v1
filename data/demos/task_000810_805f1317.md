Fix the Rust project layer by layer, compiling frequently to localize errors. Start with build.rs: it must emit `cargo:rustc-link-search=native=/usr/local/lib` and `cargo:rustc-link-lib=dylib=audioprocessor` (and rerun-if-changed as needed) so the linker finds libaudioprocessor.so at `/usr/local/lib` (confirm the header at `/usr/local/include/audioprocessor.h` for the exact exported function signatures). In src/ffi.rs, wrap the C `process_audio_file`/free pair correctly: convert the incoming Rust path to a `CString` kept alive for the duration of the FFI call, convert the returned `*const c_char` to an owned Rust `String` via `CStr::from_ptr(...).to_string_lossy().into_owned()` immediately, then call the library's own free function on the raw pointer — don't let Rust's allocator try to free C-allocated memory, and don't return a reference tied to a temporary. In main.rs, share state across async tonic handlers with `Arc<Mutex<...>>` (or similar) rather than raw shared mutable references. Build with cargo, run the server, send a gRPC request (e.g. via grpcurl or a small client), then apply `/app/corrections.patch` to the raw transcript with `patch` or `git apply`, writing only the final text to final_transcript.txt.
