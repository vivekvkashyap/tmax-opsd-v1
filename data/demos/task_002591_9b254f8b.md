First nail down the binary parsing in isolation: hand-build a 16-byte test header in Python/hexdump (magic u32 LE, project_id u32 LE, timestamp u64 LE) and pipe it into your C program to confirm the field offsets and endianness are read correctly with `fread`/`memcpy` before worrying about anything else. The core pitfall here is concurrent writers corrupting `/home/user/inventory.log` — use `flock(fd, LOCK_EX)` (or `fcntl` with `F_SETLKW`) around the open-append-write-close sequence in `extractor.c` so simultaneous invocations from parallel jobs don't interleave partial lines; test this by launching several instances against valid headers simultaneously and checking the log has clean, complete lines with no interleaving/corruption. Compile with `gcc -o /home/user/extractor /home/user/extractor.c`. For `organize.sh`, use `find /home/user/assets -name '*.bin' | xargs -P 4 -I{} sh -c 'cat {} | /home/user/extractor'` (or background jobs with a `wait`) to get real parallelism, then after all jobs complete, sort `/home/user/inventory.log` numerically by the ProjectID field (`sort -t: -k2 -n` style, adjusting for the exact field position after "ProjectID: ") into `/home/user/inventory_sorted.log`. Run the whole pipeline and diff your sorted output's line count against how many `.bin` files actually carry the valid magic number.
