Work in two independent halves and test each before wiring them together. First, extract frames: run `ffmpeg -i /app/deployment_feed.mp4 -vf fps=1 /home/user/frames/frame_%04d.jpg` (create the directory first), and confirm the numbering matches `frame_0001.jpg` style with `ls`. Second, write the C TCP server: use `socket()`/`bind()`/`listen()`/`accept()` bound to 127.0.0.1:8888, loop `accept()`-ing connections, read until you see `\n` (use a small buffer, don't assume the whole request arrives in one `read()`), parse exactly 4 digits, build the path `/home/user/frames/frame_%s.jpg`, and `stat()`/`fopen()` it. On success write the header `OK <size>\n` with `write()`/`send()` BEFORE streaming the raw file bytes (do not add extra newlines or text after the binary data), on failure write `ERR\n`. Close the client socket after each response but keep the listening socket open for the next connection. Compile with `gcc -o /home/user/streamer /home/user/streamer.c`, launch it backgrounded (e.g. `nohup ./streamer &`), and verify with `printf '0002\n' | nc 127.0.0.1 8888 | xxd | head` that the header and binary bytes look correct before considering the task done.