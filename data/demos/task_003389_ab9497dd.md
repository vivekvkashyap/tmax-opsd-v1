Build this incrementally and test each layer before combining. First write a standalone function to parse the WAV file: skip the fixed 44-byte header, then read the remainder as an array of 16-bit little-endian signed PCM samples (`int16_t`, since it's specified as 16-bit mono) — compute `max_amp` as the max absolute value across samples, and `zero_crossings` by comparing the sign of each sample to the previous one (treat a sample of exactly 0 as positive, per the spec) and counting sign changes; verify these two numbers against a small hand-computed example before trusting the full file. Separately, write a minimal blocking TCP server using raw sockets (`socket`/`bind`/`listen`/`accept`) that reads an HTTP request line, parses only the path, and manually writes a well-formed HTTP/1.1 response string with status line, `Content-Type`, and `X-Process-Time-Us` headers followed by a blank line and the JSON body — get header formatting exactly right (CRLF line endings) since a malformed response can fail parsing even with correct data. Wrap the parsing timing with `clock_gettime(CLOCK_MONOTONIC, ...)` around just the parse+compute step. Compile with `gcc -O3`, run it detached/backgrounded, and confirm with `curl` before finishing.
