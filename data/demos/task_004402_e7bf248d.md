Step 1: extract one frame per second with `ffmpeg -i dashboard_recording.mp4 -vf fps=1 frame_%d.png`, then for each frame sample the top-left 50x50 region's pixel values (e.g. with Python/PIL or ImageMagick) and check whether it's pure red (255,0,0); collect the matching second indices (0-indexed, matching frame order) into `anomaly_seconds.txt`. Step 2: `cargo new sanitizer_project`, implement a filter reading stdin line by line and writing to stdout only lines that do NOT match (case-insensitive) any of the SQLi/XSS keyword patterns or contain the raw escape byte `\x1B`/octal `\033`; test it directly against every file in `/app/corpora/clean/` and `/app/corpora/evil/` before trusting it, then `cargo build --release` and copy the binary to `/home/user/bin/sanitizer`. Step 3: use `sqlite3` to select `message` from `logs` where `timestamp_sec` is in your anomaly-seconds set, pipe that output through the sanitizer binary into `sanitized_anomalies.txt`. Step 4: write a standard 5-field cron line for the pipeline script running every 15 minutes to `pipeline.cron`.
