Read `/home/user/data/affinities.bin` as raw bytes and reinterpret every 4-byte little-endian chunk as an `f32` (e.g. `bytes.chunks_exact(4).map(|c| f32::from_le_bytes(c.try_into().unwrap()))`) — don't parse it as text. Implement Kahan summation explicitly rather than relying on a library: maintain a running `sum` and a compensation `c` (both as accumulators with enough precision, e.g. `f64`), and for each value do `y = value - c; t = sum + y; c = (t - sum) - y; sum = t`. This corrects for the rounding error that a naive `sum += value` loop accumulates over a million terms — that naive version is exactly the bug you're avoiding.

While iterating, record the running Kahan sum every time the 0-indexed element counter hits `99_999, 199_999, ..., 999_999` (i.e. every 100,000th element, 10 checkpoints total) plus the final total after all 1,000,000 elements. Format every printed value to exactly 4 decimal places (e.g. Rust's `format!("{:.4}", x)`) and write the total first, then the 10 checkpoints in order, one per line, to `/home/user/results.txt`. Compile and actually run the program so the file is generated from the real data, not asserted by hand.
