Build this as a script that computes results once, then serves them, so you can debug computation and serving separately. Load `/app/audio/sample.wav` at native sample rate (e.g. `librosa.load(path, sr=None)`), then compute mean, variance (`numpy.var`), and mean RMS energy across frames (e.g. `librosa.feature.rms` averaged, or manual sqrt(mean(frame^2)) per frame then averaged) directly on the raw sample array — print these out and sanity-check they're finite, non-degenerate numbers before wiring them into the API. For transcription, load `openai/whisper-tiny` via `transformers` (processor + model), run generation on the audio, decode the text, and separately keep the raw generated token ID sequence (don't re-tokenize the decoded text, capture the ids returned by `generate()` directly). In `/home/user/pipeline.py`, wrap all this in a FastAPI/Flask app: on `GET /api/dataset/sample_features`, check the `Authorization` header equals exactly `Bearer ds-research-secret`, returning 401/403 on mismatch or absence, otherwise return the JSON with `audio_stats`, `transcription`, and `tokens` in the exact structure specified. Run it bound to `0.0.0.0:8555` and leave it running (e.g. `uvicorn` in the foreground or backgrounded with nohup), then curl it yourself with and without the header to confirm both paths work before finishing.
