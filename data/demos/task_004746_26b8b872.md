Use `git bisect` mechanically rather than guessing: identify a known-good early commit and HEAD as bad, write a small reproducer (a corrupted binary log file with a frame whose declared length exceeds the remaining buffer), then run `git bisect start`, mark good/bad, and script the check (`git bisect run <script that builds and runs analyzer against the reproducer, exiting nonzero on crash/wrong output>`) so bisection is automated and exact — capture the resulting 40-char hash into `/home/user/bad_commit.txt`. For the fix itself, look at the bounds check around frame parsing: the bug class described (segfault or dropped valid logs after a corrupted entry) usually means the code advances the read cursor using the *declared* payload length without first validating `pos + header_size + length <= total_size`; when that check fails you must skip forward safely (e.g., advance by one byte and rescan, or skip only the malformed frame) instead of blindly trusting the length field. After patching `analyzer.c` on `main`, rebuild with `make`, verify against your reproducer, then re-integrate with Redis: run `/home/user/env/startup.sh`, and invoke `./analyzer -r 127.0.0.1 -q raw_logs -o /home/user/final_timeline.log`, confirming the ingestor's seeded frames appear correctly sorted in the output.
