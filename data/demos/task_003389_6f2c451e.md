First read `main.go` to locate the running variance formula — a naive two-pass style accumulator (`sum`, `sumSq`) computing `E[x^2] - E[x]^2` is prone to catastrophic cancellation when values are large in magnitude but differ only slightly, since squaring amplifies floating-point rounding error until the subtraction goes negative. To find the minimal failing sequence, don't guess: write a small throwaway script that starts the *original* (unfixed) server fresh, feeds prefixes/windows of `/home/user/dataset.json` in order, and checks the response after each POST, shrinking to the smallest contiguous run that first produces a negative variance — save exactly that subsequence to `/home/user/mre.json`. Then replace the variance calculation with Welford's online algorithm (track `count`, `mean`, `M2`, update incrementally per new value, and derive `variance = M2/count`), which is numerically stable for this exact scenario. Add the required trace logging on every update using `%f` formatting for each field. Finally restart the fixed server cleanly, replay the MRE sequence, and capture the final JSON response — make sure the process is left listening on port 8080 when you finish.
