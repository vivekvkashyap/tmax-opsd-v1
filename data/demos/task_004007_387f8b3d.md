Work the four steps in order since later steps depend on earlier recoveries. First, transcribe `/app/intercepted_signal.wav` with `whisper` (or another installed STT tool) and write the exact transcript to `/home/user/transcript.txt`; read the numbers it spells out carefully — they encode a seed and a boundary constraint relevant to a signed-integer range. Second, recover the deleted `fuzzer.sh` from the ext4 image `/app/workspace.img`: mount it read-only or use `debugfs`/`extundelete`/`testdisk` to walk inodes and pull back deleted-but-intact data blocks, saving the result to `/home/user/fuzzer.sh`. Third, read `/app/extract.sh` closely — bash arithmetic `$((a*b))` is 64-bit signed and silently wraps/overflows on large products, which is the actual bug class you're detecting, not floating-point precision. Fourth, build `/home/user/detector.sh`: for each number sequence in the input file, compute the running products with arbitrary precision (e.g. `bc` or `awk` with doubles/strings, not native bash arithmetic) and flag as "evil" (exit 1) any sequence whose product would exceed or wrap the 64-bit signed range (9223372036854775807); otherwise exit 0 with no output. Test against both a clean and an evil sample you construct yourself before trusting it on the hidden corpora.
