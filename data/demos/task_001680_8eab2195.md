Start by reproducing the crash with `cargo run` and reading the panic message/backtrace (RUST_BACKTRACE=1) to confirm it's an out-of-bounds/underflow in the derivative loop, not something else. In `compute_derivative`, replace the single uniform loop with three cases: index 0 uses forward difference `data[1]-data[0]`, index `n-1` uses backward difference `data[n-1]-data[n-2]`, and all interior indices use the central difference `(data[i+1]-data[i-1])/2.0`. Handle the length-0 (empty vec) and length-1 (`[0.0]`) edge cases explicitly before the loop so you don't index into an empty or single-element slice. Add a `#[cfg(test)] mod tests` block with a `#[test] fn test_boundary_conditions()` that asserts the derivative of `[1.0, 4.0, 9.0, 16.0]` matches the expected values — work this out by hand from the boundary/central formulas rather than guessing. Run `cargo test` to confirm it passes, then run the binary against `data.csv` in the project directory (don't pass args, since CLI logic reads CWD by default) and write the printed sum verbatim to `/home/user/resolution.txt`.
