Build this in clearly separated, testable stages before wiring up the server. First, OCR `/app/ui_spec.png` with `tesseract` and parse out the max-length integer and the required placeholder token (a `{...}` style string) with a regex — print the raw OCR output while developing so you catch misreads before they silently propagate into your filter logic. Second, decode `/app/raw_locales.bin` explicitly as Windows-1252, not UTF-8 or Latin-1 (use the `encoding_rs` crate's `WINDOWS_1252` decoder in Rust) — a naive UTF-8 read will either error or corrupt the accented/currency characters in the data. Third, split each line on the first `=` into key/value, and keep only entries where `value.chars().count() <= max_len` AND `value.contains(required_token)` — apply both constraints, not just one. Fourth, stand up the HTTP server (axum/tokio or similar) bound to exactly `127.0.0.1:8080`, check the `Authorization` header equals the exact expected bearer string and return 401 otherwise, and on success return the `{"translations": {...}}` JSON built from the filtered map. Keep the server running (foreground or backgrounded with `&`/`nohup`) after you're done so automated requests can reach it — verify with a local `curl` including and excluding the auth header before finishing.
