Start by transcribing the audio: run `whisper /app/intercepted_comms.wav` (or ffmpeg-preprocess it first if needed) and read the transcript text to identify the single spoken passphrase word — don't guess it, extract it directly from the transcription output, lowercase it, and set it as `SECRET_PASSPHRASE` in `auth.h`. Separately, diagnose the link failure: run `make` and read the linker error carefully — with GCC, libraries listed in `-l<name>` flags must come *after* the object files that reference their symbols, since the linker resolves symbols left-to-right and won't look back at a library already passed. Reorder the Makefile's link line so `main.o http.o crypto.o` precede `-lpthread -lcrypto` (do this for each binary target: http_server, test_suite, bench_suite) and rebuild. Run `make test` and confirm it passes before moving on to `make bench`, redirecting its console output to `/home/user/bench_results.txt`. Finally launch `./http_server 8080` in the background and manually verify with `curl` (using Basic Auth built from `admin:<extracted-passphrase>` base64-encoded) that a protected endpoint returns success, before considering the task done.
