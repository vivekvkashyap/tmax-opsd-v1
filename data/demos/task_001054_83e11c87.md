Treat this as black-box reverse engineering validated by bit-exact fuzz comparison against `/app/legacy_reporter` — don't try to disassemble the stripped binary; instead craft small, targeted CSV inputs and diff the binary's JSON output against hand-computed hypotheses to isolate the exact aggregation logic. Start minimal: a single user with one transaction to learn base output schema/rounding, then a single user with 2-3 transactions at varying timestamps to see whether `anomalous_score` depends on prior rows for that user (test if changing an earlier row's amount changes a later row's score — that reveals a per-user cumulative/windowed dependency), then add a second user to confirm scores are scoped per `user_id` and not global. Vary row order in the CSV (not sorted by timestamp) to determine whether the binary's internal ordering is by timestamp or by input order, and use duplicate timestamps to find the tie-break rule. Once you have a candidate formula, verify it against several more fixtures including negative amounts and reversed input order before trusting it. Implement `/home/user/processor.py` to read the CSV, group by `user_id`, apply whatever per-user ordering you determined, compute the aggregation you derived, round to 2 decimals matching the binary's rounding point (test whether rounding happens before or after any multiplication), then sort final output by `txn_id` and print `json.dumps` with matching key order and no extra whitespace.