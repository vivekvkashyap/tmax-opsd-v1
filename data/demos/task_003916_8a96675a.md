The root cause of "libgraph.so cannot find libmatrix.so at runtime" is that the linker recorded `libmatrix.so` as a dependency without embedding a runtime search path, so the dynamic loader only checks default library dirs at import time. Fix `build.sh`'s link step for `libgraph.so` by adding an RPATH linker flag pointing at the absolute install directory, e.g. `gcc -shared -fPIC graph.c -L./lib -lmatrix -Wl,-rpath,/home/user/mathlib/lib -o lib/libgraph.so` (RPATH must be a hardcoded absolute path per the task, not `$ORIGIN`, since the spec calls out the literal path). After editing, run `./build.sh` and confirm the fix directly with `ldd lib/libgraph.so` (should resolve libmatrix.so) and `python3 -c "import graph_wrapper"` before writing tests. Install `pytest` and `hypothesis` via pip. For the property tests, use `@given` with `hypothesis.strategies` to build a 4x4 all-zero matrix and random `steps` (1-5) / `start`/`end` (0-3) for `test_zero_matrix_paths`, asserting the path count is 0; and a random 0/1-valued 4x4 matrix with `steps=0` for `test_zero_steps`, asserting count is 1 iff `start == end`. Run `python3 -m pytest /home/user/mathlib/test_graph.py -v` and fix any failures shown, rather than assuming correctness.