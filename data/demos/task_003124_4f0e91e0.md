Design this as three decoupled components — parser, scheduler, and transport — and unit-test the first two with plain Python before wiring up WebSockets. Parser: read the BuildDSL line by line, tracking a "current target" as you see TARGET/REQUIRES/AFTER/END, ignoring blank/unrecognized lines, producing a dict per target of {tools: [...], deps: [...]}; test it against a small hand-written script first. Scheduler/assignment: for each target, filter workers to those whose tool set is a superset of the target's REQUIRES, then pick `min()` of the eligible worker names lexicographically — fail the whole build (drop connection, exit) if no worker qualifies. For execution order, this is a topological sort with tie-breaking: maintain a "ready" set of targets whose AFTER-dependencies are all completed, and whenever multiple targets are ready simultaneously, dispatch the lexicographically smallest target name first; only advance to newly-ready targets once you receive that task's "completed" reply — don't dispatch eagerly. Log every (task, worker) assignment in dispatch order as you send it, and write that exact list to `/home/user/build_schedule.json` right before shutting down after sending "done". Use the `websockets`/`asyncio` libraries and test your server manually with a simple client before trusting it.