In evasion.c, read /home/user/http_req.txt line by line, locate the line starting with "Cookie:", then within it find the `auth_token=` substring and extract the value up to the next `;` or end of line (don't assume it's the first cookie in the header). Write exactly `<script>alert('<value>')</script>` with no trailing newline or extra text to /home/user/http_res.txt. Compile with `gcc -o evasion evasion.c`. For the checksum collision, write a small helper (Python or C) that computes `sum(bytes) % 256` for both /home/user/benign.bin and your compiled evasion binary; since appending bytes to the end of a valid ELF only adds unreferenced trailing data (harmless to execution), append exactly one padding byte whose value is chosen so that `(current_checksum_of_evasion + pad) % 256 == checksum_of_benign` — solve for `pad = (target - current) % 256` and append that single byte (or more, distributing the needed delta) to a copy of the compiled binary, saving the result as /home/user/evasion_final.elf. Make it executable (`chmod +x`), then verify by running it (confirm http_res.txt is written correctly) and re-computing both checksums to confirm they match.
