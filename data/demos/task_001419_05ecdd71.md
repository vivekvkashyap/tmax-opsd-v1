Write `/home/user/process.cpp` as a clear pipeline of stages, logging as you go. Parse both CSVs (skip header row), and normalize each coordinate to 1 decimal place using proper half-away-from-zero rounding (e.g. `std::round(x * 10.0) / 10.0`, being careful with negative numbers so `-1.55` rounds consistently) — don't rely on naive `printf`-style truncation, and format outputs with fixed 1-decimal precision (e.g. `std::ostringstream` with `std::fixed << std::setprecision(1)`) so values like `5.0` don't print as `5`. For deduplication, hash each normalized `(x,y,z)` triple into a key (e.g. combine the three formatted strings, or a custom hash for an `unordered_map`/`unordered_set`) and keep only the first id seen per key, per file, preserving the original file order. For the inner join, look up each deduplicated-A key in the deduplicated-B map and collect matches. Sort the joined output rows lexicographically by `id_A` before writing `/home/user/joined_points.csv` with the exact header `id_A,id_B,x,y,z`. Append the five required log lines to `/home/user/pipeline.log` in order as each pipeline stage completes, using the exact counts produced by your own processing (not hardcoded numbers). Compile and run the program, then diff your outputs against your own manual expectation to sanity-check formatting and rounding edge cases.