Work this in three phases before writing the script. First, run `tesseract /app/crash_alert.png stdout` (or convert/preprocess with `convert` if OCR is noisy) to recover the SALT value printed on the crash screen — do not guess it. Second, since the docs branch was hard-deleted, recover it with git forensics: run `git -C /app/system_repo reflog --all`, `git fsck --unreachable --no-reflogs`, and `git cat-file -p <blob>` / `git show <commit>` on dangling commits to locate `validation_rules.txt`, which defines the initial database state, the checksum formula, and which ACTIONs to apply. Then write `/home/user/process_journal.sh` as a pure Bash/awk state machine: read the journal file line by line, split into ACTION VALUE CHECKSUM, recompute the checksum using the recovered SALT and formula, skip any line whose checksum doesn't match, and otherwise apply ADD/SUB/MUL to the running state using `$(( ))` integer arithmetic (careful with modulo of negative intermediate values and blank/trailing lines). Print only the final integer. Test against several hand-built journals with both valid and corrupted checksums before trusting the script.
