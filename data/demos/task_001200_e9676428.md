Set up a standard Rust binary crate (`cargo new`) rather than a single script, so `cargo run --release` works out of the box. Parse the CSV manually or with a crate (e.g. `csv`), validating each row: skip it entirely if any field is missing or if `weight` fails to parse as a non-negative integer — don't let a parse error panic the program, just `continue`. Accumulate weighted degree in a `HashMap<String, u64>`: for a normal edge, add the weight to both endpoints; for a self-loop (source == target), add the weight only once, not twice — this is the detail naive implementations get wrong. After accumulating, filter out nodes below the threshold, then sort by a composite key: descending by score, and for ties, ascending by node ID (implement this as a custom `sort_by` comparator, not two separate sorts). Apply the offset/limit as a plain slice operation on the sorted vector. Serialize the paginated slice to JSON with the exact two-key schema (`node_id`, `score`) using `serde` derive or manual `serde_json::json!` construction, and write it to the output path. Double check your accumulation logic against the input file by hand for a couple of nodes before trusting the final output.