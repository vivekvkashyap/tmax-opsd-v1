Implement the recursive walk with explicit cycle detection rather than relying on Python's default recursion depth or naive `os.walk` (which can hang or crash on symlink loops). For each symlink encountered, resolve it step-by-step (e.g., follow `os.readlink` iteratively, or use `os.path.realpath` with a visited-set of `(device, inode)` pairs or of intermediate paths) and detect a cycle when you revisit an already-seen path/inode before reaching a terminal real file or directory; only delete symlinks confirmed cyclic — leave symlinks that eventually resolve to a real target untouched, even if their target lies outside the tree. Cap traversal depth or track a visited set to guarantee termination instead of trusting the OS to error out. For each valid regular file (after resolving safe symlinks), invoke `/app/analyzer <path>` via `subprocess.run`, capture stdout, and append it to the shared log under an `fcntl.flock(fd, fcntl.LOCK_EX)` critical section (open in append mode, flock, write, flush, unlock) so concurrent invocations across directories don't interleave lines. Test against both a self-referencing symlink (`a -> a`) and mutual/deep cycles to confirm no hang before running on real data.