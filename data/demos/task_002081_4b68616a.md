Recover the two secrets from the image first, independently of the JWT-building logic. Run OCR (e.g. `tesseract /app/dev_notes.png stdout`) and read the output carefully to find the labeled audience string and the SHA-1 hash exactly as printed — trim whitespace/newline artifacts OCR often introduces. The SHA-1 hash is described as a common dictionary word, so crack it by hashing candidates from a small common-password/word list (or a short brute-force over lowercase dictionary words) with `hashlib.sha1(candidate.encode()).hexdigest()` and comparing against the recovered hash until you find a match — don't guess, verify by hash comparison. Then build `forge_token.py` to exactly match standard unsigned-JWT construction: header `{"typ":"JWT","alg":"none"}`, payload with `user`, `role` (as an int, not stringified), `aud`, and `override_code` set to the cracked plaintext — serialize each with compact JSON (`json.dumps(obj, separators=(',',':'))` to avoid stray whitespace), base64url-encode each part and strip `=` padding manually (`base64.urlsafe_b64encode(...).decode().rstrip('=')`), and join as `header.payload.` with a trailing empty signature segment. Test with a couple of different username/role_id argv pairs to confirm deterministic, byte-identical formatting.