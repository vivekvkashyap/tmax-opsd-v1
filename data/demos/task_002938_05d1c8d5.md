Start by loading both reference WAVs (parse the RIFF/fmt/data chunks properly, get sample rate and 16-bit PCM samples as float64) in a quick Python or Go script, then apply a window function (e.g. Hann) and compute an FFT (numpy.fft or a Go FFT lib) on each. Plot or print the magnitude spectrum and diff the two: look for a frequency bin present in the poisoned file's spectrum with anomalously high magnitude/energy relative to the clean file's spectrum at the same bin — this is likely a narrow injected sine tone rather than a broadband change. Once you find the anomalous frequency and a magnitude threshold that cleanly separates the two references, port that exact logic into Go: read WAV headers/PCM data correctly (use a library like go-audio/wav or hand-roll the header parsing), window and FFT the samples, compute magnitude/energy in the bin(s) near the anomaly frequency, and compare to a threshold derived from the reference statistics (not hardcoded arbitrarily) to decide exit 0 (clean) vs exit 1 (poisoned). Handle files of varying length/sample rate so bin indices map correctly to Hz. Test against both reference files first before trusting the detector on unseen corpora.
