This is a fuzz-equivalence task: your compiled `/home/user/bin/seq_extractor` will be run against synthetic frame directories and its output string compared to a hidden reference implementation, so focus on matching the specified algorithm exactly rather than guessing outputs. Extract frames from the mp4 with `ffmpeg -i /app/fluorescence_sensor_run01.mp4 frame_%04d.png` (or equivalent) to inspect the format, but the real deliverable takes an arbitrary frame directory as argv[1]. In C++, use `stb_image.h` (from `/app/vendor/`) to load each 16x16 grayscale PNG into a row-major `double` matrix (pixel/255.0 or raw intensity — follow the task's stated convention consistently), fetch Eigen (e.g. via a header-only download/vendoring step in your build) and run `Eigen::JacobiSVD` to get the first left singular vector `U.col(0)`, then multiply that vector by the 4x16 basis loaded from `/app/nucleotide_basis.csv` to project into 4D nucleotide space. For each frame pick the coordinate of maximum value in the 4D projection and map its index to A/C/G/T in a fixed, consistent order; concatenate one character per frame across all frames in filename order and print the whole string to stdout, with no other output. Build the visualization script separately, computing singular-value ratios per frame and plotting with matplotlib to `/home/user/svd_variance.png`.
