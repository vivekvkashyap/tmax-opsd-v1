Build this pipeline in clearly separated stages and validate each before moving on. First, call `/app/embed_oracle "<item_text>"` via `subprocess` for every row in `/home/user/items.csv`, splitting the printed line on whitespace into 5 floats, and cache the resulting embeddings by `item_id` — don't hardcode any embedding values, always derive them from the binary's actual output. Second, read `/home/user/history.csv` and compute global `alpha` (sum of all `clicks`) and `beta` (sum of all `impressions - clicks`) across the entire dataset, then compute each row's `smoothed_ctr = (clicks + alpha) / (impressions + alpha + beta)` using those global priors (not per-user priors). Third, for each query row in `/home/user/queries.csv`, compute cosine similarity between the target item's embedding and each of the querying user's historical item embeddings, clamp negative similarities to 0 as the weight, and take the weighted average of `smoothed_ctr` — falling back to the global mean `alpha/(alpha+beta)` whenever the user has no history or all weights are zero. Watch for edge cases: users appearing only in queries.csv (not history.csv), and division-by-zero when total weight is 0. Write `/home/user/predictions.csv` with header `user_id,target_item_id,predicted_ctr`, rounding to exactly 6 decimal places, and sanity-check a couple of rows by hand before finishing.
