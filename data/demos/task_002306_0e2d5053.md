First determine the true frame count of `/app/video/archive_footage.mp4` yourself (e.g. `ffprobe -v error -count_frames -select_streams v:0 -show_entries stream=nb_read_frames archive_footage.mp4`, or fps * duration) rather than assuming a round number — your script must reject any frame number strictly greater than that real count. Write `/home/user/chunk_decoder.py` to read all bytes from `sys.stdin.buffer`, then validate the binary layout in the exact order given: first 4 bytes must equal `b'SUBZ'` (else print `INVALID_MAGIC`), next byte is the encoding ID mapped to `utf-8`/`shift_jis`/`cp1252` (else `INVALID_ENCODING`), next 4 bytes big-endian frame number and next 2 bytes big-endian payload length (`struct.unpack('>IH', ...)`), then confirm enough bytes remain for the full payload (else `TRUNCATED_PAYLOAD`) — check truncation only after confirming the header itself is complete, since a too-short buffer can fail at multiple stages and the checks must happen in the right order to match the spec exactly. Decode the payload with `errors='replace'`, and only after all structural checks pass, validate the frame number against the actual frame count (else `OUT_OF_BOUNDS`), finally printing `FRAME_{frame:06d}: {text}`. Make the script executable with the correct shebang, and self-test with hand-built byte strings covering each failure branch before considering it correct.
