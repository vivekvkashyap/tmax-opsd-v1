Do the whole task in one SQL statement issued from your Python script (via `sqlite3`), rather than post-processing in Python, since the task requires the dedup+aggregation logic to live in SQLite itself. First build a CTE that deduplicates: `ROW_NUMBER() OVER (PARTITION BY emp_id ORDER BY updated_at DESC)` and filter to `rn = 1` to keep only the latest row per employee. Then build a `WITH RECURSIVE` CTE over the manager/employee edges (starting from each employee, or from roots down) that walks the hierarchy to associate every employee with all of their ancestors (or equivalently, for each manager, collect the full subtree of descendants) — the classic pattern is a recursive CTE that starts `SELECT emp_id, emp_id AS ancestor FROM dedup UNION ALL SELECT d.ancestor, r.manager_id FROM ... JOIN ... WHERE r.manager_id IS NOT NULL`, then group by ancestor and sum the deduplicated sales_amount of everyone in that ancestor's subtree (including themselves). Order the final `emp_id` ascending, and have Python write the query results straight to `aggregated_sales.json` with the exact two-key schema, without altering the source database (open it read-only or simply issue no write statements).
