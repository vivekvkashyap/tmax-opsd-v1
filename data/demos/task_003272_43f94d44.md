Write the C++ program as a straightforward text-scan-and-substitute pass: read the whole markdown file into a string, then use a loop that finds each `{{...}}` placeholder (e.g. via string search for `{{` and the matching `}}`), parses its type prefix (`GCODE_STATS:` or `ELF_ENTRY:`) and the file path that follows, computes the replacement, and stitches the result together — do this rather than trying to build a generic templating engine. For GCode stats, only scan lines beginning with `G0` or `G1`, and for each, look for `X`, `Y`, `Z` immediately followed by a float (use `sscanf`/manual parsing, not a full G-code parser), tracking running maxima; format each with exactly one decimal place. For the ELF entry point, `#include <elf.h>`, open the file in binary mode, read the `Elf64_Ehdr` header struct directly into memory, and print `e_entry` in lowercase hex — verify your struct read matches file byte order (little-endian) rather than assuming. Resolve placeholder paths relative to `/home/user/` as specified. Compile exactly with the given g++ flags, run the binary, then in bash append `\n## Manifest\n` and the output of `sha256sum /home/user/assets/*` to the output file — check the manifest section literally matches the format before finishing.
