First use `ffmpeg -ss 00:00:10 -i /app/screencast.mp4 -frames:v 1 /home/user/reference_frame.jpg` to grab the exact reference frame, then compute its SHA-256 once and reuse that value for comparisons. Build `/home/user/validate_doc_archive.sh` (executable, proper shebang) as an ordered sequence of gates that each `exit 1` immediately on failure: (1) try to take an exclusive lock on `<archive>.lock` with `flock -w 2 -n` (or a `timeout 2 flock`) and fail if it can't acquire it; (2) verify the file is a valid gzip/tar via `tar tzf` succeeding and extract to a temp dir, checking `manifest.json`, `metadata.csv`, `frame.jpg` are all present; (3) validate `manifest.json` parses as JSON (e.g. `python3 -c "import json,sys; json.load(open(...))"` or `jq`) and reject any manifest key containing `..` or starting with `/` before touching those paths; (4) for every manifest entry, confirm the file exists and its `sha256sum` matches; (5) parse `metadata.csv` for a `reference,true` row and, if present, compare `frame.jpg`'s SHA-256 against your precomputed reference hash. Clean up temp extraction dirs and always release the lock. Test against both corpora before finishing.
