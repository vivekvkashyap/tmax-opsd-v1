Work in pandas with explicit, ordered steps rather than one clever chained expression — this pipeline has enough stages that mistakes compound silently. First dedupe: sort by `etl_run` ascending then `drop_duplicates(subset=["ts","sensor"], keep="last")` so the highest `etl_run` survives per (ts, sensor). Compute the GLOBAL min/max timestamp across the whole deduplicated dataset (not per-sensor) and build one `pd.date_range(..., freq="5min")`. Then loop/group per sensor, reindex each sensor's series onto that full global index (this is what introduces the gaps to fill), and use `.interpolate(method="linear")` — interpolate does not extrapolate past a series' existing first/last valid values, so out-of-range slots correctly stay NaN as required. For the rolling average, use `rolling(window=6, min_periods=1).mean()` on the interpolated column (right-aligned is the pandas default). Round to 2 decimals, drop remaining NaNs, reorder/rename to exactly `ts,sensor,rolling_temp`, sort by sensor then ts, and write with `index=False`. Verify by re-reading your own output and spot-checking a sensor's boundary rows for correct NaN handling before finalizing.
