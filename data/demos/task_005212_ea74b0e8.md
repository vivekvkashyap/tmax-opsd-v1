Build the C++ generator as three distinct passes rather than one tangled function. First, write a small recursive-descent (or shunting-yard) expression parser/evaluator that supports `+ - * ( )` integers and lookups into a symbol table of already-defined variables — parse `config.expr` line by line in file order (`DEF NAME = EXPR`), evaluating each expression against variables defined so far and storing the result, since later definitions may reference earlier ones. Second, emit `src/generated_config.rs`: collect all (name, value) pairs, sort by name alphabetically, and print `pub const NAME: i32 = VALUE;` lines. Third, read `previous_config.txt` (`NAME=VALUE` per line) into a map, then for each newly computed variable: if absent from the old map emit `+ NAME=NEWVALUE`, if present but different emit `~ NAME=OLD->NEW`, if unchanged emit nothing — sort these diff lines alphabetically by name too, and note that variables only in the old file (removed) are not part of the required diff format. For `ci_pipeline.sh`, chain `g++ -std=c++17 generator.cpp -o generator`, run `./generator`, then `cargo build` inside `/home/user/rust_app`, propagating failure via `set -e` or explicit exit-code checks, and `chmod +x` the script itself before running it end to end.
