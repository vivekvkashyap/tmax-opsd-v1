Treat the oracle as a black box you characterize empirically before writing any Rust: run `/app/graph_oracle nodes.csv edges.csv` against the small sample data in `/home/user/sample_data/` and feed it a range of hand-crafted queries — a bare node id, a single edge type, chains of 2-5 types, nonexistent nodes, edge types with no matches, empty input lines — recording exact stdout for each so you can pin down the query syntax, the traversal semantics (does it do BFS frontier expansion per edge-type step, does it dedupe nodes reached multiple ways, how are missing/empty results formatted), and the exact output line format including whitespace. Only once you have a hypothesis that matches every observed input/output pair should you implement it in Rust: parse both CSVs into adjacency structures keyed by (source, edge_type), process stdin line-by-line maintaining a "current node set" that gets replaced (not unioned) at each step of the query, sum weights of the final distinct node set, and print nothing extra to stdout. Build with `cargo build --release` so the binary lands at `/home/user/graph_tool/target/release/graph_tool`, then re-run your same hand-crafted queries against both binaries to confirm byte-identical output before trusting the fuzzer.