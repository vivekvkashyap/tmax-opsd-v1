First recover the model constants by OCR'ing `/app/model_params.png` (e.g. `tesseract /app/model_params.png -`), reading off the three labeled parameter values for alpha, beta, and n exactly as printed — sanity-check the OCR output for common misreads (0/O, 1/l) since a wrong constant will silently make every simulation output wrong. Then implement `/home/user/simulate_gene.py` to follow the spec literally rather than "close enough" math: compute GC-content as (count of 'G' + count of 'C') / length using the sequence as given (handle case consistently, e.g. uppercase it first since DNA letters could be either case), special-case an empty sequence to print `0.0000` and exit immediately, otherwise run exactly 100 Euler steps with `dt=0.1` using precisely the update rule given — `X = X + dt * (alpha * (1.0/(1.0+X**n)) - beta*X)` — applied in that exact order each iteration (don't reorder operations or use a different integrator, since the task demands bit-exact equivalence with a hidden reference across many random sequences). Format the final X to exactly 4 decimal places, printing nothing else (no debug prints, no trailing newline issues). Test your script manually on a handful of sequences of varying length and GC content, including edge cases like all-G, all-A, and very short sequences, to confirm formatting and stability before trusting it against the fuzz corpus.
