Fix the environment first, then work the forensics pipeline step by step, sanity-checking each intermediate result numerically. (1) Resolve the `requirements.txt` conflicts by relaxing pinned versions (use compatible recent-but-mutually-compatible ranges for `scapy`, `numpy`, `opencv-python`) and install into a fresh venv; verify each import works before moving on. (2) Locate the noise matrix in `/app/memory.dmp` by searching the raw bytes for the ASCII marker `NOISE_SEED_V1_8899` (e.g. read the file, find the byte offset of that substring, then read exactly 64*64*4=16384 bytes immediately following it and interpret with `numpy.frombuffer(..., dtype=np.float32).reshape(64,64)`). (3) Parse the pcap with `scapy` filtering UDP packets to port 1337, and for each payload unpack the first 4 bytes as a little-endian uint32 frame index (`struct.unpack('<I', ...)`) then the remaining 16384 bytes as the float32 corrupted crop array. (4) Read the corresponding frame from the mp4 with OpenCV, convert to grayscale, normalize to [0,1], and take the exact center 64x64 crop matching the same crop convention (verify crop dimensions/centering logic explicitly). (5) Invert the blend algebraically per frame: `Noise_est = (Corrupted - 0.5*Original)/0.5`, then average all per-frame estimates elementwise before saving with `numpy.save` to the required path — averaging across all recovered frames, not just one, is what gives the tight tolerance needed.
