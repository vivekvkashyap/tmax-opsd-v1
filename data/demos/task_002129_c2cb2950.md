Work through this as two separate passes over the parsed records rather than trying to do it in one loop. First, deserialize `/home/user/backups.jsonl` with `serde`/`serde_json` line-by-line into a struct with `id`, `parent_id`, `size_bytes`, `region`. Build a `HashMap<id, children_count>` by iterating all records and incrementing the count for each non-null `parent_id`'s entry (initialize every id to 0 first so backups with no children still appear). Second, group records by `region` into a `HashMap<region, Vec<&Record>>`, then for each region sort its vector by `(size_bytes descending, id ascending)` — this ordering is what determines the tie-break rule described in the task — and assign rank = 1-based position in that sorted order. Merge the two computed maps keyed by `id`, then sort the final rows alphabetically by `id` before writing. Use the `csv` crate or manual `write!` with a trailing header line `id,children_count,region_rank`, and run `cargo run` to generate `/home/user/critical_backups.csv`, then spot-check a couple of rows by hand against the source JSONL to confirm both metrics before considering it done.
