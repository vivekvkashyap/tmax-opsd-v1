Structure the script as one loop over `/home/user/quarantine/*` with clear priority: check malicious patterns first, then credit-card pattern, then fall through to clean. For malicious detection, grep each file for the literal strings `../`, `..%2f`, `%2e%2e%2f`, `%2e%2e/`, `..\` (use `grep -F` with `-e` per pattern, or a single `grep -F -e ... -e ...`, to avoid regex metacharacter surprises from `.` and `%`). On a match, look up the filename in `upload_log.csv` (`awk -F, -v f="$file" '$1==f {print $2}'`) to get its IP, append to a running set (accumulate into an array/temp file, dedupe+sort once at the end with `sort -u`), then delete the file. For non-malicious files, grep for `\b[0-9]{16}\b` with `grep -P` (Perl regex needed for `\b`); if it matches, encrypt with the exact `openssl enc` command given and delete the original; otherwise `mv` it to `clean_uploads/`. Build `malicious_ips.txt` and `block_rules.ipv4` only after processing all files (so the sort/uniq pass sees the complete set), and confirm at the end that `quarantine/` is empty and every IP appears in both output files in matching sorted order.
