Transcribe the audio first to get the actual investigative lead rather than guessing at the vulnerability: run a speech-to-text tool (e.g. `whisper /app/bisect_report.wav` or similar) on `/app/bisect_report.wav` and read the transcript carefully — it should point you at where to look and what pattern shape to expect, rather than telling you the exact detection logic to hardcode blindly. Then recover the actual payload from the crash artifact: since WAL-only data is uncommitted, don't rely on plain `sqlite3 pipeline.db "SELECT * FROM logs"` (which reads the committed db file and may miss it) — instead open the db+wal together with the sqlite3 CLI/library so the WAL is replayed (e.g. `sqlite3 /app/pipeline.db "PRAGMA wal_checkpoint(FULL); SELECT * FROM logs;"` copied into a scratch dir first, since checkpointing mutates the files) or inspect the WAL frames directly to extract the last uncommitted message. Use what you learn from the audio and the recovered message to determine the exact structural pattern (a fixed prefix followed by a fixed-length hex suffix), then write `detector.py` to scan the target file's contents for that pattern with a regex, printing REJECT/exit 1 on a match and ACCEPT/exit 0 otherwise. Validate against a few files in `/app/corpus/evil/` and `/app/corpus/clean/` by hand before trusting it broadly.