Do the aggregation in SQL, not in Go: use a window function like `AVG(amount) OVER (PARTITION BY sender_id ORDER BY timestamp ROWS BETWEEN 2 PRECEDING AND CURRENT ROW)` in your query against the sqlite3 database, then filter rows where `amount > rolling_avg` — computing this manually in Go row-by-row is error-prone and the task explicitly wants the window function pushed into the query. From the flagged transfers, build an adjacency list keyed by account id (join with `accounts` to get names), then run BFS from the "ShellCorp" node to "OffshoreVault" for the shortest-hop path — remember BFS naturally finds fewest hops, not DFS. Separately tally out-degree per node from the same flagged edge set, and when picking the max, break ties by alphabetical account name, not numeric id. Write results to the exact JSON key names given (`shortest_path`, `highest_out_degree`). Set up your Go module first (`go mod init`, `go get github.com/mattn/go-sqlite3`, CGO enabled) before writing logic, and test the SQL query standalone against the db with `sqlite3 financial_audit.db` before wiring it into Go.
