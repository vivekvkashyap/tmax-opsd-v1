This is a black-box behavioral-cloning task: you must reverse-engineer the oracle's exact semantics through empirical testing, not just from the prose spec, since fuzz equivalence testing will exercise edge cases. Start by running `/app/log_archiver_oracle` on small hand-crafted config/log pairs you construct yourself (vary `min_level`, `source_match`, `ignore_encoding_errors`, presence/absence of each key) and diff its stdout against your Python implementation's output byte-for-byte, including encoding. Key implementation details to get exactly right: parse the log with Python's `configparser` for the `.ini`; open the log file as raw bytes and decode with `encoding="shift_jis", errors="replace"` or `"strict"` depending on the flag; split records on a line that is exactly `---` (not just any `---` substring); match each record's header with a regex like `\[(.*?)\] (.*?): (.*)` on the first line; compare severity using the DEBUG<INFO<WARNING<ERROR<CRITICAL ordering; treat `source_match` as substring containment; and re-encode final output as UTF-8 to stdout, preserving whatever exact chunk boundaries (including trailing `---`) the oracle emits — mismatched chunk boundaries or off-by-one severity comparisons are the most common source of divergence, so test both inclusive/exclusive threshold behavior against the oracle directly.