Treat this as four independent, verifiable sub-tasks — do them in order and check each output before moving on. (1) Copy auth_service.cpp to auth_service_fixed.cpp, then replace the concatenated SQL string exactly as specified (parameterized placeholder + the exact comment on the next line), and swap the ECB cipher call for CBC, passing a 16-byte IV of all '0' characters into the init call alongside the key. (2) Decrypt db_config.enc with `openssl enc -d -aes-256-ecb -K <old-hex-key>` to recover the plaintext, inspect it to confirm it looks correct, then re-encrypt with `openssl enc -aes-256-cbc -K <new-hex-key> -iv <hex-iv>` writing raw binary (no `-a`/base64 flag) to db_config_rotated.enc — verify by decrypting your own output back with the new key/iv before trusting it. (3) Generate the keypair with `ssh-keygen -t ed25519 -N "" -f /home/user/.ssh/deploy_key`, then build the authorized_keys line by prepending the exact restriction string (with the required trailing space) directly in front of the public key content, not on a separate line. (4) Validate the cert chain with `openssl verify -CAfile root.crt -untrusted intermediate.crt server.crt` and write VALID or INVALID based on the actual command output, not an assumption.