Implement the C program by directly following the Pearson formula rather than reaching for a stats library: parse ratings.csv into an array of `(user_id, ratings[5])` structs, then for the target user compute its mean once, and for every *other* user compute that user's mean and the standard three sums — sum of `(x_i - x̄)(y_i - ȳ)`, sum of `(x_i - x̄)^2`, sum of `(y_i - ȳ)^2` — over the 5 items, then `r = num / sqrt(denx * deny)`, treating the case where either denominator is exactly 0 as `r = 0.0` to avoid dividing by zero/NaN. Track the best `(user_id, score)` pair using strict `>` comparison so that on ties the first-encountered (lowest, since CSV is presumably ordered) user_id wins — read users in file order and don't resort. Format the output with `printf` using `%.4f` for the score, matching `Match: [id], Score: [score]` exactly, including no extra spaces. Write the Makefile with a `default` (or first) target linking `-lm`, and a `clean` target removing the binary. The run_pipeline.sh script should `make clean`, `make`, then invoke `./recommender 1`, and must be `chmod +x`'d. Run it yourself and check top_match.txt's exact contents before finishing.