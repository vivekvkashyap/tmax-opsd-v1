Since `/app/validator` is interactive, probe it systematically rather than guessing: drive it with `expect` scripts or a pty-aware wrapper (plain pipes may not work since it prompts on a tty) feeding it a battery of crafted strings — vary prefix presence, number of `@` characters, alphanumeric vs special characters in the local part, and suffix variations — and record `[OK]`/`[REJECTED]` for each to reconstruct the exact validation rule set (prefix required, local-part character class, domain suffix, count of `@`). Cross-check edge cases (empty local part, uppercase in domain, multiple `@`, wrong suffix) since these are exactly what an adversarial corpus will target. Once you have the full rule set confirmed by direct experimentation (not assumption), implement it natively in Rust using `std::env::args()` for the file path argument, read and trim the single line, apply the rules with straightforward string/char checks (avoid needing a regex crate if possible to reduce dependencies, or use one if already available), and print exactly `VALID`/exit 0 or `INVALID`/exit 1. Compile with `rustc -O /home/user/classifier.rs -o /home/user/classifier` and test it against your own held-out valid/invalid strings before considering it done — it must never invoke `/app/validator` itself.