Start by running `/app/bin/audio_processor /app/reference.wav &` and noting its PID with `$!`. Since the SQLite file was unlinked but the fd stays open, find it via `ls -l /proc/<pid>/fd/` (look for a deleted regular file, usually named like `(deleted)` pointing at a `.db` or tmp path) and recover its bytes with `cp /proc/<pid>/fd/<N> /tmp/recovered.db`, then open it with `sqlite3 /tmp/recovered.db` and inspect the schema (`.tables`, `.schema`) and query the chunks table to see which row has an abnormal size value — that reveals the exact chunk ID and size that trigger the infinite loop. Cross-check by inspecting `/app/reference.wav` directly with `xxd`/`hexdump` to locate that same chunk ID and size in the raw file, confirming your understanding of the WAV chunk layout (4-byte ASCII ID + 4-byte little-endian size, RIFF walk). Then write `detector.c` that manually parses the RIFF header and walks chunks (ID + size, skipping `size` bytes to the next chunk, handling odd-byte padding) checking specifically for that chunk ID and its size value, exiting 1 if found and 0 otherwise — don't rely on any external tool. Validate exhaustively against both `/app/corpus/clean/` and `/app/corpus/evil/` before finishing.