First extract the audio content from /app/voicenote.wav using a speech-to-text tool (e.g. whisper, or any available transcription utility) rather than guessing standard biochemistry conventions — the voice note deliberately overrides the default hydrophobic/polar groupings and supplies a baseline-shift constant that you must recover from what is actually spoken, not assume. Transcribe carefully and re-listen/re-run if the transcript is garbled, since a wrong amino-acid grouping or wrong constant will silently fail every fuzz case. For the energy formula, implement the log-sum-exp term as `m = max(x,y,z); m + ln(exp(x-m)+exp(y-m)+exp(z-m))` so it never calls exp() on an unshifted large value — a naive `math.log(math.exp(x)+math.exp(y)+math.exp(z))` overflows for the stated input range. Parse sys.argv positionally (sequence, x, y, z), count residues per the transcribed categories (anything unmatched contributes to no bucket), sum weighted counts plus the stable LSE term plus the transcribed baseline shift, and print with `f"{energy:.4f}"` and nothing else. Test against edge cases like x=y=z and very large/negative floats before considering it done.