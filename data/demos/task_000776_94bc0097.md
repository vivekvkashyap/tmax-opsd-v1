Set up the environment first (`python3 -m venv /home/user/venv`, activate, `pip install sentence-transformers numpy scipy scikit-learn`), then write one script that does everything in a single deterministic pass so ordering is consistent. Load `metadata.csv`, sort dataset IDs numerically (parse the trailing integer from `dataset_N`, don't string-sort or you'll get 0,1,10,11...19,2,3... instead of 0..19) to fix embedding order. Encode all 20 descriptions with `SentenceTransformer('all-MiniLM-L6-v2')`. Compute the full pairwise cosine-similarity matrix (e.g. `sklearn.metrics.pairwise.cosine_similarity`) and the pairwise absolute `year_created` difference matrix using the same node ordering; extract each matrix's upper triangle excluding the diagonal with `numpy.triu_indices(n, k=1)` so both flattened arrays align element-for-element, then run `scipy.stats.pearsonr`. For the regression, fit `sklearn.linear_model.LinearRegression` with similarity as X and year-diff as y, then compute in-sample MSE. Separately, find the argmax cosine similarity between dataset_0's embedding and every other dataset's embedding (excluding itself) to answer the retrieval question. Write all three results to `/home/user/report.json` with the exact specified keys, rounding to at least 4 decimal places.