Build this in two clean layers so determinism is trivially preserved: a pure computational kernel and a shell orchestrator. In `bootstrap.cpp`, read `data.txt` into a `std::vector<double>` once, then read all 10,000 index lines into a 2D structure (e.g. `vector<vector<int>>`) before entering the parallel region — never do I/O inside the `#pragma omp parallel for` loop. Give each iteration its own private accumulator, write results into a pre-sized output array indexed by iteration number (not appended), so the OpenMP threads never race on a shared write location; only after the parallel loop finishes should you print the array sequentially in original line order — this preserves line order despite unordered thread execution. Compile exactly as specified with `-fopenmp` inside `run_analysis.sh`. For the CI extraction, redirect the executable's stdout to a file, then use `sort -n` on it and `sed -n '250p;9750p'` (or `awk 'NR==250{...} NR==9750{...}'`) to pull the two percentile lines — don't try to compute percentiles in C++. Format the final two values into `/home/user/ci.txt` matching the exact required label strings. Test end-to-end but do not invoke the script directly in your final answer.