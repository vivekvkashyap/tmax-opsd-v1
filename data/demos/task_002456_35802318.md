Work the three phases in order, verifying each before moving to the next. Phase 1: use `git log --all --diff-filter=D --summary` (or `git log --all --full-history -- secrets.json`) to find the commit that deleted `secrets.json`, then `git show <commit>^:secrets.json` (the parent revision, where the file still existed) to recover its contents, extract the 32-char hex key, and write exactly that string to `/home/user/key.txt`. Phase 2: since the script exits silently with no logs, trace its actual syscalls with `strace -f -e trace=open,openat python3 process.py 2>&1 | grep ENOENT` (or similar) to find the exact missing file path it tries to open, then create that file containing exactly `TOKEN_OK`. Phase 3: open `process.py`, find where it accumulates a running total with plain `float` addition, and replace that with `decimal.Decimal` arithmetic (convert each CSV value via `Decimal(str(val))`, sum them, then quantize to 2 decimal places) before formatting the `Total: <amount>` output line with exactly two decimals. Re-run the script after each phase to confirm progress rather than guessing all three fixes at once.