Work the three parts in order and test each independently before wiring them together. For Part 1: open `/app/vendored/modproxy/main.go`, find the hardcoded privileged port in the `ListenAndServe` call and change it to `:8080`; then edit the `Makefile` to export `GO111MODULE=on` before the `go build` invocation (either as an env line in the recipe or via `export` at the top) — a naive attempt often forgets the Makefile fix and only patches the Go source, leaving the build broken. Run `go build` yourself and curl `localhost:8080/health` to confirm a 200 before moving on. For Part 2: write a small Go program that reads each JSON file in a target directory, parses `requested_version`/`minimum_required_version` with proper semver comparison (not string comparison), recomputes SHA256 of `package_payload` and compares hex-encoded to `sha256_checksum`, and checks `graph_depth > 10`; emit `EVIL` if any check fails, else `CLEAN`, one line per file in `filename : LABEL` format, appended (not overwritten) to the log. Test it manually against both corpora before scripting Part 3. For Part 3: background the proxy binary with `&`, capture its PID, run the classifier against both corpus directories appending to the same log file, then `kill` the PID gracefully (SIGTERM) and wait for it to exit.
