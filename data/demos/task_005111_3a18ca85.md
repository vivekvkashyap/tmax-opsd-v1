Start with the integrity check: run `sha256sum -c /home/user/manifest.txt` (or `cd /home/user/app && sha256sum -c ../manifest.txt` depending on path format) from the directory containing the files, and identify which file reports FAILED — that's your tampered file. Open that file and trace precisely how untrusted input flows into output (e.g., a request parameter concatenated directly into an HTML/JS response without encoding, or into a shell/SQL command, etc.) — the specific data flow you find determines which CWE class it belongs to; look up the matching CWE ID rather than guessing. For the exploit payload, craft the minimal input string that would execute in that exact injection context and pop `alert('pwned')`, writing only the payload string (no surrounding text) to payload.txt. For the CSP fix, edit config.json's `Content-Security-Policy` value directly so it reads exactly `default-src 'self'` — remove any wildcard `*` or `'unsafe-inline'`/`'unsafe-eval'` tokens, keeping valid JSON. Finally write the audit report using the exact two-line format given, substituting the real filename and CWE number you discovered — verify by re-reading the report and comparing line-by-line against the required template.