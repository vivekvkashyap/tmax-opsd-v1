Fix the C library link error first: run `make` in `/home/user/mathlib/` to see the undefined-symbol error, then edit the `Makefile`'s compile/link command to append `-lm` after the source files (math functions like `sqrt`/`pow` require explicitly linking libm), rebuild `libmathops.so`, and sanity-check with `nm -D libmathops.so | grep compute_max_variation` plus a tiny ctypes smoke test before moving on. For video processing, use OpenCV (`cv2.VideoCapture`) to iterate frames, convert each to grayscale, and compute mean pixel brightness per frame into a Python list; pass that list to the C function via `ctypes` using `POINTER(c_double)` and correct `argtypes`/`restype` declarations, with `window_size=5`. For the DB migration, use `ALTER TABLE video_stats ADD COLUMN max_variation REAL` via `sqlite3`/Python's `sqlite3` module — check `PRAGMA table_info` first so re-running doesn't error if the column already exists. Build the FastAPI app so `GET /api/v1/process` performs the video read, ctypes call, and DB insert synchronously inside the handler (not precomputed), returning the JSON shape specified; run it with uvicorn bound to `127.0.0.1:8000` in the background (e.g. nohup) and confirm with `curl` that the endpoint responds correctly before finishing.