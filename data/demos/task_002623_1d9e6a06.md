First run `tesseract /app/task_req.png -` (or similar OCR) to recover the metric name and its parameter value — don't guess or hardcode them. Then open `/home/user/process.py`: the bug is a naive `for n1 in nodes: for n2 in nodes: G.add_edge(n1, n2)` cross-join that creates edges between every pair regardless of the actual data. Replace this by iterating each JSONL record and adding an edge only from `src` to each entry in its own `dst` list (`G.add_edge(data['src'], dst_node)` for `dst_node in data['dst']`), so only edges explicitly declared in the dataset exist. Use the OCR'd metric name to dispatch to the correct `networkx` function (e.g. `nx.pagerank`, `nx.betweenness_centrality`, etc.) passing the OCR'd parameter value correctly (e.g. `alpha=` for pagerank). Round each score to 4 decimal places when serializing. Finally, stand up a minimal HTTP server (stdlib `http.server` or Flask) bound to `127.0.0.1:8080` serving `GET /metric` with the JSON scores, and launch it detached/backgrounded (e.g. `nohup ... &` or a daemon thread) so it stays alive for the verifier to query after your shell session ends.