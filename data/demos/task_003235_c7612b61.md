Build the pieces in order and test incrementally. In `rate_filter.c`: read stdin line by line, parse `<timestamp> <ip> <message...>`; maintain your own simple hash table or linked list keyed by IP string mapping to a per-timestamp hit count (implement open addressing or chaining by hand — no external libs); when you see a new timestamp differing from the last one processed, clear/reset the structure (or free and reallocate) before processing further lines; for each line, look up/insert the IP, increment its count, and print (echo) the line only if the pre-increment count was `< MAX_REQ`, otherwise drop it — since timestamps arrive non-decreasing you only need to track the current timestamp's counts. Gate `MAX_REQ` behind `#ifndef MAX_REQ #define MAX_REQ 3 #endif` so `-DMAX_REQ=2` and `-DMAX_REQ=5` at compile time produce the strict/standard binaries per `build.sh` (use `gcc -O2 -DMAX_REQ=N`). For `prop_test.py`, generate the 10,000-line chronological synthetic log yourself, pipe it into the standard binary via `subprocess`, then group the input and output lines by (timestamp, ip) to check both the "at most 5 kept" and "exactly min(N,5) kept" invariants before writing PASS/FAIL to the log.