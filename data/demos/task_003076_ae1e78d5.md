Design the pipeline as one pure function (input CSV path, output dir) with no ordering-dependent state, since Part 2 explicitly tests byte-for-byte reproducibility — avoid anything that depends on dict/set iteration order, wall-clock time, or unsorted directory listings; always sort your final aggregation by category before writing. Validate each row against all four schema rules independently (id: exactly 8 alphanumeric chars via regex; category: exact case-sensitive membership in the three allowed values; price: parses as float and is >= 0.0; quantity: parses as int and is >= 1) and route any failure to append the id to `invalid_ids.txt`, never to aggregation — a common bug is only checking some rules or checking them in a way that lets a bad row slip into revenue totals. Sum `price*quantity` per category using a stable accumulation (not floating rounding at each row), then format each total to exactly 2 decimals at the end. For `test_reproducibility.sh`, actually clear and rerun the pipeline twice on the same input and compare `sha256sum` output rather than assuming determinism, then separately verify the invalid-row count on batch_B, and only write `REPRODUCIBILITY_PASS` if every check truly passed — run the script yourself and inspect `/home/user/test_result.log` before finishing.