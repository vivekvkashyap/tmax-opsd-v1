Parse `/home/user/doc_rules.ini` line by line (simple `Key=Value` splitting, e.g. `while IFS='=' read -r k v; do declare "$k=$v"; done < doc_rules.ini`) rather than hardcoding the paths, then `mkdir -p` each of the four directories. For each extensionless file in `Incoming`, identify its real archive type by inspecting magic bytes/header (e.g. `file <path>` or checking the first two bytes for gzip's `1f 8b`) rather than trusting any naming convention, then extract with `tar -xf` (which handles both plain and gzip tar automatically when given `-xf` without forcing `-z`) into `Processed`. For each `.md` file in `Processed`, use `sed 's/^# /HEADER: /'` to transform header lines, write to a matching `.txt` file, then delete the original `.md`. For the incremental backups, the key technique is GNU tar's `--listed-incremental=<snapshot-file>`: run `tar --listed-incremental=$Snapshot -cf backup_0.tar -C <parent> processed` for the level-0 backup, then after adding `changelog.txt`, run the exact same command again (same snapshot file) to produce `backup_1.tar` — tar automatically detects the snapshot state and only includes changed/new files. Verify with `tar -tf backup_1.tar` that only the new file appears, not the untouched `.txt` files.