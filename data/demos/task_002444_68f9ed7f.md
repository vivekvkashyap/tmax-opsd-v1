For Task 1, extract the exact-timestamp frame with `ffmpeg -y -ss 00:00:02 -i /app/training_source.mp4 -vframes 1 -pix_fmt gray /tmp/frame.png` (place `-ss` before `-i` for fast seek, but verify frame timing is correct — using `-vframes 1` after seeking to the target timestamp), then compute the mean of the pixel array with numpy/PIL (`np.mean(np.array(Image.open(...)))`) and write it rounded to exactly 2 decimals, no extra text, to `/home/user/frame_avg.txt`. For Task 2, remember float16's finite range is roughly ±65504 — write `/home/user/filter_corpus.py` (chmod +x, shebang line) that reads the CSV (handle multiple rows/columns, commas with optional whitespace), parses each value as a Python float, and exits 1 immediately if any value is `inf`, `-inf`, `nan`, or has `abs(value) > 65504`; otherwise exits 0. Don't just cast to `numpy.float16` and check for `np.isinf` after the cast — do the threshold check on the original float64 value directly, since values just above 65504 might round differently depending on cast behavior. Validate against `/app/corpus/clean/` and `/app/corpus/evil/` to confirm perfect accept/reject separation.