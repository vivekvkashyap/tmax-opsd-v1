Work through this as a strict pipeline and be careful about ordering/state that affects reproducibility. Load `/home/user/reaction_rate.csv`, then integrate `rate` over `time` with `scipy.integrate.cumulative_trapezoid(rate, time, initial=0)` (not a manual trapezoid loop, which is easy to get off-by-one on) to get cumulative product. Fit both models with `scipy.optimize.curve_fit` using exactly the specified initial guesses — different guesses can converge to different local optima. Compute SSE from residuals against each fit, then AIC with the exact formula given (`n*ln(SSE/n) + 2p`); pick the lower-AIC model and record its first parameter (L or Vmax) from the fit on the *original* (non-bootstrapped) data. For the bootstrap: call `numpy.random.seed(42)` exactly once, immediately before the loop (not inside it, not before other random calls) — reseeding location changes the entire resample sequence. In the loop, resample `(time, cumulative_product)` index pairs with replacement using `numpy.random.choice`, refit only the best model, wrap the refit in a try/except catching the failure-to-converge case, append only successful parameter estimates, and skip (don't replace) failed iterations. Finally compute the 2.5/97.5 percentiles of the collected bootstrap estimates with `numpy.percentile`, round all values as specified, and write the JSON with exactly the required keys to `/home/user/model_results.json`.