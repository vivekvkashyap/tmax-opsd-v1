Write `/home/user/process_logs.py` as a streaming line-by-line parser, not a bulk loader.
- Open the file and iterate with `for line in f:` (never `.read()`/`.readlines()`) so it scales to huge files.
- Per line, run three independent regex searches (not one combined pattern, since the surrounding text and ordering of fields varies per line): a timestamp matching `\d{4}-\d{2}-\d{2} \d{2}:\d{2}:\d{2}` (it may be inside brackets, after "Time:", etc. — search anywhere in the line, don't anchor), a strict IPv4 pattern, and an email pattern. Use `re.search` for each and only keep the line if all three matches succeed.
- Build the SQL line with an f-string/`.format()` exactly matching the required template, including the trailing semicolon and single-quoted values — get the literal spacing and column order right since the grader does an exact-content diff.
- Append each generated statement to a list/file, one per line, in the order lines are encountered.
- Actually execute the script once written so `/home/user/clean_inserts.sql` is generated, then spot-check a couple of lines against the source log by hand before finishing.
