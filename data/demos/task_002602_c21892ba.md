Implement this in Go using only `encoding/csv` and standard containers — no external graph libraries. (1) Parse `nodes.csv` into a map from node id to its `size_gb`, and parse `edges.csv` into an adjacency list of `source -> []{target, weight}`. (2) Implement Dijkstra's algorithm from scratch using a `container/heap` priority queue (or a simple O(V^2) scan since the graph is small) tracking shortest distance and a predecessor map from `s3_deep_archive`, terminating once `prod_main_db` is settled. (3) Reconstruct the path by walking predecessors backward from `prod_main_db` to the source, then reverse it to get forward order. (4) Sum `restore_time_min` along the path edges for Total Time, and sum `size_gb` for every *node* on that path (including both endpoints) for Cumulative Size. (5) Format the output file exactly as specified, with `->` separating path node ids, and run `go run /home/user/recovery.go` to confirm it compiles and produces `/home/user/recovery_plan.txt` with no third-party imports.
