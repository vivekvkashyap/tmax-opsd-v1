Build this as a small `http.server`-based Python service (or similar stdlib HTTP server) with a custom request handler for GET on `/audit`. First check the `Authorization` header exactly equals `Bearer sec-audit-2024`; if missing or wrong, send a 401 response body before doing any other work — don't invoke the binary at all in that path. On a valid request, use `subprocess.run(["/app/log_extractor"], capture_output=True)` to grab stdout, split it into lines, base64-decode each line, and `json.loads` the decoded bytes to get the event dict. Redact SSNs with a regex like `\d{3}-\d{2}-\d{4}` replacing matches with the literal string `[REDACTED_SSN]` — do this redaction pass on `event_data` BEFORE HTML-escaping it, since escaping first would corrupt characters the SSN regex needs to match cleanly is not an issue here but order still matters for correctness of the final string. For HTML-escaping, don't rely on `html.escape` blindly — check its default quote-escaping matches the exact substitution list required (`<`,`>`,`&`,`"`,`'`), and note `&` must be escaped first before other replacements to avoid double-escaping. Serialize the full list of processed events as a JSON array with `Content-Type: application/json`, then start the server and leave it running in the background.