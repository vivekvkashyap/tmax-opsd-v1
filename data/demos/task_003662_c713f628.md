Split this into three independent, verifiable pieces before wiring them together. (1) DTMF decoding: don't guess the digits — extract them programmatically, e.g. `multimon-ng -t wav -a DTMF /app/alert.wav` or an equivalent DTMF-decoding approach (goertzel/FFT tone-pair detection at the standard DTMF frequency table), and use the decoded digit string as `$DEPLOY_ID` at runtime rather than hardcoding any guessed value. (2) `verify_telemetry.sh`: parse with `jq` defensively (`jq -e` and check exit status / null fields) rather than naive grep, validate `device_id` against a strict allowlist regex (`^[A-Za-z0-9-]+$`, note underscores are NOT allowed per spec) and `temperature` as a number in `[-50,150]` inclusive using `jq`'s numeric comparison — reject on any parse failure, missing field, or out-of-pattern character (this is what blocks shell-injection payloads); exit 0 only when both checks pass, exit 1 otherwise. Test this script directly against the clean/evil examples you can find under `/app/corpus/` before wiring it into deploy. (3) `deploy.sh`: `ping -c 1 127.0.0.1` and check its exit code, `mkdir -p /home/user/deployments/$DEPLOY_ID`, loop over `/app/incoming/*.json` calling your validator per file and copying only passing files, then `ln -sfn` (force, no-dereference) the staging dir onto `/home/user/current_telemetry` for atomic rolling update.