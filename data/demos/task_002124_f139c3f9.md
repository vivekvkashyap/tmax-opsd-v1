Treat `/app/legacy_router` as a black box you must characterize empirically, not read a spec for. Run `strings /app/legacy_router` for clues, then actively fuzz it: invoke it with varying `id=` values (valid/invalid hex, different lengths) and varying `config=` JSON nesting depths, observing exit codes/output/crashes for each to infer the exact boundary conditions (e.g., at what id length or hex-validity it rejects, and at what JSON nesting depth it breaks) — use `strace` if behavior is ambiguous. Cross-check your inferred rules against `/app/corpus/clean/` (must all be safe under your rule) and `/app/corpus/evil/` (must all violate it) to sanity-check your derived constraints before finalizing them. Then write `/home/user/api_filter.py` using only `urllib.parse` and `json`: for each URL line, parse query params, `json.loads` the `config` value (recursively compute its max nesting depth), check the `id` value against the hex-format/length rule you derived, and print `ACCEPT: <url>` or `REJECT: <url>` accordingly. Make the script executable and run it against both corpus files to confirm 100% correct classification before considering it done.
