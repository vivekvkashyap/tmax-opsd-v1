Start by reading process.go's Normalize function carefully rather than assuming the bug's location — the description says output is all zeroes, so look for anywhere a float division might be getting truncated to integer arithmetic before the division happens (a common Go pitfall: casting operands to int before dividing loses the fraction). Fix it to do the subtraction and division entirely in float64: `(v - min) / (max - min)`, preserving the min==max special case that returns all zeros. Second, set up the Go module (`go mod init data-pipeline` inside /home/user/pipeline), add `github.com/stretchr/testify` as a dependency (`go get`), and write a table-driven test in process_test.go that calls Normalize with the known input CSV values and asserts each result against the hand-computed expected value using `assert.InDeltaSlice` with delta 1e-4 — compute the expected values yourself from the (value-min)/(max-min) formula rather than hardcoding numbers you haven't verified. Third, write run_pipeline.sh to cd into the pipeline dir, run `go mod tidy` (or `go get`) to fetch dependencies, run `go test`, and only if that exits 0 proceed to `go run process.go` to regenerate processed.json — chmod +x the script and run it yourself to confirm the JSON output and passing tests before finishing.
