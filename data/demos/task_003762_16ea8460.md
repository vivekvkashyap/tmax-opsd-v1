Do the four sub-tasks in dependency order and validate each before moving on. (1) Transcribe `/app/anomalies.wav` (e.g. with `whisper` or `SpeechRecognition`) and manually convert the spoken NATO-style phonetic letters + digits into machine IDs yourself (letter-word -> first letter, spoken digit-words -> digits) rather than assuming a fixed mapping — read back the transcript carefully since these IDs drive the rest of the pipeline. (2) For `/app/telemetry.csv`, don't assume uniform encoding: read the file in binary/line-by-line mode and try decoding each line as UTF-8 first, falling back to `cp1252`/`iso-8859-1` on a `UnicodeDecodeError`, then re-encode everything to UTF-8 — a blanket `.decode('utf-8')` over the whole file will crash. (3) Filter to only the machine IDs you transcribed, drop rows where `temperature` isn't parseable as float or falls outside [-50.0, 150.0], and dedupe by SHA-256 of the concatenated `timestamp+machine_id` string, keeping first occurrence (compute the hash yourself in code, don't skip this validation step). (4) Serve the cleaned, chronologically-sorted-per-machine records over an HTTP server on exactly 127.0.0.1:8080 at `GET /api/v1/anomalies?id=<machine_id>`, returning a JSON array with keys in the exact order/names specified. Test each endpoint yourself with curl for each recovered ID before finishing.
