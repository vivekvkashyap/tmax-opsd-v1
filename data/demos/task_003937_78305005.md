First recover the filtering rule from the image at `/app/rules.png` — since it's a rendered text label, use an OCR tool (e.g. `tesseract /app/rules.png stdout`) rather than guessing; the rule specifies which `Interaction_Type` values are valid. Materialize the filtered undirected graph by filtering `/home/user/network.tsv` on those valid types and emitting both `(A,B)` and `(B,A)` edge pairs into an edge-list file (e.g. with `awk`), then dedupe with `sort -u`. For `/home/user/query.sh`, implement BFS from the start node using this edge list: maintain a visited set and a frontier, and at each hop look up neighbors of the current frontier (a `join`/`awk` lookup against the edge list works well), expanding until the target is found or the frontier is empty. Handle the two edge cases explicitly: same start/end node prints `0`, and an exhausted frontier without finding the target prints `-1`. Keep the script printing only the integer (no extra debug text) since a fuzzer will diff its stdout against a hidden oracle across many random node pairs — test it yourself against hand-computed paths before considering it done, and make sure performance scales to ~500 nodes/1000 edges (avoid O(n^3) reprocessing of the whole edge list per hop if possible).