Start by OCR'ing `/app/db_schema_notes.png` with `tesseract` and reading the extracted text carefully to identify the exact deadlock-avoidance rule (which stages must precede a `$merge` into a specific target collection, and in what order/fields) — this rule, not intuition, defines what counts as "safe". Then design `analyze_pipelines.py` to load the input array of pipelines and, for each pipeline, scan its stages in order: if the pipeline contains a `$merge` stage targeting the collection named in the note, verify that a qualifying `$match` stage (on the field named in the note) and a qualifying `$sort` stage (on the field/direction named in the note) both appear earlier in the stage list than that `$merge`; if the pipeline doesn't merge into that collection at all, treat it as automatically safe. Reject (drop) any pipeline failing the ordering/field checks; keep everything else byte-for-byte identical to the input, since the verifier compares clean-corpus output structurally against the original. Before trusting it on the real graded corpora, hand-test your script against a couple of pipelines you construct yourself that deliberately violate ordering (checks after `$merge`) and missing-field cases, to confirm your stage-order logic — not just presence/absence — is correct.