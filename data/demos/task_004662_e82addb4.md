Treat `/app/audit_oracle` purely as a black-box you must characterize experimentally — do not guess its logic from the task description alone. Inspect it with `file`/`strings` for clues about expected input format, then feed it small hand-crafted HR/access-log/comm-graph fixtures on stdin (try JSON, CSV, and combined multi-section text) and observe its JSON output on stdout; use `strace`/`ltrace` to see what it reads. Run controlled experiments: hold most fields fixed and vary exactly one attribute at a time (an employee's department, one access log's timestamp or document tag, one graph edge) across runs, and diff the flagged-ID output to infer which conditions across the relational, document, and graph data cause an employee to be flagged — build up the full rule set (which department(s) matter, what access-log condition, what graph relationship) from these observations rather than assuming a fixed set of criteria. Once your independent pipeline reproduces the binary's output on your own varied test cases, implement it against the real data sources and wrap it in an HTTP service on `0.0.0.0:8080` at `POST /api/audit`, checking the `Authorization: Bearer COMPLIANCE_AUDIT_77x` header and reading data-source paths from the JSON request body (not hardcoded paths) so it generalizes to the verifier's new, unseen datasets. Start it in the background and write "SERVICE STARTED" to `/home/user/service_ready.log` once ready.