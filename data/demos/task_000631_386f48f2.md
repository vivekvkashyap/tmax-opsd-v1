Work through this chained forensics task step by step, verifying each intermediate value before using it in the next step. (1) For JWT validation: load the secret from `jwt_secret.txt`, then for each token in `captured_tokens.txt` use PyJWT's `jwt.decode(token, secret, algorithms=["HS256"])` inside a try/except catching `ExpiredSignatureError` and other `InvalidTokenError`s — do not manually parse the JWT parts. Among tokens that decode successfully (not expired), filter to the one whose payload `sub`/`user` claim equals `admin`, then read its `jti` claim. (2) For the zip PIN: since it's a 4-digit numeric PIN, brute-force all 10000 combinations from `0000` to `9999` — use a tool like `zip2john`+`john`, or a Python loop calling `zipfile.ZipFile.extractall(pwd=...)` (note: Python's builtin zipfile PIN check can be slow/unreliable for certain zip crypto; `unzip -P` in a shell loop is a robust fallback), catching the wrong-password exception and moving to the next candidate. (3) For the payload: the description tells you the encoding order (text→hex→base64), so reverse it as base64-decode first, then hex-decode the result to get plaintext. (4) Write all three recovered values into `/home/user/audit_report.json` with exactly the specified keys.