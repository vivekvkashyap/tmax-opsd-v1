Structure the Go program around `filepath.Walk` (or `filepath.WalkDir`) over `/home/user/etc_mock`, filtering to files with a `.conf` suffix whose `Info().ModTime()` is within `time.Now().Add(-24*time.Hour)` — get the comparison direction right (mtime after the cutoff, not before). For each matching file: strip the `/home/user/etc_mock/` prefix from its path, replace `/` with `_` to build the flattened name, then create a hard link with `os.Link(srcPath, filepath.Join(snapshotDir, flatName))` (not a copy — the verifier checks inode equality) into `/home/user/snapshots/<snapshot_name>` which you must `os.MkdirAll` first. Compute SHA256 with `crypto/sha256` + `io.Copy` into the hasher, and store lowercase hex (`hex.EncodeToString`) keyed by the *original full path* in a map, then marshal to JSON. For the atomic manifest write: `os.WriteFile` to a temp file in the same directory (e.g. `manifest.json.tmp`), then `os.Rename` it onto `manifest.json` — same-directory rename is what makes it atomic, don't write cross-filesystem. For the `current` symlink, don't call `os.Remove` then `os.Symlink` (that has a window with no symlink); instead create a new symlink at a temp path (e.g. `current.tmp`) pointing to the snapshot dir, then `os.Rename(tmpLink, "current")` to atomically swap it. Run with `go run /home/user/config_manager.go my_run` and confirm each verification point manually before finishing.