Implement this as a straightforward, careful C++ pass rather than trying to be clever — the whole task hinges on getting whitespace tokenization, ASCII summation, and the parity-based X/Y accumulation exactly right, since any off-by-one or wrong modulus check will silently produce a wrong anomaly. Read files in sorted filename order (use `std::filesystem` + `std::sort`, or glob and sort the paths) so file index/order is deterministic and matches the binary layout expected downstream. For each file: split on whitespace, and for every token compute `ascii_sum` as the sum of character codes, then update X by +/-length depending on `ascii_sum % 2`, and Y by +/-length depending on `ascii_sum % 3` — implement this as a small pure function you can unit-test on one or two toy strings by hand before running it over all 100 files. Write the 100 (X,Y) `int32_t` pairs to `/home/user/archive.bin` with `ofstream` in binary mode, native byte order, contiguous (no padding) — verify the file is exactly 800 bytes afterward. For the anomaly step, compute the floating-point centroid of all points, then Euclidean distance per file, and pick the max (tie-break to the alphabetically-first file) — write only that filename (no newline issues) to `/home/user/anomaly.txt`. Compile with plain `g++` and run once end-to-end, then sanity-check the two output files' sizes/contents.
