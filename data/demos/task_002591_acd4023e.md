First recover the two hidden parameters from `/app/instructions.wav` before writing any SQL — install `ffmpeg`/`openai-whisper` (or another local STT tool) to transcribe the audio, and read the transcript carefully for the exact excluded tag name and the exact page size number; don't guess these values. Then inspect `/app/research_data.db`'s schema and a few rows with `sqlite3` to confirm column names/types match the description. Implement the traversal as a single recursive CTE in SQLite: base case selects `start_id` only if its tag isn't the excluded tag; recursive case joins `lineage` from the current frontier's `source_id` to `target_id`, joining `datasets` to filter out excluded-tag rows, and stops recursing once `depth` reaches `max_depth` (depth 0 = start node only). Be careful that exclusion applies both to whether a node appears in results AND whether traversal continues through it — an excluded node should not be used as a bridge to reach further descendants. After getting `DISTINCT id` sorted ascending, apply pagination as a simple offset/limit slice using `(page_number-1)*page_size`. Test your script against a handful of manually chosen `start_id`/`max_depth` values, tracing the expected reachable set by hand from the DB (e.g. with a few manual `sqlite3` queries) to confirm correctness before trusting it broadly.
