First unblock the install: inspect `/app/walparser-0.5.1/setup.py` for a guard that raises unless a specific environment variable is set to a specific value (search for `os.environ` checks) — either export that variable before running `pip install .` / `pip install -e .`, or edit `setup.py` to remove the check, then confirm `import walparser` works and `walparser.parse_stream` is callable. For the bulk rename step, don't decompress whole files to disk: open each `.gz` with Python's `gzip.open(path, 'rb')`, `read(8)` to get just the first 8 bytes of the decompressed stream, and unpack them as a little-endian 64-bit unsigned int with `struct.unpack('<Q', chunk)` to get the Unix timestamp, then `os.rename`/`shutil.move` the *original compressed* file to `state_<timestamp>.wal.gz`. For parsing, iterate the renamed files, open each with `gzip.open(..., 'rb')`, pass the file object to `walparser.parse_stream`, and collect the `key` field from every returned dict into a sorted, de-duplicated list. For checksums, hash the compressed `.gz` bytes (not the decompressed content) with `hashlib.sha256`. Assemble `{new_filename: {"checksum": ..., "keys": [...]}}` and write it as `/home/user/manifest.json`; spot-check a few entries manually before considering the task done since the grader scores F1 against a hidden reference.
