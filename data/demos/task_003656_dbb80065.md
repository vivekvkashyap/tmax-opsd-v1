Run OCR on `/app/diagram.png` (e.g. `tesseract diagram.png -`) to recover the list of `Source -> Destination` edge lines — clean up any OCR noise (stray spaces, misread arrows) before trusting each line. For each recovered edge, add a corresponding `add_edge("Source", "Destination")` call inside `initialize_graph()` in `graph_config.cpp`, matching the exact node name strings you extracted. For the Makefile, the classic cause of a threads/static-lib link failure on modern toolchains is placing `-lpthread` (or `-lstatic-lib`) before the object files on the g++/gcc command line — linkers resolve symbols left-to-right, so library flags must come AFTER the `.o` files that need them; reorder the link rule accordingly. Run `make clean && make` to confirm `/app/validator/api_validator` builds cleanly. Finally check `main.cpp`/your changes emit only `VALID`/`INVALID` with no extra prints, and manually test a few multi-hop sequences (including ones with edges you didn't add) before trusting the fuzz-equivalence check.
