Read `/home/user/server_logs.csv`, filter rows to `server_id == 'SRV-001'` only, add a `latency_sec = latency_ms / 1000` column, and load the result into a SQLite DB at `/home/user/metrics.db` in a table `server_metrics(timestamp TEXT, latency_sec REAL, status TEXT)` using Python's `sqlite3` (or pandas `to_sql`) — verify row/column counts against the source filter before moving on. For the Bayesian step, write `/home/user/infer.py` to query counts of `status='FAIL'` and `status='OK'` for SRV-001 from the DB (not recomputed from the raw CSV, to keep the pipeline honest), treating FAIL as the Beta-Binomial "success" count and OK as "failure" count. Apply the conjugate update: posterior_alpha = prior_alpha(2) + fail_count, posterior_beta = prior_beta(10) + ok_count, and posterior_mean = posterior_alpha / (posterior_alpha + posterior_beta) — this closed-form update is exact, no sampling/MCMC needed. Round the mean to exactly 4 decimal places and write `{"posterior_mean": <value>}` to `/home/user/result.json`. Double check you're counting only SRV-001 rows from the DB table, since a stray join or missing filter is the most common source of an off value here.