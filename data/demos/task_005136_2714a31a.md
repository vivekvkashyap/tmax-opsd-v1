The broken script's bug is that it nests two independent loops over the full `datasets.csv` for every relationship row instead of looking up the specific source and target names for each relationship's `source_id`/`target_id` — rewrite it as a real join: for each relationship row, look up `name` where `id == source_id` and where `id == target_id` (e.g. build an associative array/map from id->name first, or use `awk -F, 'NR==FNR{...}'` two-file join, or just do it in a short Python/awk one-liner instead of shell nested loops) and emit exactly one `Source|Target|Transformation` line per relationship, skipping headers. Redirect the corrected script's output to `/home/user/edges.txt`. For the shortest lineage path, treat the edges as a directed graph and run BFS from `Raw_Climate_Data` to `Global_Warming_Model` (BFS guarantees fewest transformations/hops, unlike DFS or picking the first path found) — write the resulting node sequence, one name per line, to `/home/user/path.txt`. Double check the join produces exactly one line per relationships.csv row (not a cross product) before moving to the graph step.