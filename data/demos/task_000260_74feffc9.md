Build this as four separable pieces, testing each before wiring them together. Compile the proto with `python -m grpc_tools.protoc -I. --python_out=/home/user --grpc_python_out=/home/user deps.proto` to get the generated stubs. For the server's dependency resolution, avoid `ldd` entirely: run `readelf -d <binary>` and parse lines containing `(NEEDED)` to extract each `Shared library:` filename, then resolve each name to an absolute path by checking the four given directories in the specified order, skipping unresolved ones — do a BFS/DFS over these NEEDED edges (tracking a visited-set keyed by resolved path) so already-seen libraries aren't reprocessed and cycles can't cause infinite recursion. Structure this traversal as a pure function taking a resolver callback, so `test_server.py` can inject a Hypothesis-generated mock graph (dict of node -> list of children, including cycles) instead of calling readelf, and assert every reachable node is visited exactly once and the algorithm terminates. Finally, have `client.py` invoke the RPC on `/usr/bin/sqlite3`, sort the resulting paths plus the binary itself, write `bundle_list.txt`, and build the tar with `tar --absolute-names -cf minimal_sqlite3.tar -T bundle_list.txt` (or equivalent) so the archive preserves absolute paths.
