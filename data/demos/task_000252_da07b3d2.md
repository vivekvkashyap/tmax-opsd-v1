Implement this pipeline as discrete, independently-checkable steps in Go using only the standard library. First, load `vocab.json` with `encoding/json` into a `map[string][5]float64` and the projection matrix with `encoding/csv` into a `[5][2]float64`, printing them out once to sanity-check parsing. Second, for each `.txt` file (use `os.ReadDir` on `/home/user/etl_data/`), lowercase the content and split on whitespace with `strings.Fields`, look up each token in the vocab map (skip misses), and accumulate then divide by the count of known words to get the 5-d document embedding — handle the zero-known-words case explicitly to avoid divide-by-zero. Third, apply the matrix multiply exactly as specified (`Reduced[j] = sum_i Document[i]*Matrix[i][j]`) — write it out longhand with two nested loops rather than trying to be clever, since transposing i/j is the most common mistake here. Fourth, compute Euclidean distance to the fixed query vector `[0.85, -0.15]` for every document, sort by (distance, filename) for deterministic tie-breaking, and take the first three. Write only the filenames, one per line, to `/home/user/top_docs.txt`. Compile and run (`go run pipeline.go` or build then execute) and inspect the output file before considering it done.
