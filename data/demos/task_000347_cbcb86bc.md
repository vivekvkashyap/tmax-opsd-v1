The core difficulty is correctly parsing RFC4180-style CSV where `translation_text` fields contain literal embedded newlines inside quotes — use the Rust `csv` crate's reader (which handles quoted multi-line fields automatically) rather than splitting the file by `\n` yourself, which would break records apart. Parse each row's `timestamp` with a date/time library (e.g. `chrono`) to derive the `YYYY-MM-DD-HH` bucket key. Group records by `(bucket, locale)`, sort each group by original timestamp ascending, and keep only the first 2 per group — do this with a stable, deterministic sort/grouping (e.g. a `BTreeMap` or explicit sort by keys) rather than relying on HashMap iteration order. Mask `translator_id` to the literal string `"MASKED"`. In `translation_text`, replace real embedded newlines/CRLF with the two-character literal sequence `\n` (backslash-n) so each output row is single-line — do this replacement after extracting the field, not before parsing. Write output rows sorted by bucket, then locale, then original timestamp, with the exact header line. Wire compilation (`cargo build --release` or similar) and execution into `run_pipeline.sh`, `chmod +x` it, and run it to produce `processed_samples.csv`.
