The core fix is ordering the reduction deterministically despite parallel completion, not making the sum itself parallel-safe. Split the input file with `split -l 1000 input.txt chunk_` so chunk names sort alphabetically in the same order as the data. Launch one background `awk` job per chunk (`awk '...' chunk_XX > partial_XX &`), then `wait` for all jobs to finish — but crucially, do the final addition by iterating over the chunk names in sorted order (`for f in $(ls chunk_* | sort)`) and accumulating their partial sums sequentially with a single `awk` (or a running shell arithmetic pass) in that fixed order, regardless of which background job actually completed first. This guarantees the same floating-point rounding every run. Print only the final `%.15f`-formatted sum, then clean up all `chunk_*`/partial temp files, and `chmod +x` the script. For the convergence test, call your script on `head -n N /home/user/samples.txt` for each N in {1000..5000}, appending `"<N> <result>"` lines to `/home/user/convergence.log`, and diff your log against a fresh from-scratch run to confirm reproducibility before finishing.