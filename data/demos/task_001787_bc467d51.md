Before writing the ETL logic, get the vendored `polib` importable: try importing it and reproduce the `TypeError` first, then open `/app/polib-1.2.0/polib.py`, locate the class handling `msgstr` (its constructor/default value and any place it calls `len()` or does string ops on `msgstr` assuming it's always a string), and fix the type mismatch (e.g. default it to an empty string instead of `None`, or otherwise make it behave as documented) so parsing and truthiness/length checks succeed — then `pip install -e /app/polib-1.2.0` (or add it to `sys.path`) so your script actually uses the fixed vendored copy, not any other polib that might be installed. For extraction, regex each line of `ui_strings.txt` for the quoted string following `UI_EVENT:` (e.g. `UI_EVENT: "(.*?)"`). Load the `.po` file with the fixed `polib.pofile()`, build a `{msgid: msgstr}` lookup, try an exact key match first, and only fall back to `difflib.get_close_matches(s, msgids, n=1, cutoff=0.85)` when no exact match exists, mapping to `None` if that also comes up empty. Write the resulting dict to `translated_ui.json` keyed by the original extracted (untranslated) string.
