Install `pandas` and `pandera` first (`pip install pandas pandera`). In `/home/user/pipeline_test.py`, load `/home/user/input.csv` with pandas and record its row count as `original_rows` before any filtering. Apply the domain rules as boolean masks — keep rows where `value >= 0.0` AND `category` is in `{'X','Y','Z'}` — and record the resulting row count as `valid_rows`. Only after this manual cleaning step, define a `pandera.DataFrameSchema` with `Column(int)` for `id`, `Column(float, Check.ge(0.0))` for `value`, and `Column(str, Check.isin(['X','Y','Z']))` for `category`, then call `schema.validate(df)` — wrap this in a try/except so `schema_passed` is set to `true` only if validation succeeds without raising, and let it genuinely re-validate the cleaned data (don't just hardcode true). Write the validated dataframe to `/home/user/output.csv` with `index=False`, preserving column order `id,value,category`. Finally dump `{"original_rows": ..., "valid_rows": ..., "schema_passed": ...}` to `/home/user/metrics.json` with exactly those three keys. Run the script and confirm both output files were created before finishing.
