Start by extracting the spec from the image with `tesseract /app/model_spec.png stdout` (try `--psm` variants if the first pass garbles text) and read off the ODE definitions, the two rate constants, initial conditions, the required integration method, and the step size — transcribe these exactly as OCR'd rather than assuming typical textbook values. Scaffold the Rust project with `cargo new`, and implement the specified method by hand (no ODE crates): for a fixed-step method applied N times from t=0, keep the loop structure straightforward — a stepping function that takes current state and dt and returns next state, called N times per input line. Read stdin line-by-line, parse each as an integer N, reset to the initial conditions for every line (simulations are independent, not cumulative across lines), run N steps, and print `y,z` each formatted to exactly 6 decimal places via `format!("{:.6}", ...)`. Build with `cargo build --release` and manually smoke-test by piping a few small integers (e.g. 0, 1, 10) into the release binary to sanity-check output shape before considering it done — the grading compares your binary's output against an independent reference across many random inputs, so numerical precision and step logic must match the spec exactly.