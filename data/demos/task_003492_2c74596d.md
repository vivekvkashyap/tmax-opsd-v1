Implement tokenization as a strict character-level transform: lowercase everything, strip any character that isn't `a-z`, `0-9`, or space (delete punctuation entirely, don't replace with space), then split on single spaces and drop empty tokens — get this exact order right since it determines every downstream vector. Build each document/query as a term-frequency map (e.g. `std::unordered_map<std::string,int>`), then for cosine similarity compute dot product over the intersection of terms and divide by the product of L2 norms; when scanning for the best-matching document per query, use a strict `>` comparison so ties keep the earliest (lowest-index) document, matching the required tie-break rule. Accumulate the vector of top-1 scores across all queries, then compute sample mean and *sample* standard deviation (divide sum of squared deviations by N-1, not N) and the 95% CI as mean ± 1.96*s/sqrt(N). Write `summary.csv` with a header row, one `QueryIndex,TopCorpusIndex,CosineSimilarity` line per query in query order, then a `Mean,CI_Lower,CI_Upper` header and one values row — format every float to exactly 4 decimal places with `printf("%.4f", ...)`. Compile with the exact specified `g++ -std=c++17 -O3` command and actually run the binary so `/home/user/summary.csv` is produced, then spot-check a couple of the printed matches by hand against the corpus text to catch tokenization bugs early.