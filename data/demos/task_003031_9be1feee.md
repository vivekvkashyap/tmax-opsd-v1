Write a FASTA parser that reads `/home/user/sequences.fasta` and groups each multi-line sequence block (lines after a `>` header, until the next `>` or EOF) into a single string per record — don't just process line-by-line, since sequences wrap at 80 chars. Collect the list of full sequences, then use `multiprocessing.Pool(4)` to map a worker function over that list that returns `(len(seq), (seq.count('G')+seq.count('C'))/len(seq))` for each sequence — this is the required parallelization, not optional. With the resulting arrays of lengths (x) and GC ratios (y), fit `y = slope*x + intercept` via `scipy.stats.linregress` (or equivalent least-squares) — do not hardcode or guess the slope/intercept, they must come from actually running the regression on the parsed data. Format both values with exactly 6 decimal places and write `slope: <value>\nintercept: <value>` to `/home/user/regression_results.txt`, matching the required key names and precision exactly. Run the script and eyeball that lengths/GC ratios look plausible (100–1000 length range, ratios near 0.4-ish) before finishing.
