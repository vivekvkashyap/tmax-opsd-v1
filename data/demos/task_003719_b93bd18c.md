Read `custom_crypto.h` carefully to understand the exact token-generation algorithm (hash function and how username/password are combined, including any delimiters or salts) — do not guess the format. Write a small C++ brute-forcer that includes the header, reads each candidate password from the wordlist, calls the header's own token-generation function with username `admin` and that candidate, and compares the result byte-for-byte against the contents of `admin_token.txt`. Stop at the first match — that is the recovered shared secret. Then call the same generation function with username `system_root` and the recovered password to produce the forged token, and write only that exact string (no extra whitespace or newline artifacts) to `forged_token.txt`. Key pitfalls: reimplementing the hash/token logic by hand instead of calling the header's real functions (easy to introduce subtle mismatches), and trimming/adding newlines inconsistently when comparing or writing files — read and write files raw and strip only for comparison.
