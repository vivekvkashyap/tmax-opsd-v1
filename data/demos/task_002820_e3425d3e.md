Since you lack root to install packages, first fetch the SQLite amalgamation (sqlite3.c/sqlite3.h) and a single-header JSON library into /home/user/ so you can compile everything together with `gcc etl.c sqlite3.c -o etl_bin -ldl -lpthread`. Build the query in layers rather than one giant join: (1) SELECT customers WHERE region='Europe' ORDER BY id ASC LIMIT 2 OFFSET 2 — get pagination exactly right, page size 2 and page 2 means skipping the first 2 matching rows, not the first 2 rows of the whole table; (2) for each returned customer, SELECT orders WHERE customer_id = ? ordered consistently; (3) for each order, SELECT product_name FROM order_items WHERE order_id = ?. Use prepared statements (sqlite3_prepare_v2/sqlite3_bind_int) in nested loops to avoid SQL injection and to correctly scope each subquery to its parent row. Assemble the nested JSON structure exactly matching the given schema (customer_id, name, orders[] each with order_id, total, items[]), serialize with your JSON library, and write to /home/user/output.json. Run the compiled binary and inspect the output file with `python3 -m json.tool` to confirm it's valid and structurally correct before finishing.
