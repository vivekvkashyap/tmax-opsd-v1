Build the ETL script step by step and verify each stage independently before moving on. Load the CSV with pandas using `pd.to_datetime(..., utc=True, format='mixed')` (or per-row parsing) so mixed ISO/offset timestamp formats all normalize to UTC — a naive single-format parser will fail on rows that differ in style. Set the timestamp as the index, then use `df.resample('5min', origin=<start>, closed='left', label='left')` restricted to the window 2023-10-01 10:00 through 11:00 UTC, taking the mean per bin — build the full 13-bin index explicitly (`pd.date_range` with freq='5min') so empty bins appear as NaN rather than being silently dropped. Apply `.interpolate(method='linear', limit_direction='both')` per column so both interior gaps and any trailing/leading NaNs are filled consistently. Write to SQLite via `sqlite3` (or `df.to_sql`) with timestamp formatted as a plain string 'YYYY-MM-DD HH:MM:SS' (not ISO with 'T'), and float columns as REAL. For the report, compute max/min from the same cleaned dataframe you wrote to the DB (not raw data), round to 1 decimal, and render through a Jinja2 Template matching the exact literal report structure given in the task — check whitespace/line exactness against the spec before writing the file.
