Implement the row-cleaning logic purely with `awk` (or `sed`/`grep`), field-by-field, rather than reaching for Python. Process line-by-line: use `awk -F,` to count fields and drop any data row where `NF != 5` (this must run before other checks, since malformed rows can otherwise crash column-index logic); for the temperature field (`$3`), if it's empty substitute the literal `25.0`; for the humidity field (`$4`), drop the row if the numeric value is `< 0.0` or `> 100.0` using `awk`'s native numeric comparison (careful with string vs. numeric comparison pitfalls — quoting `$4` for comparison against `0` should coerce correctly in awk, but verify with a boundary test like exactly `0.0` and exactly `100.0`, which must be KEPT since the rule is strictly-less/strictly-greater). Preserve the header line unchanged and keep row order. Write the result to `/home/user/cleaned_sensor_data.csv`, then have the script invoke `python3 /home/user/tune.py /home/user/cleaned_sensor_data.csv` as the final step — don't reimplement tune.py's logic, just call it. Test your awk one-liner against a few hand-crafted rows (missing field, out-of-range humidity, missing temperature) before running it on the full file.