Use `git bisect` properly rather than manually scanning history. Start with `git bisect start`, mark current `HEAD` bad and `v1.0` good (`git bisect bad HEAD; git bisect good v1.0`), then at each step run the actual assertion check (e.g. `git bisect run python3 check_uptime.py` if the script's own `assert` failing gives a nonzero exit code — verify that first, or wrap it in a small runner script that exits nonzero on `AssertionError`). Let bisect narrow to the exact commit, capture its full hash with `git rev-parse HEAD` at the point bisect reports as first-bad, then `git bisect reset` and write that 40-char hash to `/home/user/bad_commit.txt`. For the fix, don't just revert the commit — understand the root cause: aggregating many tiny floating point deltas with plain `sum()` loses precision due to non-associative floating point addition; replace it with `math.fsum()` (exact/compensated summation) wherever the regression accumulates the delay/downtime values, without touching the assertion. Rerun the fixed script against `heartbeats.pcap`, and write its printed uptime, rounded to exactly 5 decimal places, to `/home/user/final_uptime.txt`.