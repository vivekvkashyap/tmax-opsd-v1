Two independent problems here: fix a broken vendored package, then write a graph-cycle/reachability validator on top of it. For the package, diff/inspect `/app/vendor/rdflib/rdflib/__init__.py` and `setup.py` for anything referencing a module or dependency name that doesn't exist (an obviously fake import or requirement injected as a perturbation) — remove that broken line/requirement, then `pip install /app/vendor/rdflib` and confirm `import rdflib; rdflib.Graph()` works before moving on. For the validator, load the .ttl with rdflib's parser, then for every node typed `db:Backup` with `db:backupType "Differential"`, walk the `db:dependsOn` chain (following each dependency link) while tracking visited node IDs in a set per traversal path — if you revisit a node already on the current path, that's a cycle -> INVALID; if you reach a node with no outgoing `db:dependsOn` and its backupType isn't "Full", that's a dead end -> INVALID. Only if every Differential's chain terminates cleanly at a Full backup is the manifest VALID. Print exactly `VALID` or `INVALID` with nothing else (no debug prints), and test against small hand-built manifests covering a clean chain, a cycle, and an orphaned differential before trusting the corpus run.
