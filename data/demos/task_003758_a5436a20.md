Work from the crash oracle, not guesswork. First recombine the split archive (`cat corpora.tar.gz.part* > corpora.tar.gz && tar xzf corpora.tar.gz`) to get `clean/` and `evil/` samples. Use `xxd`/`hexdump -C` on both sets to diff their byte layout: look for a fixed-length magic byte sequence at the start of clean files that's missing or altered in evil ones, then a length field immediately after — check whether that length field's declared value actually matches the remaining file size (a mismatch is a classic out-of-bounds trigger for a segfault, so never trust it blindly when reimplementing). For the rest of the payload, don't assume ASCII: iterate byte-by-byte with a proper UTF-8 decoder (checking continuation-byte counts and rejecting overlong/surrogate encodings) rather than just calling a locale-dependent library function, since evil files may contain subtly invalid multi-byte sequences (e.g. Windows-1252/Shift-JIS bytes that only fail strict validation). Write `/home/user/filter.c` to: read the file as raw bytes, verify the magic, verify the length field against actual size before trusting it, validate the remainder as strict UTF-8, and exit 0/1 accordingly — never loop or trust attacker-controlled length values for allocation/reads. Test against both provided corpora before assuming correctness, since the hidden validation set follows identical rules.
