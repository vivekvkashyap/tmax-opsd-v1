This task has three independent parts — tackle them separately and verify each in isolation.

For the video: use `ffmpeg -i /app/boot_diagnostic.mp4 -vf fps=<high-rate> frame_%04d.png` (or similar) to dump frames, then inspect frames around any red-flash moment (scan filenames/timestamps for a frame where the average color is dominated by red, e.g. with ImageMagick `convert frame.png -format "%[fx:mean.r],%[fx:mean.g],%[fx:mean.b]" info:` or just eyeball a handful of candidate frames) to read off the overlaid 8-character hex seed text; write exactly that 8-character string (no prefix/suffix) to `/home/user/diagnostic_seed.txt`.

For storage monitoring: write `monitor_storage.sh` using `du -sm` to get directory size in MB, and if over 50MB, delete oldest files first (sort by mtime via `ls -t` reversed or `find -printf` with `stat`) in a loop until under 40MB, then append the alert line with `$(date +%s)` only if a deletion actually happened. Install it via `crontab -l | { cat; echo "*/5 * * * * /home/user/monitor_storage.sh"; } | crontab -`.

For the ACL compiler: do NOT try to disassemble the legacy binary. Instead, treat it as a black box — feed it varied stdin payloads and diff outputs to infer the transform. Given the hint that it XORs input bytes against the seed you extracted (repeating the seed as a keystream) and hex-encodes the result inside a fixed `Match User iot_admin` / `AuthorizedKeysFile` template, write and compile a Go program in `/home/user/acl_compiler.go` implementing exactly that XOR-then-hex-encode-then-template logic, then confirm your binary's output byte-for-byte matches the legacy tool's output across several test payloads including edge cases (empty input, input longer than the seed, non-printable bytes) before considering it done.