Focus only on `generate_proposal`: the correct multivariate-normal proposal step is x_new = x + L*z, where L is the lower-triangular Cholesky factor of the covariance matrix (not the covariance matrix itself). Use Eigen's `LLT<Matrix3d>` decomposition on `covariance`, extract the lower-triangular factor via `.matrixL()`, and multiply it by the standard-normal vector z, replacing the buggy `covariance * z` line — leave every other part of the file (seed, iteration counts, target distribution) untouched. Before that, fetch and extract Eigen 3.4.0 so headers resolve under `/home/user/eigen-3.4.0` (verify with a quick `#include <Eigen/Dense>` test compile), then build with the exact g++ command given. After running the binary, sanity-check `/home/user/posterior_mean.csv` has exactly three comma-separated floats; if the sampler still looks biased or over-rejects, re-check that `.matrixL()` is applied correctly (it returns a triangular view — make sure you're multiplying it against the vector, not the full matrix, and that dimensions line up).