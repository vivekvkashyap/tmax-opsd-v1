Read `src/lib.rs` and the failing test carefully rather than guessing — the description already tells you the two bug classes to look for: an f32-precision accumulator causing drift, and an off-by-one loop bound. Fix the loop to iterate over exactly `steps` sub-intervals for a proper left Riemann sum (i.e. `0..steps`, not an inclusive range that adds one extra term), and switch any f32 casts in the accumulation path to pure f64 arithmetic so precision isn't lost across a million iterations. Run `cargo test` in `/home/user/num_calc` to confirm the fix passes before moving on. Then extract the corrected `left_riemann_sum` function verbatim into a standalone `/home/user/mre.rs` with its own `main()` that calls it with the exact arguments specified (0.0, 100.0, 1000) and prints the result to stdout — compile and run it directly with `rustc /home/user/mre.rs && ./mre` to confirm it builds and executes cleanly, matching the same corrected logic used in the library.
