First OCR `/app/formula.png` (e.g. `tesseract /app/formula.png -`) to recover the exact bucket formula relating SIZE, MASK, and ID, then cross-check by running `/app/oracle` on a few hand-picked values and confirming your parsed formula reproduces its output before hardcoding it into classifier.c. Fix the provided classifier.c: remove/free the leaked malloc, cast argv values to long long via atoll to avoid overflow, and compute the result using the recovered formula, routing the final modulo through the given `safe_mod` from helper.cpp (adapt for int64 semantics as needed) so linkage is exercised — get sign/truncation behavior of the modulo to exactly match the oracle. Write a Makefile that compiles helper.cpp to an object file, compiles classifier.c, and links them into `/home/user/organizer/classifier`. Validate with valgrind/ASan and by diffing your output against `/app/oracle` across many random positive integer inputs before finishing.
