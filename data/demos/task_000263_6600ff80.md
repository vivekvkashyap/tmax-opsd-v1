Sequence the four requirements and test each before moving to the next. Expect script: run `/home/user/iot_provision.sh` manually first to see its exact prompt text and whether the PIN input is echoed or hidden (`read -s`), then write `provision.exp` with `spawn`, matching `expect "Device ID"`/`send "edge-node-99\r"`, `expect "PIN"`/`send "4921\r"`, and `expect eof`; confirm `/home/user/device.conf` is actually created after running it. C++ server: implement a straightforward blocking TCP accept loop on 127.0.0.1:8080 with `socket`/`bind`/`listen`/`accept`, `recv` up to 1024 bytes, append the payload plus `\n` to `/home/user/data/payload.log` (create the file/directory if missing), close the connection, and loop; compile with `g++` and background it (`nohup ... &`). Port forwarding: `socat TCP-LISTEN:9090,fork,reuseaddr TCP:127.0.0.1:8080 &` — test with `echo test | nc localhost 9090` and confirm it lands in the log before moving on. Permissions: only after the log file exists, `chmod 600` it, then `setfacl -m u:guest:r /home/user/data/payload.log`, and verify with `getfacl` that the entry is present exactly as expected — order matters, since re-running chmod after setfacl can strip the ACL mask.
