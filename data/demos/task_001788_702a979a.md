Reverse-engineer the format empirically before writing any replacement code: run `/app/xpak_pack` on a series of small, controlled test inputs (a single tiny text file, a file with a long run of one byte, an empty file, multiple files) and inspect the resulting `.xpak` and `.manifest` byte-for-byte with `xxd`/`hexdump -C`, changing one variable at a time (filename length, file count, content pattern) to isolate what each header field encodes and how the compression scheme behaves on edge cases like runs longer than 255 bytes or empty files. Confirm hypotheses (e.g. "looks like a magic tag + little-endian counts + RLE-compressed payload + CRC32 checksum") by predicting output for a new test file and diffing your prediction against the real binary's output before committing to the format in your Python port. Implement `/home/user/xpak_pack.py` using Python's `struct` module for exact binary packing/endianness and `zlib.crc32` for checksums, and write the manifest as literal text lines matching the observed format precisely (field order, delimiters, hex case, newline style). Validate by running both the oracle and your script on the same inputs and diffing the outputs with `cmp`/`diff` before considering it done.
