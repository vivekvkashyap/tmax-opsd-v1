Read `worker.go` closely first to understand exactly what it does: it lists `.txt` files in a directory, distributes them across a fixed worker pool via a jobs channel, has each worker count words per file, sends per-file counts to a results channel, and sums them once all workers finish — replicate this concurrency pattern in Python (e.g. a `queue.Queue` for jobs, a `ThreadPoolExecutor` or manual threads for workers, a thread-safe accumulator or another queue for results) rather than just summing sequentially, so the semantics genuinely mirror the Go version; match its exact output format `Total words: <count>` including capitalization and spacing. For cross-compilation, use `GOOS=linux GOARCH=amd64 go build -o worker` and `GOOS=windows GOARCH=amd64 go build -o worker.exe` from `worker.go`, then move each binary into its required build subdirectory. In your orchestrator script, run the Linux binary and `py_worker.py` on the same `data/` directory, capture both stdouts exactly (strip only trailing newline, not internal whitespace), and compare them for byte-for-byte equality before writing `TEST PASSED: <output>` or `TEST FAILED` — test this comparison logic manually first so a subtle formatting mismatch doesn't cause a false failure.
