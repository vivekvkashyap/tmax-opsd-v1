First recover the port and auth token from the image with `tesseract /app/spec.png stdout` — do not guess or hardcode values without OCR'ing them. Fix the C++ side by wrapping the function declaration in `extern "C" { ... }` in processor.h (and matching in processor.cpp) so cgo sees C linkage rather than a mangled C++ symbol; build the shared lib with CMake (`cmake . && make`) and note the output path (usually a `lib/` or build dir). In server.go's cgo comment, add proper `#cgo LDFLAGS: -L<path-to-libprocessor-dir> -lprocessor` plus the include path, and at runtime export `LD_LIBRARY_PATH` to that same directory before starting the server (or the binary will fail to find the .so even though it links at build time). For the /process handler: split the input slice in half, launch two goroutines each calling the cgo function on its half and sending the result on a channel, wait for both channels, concatenate, then `sort.Ints` the whole combined slice before responding — order of concurrency vs. final sort matters, don't sort each half separately. Check the Authorization header equals `Bearer <token>` exactly, returning 401 otherwise, and confirm with `curl` that the server is actually listening on 0.0.0.0:<port> before finishing.
