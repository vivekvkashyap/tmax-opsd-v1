Two independent fixes are required before the pipeline can even run correctly. First, find where `/app/bc-1.07.1`'s source initializes the global `scale` variable (search `bc/main.c` and related init code for `scale = 0`) and change that initialization to `20` so `bc` performs floating-point division/multiplication with real precision instead of truncating to integers; rebuild with the package's normal `./configure && make` flow and install the resulting binary to `/home/user/bin/bc`, adding that directory to PATH, then sanity-check with something like `echo "5/2" | bc` to confirm you now get a fractional result rather than truncation. Second, write `/home/user/process_embeddings.sh` using only bash/awk/sed/paste plus your fixed `bc` (no Python/Perl) to: parse the dataset and projection-matrix CSVs, compute each dataset row's projection as a dot product against every matrix column (loop over rows/columns, accumulate via `bc` or awk-driven `bc` calls), do the same for the query vector, then sum absolute differences (Manhattan distance) between each projected row and the projected query, and print the 0-based index of the minimum (lowest index on ties). Since correctness is checked by fuzzing against a hidden numeric oracle across many random matrix sizes, test your script yourself on a few hand-computed small examples (e.g. verify against a quick numpy check) to catch precision or indexing mistakes before considering it done.