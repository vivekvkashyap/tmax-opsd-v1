Do the join and filter with standard shell tools before writing any C++: `join` requires sorted input on the join key, so sort both files (minus header) by `id` numerically, join them, then re-attach a header, or alternatively use `awk 'NR==FNR{a[$1]=$2;next}{if($1 in a) print $1, a[$1], $2}'` reading both files. Filter to rows where `pca_components == 10` and feed only the `accuracy` column into your C++ program (via stdin or an intermediate file). In the C++ program, compute the sample mean, sample standard deviation (divide sum of squared deviations by N-1, not N), and the 95% CI as `mean ± 1.96 * (stddev / sqrt(N))`. Format the final line with exactly 4 decimal places using `std::fixed` and `std::setprecision(4)` (or printf `%.4f`), matching the exact string template `Mean: X.XXXX, CI: [Y.YYYY, Z.ZZZZ]`. Compile with `g++ -std=c++17` and run it so `/home/user/ci_output.txt` is produced, then double check by re-deriving the numbers independently (e.g. quick awk/bc sanity check) before trusting the C++ output.
