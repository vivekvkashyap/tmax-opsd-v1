Build the pipeline with `awk` as the core filter since it naturally handles CSV column logic: for each input file, print the header line unconditionally, then for data rows use field-based conditions like `awk -F, 'NR==1{print $1","$2","$3; next} $5!="ERROR" && $3!="999.9" {print $1","$2","$3}'` — note the status and temperature checks must be exact string matches, and column extraction must happen after filtering, not before (filtering on the full row using original column positions, since column 5 = status, column 3 = temperature, using the raw 5-column input). Loop over `/home/user/raw_data/*.csv`, create `/home/user/clean_data/` with `mkdir -p`, and write each cleaned file preserving its original basename. After the script runs, generate checksums with `sha256sum /home/user/clean_data/*.csv > /home/user/checksums.txt` exactly as specified so the format matches what's expected. Test the awk filter against one sample file manually first and inspect the output rows against the raw data by eye to catch off-by-one column errors before trusting the full script.
