Work in three stages. First, treat `/app/bin/legacy_calc` as a black-box oracle: probe it with a range of expressions (nested parentheses, negative results, division truncation) and record `Result` vs `Checksum` pairs so you can algebraically recover the checksum formula's structure (it is `(abs(result) * MULT) % MOD`), then use tesseract or another OCR tool on `/app/spec.png` to read off the actual MULT and MOD constants rather than guessing them — read the image, don't hardcode numbers you haven't verified. Second, write a correct recursive-descent or shunting-yard expression evaluator in Go supporting `+ - * /` and parentheses with standard precedence, being careful that division matches the oracle's truncation-toward-zero behavior (Go's native integer division already truncates toward zero, but verify negative-operand cases explicitly). Third, before declaring done, write your own fuzz/comparison script that generates many random expressions and diffs your Go binary's stdout against the legacy binary's stdout byte-for-byte, including exact spacing and newline in the `Result: X, Checksum: Y` format — the fuzz harness that grades you will be far more thorough than a handful of manual test cases.
