First extract the filtering rule from the audio: use a local speech-to-text tool (e.g. `whisper` if available, or any installed transcription utility) on `/app/voicenote.wav` to get the exact spoken threshold and statistical test — do not assume a rule, derive it from the transcript. Then implement `/home/user/detect_anomaly.go` to parse the TSV (skip the header row, split on tabs, parse `token_count` and `latency_ms` columns as floats), and compute the Pearson correlation coefficient manually using standard `math` (means, covariance, and standard deviations: `cov/(stdX*stdY)`) — do not use any external stats package. Apply the exact comparator and threshold the audio specifies (pay attention to strict vs. non-strict inequality and the sign/direction of "reject") to decide accept vs reject, then `os.Exit(1)` or `os.Exit(0)` accordingly, no other output requirements. Validate your correlation function against a couple of hand-computed examples (e.g. perfectly correlated and perfectly anticorrelated toy vectors) before running it against the clean/evil corpora, since a sign or off-by-one bug in the correlation formula will silently invert your pass/fail decisions across the entire test suite.