Compile `generate_data.c` with `gcc generate_data.c -o generate_data`, run it, and redirect stdout to `dataset.txt`. For the numerical fix, do not just clip or add epsilons — rewrite the log-sum-exp expression using `numpy.logaddexp(50*(x-3.14)**2, 50*(y+2.71)**2)`, which is mathematically identical to `log(exp(a)+exp(b))` but avoids overflow by internally subtracting the max term before exponentiating; verify by comparing outputs against the naive form on small inputs where both are finite. Add `sys.argv` parsing for `x0`/`y0` (as floats), keep `method='Nelder-Mead'`, and print only `"{x} {y}"` from `res.x` — no labels, no extra output, since evaluate.sh will parse this line directly. In `evaluate.sh`, loop over `dataset.txt` lines with `while read -r x0 y0`, invoke `python3 optimizer.py "$x0" "$y0"`, capture the two printed numbers, accumulate squared distance to `(3.14, -2.71)` per point, average, divide each point's squared distance by 2 as specified, then average across points and write `printf "MSE: %.6f\n"` to `final_mse.txt`. Make both `optimizer.py` and `evaluate.sh` executable and test the whole chain end-to-end from a clean directory before finishing.
