First recover the four weights from the image with OCR (`tesseract /app/weights.png stdout`), parsing the `w1=..., w2=..., w3=..., w4=...` text yourself and sanity-checking the parsed floats look plausible (including the negative sign on one weight) rather than trusting OCR blindly — re-run with different `--psm` settings if a character looks wrong. Then implement the log-sum-exp trick correctly: for each input line's 4 values, compute `wi*xi` for all i, take `m = max_i(wi*xi)`, and return `m + log(sum_i exp(wi*xi - m))` — never call `exp` directly on the raw `wi*xi` products first, since with inputs up to ±2000 and weights up to 2.0 that overflows/underflows to Inf/NaN before you can even subtract the max. Parse stdin line-by-line with `bufio.Scanner`, split on commas, and use `strconv.ParseFloat` for each value; format output with `fmt.Printf("%.6f\n", result)` exactly as specified since the fuzzer compares bit-for-bit. Build with `go build -o /home/user/normalize /home/user/normalize.go` and manually test a few extreme-magnitude lines (e.g. values near ±2000) to confirm no Inf/NaN appears before considering it done.
