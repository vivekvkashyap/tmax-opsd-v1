Transcribe `/app/authorization_pin.wav` first (try `SpeechRecognition` with a local/offline recognizer, or `ffmpeg` + `pocketsphinx`) to recover the spoken 4-digit PIN — convert spoken number words to digits carefully, do not guess. For the JSONL parsing, note that Python's `json.loads` will raise on lone unicode surrogates like `\ud800` (they're valid inside a JSON string literal per the JSON spec but Python's UTF-8 encoder chokes when re-serializing) — read the file as raw text, and when `json.loads` fails on a line, catch the exception and repair it (e.g. regex-replace bare `\uD800-\uDFFF` escape sequences that aren't part of a valid surrogate pair, or decode using `errors='surrogatepass'`) rather than dropping the line. Group events by `node_id`, sort each group by `timestamp` ascending, and fold `add` (elementwise +) and `multiply` (elementwise *) operations starting from `[0,0,0]`. After computing each node's final vector, discard any node where the sum of squares of its components exceeds 1,000,000. Build a small Flask/FastAPI app binding `127.0.0.1:8080` with `GET /state/<node_id>` that checks the `X-Auth-Pin` header against the transcribed PIN (401 if missing/wrong), returns 404 for missing/discarded nodes, and 200 with `{"node_id": ..., "state": [...]}` otherwise. Run it in the foreground so it stays reachable for testing.
