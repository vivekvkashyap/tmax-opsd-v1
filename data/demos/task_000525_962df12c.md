Parse derivations.tsv into an adjacency structure keyed by (source, target) -> relation, then find simple directed 3-cycles A->B->C->A where every one of the three edges has Relation exactly "DERIVED_FROM" — for each edge A->B with that relation, look for an edge B->C also DERIVED_FROM, then check whether an edge C->A DERIVED_FROM exists; skip any triple where a required edge is missing or has a different relation like CITES. Dedupe cycles that are rotations of each other (A->B->C->A vs B->C->A->B are the same cycle) by canonicalizing each cycle's node set. For each surviving cycle, join against metadata.tsv to map each dataset ID to its ResearcherName, and build the output objects with `cycle_nodes` and `researchers` each sorted lexicographically and deduped. Sort the overall list of cycle objects by their first `cycle_nodes` entry. Write this exact structure as pretty JSON to `/home/user/deadlocks.json` using only the standard library (csv/json modules, plain dict/set logic) — no networkx or other third-party graph library, since none are guaranteed installed.
