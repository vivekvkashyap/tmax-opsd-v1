Build this pipeline in the order it executes, verifying each stage before moving to the next. First write the Expect script: use `spawn /home/user/fetch_backup`, `expect "Password for backup server: "`, `send "SecureRest0re!\r"`, then `interact` or `expect eof` so the child's output (including the tarball creation) completes before the script exits — a common mistake is not waiting for eof, which can truncate the interaction. Run it and confirm `/home/user/mail_data.tar` actually appears before continuing. Extract with `tar -xf` into `/home/user/mail_data/` and confirm the two config files exist. For the C program, use `mkdir()` and `symlink()` directly (not shell mkdir/ln) per the spec, and call `system()` to invoke `setfacl` for the exact ACL entries requested — note the distinction between granting a permission bit and having the *exact* rwx triple; use `setfacl -m u:nobody:r-x <dir>` and `setfacl -m u:nobody:r-- <file>`, and confirm the ACLs also don't get widened by inherited default entries. Verify final state with `getfacl` and `readlink` on the symlinks before declaring done. Never use sudo — run everything as `user`.
