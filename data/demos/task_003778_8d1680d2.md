Build this as three separable pieces and verify each in isolation before wiring them together. First, write the Rust binary: read all stdin bytes (`io::stdin().read_to_end`), transform each byte with `byte.rotate_right(3) ^ 0x8C`, and write the buffer to stdout — test it manually with `echo -n "test" | cargo run` before touching git. Second, create the bare repo with `git init --bare`, then write the `post-receive` hook as a script that reads the pushed commit hash from stdin (the hook receives `<old> <new> <ref>` lines), uses `git --work-tree=<dir> --git-dir=<repo> checkout -f <hash>` to materialize the release into `/home/user/releases/release_<hash>`, runs `cargo build --release` there, and health-checks by piping `/app/voice_record.wav` into the built binary with output to `/dev/null`, checking `$?` — `set -e` inside the hook helps abort on failure. Only after the health check succeeds should the hook atomically retarget the `current` symlink (write to a temp symlink name and `mv`/`ln -sfn` for atomicity). Finally set up your workspace repo, commit, add the bare repo as a remote, and push — watch the hook's stderr output for failures during a dry run.
