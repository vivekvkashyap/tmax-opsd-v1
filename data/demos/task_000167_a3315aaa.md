This task has four independent parts — tackle them separately and verify each in isolation. (1) Video: use `ffmpeg -i /app/router_led.mp4 out_%04d.png` (or read frames via a Python cv2/PIL loop) to pull every frame, then classify each as "red" if its dominant/average color is pure red (255,0,0) vs green; walk the sequence and count transitions into a red run (a contiguous red block = one event, not one frame) — write just the integer count to `/home/user/link_drops.txt`. (2) Legacy parser: don't guess the oracle's formatting rules — pipe varied sample lines (with timestamps, IPs, mixed text) into `/app/legacy_analyzer_oracle` via stdin and observe its stdout to infer the exact transformation rules (case, spacing, delimiter changes), then implement identical logic in `net_analyzer.go`, compiling with `go build -o /home/user/net_analyzer`; test many samples against the oracle before finishing. (3) Expect script: use `spawn`, `expect "Username:"`, `send "admin\r"`, and repeat for each prompt/response pair in order, ending with `expect eof` or similar so the script doesn't hang. (4) Cron: write a line in standard 5-field syntax scheduling every 5 minutes pointing at the absolute path of `test_router.exp`, and validate the syntax makes sense (`*/5 * * * *`).
