Treat `/app/etl_oracle` purely as a black-box you probe experimentally, not something to disassemble line-by-line: generate small synthetic CSVs (`timestamp_ms,value` pairs) that vary one property at a time — timestamp gap size, ordering, and value bounds — and run the oracle on each (`./etl_oracle file.csv; echo $?`) to bisect the exact thresholds (e.g. try gaps of 49, 50, 51 ms to confirm the exact required interval; try values at 0.999, 1.000, 1.001 to confirm inclusive/exclusive bounds; try a duplicate timestamp and a reversed pair to confirm monotonicity is strict). Once you've empirically pinned down the three rules (fixed inter-sample gap, strict ordering, value range), implement a straightforward C program that reads lines with `fscanf`/`strtol`+`strtod`, tracks the previous timestamp, and rejects (exit 1) on the first violation of any rule, exiting 0 only after reaching EOF cleanly. Compile with `gcc -O2 -o /home/user/filter_etl /home/user/filter_etl.c`, then validate against every file in `/verify/clean/` and `/verify/evil/`, checking exit codes with `echo $?` after each, and refine your inferred constants if any file in either corpus disagrees with your program.
