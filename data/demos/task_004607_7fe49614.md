Extract frames with `ffmpeg -i /app/data/video.mp4 -vf fps=1 /home/user/frames/frame_%03d.jpg` so naming and 1fps sampling line up with second-based ground truth. Build simple, robust visual features (e.g. per-channel color histograms and/or mean brightness via OpenCV/PIL) for every image in `/app/corpus/clean` and `/app/corpus/evil`, then train `GaussianNB`, tuning `var_smoothing` via `GridSearchCV`/cross-validation rather than picking a value blindly — the hidden holdout corpus must also classify correctly, so avoid overfitting to a single feature dimension. Persist the fitted model (e.g. with joblib) and write `/home/user/filter.sh` as a thin wrapper: it takes an image path, calls a small Python inference script, and exits 0 for clean / 1 for evil based on the script's output — verify it actually returns real exit codes, not always 0. Run `filter.sh` over every extracted frame, collect the ones flagged evil into `/home/user/anomalies.txt`, and separately time a loop over all frames (wall-clock seconds / frame count) to compute FPS for `/home/user/fps.txt`.
