Normalize encodings first, then do everything else on clean UTF-8 text. Convert each file with `iconv`: `iconv -f UTF-16LE -t UTF-8 sensor_a.csv` and `iconv -f ISO-8859-1 -t UTF-8 sensor_b.csv` (or do this inside Python with `open(path, encoding=...)`). After conversion, strip blank lines and drop exact-duplicate rows — a Python `set`/dict keyed on the raw row tuple, or `sort -u` in shell, both work; do this before aggregation so duplicates don't skew averages. For each row, truncate the ISO-8601 timestamp to the hour by zeroing minutes/seconds (string slicing to `YYYY-MM-DDTHH:00:00Z` works since the format is fixed-width) and group by that truncated hour per sensor, computing the mean `value` for each hour bucket per sensor. Then intersect the two sets of hour keys — only hours present in *both* sensors' aggregates — and for each compute `round(abs(avg_a - avg_b), 1)`. Sort the intersection by hour string (which sorts chronologically since it's ISO-8601) and write `/home/user/output/hourly_distance.csv` with header `hour,distance`, creating the output directory first with `mkdir -p` / `os.makedirs`. Double check the rounding matches Python's banker's-rounding-free expectation (e.g. use `round()` and verify against the example values before trusting it).
