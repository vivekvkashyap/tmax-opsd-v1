Break this into independent, verifiable steps. Parse fstab_mock by filtering out comment lines (starting with `#`) and matching the line whose first whitespace-separated field is `UUID=A1B2-C3D4`, extracting its second field as the mount point — a simple awk/grep or Python split is enough; don't hardcode the answer, derive it from the file since the UUID-to-path mapping is the thing being tested. For the health check, attempt an HTTP GET to `http://127.0.0.1:9090/health` with a short timeout and catch connection-refused/timeout exceptions explicitly (Python's `requests` with `timeout=` wrapped in try/except, or `urllib`) — since nothing is expected to be listening on that port here, your code path for "DOWN" must be exercised and correct, not just the happy path. For the timestamp, use Python's `zoneinfo` (or `pytz`) with `ZoneInfo("Asia/Tokyo")` to get the current time in that zone, then format it manually as `YYYY-MM-DD HH:MM:SS JST` — note you likely need to hardcode the literal "JST" abbreviation rather than trust `%Z`, since that can render inconsistently depending on the platform's tzdata. Write the three fields to capacity_report.json with the exact key names given. Finally generate the TLS cert with `openssl req -x509 -newkey rsa:2048 -nodes -keyout tls.key -out tls.crt -days 365 -subj "/CN=localhost"`, and verify it with the exact openssl commands the grader will use before finishing.
