Automate the interactive wizard by driving it as a subprocess rather than trying to parse its prompts textually: launch `config_wizard.py` with `subprocess.Popen(..., stdin=PIPE, stdout=PIPE, text=True)`, then write each expected answer followed by a newline to stdin in the same order the prompts appear (`production\n` then `8123\n`), flushing after each write, and wait for the process to exit and confirm `app.conf` was created before proceeding — a naive approach that sends all input at once via `communicate()` upfront usually works too if timing between prompts doesn't matter, but confirm the script actually reads via `stdin.readline()` first. After the wizard succeeds, launch `server.py` detached (e.g. `subprocess.Popen([...], start_new_session=True)` or shelling out with `nohup ... &`) so it outlives the pipeline script. For the monitor, use `urllib.request`/`http.client` to GET `/status`, parse the JSON, and format the log line exactly as specified using the parsed `environment` and the known port, writing it to `/home/user/deployment_status.log`. Add a short sleep between starting the server and querying it so the socket is bound first.
