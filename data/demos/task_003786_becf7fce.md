Build and validate each stage independently before wiring the root-finder together. Compile with `gcc -O3 /home/user/thermal_sim.c -lm -o /home/user/thermal_sim`, then sanity-check it by running it with a couple of test x values to confirm it prints a plain float to stdout. For the Newton-Raphson bash script, capture the binary's stdout via `$(...)` command substitution and strip any trailing newline before doing arithmetic — use `bc -l` or `awk` since bash can't do floating point natively. Implement F(x) = thermal_load(x) - 0.25 as its own function/step so you compute it identically at x, x+h, and x-h. For the central-difference derivative, evaluate the binary at x+0.001 and x-0.001 (fresh subprocess calls each time — don't reuse cached values across iterations), then F'(x) = (F(x+h)-F(x-h))/(2h). Loop the update x = x - F(x)/F'(x) exactly 5 times starting from x0=1.0, printing each intermediate x to sanity-check convergence. Finally format the last x to exactly 4 decimal places (e.g. with `printf "%.4f\n"`) and write only that to `/home/user/result.txt`, then `chmod +x` the script.
