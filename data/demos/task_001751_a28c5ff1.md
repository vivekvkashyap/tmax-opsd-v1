Treat `/app/legacy_processor` as a black box you must characterize empirically, not guess at. Build a series of small, targeted CSV probes: one sensor with an interior gap, one with missing values at the very start, one with missing values at the very end, one with an entirely-empty column, and one with multiple sensors — run each through `/app/legacy_processor in.csv out.csv` and diff the output byte-for-byte against hypotheses (linear interpolation vs. other methods, which fill strategy applies at edges, decimal rounding precision, column order, tie-breaking in sort, line endings, presence/absence of header and index). Only lock in a rule once you've confirmed it against a probe designed to distinguish it from alternatives. Implement the Python script with pandas per-sensor interpolation and use `concurrent.futures`/`multiprocessing` to process sensor columns in parallel via the `--workers` argument, then reassemble, round, sort (timestamp then sensor name), drop remaining NaNs, and write CSV with matching separators/line endings. Re-run your script against the binary on fresh random CSVs (varying size, column count, missingness patterns) until outputs are bit-identical.
