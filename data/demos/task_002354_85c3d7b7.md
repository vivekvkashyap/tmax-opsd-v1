Diagnose each of the three failures independently before touching code. First, run `./build.sh` and read the linker error carefully — a shared library calling libm functions (sin/cos/log) that fails to load usually means the math library wasn't linked; add the missing `-lm` flag to the gcc invocation. Second, open `calculate_score` and compare it line-by-line against the required formula `(cpu_usage * 1.5) + (memory_usage * 2.0) / math.log(uptime_days + 2)` — Python operator precedence means `/` binds tighter than `+`, so check the buggy version doesn't add stray terms or misplace parentheses; rewrite it to exactly mirror the formula's grouping. Third, reproduce the `TypeError` in `serialize_result` by running the script and reading the traceback — `base64.b64encode` requires a `bytes`-like object, so encode the JSON string (e.g. `.encode('utf-8')`) before passing it in, and decode the result back to a string for output. After each fix, rerun `./build.sh` then `python3 aggregate_metrics.py` and inspect `/home/user/output.txt` to confirm no exceptions and plausible base64 output.
