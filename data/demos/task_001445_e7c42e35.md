Tackle the three stages independently. Stage 1: the Makefile's linker error is almost certainly a missing `-fPIC` on compilation and missing `-shared` on the link step — object files built without `-fPIC` can't be linked into a `.so` on most Linux setups, so add both flags and confirm `libjit.so` builds and `file libjit.so` reports it as a shared object. Stage 2: use `ffmpeg -i /app/ci_run.mp4 out_%04d.png` (or read frames directly via OpenCV/PIL) to get every frame in order, then for each frame index check the pixel at (0,0) — watch out for BGR-vs-RGB channel order depending on your library, and require an exact match on pure red (255,0,0), not "reddish"; write matching 0-indexed frame numbers sorted ascending to `red_frames.txt`. Stage 3: write a byte-scanner over the hex-decoded payload that slides a window across every offset checking for the three two-byte opcode sequences (`0F 05`, `0F 34`, `CD 80`) anywhere in the buffer, not just at instruction boundaries — print `EVIL`/exit 1 on any match, else `CLEAN`/exit 0. Self-test your sanitizer against hand-crafted payloads containing each restricted sequence at different offsets, plus clean payloads that share partial bytes (e.g. a lone `0F` not followed by `05`/`34`) to make sure you're not over- or under-matching.