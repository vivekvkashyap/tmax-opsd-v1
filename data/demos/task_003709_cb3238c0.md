The core algorithm is classical multidimensional scaling's embeddability test: parse the `.pdist` file's `i j distance` lines into a symmetric NxN matrix D (zero diagonal), square it elementwise, form the centering matrix J = I - (1/N)·11ᵀ, compute the Gram matrix G = -0.5·J·D²·J, take its eigenvalues (e.g. `numpy.linalg.eigvalsh` since G is symmetric), and the structure is valid 3D-embeddable iff the smallest eigenvalue ≥ -tolerance; exit 0 if valid, 1 otherwise. Implement this in `/home/user/filter_edm.py` taking one positional path argument. For the tolerance value: it is not stated in the text task — it's spoken in `/app/audio/lab_memo.wav`, so transcribe that audio (e.g. install/use a speech-to-text tool, or `ffmpeg` to preprocess then a transcription model) to recover the exact numeric tolerance the PI specified, and use that recovered value rather than guessing a round number. Before finishing, test your script against both `/app/corpora/clean/` (expect exit 0 on all) and `/app/corpora/evil/` (expect exit 1 on all) to confirm both the math and the transcribed tolerance are correct — a slightly wrong tolerance will show up as borderline-case misclassifications.