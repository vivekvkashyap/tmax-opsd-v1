Debug this by tracing each of the three described symptoms to a specific line rather than rewriting the script wholesale. For the serialization crash, either convert the datetime object to an ISO string before adding it to the output dict (e.g. `dt.isoformat()`), or supply a `default=str` handler to `json.dump` — check what the expected timestamp format should look like. For the uptime formula, re-derive it from its definition: cumulative uptime is successful pings divided by total pings processed so far, not `successful / (total - successful)` — fix the arithmetic directly. For the rolling average, the bug is that `latencies[i - 4 : i + 1]` uses negative indexing when `i < 4`, wrapping around to grab elements from the end of the list; fix by clamping the start index with `max(0, i - 4)` instead of `i - 4`. After each fix, run the script against the given ping_logs.jsonl and manually verify the first few entries by hand-computing cumulative uptime and rolling average to confirm your formulas are right before trusting the full output file.