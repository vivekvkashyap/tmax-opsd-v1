In `build.rs`, call `prost_build::compile_protos(&["../message.proto"], &["../"])` (adjust paths so it resolves relative to the crate) to generate the Rust protobuf types, and separately use the `cc` crate (`cc::Build::new().file("../checksum.c").compile("checksum")`) to compile the C file into a static lib that gets linked automatically — make sure `cargo:rerun-if-changed` directives point at both source files. In `main.rs`, include the generated code via `include!(concat!(env!("OUT_DIR"), "/polyglot.rs"))` (or the package name used in the .proto), declare `extern "C" { fn compute_fec(data: *const u8, len: usize) -> u32; }`, and call it inside an `unsafe` block passing the payload's pointer/length. Build the payload as the literal byte string `b"INTEGRATION_TEST_DATA"`, construct the `MessageRecord { payload: payload.to_vec(), fec: computed_fec }`, and serialize with `prost::Message::encode` into a `Vec<u8>`, writing those raw bytes (not the debug string) to `/home/user/workspace/output.bin` with `std::fs::write`. To sanity-check before trusting the run, manually verify your FEC arithmetic matches a hand-trace of the C loop's XOR/rotate-left-5 logic over the payload bytes, and confirm the file is written as raw protobuf bytes, not text.