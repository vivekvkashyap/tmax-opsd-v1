Parse the four CLI args with `argparse` first. Build one parameterized SQL query (never f-string interpolation) that self-joins `wait_graph` three times to find 3-cycles: `wait_graph` aliased as e1(A->B), e2(B->C), e3(C->A), with `e1.src_tx = :start_tx AND e3.dst_tx = :start_tx AND e1.dst_tx = e2.src_tx AND e2.dst_tx = e3.src_tx`, filtering `e1.wait_time_ms + e2.wait_time_ms + e3.wait_time_ms >= :min_total_wait`, and be careful to also exclude any degenerate cycles where B or C equals A (since A->B->C->A must be a genuine 3-node cycle, not reusing the start node early). Push the ORDER BY (total wait DESC, then B ascending, then C ascending) and the `LIMIT :limit OFFSET :offset` into the same SQL statement so pagination and sorting happen in the database, not in Python post-processing — this matters because the grader checks the query performs sorting/pagination via SQL. Execute with `cursor.execute(sql, {...})` using named or `?` placeholders, then serialize each row as `{"path": [A,B,C,A], "total_wait": total}` to `/home/user/deadlock_report.json`. Verify by manually tracing a couple of known cycles against your query's output before trusting it.
