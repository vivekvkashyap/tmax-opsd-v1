Build this as a real long-running watcher, not a one-shot batch converter — the grader will drop a new file into `raw_datasets/` well after your turn ends and expect a fresh output within seconds. Use `inotifywait -m -e create,moveto /home/user/raw_datasets` (or a Python `watchdog` Observer loop) inside `auto_process.sh` so it blocks forever reacting to each new `.tsv`. For each event, filter to `*.tsv`, then convert: read the file with the UTF-16LE codec explicitly (e.g. Python `open(path, encoding='utf-16le')` or `iconv -f UTF-16LE -t UTF-8`) — do not assume UTF-8, plain `cat`/naive reads will mangle or fail on the BOM/2-byte chars. Parse the first line as the TSV header, split remaining lines on tabs, zip header keys to values per row, and dump as a JSON array of objects (`json.dumps`, preserving row order) to `clean_datasets/<basename>.json`, keeping keys as strings even for numeric-looking values. Make the script executable (`chmod +x`), and after creating it, actually launch it detached (`nohup ./auto_process.sh &` or `disown`) so it keeps running as a background process after your shell turn ends — verify with `ps`/`pgrep` before finishing.