First recover the passphrase by transcribing `/app/voicemail.wav` (e.g. via `whisper`/`ffmpeg`-assisted transcription or a speech-to-text library) — treat the transcript as the literal passphrase bytes, don't paraphrase it. For the C++ tool: derive the AES-256-CBC key from the passphrase the same way the payload was encrypted (commonly `EVP_BytesToKey` or a direct SHA-256 digest of the passphrase — test against `/app/sample_payload.enc` and its known-correct plaintext shape to confirm you picked the right KDF), base64-decode the file, take the first 16 bytes as IV and the rest as ciphertext, and decrypt with OpenSSL's EVP API (`EVP_DecryptInit_ex`/`EVP_DecryptUpdate`/`EVP_DecryptFinal_ex`). Parse line 1 as the subnet and treat the rest as one or more concatenated PEM certs (split on `-----BEGIN CERTIFICATE-----`/`-----END CERTIFICATE-----` boundaries). For each cert, build and verify a chain against `/app/root_ca.pem` using OpenSSL's `X509_STORE`/`X509_STORE_CTX_verify` APIs, keeping only certs that verify, and extract each valid cert's subject name (`X509_NAME_oneline` or similar) in the exact output format required. Write `harden_ssh.sh` to edit the given config in place, updating or appending `PasswordAuthentication no` and `PermitRootLogin no` (handle both commented-out and already-present directives). Compile exactly as specified and test against the sample payload before trusting your logic on the hidden one.