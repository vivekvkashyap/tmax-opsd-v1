Split this into the two independent objectives and validate each before combining results. For the video forensics: use `ffmpeg -i /app/sandbox_review.mp4 frame_%04d.png` (or similar) to dump all frames, then run `tesseract` OCR on frames sequentially (or in a binary-search fashion around where you suspect text appears) looking for the exact target string; once you locate the first frame where it's fully legible, write just that integer to the output file — don't hardcode a guessed frame number, derive it from actual OCR output, and note ffmpeg's frame numbering convention (0- vs 1-indexed) matches what the verifier expects. For the leak detector: implement two independent checks — a regex for the exact secret pattern `SBX_SECRET_[0-9a-f]{16}` scanned across the raw file text, and a separate scan for the hex-encoded ELF magic `7f454c46` allowing for either spaced (`7f 45 4c 46`) or unspaced hex, case-insensitively (normalize by stripping whitespace and lowercasing before searching). Exit 1 if either check fires, else exit 0. Test both checks against deliberately crafted clean/evil sample files (including edge cases like non-ELF hex dumps and non-matching hex-like strings) before trusting it against the hidden corpora.