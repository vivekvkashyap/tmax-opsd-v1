Bring the stack up with `docker-compose -f /app/docker-compose.yml up -d` and wait until Neo4j's bolt port (7687) actually accepts connections before doing anything else (it takes a while to initialize). Write `/home/user/indexes.cypher` with two `CREATE INDEX ... FOR (n:Employee) ON (n.emp_id)` / `FOR (n:Department) ON (n.dept_name)` statements (Neo4j 4.4 syntax) and apply it with `cypher-shell -u neo4j -p password -f indexes.cypher`. For the Go proxy, use an official Neo4j Go driver and a Redis client library (or raw RESP if you want zero deps), and structure `/query` as: read the raw body, compute `sha256` hex of the trimmed query text, `GET` that key from Redis and return it immediately if present; otherwise run the firewall check. Implement the firewall as a case-insensitive scan for whole-word mutating keywords (`CREATE`, `MERGE`, `SET`, `DELETE`, `REMOVE`, `DETACH`, `DROP`, `LOAD CSV`, `CALL apoc`, `CALL db`) — use word-boundary matching, not naive substring search, so it isn't fooled by mixed casing but also doesn't false-positive on read-only queries; on a hit, return 403 immediately without touching Neo4j. On a clean query, execute it against Neo4j, marshal the result as JSON, `SET` it in Redis with a 60s TTL, and return it. Test locally against both corpus directories with `curl -X POST --data-binary @file` before trusting the full run.
