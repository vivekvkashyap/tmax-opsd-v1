Start by running `tesseract /app/config_label.png stdout` to OCR the three parameters (WINDOW, MAX_DEV, ENCODING); parse the extracted text with a regex rather than hardcoding — your script's behavior must derive from what OCR actually returns, and double check OCR output for stray characters before trusting it. In `validator.py`, open the file with `open(filepath, encoding=<ENCODING>, errors='strict')` and catch `UnicodeDecodeError` to return False. Use pandas: read CSV, parse `timestamp` as datetime and set as index, then `.resample('1min').ffill()` (or equivalent) for gap-filling, compute `.rolling(WINDOW).mean()`, and compare `abs(temp - rolling_mean) > MAX_DEV` row-wise, skipping NaN rolling-mean rows (early window). For sanitization, scan the raw `notes` column (case-insensitive) for `<script>` and `javascript:` substrings before/regardless of numeric checks. Wrap all pandas/parsing logic in try/except returning False on any failure. For the cron piece, write `pipeline.sh` with `echo "Pipeline running"`, chmod +x, then install `* * * * * /home/user/pipeline.sh` via `crontab -u user -` (or edit crontab file directly) and confirm with `crontab -l`. Test your validator against both a clean and a deliberately corrupted CSV you construct yourself before considering it correct.