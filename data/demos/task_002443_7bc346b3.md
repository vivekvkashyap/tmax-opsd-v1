First reproduce the crash with `strace -f -e trace=open,openat ./calc_engine -file /home/user/tx.wal 2>&1 | tail -30` to see exactly which config path it tries and fails to open (create any needed parent directories too); write `100.0` into that discovered path, not a guessed one. Next, don't try to patch the Go source (you likely don't have it) — instead write a small Go (or any script) recovery tool that reads `/home/user/tx.wal` line by line, splits each line on a single space, and keeps only lines where the first token is exactly one of `ADD`/`SUB`/`MUL`/`DIV` and the second token parses as a valid float via `strconv.ParseFloat` (or equivalent) — discard everything else, writing survivors to `/home/user/recovered.wal` in original order. Then run `./calc_engine -file /home/user/recovered.wal` and capture its printed output verbatim into `/home/user/result.txt` (no extra whitespace/newline issues) — do not hand-calculate the result yourself, let the binary compute it from your cleaned log.