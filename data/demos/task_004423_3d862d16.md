Design the SQL and the C++ program together, then validate against the schema before writing the report. Connect via the SQLite3 C API (`sqlite3_open`, `sqlite3_exec`) and issue `CREATE INDEX IF NOT EXISTS` statements for the two specified indexes. Use a parameterized/prepared statement (`sqlite3_prepare_v2` + `sqlite3_bind_int`) for the `sensitivity_level >= ?` filter combined with the fixed 2023 timestamp range in the WHERE clause — never string-concatenate the CLI argument into SQL. Compute in-degree and out-degree per employee by aggregating the filtered communications (e.g. two `GROUP BY` queries or a single pass accumulating into `std::map<int,pair<int,int>>` for sender/receiver counts), then join against `employees` for name/department, compute `BrokerScore = InDegree*OutDegree`, and sort with a custom comparator: BrokerScore descending, then Name ascending as tiebreaker — apply this sort to the *entire* result set (including zero-score employees) before truncating to the `limit` argument, since ties near the cutoff matter. Write the CSV with the exact header `EmployeeID,Name,Department,InDegree,OutDegree,BrokerScore`. Compile with `g++ ... -lsqlite3`, run `./audit_tool 3 3`, and inspect `/home/user/audit_report.csv` yourself, cross-checking a couple of degree counts by hand against the raw table to catch join/aggregation mistakes.