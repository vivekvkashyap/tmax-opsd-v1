This is a black-box function-identification problem — don't guess a formula and hope; derive it from structured probing. Run `/app/metric_oracle` on inputs chosen to isolate properties: identical arrays for both args (what does a "perfect match" print?), one array negated relative to the other, one array with zero variance (constant values — check what happens at the denominator-degenerate case, since the spec calls that out as special-cased), and small hand-computable arrays where you can work out several classic bivariate statistics (covariance-based measures, distance-based measures, rank-based measures) by hand and compare against the oracle's printed value to see which one matches across many trials, not just one. Once a candidate formula matches on several structured cases, stress-test it against dozens of random integer arrays of varying length before committing. Implement the confirmed formula natively (no shelling out to the oracle) in whatever language is convenient, formatting output to exactly 4 decimal places including the sign, and handle the zero-variance denominator case exactly as observed. Make `/home/user/fast_metric` executable and confirm its argument parsing accepts the same two-string invocation shape.
