Implement the tokenizer exactly as specified rather than approximating it with a regex crate or split_whitespace alone: lowercase the whole string first, then replace every non-alphanumeric character with a literal space character (do this pass over the lowercased string, not the original), then split on whitespace and drop empty tokens. Count total tokens N and count exact matches of the token "anomaly" as k — be careful that substring matches like "anomalies" must NOT count, only the exact token. Apply the Beta-Binomial posterior mean formula directly: posterior_alpha = 2 + k, posterior_beta = 2 + N - k, expected_value = posterior_alpha / (posterior_alpha + posterior_beta); don't shortcut this as (k+2)/(N+4) without double-checking the algebra matches. Write the Rust source to `/home/user/prepare_data.rs`, compile with `rustc` (or set up a Cargo project at `/home/user/data_prep` if you prefer), run the resulting binary so it reads `/home/user/dataset.txt` and writes valid JSON (correct types: integers for counts, float for the probability) to `/home/user/tracking.json`. Manually re-derive N and k by inspecting the actual dataset content to sanity-check your program's output before finishing.