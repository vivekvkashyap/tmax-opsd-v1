Start by running `tesseract /app/config.png stdout` (try a couple of `--psm` values if the text is garbled) to recover the exact CRON schedule and the REGEX pattern — do not guess these values, read them from the OCR output. Then write `/home/user/filter.py` so it reads the input file line by line, uses a strict anchored regex for the whole line like `^\[(?P<ts>[^\]]+)\] (?P<level>INFO|WARN|ERROR) - (?P<payload>.*)$`, validates the timestamp with `datetime.fromisoformat` (or `datetime.strptime` with the exact format) wrapped in try/except so things like Feb 30 are rejected, checks LEVEL against the exact allowed set, and matches PAYLOAD with `re.fullmatch` against the regex extracted from the image (remember Python `re` needs `re.UNICODE`, which is default, and the pattern already encodes the Japanese Unicode ranges — don't add extra unicode flags or normalize the payload). Drop any line failing any check; write passing lines unchanged (including trailing content) to the output. Build `/home/user/run_pipeline.sh` calling `python3 /home/user/filter.py /tmp/incoming.log /tmp/processed.log`, chmod it executable, then install via `crontab -l | { cat; echo "<CRON-from-image> /home/user/run_pipeline.sh"; } | crontab -` for the `user` account, using the exact schedule string you OCR'd.