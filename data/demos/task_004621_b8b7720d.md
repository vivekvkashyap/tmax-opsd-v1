Recover the model parameters yourself rather than guessing: run `tesseract /app/model_params.png stdout` (adjust `--psm` if needed) and parse the printed alpha/beta/delta/gamma and x0/y0 values out of the OCR text — use exactly what it reports, don't approximate. Implement the RK4 integrator by literally translating the Lotka-Volterra ODEs (dx/dt, dy/dt as functions of current x,y and the four extracted parameters) into the four k1..k4 stage evaluations per the standard RK4 update formula, with `dt = t_end / num_steps` and looping `num_steps` times starting from the OCR'd initial conditions. Parse `t_end` as float and `num_steps` as int from `sys.argv`, and format the final line as `f"{x:.4f} {y:.4f}"` exactly — no extra whitespace or labels, since a fuzz harness will diff your stdout byte-for-byte against a reference binary across hundreds of random `(t_end, num_steps)` pairs. Before trusting your implementation, sanity-check it against a couple of hand-computed or independently-integrated (e.g. scipy odeint) values to make sure your RK4 stages match the standard formula and you didn't transpose x/y anywhere.
