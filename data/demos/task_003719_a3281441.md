Run `tesseract /app/restore_directive.png stdout` first and parse the resulting text for the `Archive:`, `RecoveryKey:`, and `ServerPort:` lines with simple string/regex extraction — don't assume fixed values, derive them from whatever OCR returns. Extract the archive with `tar -xzf <archive_path> -C <destdir>` to get `disk_info.log`, then parse its `Device: X, Mountpoint: Y, Type: Z, Options: W` line (e.g. with `sed`/`awk`/regex splitting on `, ` and `: `) and reformat it as `X Y Z W 0 2`, writing that single line to `/app/parsed_fstab.conf`. Build the Python server with `http.server`'s `BaseHTTPRequestHandler` (or Flask) bound to `127.0.0.1` on the OCR-extracted port, implementing `GET /health` returning JSON `{"status": "ok"}`, `GET /key` returning the extracted recovery token as plain text, and `GET /fstab` returning the exact formatted fstab line built in the previous step (read it from the file or keep it in memory — keep both consistent). Launch the server in the background and verify each endpoint with `curl` before finishing, since the verifier will query all three live.