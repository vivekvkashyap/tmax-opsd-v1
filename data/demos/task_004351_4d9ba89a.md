First inspect `/app/formula.png` yourself (open it or run OCR such as `tesseract` if available, or view it directly if your tooling supports images) to read the exact handwritten inequality relating `val_A` and `val_B` — do not guess a formula, extract it precisely since the grading corpus is adversarial and every row in the "evil" fixtures violates it while every row in "clean" fixtures satisfies it. Once you have the exact condition, implement `/home/user/filter.c` to: parse the CSV header and rows (handle the `payload` field which may contain commas/quotes if CSV-quoted), apply the condition as an integer/long comparison to avoid overflow, replace `id` with the literal string `MASKED` for surviving rows, and perform a stable sort by `val_A` (e.g., collect all passing rows into an array with an index, then use a stable sort algorithm or decorate-sort-undecorate with qsort comparing val_A then original index, since C's qsort is not guaranteed stable). Compile with `gcc -O2 filter.c -o filter`. Test against a hand-built clean CSV (values satisfying your extracted condition) and an evil CSV to confirm exact rejection/preservation behavior before relying on it.
