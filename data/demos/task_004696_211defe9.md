In `/app/libauthtoken-1.2.0`, first fix the build: open the `Makefile` and add the missing `-lcrypto` to the linker flags (OpenSSL linking), since the link step currently fails without it. Then open `src/validator.cpp` and locate where the `exp` claim is read/validated; add an explicit bounds check that treats a missing `exp` field as invalid and rejects any `exp` value greater than the max valid Unix timestamp `2147483647` (watch for integer overflow when parsing a huge value like `9999999999` — parse it into a wide-enough integer type, e.g. `int64_t`/`long long`, before comparing, so the overflow itself doesn't silently wrap into a "valid" small number). Rebuild with `make` to produce `token_tool`, and sanity check by validating a token with an oversized `exp`. For `/home/user/audit_wrapper.sh`, call `token_tool validate "$1"` and branch on its output/exit status: if the tool rejects it, status=INVALID; if it accepts it, inspect the decoded subject for injection patterns (`<script>`, `UNION SELECT`, etc. — grep case-sensitively as the spec examples show) and set status=MALICIOUS when found, else VALID. Print the single required `AUDIT_LOG: ...` line with the current Unix timestamp via `$(date +%s)`, and fuzz-test your script against many token variants (valid, expired, missing exp, overflowing exp, XSS/SQLi subjects) to make sure formatting and branching are exact before finishing. Make the script executable.
