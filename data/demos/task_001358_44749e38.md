Determine the video's true frame count first, using a real tool (e.g. `ffprobe -v error -select_streams v:0 -count_packets -show_entries stream=nb_read_packets -of csv=p=0 /app/surveillance.mp4`) rather than guessing, so you can hardcode or precompute it correctly in the server. Write `server.c` as a simple blocking TCP server (socket/bind/listen/accept loop, one connection per command, read until `\n`, respond, close) — for `TIME`, use `strftime` with a `struct tm` obtained via `localtime_r` after ensuring `TZ=Asia/Tokyo` is set in the process environment (setting `TZ` and calling `tzset()`, or relying on the supervisor's exported `TZ`), formatting exactly `%Y-%m-%d %H:%M:%S %Z` so the zone abbreviation prints as JST. For `CRASH`, just call `exit(1)`. Compile with the exact hardening flags given in the task, in the exact order, and verify with `checksec` or `readelf`/`file` that PIE/relro/canary landed. Write `supervisor.sh` as a loop that exports `TZ=Asia/Tokyo`, launches `/home/user/server`, waits on it with `wait $!`, sleeps under 1 second, and relaunches — run it backgrounded and manually `nc`/telnet each command yourself to confirm restart-after-crash before finishing.