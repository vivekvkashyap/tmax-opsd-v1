Work through this as five independent checks that each earn partial credit, so do not let one blocker stop the rest. First, OCR `/app/policy.png` with `tesseract /app/policy.png -` (or pytesseract) and read off the two octal permission modes and the target SHA-256 hex string exactly as printed — do not guess values. Second, `chmod` the stated files in `/home/user/app/` to those exact octal modes. Third, compute `sha256sum` over each file in `/home/user/backups/` and compare against the extracted hash string to find the matching backup, then copy (not move — don't delete backups) it over `/home/user/app/bin/secure_exec`. Fourth, generate a self-signed cert with `openssl req -x509 -newkey rsa:2048 -nodes -keyout server.crt -out server.key ... -subj "/CN=secure-internal.local"` (get the CN exactly right, case-sensitive) into the specified certs path, then apply the OCR'd permission mode to `server.key`. Fifth, `grep -riPl` the source directory for lines matching a password-assignment regex (case-insensitive, `password` followed by `=` and a quoted value) and write matching absolute file paths as a JSON array to the vuln report path — verify manually which files actually contain such a pattern rather than assuming.