Start by extracting the schema text from `/app/schema.png` with `tesseract /app/schema.png stdout` (or view the image directly) to get the exact list of allowed directed edges (Source-[Edge]->Target form) and the max traversal depth — do not assume a schema, read it precisely since OCR can misread edge names. In your Go program, model the schema as a map from (sourceNodeType, edgeName) -> targetNodeType built from the extracted rules. For each JSONL line: parse the `path` array, validate it has odd length >= 1 (alternating node/edge/node/...), validate each consecutive edge triple (node, edge, node) exists in your allowed-edges map matching direction exactly, and validate the edge count (path length - 1) / 2 doesn't exceed the max depth from the schema. Only print the `id` to stdout for paths that pass every check — a single failing hop or an over-length path must produce no output for that line, and malformed/empty paths must also be rejected. Since the grader runs your binary against separate clean and evil JSONL corpora expecting 100% correct classification on both, be careful with edge cases like an empty path array, a path ending on an edge instead of a node, and depth exactly at vs over the limit — test your binary manually against a few crafted lines covering each boundary before considering it done.