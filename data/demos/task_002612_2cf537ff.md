Treat this like fixing a systemd ordering bug by hand: edit `/home/user/deploy.sh` to (1) `tar czf /home/user/backups/deploy_backup.tar.gz -C /home/user deploy` before anything else, (2) kill any existing backend/proxy with `pkill -f` for both process patterns, (3) launch the backend (`python3 -m http.server 8081 ...`) in the background, (4) add a polling wait loop — e.g. `until nc -z 127.0.0.1 8081; do sleep 0.2; done` — that blocks until the port is actually accepting connections instead of using a fixed sleep, and only then (5) start `/home/user/proxy.sh &`. For the CI/CD piece, write a `post-receive` hook in `/home/user/repo.git/hooks/` that reads the ref from stdin, uses `git --work-tree=/home/user/deploy --git-dir=/home/user/repo.git checkout -f` to extract files, then calls `/home/user/deploy.sh`; `chmod +x` the hook. Then clone the repo to `/home/user/workspace`, overwrite `index.html` with the required text, commit, and `git push origin master` to fire the hook. Finally verify with `curl -s http://127.0.0.1:8080` piped into `/home/user/success.txt`.