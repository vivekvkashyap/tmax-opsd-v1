Treat this as black-box + static reverse engineering combined: don't guess from the training corpus alone, and don't guess from the binary alone — cross-validate both. Run `strings /app/target_ids`, `objdump -d /app/target_ids`, and `readelf -a /app/target_ids` to look for embedded literal strings (magic numbers, section names, certificate fields, regex-like patterns) and comparison constants (e.g. hex entry-point values) in the disassembly. Diff files in `training_data/blocked/` against `training_data/allowed/` — for each blocked example, hypothesize which specific byte pattern, ELF field, certificate field, or text substring triggered the block, and confirm that feature is absent from every allowed example. Implement each detection independently in Python (ELF header/entry-point/section-name parsing via manual offsets or `pyelftools`; PEM certificate text extraction for the issuer field; a bounded-proximity substring/regex scan for the injection pattern) and OR the results, printing exactly `BLOCKED` or `ALLOWED`. Test against every file in both training directories before considering it done — full agreement there is a floor, not a guarantee, for the hidden eval corpus.