Work in three clearly separated steps and verify each before moving on. First, edit `client.c`: change only the IP string passed to `inet_pton` to `127.0.0.1` and the port passed to `htons` to `8080`, matching the server's bind address/port exactly (a common naive mistake is changing only one of the two). Second, write `preflight.c` using `getgrnam("deployers")` plus `getpwuid(getuid())`/`getgrouplist` (or checking `getgroups()`) to confirm current-user group membership, and `statvfs("/home/user/deploy", &buf)` computing `buf.f_bavail * buf.f_frsize >= 1000000` — print "PREFLIGHT OK" and `exit(0)` only if both pass, else `exit(1)`. Third, write `pipeline.sh`: compile preflight first and `|| exit $?` immediately after running it, then compile server/client into `/home/user/deploy/`, launch the server with `&` in the background, `sleep 1`, run the client redirecting stdout to `result.log`, then kill the background server PID (e.g. `kill $SERVER_PID`) for a clean shutdown. Make the script executable with `chmod +x` and test the whole pipeline end-to-end, checking `result.log` contains the expected connection message.