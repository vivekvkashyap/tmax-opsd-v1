Break this into independent, testable stages before wiring them together. (1) Run `tesseract /app/legacy_dashboard.png stdout` and inspect the raw text output to locate the `API_SALT:` line; trim whitespace carefully since OCR often leaves trailing spaces or stray characters — verify by printing the extracted string with repr()/quotes so you can spot hidden whitespace. (2) For the LZC format, write a small Python script that opens the file in binary mode, reads and checks the 4-byte `LZC1` magic, then loops reading a 4-byte big-endian unsigned int (`struct.unpack(">I", ...)`) as chunk length, reads that many bytes, and feeds them to `zlib.decompress()`, concatenating results across chunks (don't assume a single chunk — the fixture is deliberately split into multiple). (3) Parse the decompressed text with a regex or manual state machine keyed on the `===CONFIG_CHANGE===` / `---` / `===================` delimiters, capturing Time/User/Module as a header block and every line starting with `+`/`-` until the closing delimiter as the changes list — don't assume exactly one diff line. (4) Build the HTTP server (Flask/http.server) with a route handler that checks the `Authorization` header against the exact recovered salt string before returning data, returns 403 on mismatch/missing, and returns `[]` for unknown modules; sort matched records by `Time` ascending before responding. Test each stage independently with print statements before combining, and leave the server running as a background process.