Start by diagnosing the vendored library build failure directly: read `/app/vendored/statslib/Makefile` and its `gen.py` script to see exactly what environment variable it checks for and what it exits with if unset — reproduce the failure by running `make` as-is, read the error, then run it again with that variable set correctly (rather than guessing) so `tables.go` gets generated and the package compiles standalone (`go build`) before you integrate it. Then scaffold `/home/user/ml-api` as its own Go module and add a `replace` directive in `go.mod` pointing the import path `github.com/local/statslib` at the local vendored directory, so `go.mod`/`go.sum` resolve without network access. Implement `main.go` as an HTTP server on `127.0.0.1:8080` with `POST /api/v1/prepare`: check the `Authorization: Bearer train-token-99` header first and reject otherwise; then validate the JSON body strictly — every row must have exactly the three keys `feature_1`, `feature_2`, `target`, all numeric, rejecting extra or missing keys or non-numeric values with 400 and the exact error body given; extract `feature_1` values and call `statslib.CalculateCI`; compute `regression_ready` from the CI bounds per the given thresholds; return the exact JSON shape specified. Compile, launch the binary in the background, and curl your own endpoint with valid/invalid auth and schema cases to confirm behavior before finishing.
