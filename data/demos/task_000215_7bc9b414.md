Tackle this as several independent fixes validated one at a time, then integrate. First OCR the spec image with `tesseract /app/design_spec.png stdout` (try `--psm` variants if text is garbled) to recover the exact priority-score formula, and implement it literally in `src/scoring.cpp` — do not guess at the weighting constants, read them from the OCR output. Second, fix cycle handling in `src/graph.cpp`: the infinite loop/leak comes from a DFS or BFS traversal that doesn't track visited nodes per traversal path, so add a visited-set (or color-marking: white/gray/black) check that detects a back-edge to a gray node as a cycle and returns/breaks instead of recursing forever, and make sure every heap-allocated node is freed on all exit paths (or better, use smart pointers) — validate with `valgrind --leak-check=full` and/or ASAN on a deliberately cyclic input like `A->B,B->A` before trusting the fix. Third, add a `TARGET_ARCH` CMake option that conditionally sets `CMAKE_CXX_COMPILER` to `aarch64-linux-gnu-g++` when `-DTARGET_ARCH=aarch64` is passed. Fourth, implement the `POST /analyze` httplib endpoint parsing comma-separated `A->B` edges into the same graph/scoring logic, returning JSON with per-node scores and a cycle boolean. Only after each piece works standalone, build the final `analyzer_cli` binary and diff its output against your own hand-traced expectations on varied edge lists (including cyclic and empty cases) to make sure formatting and score values are stable and reproducible before considering it done.
