Set up a Go module first (`go mod init`, then `go get github.com/mattn/go-sqlite3`) since CGO-based SQLite drivers need the module and dependency resolved before they'll build; ensure a C compiler is available since this driver uses cgo. In the Go program, open the DB with `database/sql` + the sqlite3 driver, then run `CREATE INDEX IF NOT EXISTS idx_backups_parent ON backups(parent_id);` as a separate `Exec` before the main query. For the traversal, write one recursive CTE: the anchor selects the row where `id = 'full_alpha'` with `depth = 0`, the recursive part joins `backups b ON b.parent_id = anchor.id` incrementing depth by 1; compute `running_total_bytes` as a window function `SUM(size_bytes) OVER (ORDER BY depth)` applied over the CTE's result set (not inside the recursive step itself, since SQLite doesn't allow window functions inside recursive CTEs). Scan rows into a slice of structs matching the exact JSON field names and types (`backup_id`, `depth`, `size_bytes`, `running_total_bytes`), then `json.MarshalIndent` and write to `/home/user/chain_summary.json`. Run the compiled program once so both the index creation and file write actually execute against the real database, then verify the index with a `sqlite3` query and re-check the JSON field order/types match the schema.
