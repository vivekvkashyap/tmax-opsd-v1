Write a single-pass C parser: read `/home/user/raw_data.csv` line by line with `fgets`/`fscanf` or manual splitting on commas, watching for the empty-`sensor_val` case (two consecutive commas) which must parse as "missing" rather than 0.0 — track missing rows separately. First pass: sum only the present `sensor_val` values and divide by their count to get the mean; then build an "imputed" array where missing entries are replaced by that mean. Second pass: compute population variance as `sum((x-mean)^2)/N` over the imputed array (N, not N-1) and take its sqrt for std_dev. Filter: keep rows where `fabs(imputed_val - mean) <= 2.0*std_dev` (note: the spec says "strictly greater than" is removed, so the boundary is inclusive of equality — double check this against the exact wording before finalizing the comparison operator). Tokenize `text_note` by counting maximal runs of non-space characters (equivalent to `strtok` on spaces, collapsing repeats) — an all-space or empty note is 0 tokens. Compute `fold = id % 3` per surviving row. Write `clean_features.csv` with `%.4f` formatting for the sensor value, and `experiment_log.txt` with the four required lines using the exact original row count, mean, std_dev, and surviving-row count, each formatted to 4 decimals where applicable — the verifier does an exact text diff, so match formatting precisely (no trailing whitespace, exact labels).
