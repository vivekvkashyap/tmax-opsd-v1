First extract the frames' color values: use `ffmpeg` or OpenCV/PIL in a small script to decode `/app/sensor_feed.mp4` frame-by-frame, average each frame's R, G, B channels (round to nearest int), and record `(frame_idx, R, G, B)` for every frame — drop any frame where R (node_id) is 0, since those are filler/background frames. Load the surviving rows into SQLite at `/home/user/sensor_data.db` in a `readings(frame_idx, node_id, parent_id, value)` table. For the Go analysis, avoid the old cross-join approach entirely: write a recursive CTE (`WITH RECURSIVE ancestors AS (...)`) that walks up `parent_id` links per node to determine each node's depth (root depth 0), then compute the "path-smoothed value" per reading as `value + depth`. Apply a window function (`AVG(...) OVER (PARTITION BY node_id ORDER BY frame_idx ROWS BETWEEN 2 PRECEDING AND CURRENT ROW)`) to get the 3-frame rolling average of that smoothed value per node. Use Go's `database/sql` with a SQLite driver to run this query and write `/home/user/results.csv` with headers `frame_idx,node_id,rolling_avg`, formatting the average to 2 decimal places. Test against a small synthetic case before trusting the full run.