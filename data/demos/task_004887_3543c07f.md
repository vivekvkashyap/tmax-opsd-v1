Build the C++ program in clear stages and test incrementally rather than writing the whole pipeline blind. For telemetry_a, parse each ts_iso strictly against the ISO8601 pattern (e.g. using `std::get_time` with `%Y-%m-%dT%H:%M:%S` after manually stripping/validating the trailing 'Z' and the '.mmm' fraction) — any row that doesn't match this shape (missing 'T', missing 'Z', garbage text) must be dropped and counted as invalid; truncate (don't round) the fractional seconds into whole epoch seconds. For telemetry_b, integer-divide `ts_unix_ms` by 1000 to truncate to epoch seconds. Use a `std::map<std::pair<long,std::string>, {sum_temp,count_temp,sum_ram,count_ram}>` keyed by (epoch_sec, machine_id) to accumulate sums separately per metric, since a group may only have one of the two metrics present — average only over the metrics that actually appeared, and emit 0.0 for the metric with zero count. Write merged_output.csv sorted deterministically (e.g. by epoch_sec then machine_id) with averages formatted to exactly 1 decimal place (use `std::fixed << std::setprecision(1)`), and no header. Emit processing_log.json with the four required integer counters, matching your actual parse/group counts exactly. Finally create the SQLite schema and bulk-load with `sqlite3 telemetry.db ".mode csv" ".import merged_output.csv merged_telemetry"` — verify the import didn't treat the file's absence of header as an extra row.