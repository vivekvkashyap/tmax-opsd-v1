Decode the optical stream first: use OpenCV or `ffmpeg` to iterate every frame of `traffic_pulses.mp4`, sample the center pixel's brightness (average of its RGB channels, or convert to grayscale), and classify each frame as bit `1` if brightness > 127 else `0`, in frame order. Group consecutive bits into bytes of 8, big-endian, convert each byte to its ASCII character, and concatenate to reconstruct the JSON string — verify it parses as valid JSON before moving on, since an off-by-one frame or wrong threshold will produce garbage. For each `{id, encoded_payload}` entry, hex-decode then base64-decode the payload to get a JSON object with a `filename`. Because the attacker uses single and double URL-encoding to hide `../`, don't just regex the raw string — iteratively URL-decode (`urllib.parse.unquote`) until the string stops changing (a fixed point), then check the fully-decoded string for `../`, `..\\`, and equivalent traversal patterns. Write `/home/user/results.json` mapping each id (as a string) to `1` (malicious) or `0` (benign), matching the required accuracy threshold of 0.95 against the hidden ground truth.