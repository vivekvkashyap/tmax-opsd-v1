Fix the dependency before writing any application code. Run `make` in `/app/cJSON-1.7.15` and read the actual error; a vendored package's Makefile can have a simple typo'd tool name (e.g. the compiler variable pointing at a binary that doesn't exist) — find and correct that line, then confirm `libcjson.so`/`.a` actually get produced before building anything against it. For the server, write plain C using `sqlite3` (`-lsqlite3`) to query `customers`, and implement the two masking functions carefully: email masking keeps only the very first character of the local part before `@`, replaces the remainder of the local part with literally `***`, and preserves the domain unchanged; credit-card masking replaces every digit except the last four with `X` (don't touch separators if present, and don't just fix a length assumption — walk from the end). Build the JSON array with cJSON, not manual string concatenation, to avoid escaping bugs. For the TCP layer, use a standard blocking `socket`/`bind`/`listen`/`accept` loop on `127.0.0.1:8000`; read the client's line, compare it exactly against `RUN_ETL SECRET_TOKEN_42\n`, and on match perform the query+mask+serialize and write the JSON body followed by `\nEND\n`, otherwise write `ERROR\n`. Compile with both `-lcjson` (with `-L`/`-I` pointed at the vendored build dir) and `-lsqlite3`, then leave the compiled server running in the background and test it yourself with `nc` or a small client before finishing.
