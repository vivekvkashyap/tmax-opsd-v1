Use `/app/legacy_resolver` purely as a black-box oracle to generate test cases and check your own output against it — run it on many hand-crafted and randomly generated dependency graphs (nested parens, pre-release SemVer edge cases like `1.0.0-alpha` vs `1.0.0`, and cyclic graphs) and diff your binary's output against its output for each, rather than trying to reverse-engineer its internals. Fix the three named bugs at their root: rewrite the recursive-descent expression parser so it correctly recurses into arbitrarily nested `(...)` groups (a common bug is not consuming the matching close-paren correctly, or not making the "primary" rule call back into the top-level expression rule); implement full SemVer 2.0.0 precedence rules including comparing pre-release identifiers (numeric vs alphanumeric identifiers compare differently, and a pre-release version is always lower than the same version without one); and use a proper cycle-detection algorithm (Tarjan's SCC or a three-color DFS with a visited/in-progress/done state) instead of whatever ad hoc marking causes infinite loops. For the 20x speedup requirement, avoid the legacy binary's artificial slowness pattern in your own code — profile with `-O2`/`-O3`, avoid repeated string reallocation, and use efficient data structures (hash maps for label lookup, not linear scans) since the hidden test graphs are large. Compile with C++17/20, output to `/home/user/fast_resolver`, and benchmark both binaries yourself on a large synthetic graph before considering it done.