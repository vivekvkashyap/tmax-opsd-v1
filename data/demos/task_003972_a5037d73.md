Don't guess the archive password — transcribe `/app/voicemail.wav` with a speech-to-text tool (e.g. `whisper`, or `ffmpeg`+an STT library if whisper isn't available) and read the spoken phrase directly out of the transcript, then use it verbatim with `unzip -P "<transcribed phrase>" /app/evidence.zip -d /home/user/evidence/`. For the scanner, avoid executing or `ldd`-ing untrusted binaries entirely — use `readelf -h` to confirm a 64-bit ELF (`Class: ELF64`), `readelf --dyn-syms` or `objdump -T` to check for `system`/`popen` in the dynamic symbol table, and `strings` (or `grep -a`) to check for the literal `HTTP_USER_AGENT` substring. Flag a file only when all three conditions hold, and print just its basename per line. Since the score is F1 against a large hidden corpus, be conservative about false positives/negatives: test your script against the local `/home/user/evidence/` binaries first, hand-verify a few classifications with the same three tools manually, and make sure the script degrades gracefully (skips, doesn't crash) on malformed/non-ELF files rather than erroring out.