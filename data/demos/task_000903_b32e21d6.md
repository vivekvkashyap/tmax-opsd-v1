Work through this vendored-package task in strict order: build, then implement, then run. First inspect the `Makefile`/`setup.py`/`requirements.txt` under `/app/wal-config-server` for the install-breaking typo (a `pip install` invocation missing a required flag, e.g. `-r`, so it tries to install a package literally named "requirements.txt") and fix that before anything else will install. Then open `wal_server.py` and complete the two endpoints: `/append` must decode the raw request body from `CP1252` and re-encode to strict UTF-8 before appending a `{"key":..., "value":...}` JSON line to `/app/data/config.wal` (create the directory first); `/commit` must read all WAL lines, fold them into a dict keyed by `key` where later lines overwrite earlier ones, write that dict as JSON to a temp file and `os.rename()` it onto `/app/data/snapshot.json` (atomic replace, not a direct write), tar+gzip that single file into `/app/data/snapshot_<unix_timestamp>.tar.gz`, point/repoint a symlink `/app/data/current.tar.gz` at the new archive (remove old symlink first if it exists), then truncate the WAL file. Finally launch the fixed server on `127.0.0.1:9090` in the background and exercise both endpoints once to confirm the on-disk state matches before finishing.
