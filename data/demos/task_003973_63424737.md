Reverse-engineer the binary before writing any orchestration code. Run `strings /app/restore_agent` to find the required environment variable name and its hardcoded expected value (look for an env-var-like all-caps identifier paired nearby with a short token string), then export it. For the TCP handshake, run `strace -f -e trace=network,read,write ./restore_agent ...` (or `strace -s 200`) while a placeholder `nc -l 127.0.0.1 7777` is listening, to observe exactly what bytes the binary sends and what response it blocks waiting for — replicate that exact string as your health daemon's expected-read and acknowledgment-write in a small Python `socket` server, logging its activity to `/home/user/restore_drill.log`. Start the daemon, set the env var, then run `restore_agent` with `/home/user/restored_data/` as its output directory argument and confirm `manifest.json`/other files appear. Compute the Basic Auth password with `hashlib.md5` (or `md5sum`) over the restored `manifest.json` bytes exactly. Build `server.py` with `http.server` + a custom handler enforcing Basic Auth for user `backup_admin`, then launch it backgrounded (e.g. `nohup ... &`) so it stays reachable for verification.