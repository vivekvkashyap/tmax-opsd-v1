Build this pipeline in dependency order and verify each stage in isolation before wiring the next. First write the SMTP sink using Python's `aiosmtpd` or a minimal `smtplib`-compatible server bound to 127.0.0.1:8025 that, on message receipt, writes the raw RFC822 bytes to `/home/user/emails/<timestamp>.eml`; wrap it as a systemd --user unit, `systemctl --user daemon-reload`, then enable/start it and confirm with `systemctl --user status` and a manual `socket`/`curl`-style connectivity check before moving on. Next init the bare repo and add a `post-receive` hook (executable, shebang, using `curl -X POST http://127.0.0.1:8080/deploy`). For the vendored webhook-notifier package: read server.py carefully — the SMTP_PORT env var is read as a string and passed straight into `smtplib.SMTP(host, port)`, which requires an int, so wrap it in `int(...)`. Separately, open the .service file and add `After=smtp-sink.service` and `Requires=smtp-sink.service` under `[Unit]` so ordering/dependency is enforced. Install both units under `~/.config/systemd/user/`, enable+start, and only then simulate a git push end-to-end to confirm the backup tarball and .eml both land.
