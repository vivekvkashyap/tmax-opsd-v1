Two separate bugs, fix them independently and verify each before moving on. First, get it building: run `cargo build --release` in `/app/welford_cli`, read the linker error, and inspect `build.rs` for a `println!("cargo:rustc-link-lib=...")` directive that forces linking against a library that doesn't exist on this system — remove or correct that directive so linking succeeds without changing any real dependency. Second, fix the math: the current accumulator likely tracks `sum` and `sum_sq` and derives variance as `(sum_sq - sum*sum/n)/(n-1)`, which is numerically unstable (catastrophic cancellation) for large streams of big-magnitude floats. Replace it with Welford's online algorithm — maintain running `mean` and `m2`, updating incrementally per new sample using the delta-before/delta-after trick, and derive sample variance from `m2/(n-1)`. Keep the exact `<count>,<mean>,<variance>` per-line stdout format unchanged. After fixing both, `cargo build --release` and copy/symlink the resulting binary to `/home/user/fixed_welford_cli`. Validate by piping the same random f64 stream into both your binary and the reference and diffing stdout byte-for-byte before considering it done.