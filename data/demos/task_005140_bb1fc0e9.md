Chain this as discrete, verifiable bash stages rather than one monolithic script. First, deduplicate by content hash: compute `md5sum /home/user/incoming/*.dat`, group by hash, and for any hash with multiple files keep only the alphabetically-first filename, deleting the rest with `rm` (sort filenames within each hash group before picking). Second, concatenate/filter the surviving `.dat` files for `DEV01` rows only (`awk -F, '$2=="DEV01"'`). Third, bin readings into 5 fixed 3600-second intervals starting at the given epoch: compute each record's interval as `(timestamp - start_epoch) / 3600` (integer division), then for each interval index 0-4 take the max `MetricValue` seen (awk associative array keyed by interval, tracking max). Fourth — and this is where naive attempts fail — after computing maxima only for intervals that actually have data, iterate index 0 through 4 in order and forward-fill: if an interval has no entry, copy the previous interval's (already-filled) value rather than leaving it blank or zero, so gaps propagate the last known max. Write the final 5 lines as `IntervalIndex,MaxValue` with no header, sorted by index, to `/home/user/processed_dev01.csv`, and manually check row count is exactly 5.