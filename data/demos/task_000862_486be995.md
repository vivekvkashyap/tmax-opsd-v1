Treat `/app/legacy_engine` as a black box to probe empirically, not to disassemble line-by-line: generate small synthetic `nodes.csv`/`edges.csv` fixtures with a handful of distinctly-valued nodes and edges of different `node_type`/`relationship_type` combinations, then vary offset/limit and diff outputs to infer (a) which relationship-type chain constitutes the matched pattern, (b) what weight/type filters are applied at each hop, and (c) the exact multi-column sort order and direction — test one variable at a time (e.g., fix the graph and only change one node's weight near a suspected threshold) to isolate filter boundaries precisely. Once you have a hypothesis, write more targeted probe graphs to confirm sort tie-breaking order and pagination semantics (does offset/limit apply before or after sort — it should be after). Only then implement `/home/user/query_engine.cpp` with adjacency lists / hash maps for O(1) node lookups, replicate the exact filter and comparator logic, compile with the same optimization flags you'll be judged on for speed, and re-run your probe fixtures through both binaries to confirm byte-identical stdout before considering it done.
