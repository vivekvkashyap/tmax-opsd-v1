Start by reproducing the failure exactly: run `pytest fuzz_test.py -x` and confirm it fails with NaN/Inf/explosion, so you have a before/after check. Read `biquad.py`'s `process` method carefully — the state variables (`x1,x2,y1,y2`) and the intermediate accumulation are all forced into `np.float32` via `dtype=np.float32` casts and an explicit `np.float32(...)` wrap around the IIR recurrence; for a marginally-stable low-frequency resonant filter (poles near the unit circle, e.g. a1≈-1.98) this loses enough precision in the feedback loop to blow up. Fix by widening the internal accumulator and state variables to `float64` throughout `process` (input cast, `output` array dtype, `x1/x2/y1/y2` types, and dropping the float32 wrap on `y0`) while leaving the actual difference-equation math (the b0/b1/b2/a1/a2 coefficients and recurrence order) untouched — you must not change the transfer function, only its numeric precision. Re-run the fuzz test until it passes cleanly, then run `apply_filter.py` on `/app/test_audio.wav`, writing to `/home/user/filtered.wav`, and sanity-check the output isn't clipped/silent before considering it done.
