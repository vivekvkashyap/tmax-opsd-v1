Fix the C bug minimally: in `decode_stream`, before `in_idx += 3` on seeing `0xFF`, check `if (in_len - in_idx < 3) { break; }` so it never reads past `in_len`. For the Rust parser, use an explicit state machine over the decoded byte slice with an index cursor: scan forward until you find `0xAA` (discarding non-header bytes), then read type, length `L`, and require `L` payload bytes plus 1 checksum byte are actually available before consuming (bounds-check every read). Compute the checksum by XOR-folding the type byte, length byte, and payload bytes (header excluded), compare to the trailing checksum byte; on mismatch, drop this candidate and resume the header search from the next byte, not the assumed packet end. For Type 1, read 4 payload bytes big-endian via `u32::from_be_bytes`; for Type 2, `String::from_utf8`. Serialize matches with `serde_json` in the exact given shape. For architecture detection, use `#[cfg(target_arch = "aarch64")]` / `"x86_64"` conditional functions, and verify with `cargo build --target aarch64-unknown-linux-gnu`.
