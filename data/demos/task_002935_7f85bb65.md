Load the CSV carefully to avoid the type-coercion trap the task warns about: read with pandas using `dtype=str` (or read raw and parse manually) so missing ratings stay as empty strings rather than triggering an implicit float cast of `product_id`; only after dropping rows with empty/missing `rating` should you cast `product_id` and `rating` to `int` explicitly. Build per-product keyword sets by concatenating each product's review_text, lowercasing, stripping punctuation with a regex like `[^a-z0-9\s]`, and splitting on whitespace into a `set()`. For recommendations, compute Jaccard similarity (`len(intersection)/len(union)`) between product 101's set and every other product's set, sort descending by similarity with ties broken by ascending product_id, and take the top 3 — implement the tie-break explicitly in your sort key rather than relying on stable-sort side effects. For the hypothesis test, split products into two groups by whether `"excellent"` is literally in their keyword set, then run `scipy.stats.ttest_ind(group1_ratings, group2_ratings, equal_var=False)` for Welch's test and round the p-value to 4 decimals with `round(p, 4)` before writing. Print intermediate group sizes and similarity scores while developing to sanity-check before finalizing outputs.
