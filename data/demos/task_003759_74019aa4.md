First recover the lambda value: run OCR (`tesseract /app/settings.png -`) on the image and read the integer it reports — don't hardcode a guess. Then implement `/home/user/normalize_graph.sh` purely with awk/bash: read the whole matrix via awk, compute each row's sum (the degree) while storing all entries in a 2D awk array, then in the END block emit, for row i col j, `rowsum[i] - a[i][j] + lambda` when i==j (this folds -A[i][i] and +lambda into the diagonal in one step) and `-a[i][j]` otherwise, space-separated per row. Keep it purely awk/bash (no python/R/compiled code) since the grader checks tool usage as well as output. Test manually against the worked example in the task description before trusting it, then fuzz-test yourself with a few random small matrices to sanity-check edge cases like a single-row/1x1 matrix and rows with trailing whitespace. Finally `chmod +x` the script and confirm it reads from stdin and writes exactly one row of integers per input row, matching dimensions.
