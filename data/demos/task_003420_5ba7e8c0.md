Fix the vendored library first, then build the classifier on top of a working import. Open `/app/vendor/archivetool/archivetool/core.py` and find the module-level line that does a hard `os.environ['SOME_VAR']` lookup — replace it with `os.environ.get('SOME_VAR', <sane_default>)` so import no longer raises `KeyError` with no env var set, then `pip install -e /app/vendor/archivetool` (or equivalent) and confirm `from archivetool.core import verify_archive` succeeds cleanly in a fresh shell with no env vars set. For the sanitizer, first determine "is this an archive" (e.g. by extension/magic bytes like a zip local-file-header signature) — if so, wrap `verify_archive(filepath)` in try/except and exit 1 on `False` or any exception, exit 0 only if it returns truthy. For non-archives, don't assume UTF-8: try decoding as UTF-8 first, then UTF-16 (check for a BOM), and fall back to ISO-8859-1 (which never fails to decode any byte sequence) as a last resort, then search the decoded text for the literal substring `[MALICIOUS_MACRO]` to decide safe/malicious. Test against both a clean and malicious sample of each encoding before considering it done, since the verifier is an adversarial corpus that must hit 100%.
