Since the binary is stripped, black-box it rather than trying to fully disassemble: craft small synthetic HTTP requests with an `X-Auth-Session:` header containing varied `user=;role=;mac=` combinations, pipe each through `/app/auth_checker` (via stdin or as a file arg), and observe exit codes to infer the parsing/validation logic — pay special attention to what happens when you duplicate a key (e.g. two `role=` entries) versus a single one. Cross-reference by diffing sample files in `/app/traffic/clean/` vs `/app/traffic/evil/` (e.g. `grep -o 'role=[a-zA-Z]*' file` on each) to spot the structural pattern that separates them — the evil samples exploit a mismatch between which occurrence of a duplicated key the MAC check validates versus which occurrence the authorization logic actually uses (a parameter-pollution style bug), so look for headers with a repeated `role=` key rather than any single value. Build `/home/user/waf.py` to parse the `X-Auth-Session` header's semicolon-delimited pairs and flag requests where a key (particularly `role`) appears more than once as `EVIL` (exit 1), otherwise `CLEAN` (exit 0). Validate against both corpora directly (loop over every file in clean/ and evil/, run your script, check exit codes) before submitting, and keep detection general enough to survive an unseen holdout rather than overfitting to the 100 sample filenames/values.
