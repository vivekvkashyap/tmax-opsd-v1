Work top-down through extraction, computation, then serving, validating each stage before moving on. First OCR `/app/data_table.png` with `tesseract` and manually clean the result into a proper CSV with header `CPU,RAM,Disk,Network,Failure` — OCR often mangles decimal points or commas, so print the parsed rows and sanity-check numeric ranges before trusting them. In the Go program, compute the sample covariance matrix (dividing by N-1) over the four continuous columns in the specified column order, formatting each cell to exactly 4 decimal places, comma-separated, 4 rows — write a quick unit check (e.g. verify the matrix is symmetric) before wiring it into the server. Implement logistic-regression inference using the given fixed weights/bias exactly as provided (don't refit a model — these are supplied, not learned), applying the sigmoid and thresholding at 0.5. For the HTTP server on 127.0.0.1:8080, implement `GET /accuracy` and `POST /predict` per the JSON contracts given, and add middleware/checks that reject any request lacking the exact `Authorization: Bearer secret-token-99` header with a 401 before reaching the handler logic. Build and background the server, then hit both endpoints yourself with `curl` (with and without the auth header) to confirm behavior before finishing.