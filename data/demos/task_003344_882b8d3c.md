Work through this in three isolated stages and verify each before moving on. First, transform the legacy manifest: use `sed`/`awk` with a regex or field-split on ` | ` and the `: ` separators to pull filename, hash, date out of each `Artifact: ... | Hash: ... | Date: ...` line, then print `filename,hash,date` as the header followed by comma-joined fields — test on one line with `echo | awk` before running over the whole file. Second, write the Python chunker: open each `.bin` file in binary mode, `read(5242880)` in a loop until empty, write each chunk to its own file with a zero-padded `%03d` suffix, and hash with `hashlib.sha256` incrementally (update per chunk, and separately hash the whole original file) — build the nested dict exactly as specified and `json.dump` it. Watch two common mistakes: off-by-one in the chunk index/count for files that divide evenly, and hashing the chunk after concatenation instead of hashing the actual bytes written to disk. Third, for restoration, concatenate `app_v2.bin`'s chunks in numeric order (sort by the zero-padded index, not lexicographically-by-string if names could misorder) into `restored/app_v2.bin`, then sha256 it and write only the hex digest, no newline padding issues, to `restored_hash.txt`.
