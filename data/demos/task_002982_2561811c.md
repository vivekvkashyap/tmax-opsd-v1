Tackle the three defects separately and validate each in isolation before running the full benchmark. First fix the Makefile's link line — a gRPC/Protobuf C++ build needs its libraries listed after the object files (e.g. `-lgrpc++ -lgrpc -lprotobuf -lpthread`, plus whatever `pkg-config --libs grpc++ protobuf` reports); build with `make` and confirm the binary links cleanly. Second, implement RLE by scanning the string and counting consecutive identical characters, emitting `<char><count>` for each run (careful with runs >9 and case sensitivity), then Base64-encode that RLE string (a small helper function or a header-only base64 implementation is fine) — test this against a few known inputs before wiring it into the RPC handler. Third, remove the coarse global `std::mutex` around the whole RPC body; since encoding a single request is a pure, thread-local computation with no shared mutable state, the lock is unnecessary entirely — delete it (or scope any genuinely shared resource behind a much narrower lock) so requests can execute concurrently. Start the server on port 50051, then run `/app/benchmark_client` and redirect its output to `/home/user/benchmark_results.log`; if RPS is below threshold after these fixes, look for other serialization/blocking points (e.g., logging on every request, single-threaded server) rather than just tuning the lock further.