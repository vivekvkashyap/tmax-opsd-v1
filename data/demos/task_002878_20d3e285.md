Everything must happen inside one idempotent bash script — don't fix files by hand first, since the grader runs the script itself. Use `sed` with a precise pattern to replace the exact unsafe line `strcpy(buf, input);` in `/home/user/ws_grpc_proxy/c_ext/validator.c` with the two-statement safe replacement `strncpy(buf, input, 31); buf[31] = '\0';` (watch escaping of the null-byte literal and semicolons inside the sed expression — test the sed command standalone against a copy of the file first to confirm it produces valid C). Similarly `sed` the `syntax = "proto2";` line in `service.proto` to `"proto3"`. After patching, `cd` into the project and run `cargo build`, then launch the built binary in the background (`./target/debug/ws_grpc_proxy &` after a build, or `cargo run &`), add a short `sleep` (a few seconds) to let the TCP listener on `127.0.0.1:8080` come up, then issue a `curl` with the exact WebSocket upgrade headers specified (`Connection: Upgrade`, `Upgrade: websocket`, the given `Sec-WebSocket-Key`, `Sec-WebSocket-Version: 13`) using `curl -i` so headers are captured, and redirect that output to `/home/user/ws_test.log`. Make the script executable and run it once yourself end-to-end to confirm the log contains the `101 Switching Protocols` line before considering it done.
