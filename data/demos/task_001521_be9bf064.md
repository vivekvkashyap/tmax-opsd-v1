Work in two clearly separated phases: build repair, then behavior wrapping. For the build: run `make build` in `/app/rbac_engine-0.1.0`, read the exact error — a "missing separator" error on the build target means the Makefile recipe line uses spaces instead of a leading tab; open it in an editor that shows whitespace (or `cat -A Makefile`) and replace the indentation with a literal tab character. Also scan the Makefile for hardcoded environment/config values that look suspicious (e.g. a strictness flag set to a disabling value) — inspect how that variable is consumed in the Rust source to judge whether it should be enabled instead, then fix and rebuild with `cargo build --release` or `make build` until `target/release/rbac-cli` exists. For the wrapper: read `rbac-cli`'s `--help`/source to learn its CLI args and exit-code/output conventions for allow vs deny, then write `/home/user/audit.sh` to call it against `/app/schema/system_graph.yaml` and the JSON file passed as `$1`, mapping its result to exactly `ALLOW` or `DENY` on stdout with exit 0. Test against sample files in `/app/corpora/clean/` and `/app/corpora/evil/` before finishing, since a subtle graph-traversal misconfiguration will make evil requests pass silently.