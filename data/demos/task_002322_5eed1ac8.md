Attack the backend build first: run `make` in `/app/backend` and read the linker error carefully — undefined references to math functions (e.g. `pow`) mean the `-lm` flag is missing, and undefined references to pthread symbols mean `-lpthread` is missing; add whichever flags the specific error message points to in the Makefile's link step, then rebuild until it succeeds. Once it links, start both services via `/app/start.sh` and confirm with `curl` or `nc` that ports 8080 and 9090 respond. Next, diagnose the transformation bug empirically: POST `/app/sample_in.txt` to `http://127.0.0.1:8080/transform`, save the response, and `diff` it against `/app/sample_out_expected.txt` to see exactly where output diverges (e.g. an extra trailing character or a missing final run) — this points at an off-by-one in the C run-length-encoding loop's bound (`<=` vs `<` on the length), which you should fix in the source and rebuild/restart the backend. Verify the fix by re-running the same curl+diff comparison until it's empty. Finally, write `/home/user/test.sh`, make it executable, and have it curl the sample input, capture the body, and `diff -q` it against the expected output file, exiting 0 on match and 1 otherwise — leave both services running in the background when you finish.