Work the three phases separately and verify each before moving on. Phase 1: transcribe `/app/intercept.wav` using an available speech-to-text tool (e.g. `whisper` if installed, or another ASR utility) rather than guessing — listen to/inspect the transcript output carefully to extract the spoken password (it may be spelled out phonetically, e.g. NATO-alphabet-style letters plus digits, so map words to characters/digits deliberately), then use that recovered password with `openssl rsa` (or `ssh-keygen -p`) to decrypt `/app/compromised_id_rsa` into `/home/user/decrypted_id_rsa`, and `chmod 600` it. Phase 2: inspect `/app/auth_daemon` with `strings`, `objdump -d`, or a disassembler to find how it reads the `cmd_exec` and `user_profile_xss` JSON fields and what bypass condition (e.g. an `alg`/flag value) skips signature checks — this tells you which payload shapes are "malicious." Phase 3: write a C++ sanitizer that parses JSON from stdin (nlohmann/json) and rejects (exit 1) if `cmd_exec` or any field contains shell metacharacters (`;`, `|`, `&`, `` ` ``, `$`, `>`, `<`) or if `user_profile_xss` contains HTML/script markers (`<script`, `javascript:`, `<img`, `onerror=`, etc.); otherwise exit 0. Test against `/app/corpora/evil` and `/app/corpora/clean` directly, iterating on your filter rules until you hit 100% on both sets.
