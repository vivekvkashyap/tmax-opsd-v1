Treat this as four independent sub-deliverables. (1) Run `tesseract /app/evidence.png stdout` (try `--psm` variants if misread) to recover the 6-digit PIN as text — don't hardcode a guessed number. (2) Use that PIN with `unzip -P <pin> /app/malware.zip`, then `strings exfil.bin | grep -i EXFIL_PATTERN` (or disassemble if not plaintext) to recover the exact `EXFIL_PATTERN: <regex>` and use that literal regex, not an invented "credential-like" pattern. (3) `ssh-keygen -t ed25519 -f /home/user/.ssh/audit_id_ed25519 -N ""`, then write `/home/user/.ssh/config` with a `Host *` block setting `IdentityFile`, `PasswordAuthentication no`, and `Ciphers chacha20-poly1305@openssh.com`. (4) In `/home/user/sanitizer.py`, read the path from `sys.argv[1]`, `re.sub` the recovered credential regex to `[REDACTED_CRED]`, and use a DOTALL regex anchored on `-----BEGIN/END OPENSSH PRIVATE KEY-----` to replace whole key blocks with `[REDACTED_KEY]`; make sure it doesn't touch unrelated text. Test against `/app/corpus/clean/` (byte-identical output) and `/app/corpus/evil/` (fully redacted) before finishing.
