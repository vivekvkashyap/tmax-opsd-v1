Break this into independent, verifiable stages rather than writing one monolithic Go program. First extract frames with `ffmpeg -i /app/traffic.mp4 -vf fps=1 frame_%03d.png` (or similar), then for each frame decode it in Go (`image/png`), convert to grayscale, and compute pixel-intensity variance yourself (mean of squared deviations from the mean) — don't rely on external stats libraries. Second, compute the Pearson correlation between frame index and variance using the standard formula (covariance over product of standard deviations) and write the single float to `/home/user/correlation.txt`. Third, implement simple linear regression (closed-form least squares, y = a + bx) from scratch in Go, run manual 5-fold cross-validation by splitting your (index, variance) pairs into 5 folds, training on 4 and evaluating MSE on the held-out fold each time, and average the five MSEs into `/home/user/mse.txt`. Fourth, benchmark 1000 calls to your trained model's predict function using `time.Now()` before/after in a loop, average the elapsed time in microseconds into `/home/user/benchmark.txt`. Finally, start an `net/http` server on `127.0.0.1:9090` with a `/predict` handler that checks the `Authorization: Bearer my-secret-token` header exactly, parses `frame_id` from the query string, and returns JSON matching `{"frame_id": <int>, "predicted_variance": <float>}` — launch it as a background/detached process (e.g. with `&` or `nohup`) so it keeps running after your session ends, and curl it once yourself to confirm the response shape and auth check work before finishing.