Split this into two independent, self-checkable phases rather than mixing them. Phase 1: implement a standard second-order central-difference discretization of $D C'' - kC = 0$ on a uniform grid of N points — build the tridiagonal system with Dirichlet boundary rows `A[0,0]=1,b[0]=1` and `A[-1,-1]=1,b[-1]=0`, interior rows `D/dx^2 * (C[i-1] - 2C[i] + C[i+1]) - k*C[i] = 0`, solve with `numpy.linalg.solve` (or a tridiagonal solver), then compare pointwise against the closed-form $C_{exact}(x)=\sinh(\sqrt{k/D}(1-x))/\sinh(\sqrt{k/D})$ using max absolute error. Loop: start N=5, and while max error $\ge 10^{-3}$, set `N = 2*(N-1)+1` and resolve — this doubles the number of intervals each iteration; verify your loop terminates and record the final N and error. Phase 2: load `/home/user/protein_counts.csv` with pandas, and for each of the four columns run exactly 10,000 bootstrap resamples (`numpy.random.choice(data, size=len(data), replace=True)`), computing the resample mean each time; seed with `numpy.random.seed(42)` once before the whole bootstrap procedure (not per-column, and not per-resample) so results are reproducible, then take the 2.5th/97.5th percentiles of the 10,000 means as the CI bounds. Assemble both phases' results into `/home/user/analysis_report.json` matching the exact schema given, with the seed and loop logic being the two places naive attempts drift from the graded values.