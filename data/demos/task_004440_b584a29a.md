Before calling `json.loads` on any line, run a regex sanitization pass that strips `\u` escapes followed by exactly 4 characters that aren't all valid hex digits (e.g. `\uZZZZ`) — removing the whole malformed escape sequence, not just the backslash, so the surrounding string stays valid JSON. Use `concurrent.futures.ProcessPoolExecutor` (or similar) to sanitize+parse each `.jsonl` file in parallel, one worker per file (or per line-batch), then merge results in the main process. Group records by `server_id`, sort each group by `date`, and compute Jaccard similarity (`len(intersection)/len(union)`) between consecutive days' `packages` sets — the first day for each server gets similarity `1.0` by definition, not computed from a missing previous day. Then compute a 3-day trailing rolling average per server (partial windows of size 1 and 2 for the first two days). Find each server's minimum rolling average, pick the server with the lowest value overall, join against `metadata.csv` on `server_id` to get the owner email, and write the rounded (4 decimal places) result to `/home/user/report.json` in the specified schema.
