First extract the policy text from `/app/policy_scan.png` yourself — run OCR (`tesseract /app/policy_scan.png stdout`, cleaning up any misreads by cross-checking against the fields described in the dataset schema) rather than assuming boilerplate WAF rules; the exact block conditions (which string patterns, which threshold on header length, which cert fields, which cookie attributes) live only in that image and must be read off it precisely, since a wrong threshold or missing rule will cost you accuracy points. Once you have the rules transcribed, encode each as an independent boolean check in `policy_enforcer.py`, iterate `/app/traffic_dataset.jsonl` line by line, and mark a request BLOCK if *any* rule check fails — treat missing/null fields carefully (e.g., skip a rule if the relevant field is null rather than crashing or false-triggering). Do case-insensitive substring matching where the policy specifies it, and be precise about "contains both X and Y" vs "contains X or Y" logic for multi-attribute checks like cookie flags. Write results to `/home/user/decisions.jsonl` as one `{"id":..., "action":...}` per line, preserving every input id, then spot-check a handful of entries by hand against your transcribed rules before finishing.