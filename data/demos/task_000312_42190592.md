Do this in dependency order: passcode, then provisioning, then servers, then tunnel. Transcribe `/app/urgent_comms.wav` with a speech-to-text tool (whisper or similar), then normalize the words into a single uppercase alphanumeric string with no spaces/punctuation, exactly as instructed. Make `provision.py` idempotent by checking-before-creating (`os.makedirs(exist_ok=True)`, explicit `os.chmod`, skip keygen if a key already exists), and configure SSH so `ssh localhost` needs no prompts (append the pubkey to `authorized_keys` with 0600, use `StrictHostKeyChecking=no`/`UserKnownHostsFile=/dev/null`). Build the dual-protocol server with `http.server`/`BaseHTTPRequestHandler` for HTTP and raw `socket`/`socketserver.TCPServer` for the line-based TCP protocol, both logging `[HTTP] <status>` / `[TCP] <status>` on every attempt; run both as background threads/processes. Configure logrotate with `daily`, `rotate 5`, `compress`, `create 0600`. Start the SSH local forward with `ssh -f -N -L 9999:127.0.0.1:8888 localhost` so it persists in the background, and test each endpoint yourself with curl/nc before finishing.