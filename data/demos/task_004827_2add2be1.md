Treat this as three independent fixes plus one analysis script — verify each piece in isolation before wiring them together. (1) In `/app/auth_service/app.py`, change the `app.run(host=...)` call from `0.0.0.0` to `127.0.0.1` so Flask is unreachable except through the proxy. (2) In the same file, find the `jwt.decode(...)` call — it likely disables signature verification or omits an `algorithms` allowlist, which is exactly what lets an `alg: none` forged token through. Pass `algorithms=["HS256"]` explicitly and the real `SECRET_KEY`, and do not accept unsigned tokens. (3) Edit `/app/nginx/nginx.conf` to add a `server` block on 443 with `ssl_certificate`/`ssl_certificate_key` pointing at the provided cert/key pair, proxying to `http://127.0.0.1:5000`; restart via `/app/start.sh` and confirm `curl -k https://127.0.0.1/...` reaches Flask. For the audit script (`/home/user/audit.py`): parse each JSON line of `/app/logs/auth.log`, base64-decode the JWT header segment (before the first `.`) without verifying signature, and flag any token whose header's `alg` is `none` (or equivalent case variants) — cross-reference against which of those the auth log shows as accepted, then decode the payload segment to pull `user_id`. Write matching `user_id,timestamp` rows to `/home/user/compromised_users.csv`; precision/recall on this set is what's graded, so avoid over- or under-matching the none-alg detection logic.
