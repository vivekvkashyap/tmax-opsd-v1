Chain the stages in order since later steps depend on earlier recovered values. First, run OCR against /app/target_info.png (e.g. `tesseract /app/target_info.png -`) to read the 4-digit PIN printed in the image rather than assuming a value. Second, statically analyze /app/token_validator with `strings`, `objdump -d`, or `xxd` to find embedded literal strings revealing the salt and the hashing utility/algorithm it references (look for a distinctive salt string and a hash-tool name in the disassembly/strings output); once you have both the salt and algorithm, compute the Evasion Token yourself as `echo -n "<PIN><salt>" | sha256sum` (concatenation order matters — check how the string is built near where the salt appears) and cross-check the resulting hash length/format against what the binary's strings suggest it expects. Third, create /app/payload.bin with the exact required text and lock it down with `chmod 400 /app/payload.bin`. Fourth, implement delivery.sh as a raw bash TCP server (ncat/socat, not Flask/http.server) on port 8080 that parses the raw HTTP request text for the `X-Evasion-Token:` header line, compares it against your computed token, and manually writes back a well-formed `HTTP/1.1 200 OK`/`403 Forbidden` response including headers and the payload body. Background it, redirect its output to server.log, and test with curl using both a correct and incorrect token before finishing.