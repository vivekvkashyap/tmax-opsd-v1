Do the three phases in strict dependency order. Phase 1: extract frames from `/app/intercepted.mp4` with `ffmpeg -i /app/intercepted.mp4 frame_%03d.png` (or read frames directly with a Python video library), then for each of the 128 frames classify it as black (bit 0) or white (bit 1) by sampling pixel brightness (e.g. mean of a corner pixel or the whole frame) against a threshold — frames are pure black/white so any simple average works. Concatenate the 128 bits in frame order into a bitstring, then pack it into 16 raw bytes (`int(bits, 2).to_bytes(16, 'big')`), which is your AES-128 key — do not hardcode or guess it. Phase 2: decrypt `/app/traffic.log.enc` with that key using AES-128-ECB + PKCS7 unpadding (e.g. `pycryptodome`'s `AES.new(key, AES.MODE_ECB)` then strip PKCS7 padding manually or via `Crypto.Util.Padding.unpad`); inspect the resulting JSON-lines structure to learn the actual field names/nesting for headers, cookies, path. Phase 3: write `/home/user/detector.py` to parse each JSON line and flag malicious requests per the three independent criteria given in the task — remember to base64-decode the `session_data` cookie value before substring-checking it, and URL-decode the path before checking for traversal. Since scoring is F1 against a hidden log, favor implementing the criteria exactly as specified rather than adding extra heuristics, and test against the log you decrypted in Phase 2 first.