In C, read the file line by line with `fgets`/`getline`. First check each line for the literal substring `"status": "failed"` with `strstr` (cheap pre-filter) and skip it; separately compile a `regex.h` extended regex like `"action": "edit"` combined with capturing the numeric `"timestamp": ([0-9]+)` value, using `regexec` with `REG_EXTENDED` and a `regmatch_t` array to pull out the captured substring, then `atol`/`strtol` it. For each valid timestamp compute `bucket_id = timestamp / 3600` and increment a counter array/hashmap keyed by bucket_id (buckets will be contiguous starting from the first seen bucket — track min bucket_id or just accumulate in an array sized generously and iterate in ascending order). Walk buckets in order maintaining a running sum and count of prior buckets; once you have at least 5 prior complete buckets, compute their mean and check if the current bucket's count is strictly greater than 3.0*mean — stop at the first bucket satisfying this and write `ANOMALY_HOUR: <bucket_id*3600>, COUNT: <count>` to the report file, then exit.
