Work through the JWTs in `/home/user/access.log` one at a time: split each `Authorization: Bearer <token>` on `.` into header/payload/signature, Base64url-decode the header, and look for the one whose header decodes to an algorithm of `"none"` — that is the forged token exploiting the classic `alg:none` bypass (an empty or missing signature segment is a strong second signal). Base64url-decode that token's payload segment (remember to pad it correctly) to get the JSON body, and pull out the `encrypted_data` field. Read the `xor_key` string from `/home/user/app_config.json`. Base64-decode `encrypted_data` to raw bytes, then XOR those bytes against the repeating key byte-by-byte (`data[i] ^ key[i % len(key)]`) — a short Python script is the fastest way to do this reliably. The result should be readable ASCII/HTML text. Write that decoded string to `/home/user/incident_report.txt` with no leading/trailing whitespace or trailing newline (use something like `printf '%s' "$decoded" > file` rather than `echo`, which appends a newline).
