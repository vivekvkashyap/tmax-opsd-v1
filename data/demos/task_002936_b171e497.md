Start by recovering the exact rule text from the memo image: run `tesseract /app/compliance_memo.png stdout` (try preprocessing with e.g. `convert` if OCR is noisy) and read the output carefully to get the precise entity types, relation types, depth range, and jurisdiction condition — do not assume or invent the rule, derive it from the OCR text. Translate that rule into SQL: a recursive CTE starting from `entities` rows with `type='PEP'`, recursively following `relations` edges of type in the allowed set up to the max depth from the memo, reaching an entity with `type='Shell'`, and from that Shell node checking for a direct `transfers_to` edge into an entity with the specified jurisdiction value. Before running against real corpora, create indexes on `relations(source)` and `relations(target)` (and any other join columns) inside your script at connection time, since large decoy graphs will make an unindexed recursive join time out. For each `.db` file run the query and print `VIOLATION` if any match exists else `CLEAN`, in the exact `filename: STATUS` format. Test your classifier against small hand-built SQLite databases with and without the pattern (including edge cases like depth-4 chains or wrong jurisdiction) before trusting it against the real corpora.
