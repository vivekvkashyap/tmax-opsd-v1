Two prerequisites before writing SQL: (1) transcribe/listen to `/app/incident_report.wav` to recover the exact `region` value that must be applied as a WHERE filter — do not guess it, extract it from the audio. (2) inspect `/app/backups.db`'s schema (e.g. `sqlite3 backups.db .schema`) to learn the real column/table names for clusters, storage_nodes, and backup_logs, and confirm the join key linking storage_nodes to backup_logs (likely node_id). The "implicit cross join" bug to fix is a query joining nodes and logs without an explicit ON/foreign-key condition, producing a cartesian product with duplicated/inflated rows — replace it with a proper `JOIN ... ON storage_nodes.node_id = backup_logs.node_id`. To get "most recent successful backup per (node, database)" use a window function: `ROW_NUMBER() OVER (PARTITION BY node_name, database_name ORDER BY backup_timestamp DESC)` filtered to `status = 'SUCCESS'`, then keep rn = 1. Apply the region filter from the audio, project exactly the 4 required keys, and sort by node_name then database_name ascending. Since this is checked by fuzz equivalence against a hidden oracle across many random databases with the same schema, make the script take the db path as argv[1] and print JSON with `json.dumps` matching key names/order precisely — test it against several self-generated fixture databases with edge cases (multiple statuses, ties in timestamp, other regions) to confirm structural correctness, not just the happy path.
