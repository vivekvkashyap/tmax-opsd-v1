Build this as a real pipeline, not a one-off script. First use `ffmpeg -i /app/experiment_monitor.mp4 -f rawvideo -pix_fmt gray -` piped into your C program's stdin so C never has to touch the MP4 container itself — only raw grayscale bytes, one 640x360 frame at a time. In C, for each frame scan all width*height bytes and track the maximum pixel value seen so far, only updating on strictly-greater values (never >=) so ties resolve to the first pixel in row-major order — this matters because the target uses a real image with background noise, not a clean binary blob. Write each frame as a fixed 12-byte packed record (`int32 frame_index`, `float32 x`, `float32 y`) with no struct padding — use explicit `fwrite` calls per field rather than `fwrite`ing a struct, since C structs can silently insert padding. For the aggregation program, read the same 12-byte records back, split at the frame count midpoint (N/2), compute per-half means, and implement Welch's t-test (unequal variance: `t = (mean1-mean2) / sqrt(var1/n1 + var2/n2)`) writing the three floats comma-separated to `/home/user/stats.csv`. Validate your telemetry.bin numerically resembles the drawn circle centers before trusting the stats.