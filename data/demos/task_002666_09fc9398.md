Split into video analysis (to know what to look for) and in-memory archive validation. For the video: use `ffmpeg` or `opencv-python` to iterate frames of `/app/test_run.mp4` and detect the single frame whose pixels are pure blue (0,0,255) — e.g. sample a pixel or check the frame's mean/max channel values per-frame with OpenCV (`cv2.VideoCapture` + `read()` in a loop, tracking the 0-indexed frame counter) rather than assuming a position; this gives you the target `Sync-Frame` value at runtime, not a hardcoded constant, since the video is a fixture whose blue-frame index the script must actually discover. For the archive: use Python's `zipfile.ZipFile` and read entries via `namelist()`/`read()` without ever calling `extractall()` or writing to disk — for each entry, reject if `os.path.isabs(name)` or if `os.path.normpath(os.path.join('/root', name))` escapes `'/root'` (checking normalized paths, not just a literal `..` substring, since `foo/../../bar` also escapes but doesn't start with `../`). Require a root-level `build.log`, read it in-memory (`zf.read('build.log').decode()`), and regex/string-match for the exact `[EVENT]\nStatus: SUCCESS\nSync-Frame: <N>` block where `<N>` matches the blue-frame index you detected from the video. Exit 0 only if every check passes, else exit 1.