Work through the four subsystems in dependency order, testing each before moving on. First port migrate_db.py to Python 3 (the giveaway is Python-2-style `print "..."` statements — convert to `print(...)` calls) and run it once against db.sqlite3 to add the new column; verify with `sqlite3 db.sqlite3 ".schema configurations"`. Next fix the Makefile: check whether the recipe line under `solver:` uses spaces instead of a literal tab (Make requires tabs for recipe indentation) and correct that, then look at solver.c for the actual math bug — C's `sin()`/`cos()` expect radians, so if the angle loop variable (in degrees) is passed straight into `sin()` without converting via `angle * M_PI / 180.0`, the physics is wrong; fix the conversion, add `-lm` if needed, `make`, and run `./solver` to get the angle. Use that printed integer to `UPDATE configurations SET max_angle=<angle> WHERE id=1` via sqlite3, send `{"angle": <angle>}` as JSON text over a websocket client (Python's `websockets` library, `asyncio.run`) to `ws://localhost:8080`, and finally write the bare integer to solution.txt.
