Implement this incremental-backup tool as separate, testable steps in C++17. First, parse `manifest_v1.csv` into a map from relative path to old checksum (simple line-by-line split on the comma). Second, recursively walk `/home/user/project_data/` (use `std::filesystem::recursive_directory_iterator`), and for each regular file compute its relative path (relative to the project_data root — this is the exact string that must match manifest entries and appear in the binary archive) and its SHA-256 (shelling out to `sha256sum <file>` via `popen` and parsing the first whitespace-delimited token is simplest and reliable). Third, classify each current file as "changed" if its path is absent from the old manifest or its checksum differs from the old entry — files present only in the old manifest (deleted files) are simply dropped, not included anywhere. Fourth, sort changed files alphabetically by relative path, then write the binary archive: 4-byte magic `ARCH`, then per file a little-endian uint32 path length, the raw (non-null-terminated) path bytes, a little-endian uint64 file size, then the raw file bytes read in binary mode — use explicit fixed-width types and write byte-by-byte or via `memcpy` into a buffer to guarantee endianness rather than relying on struct padding. Finally write `manifest_v2.csv` listing ALL current files (changed and unchanged) with their fresh checksums, sorted alphabetically, matching the exact `filepath,sha256sum` format with no header. Compile with the exact g++ command given and run it, then manually inspect the binary with `xxd`/`od` to sanity check the byte layout before finishing.