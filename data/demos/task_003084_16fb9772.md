Implement this as pure, careful string/byte manipulation in C++ with your own base64 encoder (a standard 64-char alphabet table with padding, implemented byte-by-byte over 3-byte groups) — do not reach for external libs. Read `/home/user/challenge.txt` and strip trailing newline/whitespace. For the token: reverse the challenge string (`std::reverse` on a `std::string` copy), then base64-encode the reversed bytes. For the payload: take the literal string `COMMAND:flag_captured_successfully`, XOR every byte with the integer length of the *original, unreversed* challenge string (compute this length from what you actually read, not a value you assume), then base64-encode the resulting byte array (note: after XOR the bytes may not be printable ASCII, so encode raw bytes, not a C-string — watch for embedded null/8-bit values breaking naive `strlen`-based logic). Concatenate as `<token>.<encoded_payload>` and write to `/home/user/evasion_payload.txt`. Compile with `g++ -o /home/user/crafter /home/user/crafter.cpp` and run it to actually produce the output file, then spot-check your base64 encoder against a known test vector before finishing.