Treat this like securing SSH keys: use `chmod 600` on every `*.list_config` file in `/home/user/mailing_lists/` (loop with `for f in /home/user/mailing_lists/*.list_config; do chmod 600 "$f"; done`) so only the owner can read/write — verify with `stat -c '%a %n'`. Then confirm `monitor.py` is already correctly rejecting non-owner-only files by design; your job is just to fix the permission bits, not the script logic, then start it in the background (`nohup python3 /home/user/monitor.py &` or similar) and give it a moment to write `/home/user/status.json`. For HTTPS, generate a self-signed cert/key pair with `openssl req -x509 -newkey rsa:2048 -keyout /home/user/certs/key.pem -out /home/user/certs/cert.pem -days 365 -nodes -subj '/CN=localhost'`. Write `server.py` using `http.server.HTTPServer` wrapped with `ssl.SSLContext.wrap_socket` (or `context.load_cert_chain` + `wrap_socket`) bound to port 8443, serving the JSON file at the root path. Launch both scripts detached (nohup/disown or `&` with disowning) so they survive your session ending, and verify with `curl -k https://localhost:8443/status.json` and `ps aux | grep -E 'monitor.py|server.py'`.