Run `tesseract /app/weights.png -` (or convert to a higher-contrast image first if OCR is noisy) to recover the three weights W1/W2/W3 and THRESHOLD; sanity-check them (weights should plausibly sum near 1.0) before hardcoding them as constants in your C program. Then write the CSV line parser with `strtok`/manual splitting, being careful with buffer sizes for the SensorName field. Implement the uppercasing loop as a byte-for-byte scan that only touches bytes in `'a'..'z'`, leaving any byte with the high bit set (UTF-8 continuation/lead bytes) untouched — do not use `toupper()` or locale-aware functions, which can mangle multi-byte sequences. Maintain a 3-slot rolling buffer of the last valid values (shift-in on each new valid reading, before moving to the next line) and only fill gaps from the weighted sum once you have 3 valid entries seen so far; otherwise use THRESHOLD. Use `%.2f` for output. Build with `gcc -O2` and test manually with crafted CSV streams (including gap sequences at the very start) before compiling to `/home/user/processor`, since the grader fuzzes against a hidden reference implementation and any off-by-one in when history updates will diverge quickly.