Work through the four sub-tasks independently, then assemble the JSON report last so a mistake in one step doesn't block the others. For integrity: compute `sha256sum` on every file in the directory and diff against `manifest.sha256` (e.g. `sha256sum -c manifest.sha256` will flag the mismatch) — the manifest was generated before a later edit, so the file with a hash mismatch is the tampered one. For cracking: open `users.db` with `sqlite3`, dump `username, password_hash`, then hash each word in `wordlist.txt` with MD5 and match against the stored hashes (a one-line Python loop with `hashlib.md5` is simplest); don't assume the wordlist order matches insertion order. For vulnerabilities: read `server.py` and locate the exact line where a raw SQL string is built via f-string/concatenation and passed to `execute()` (SQLi), and the exact line where user-controlled input is interpolated directly into an HTML/response string without escaping (XSS) — report the literal line numbers, not the function name. For redaction: use a regex anchored to exactly 16 digits in 4-4-4-4 hyphen groups, replace the first three groups with `XXXX` and keep the last group verbatim, write to `messages_redacted.txt`, then hash that output file — don't hash the original. Only after all four are solid, build the final JSON in the exact schema given.
