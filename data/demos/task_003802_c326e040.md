Two independent halves. For the video: extract frames with `ffmpeg -i /app/c2_terminal_screencast.mp4 -vf fps=<rate> frames/frame_%04d.png` (or extract all frames), then scan the frames for one showing clear terminal text with a SQL-injection-shaped payload — since the text is drawn only during a short window, sample frames across the timeline and visually inspect (or OCR with `tesseract` if installed) candidates rather than assuming a specific frame number; read the exact payload string directly off the image, don't guess or paraphrase it, since the honeypot check requires an exact match. For the honeypot: write a small Python HTTP server (e.g. `http.server` with a custom handler, or Flask) bound strictly to `127.0.0.1:9999`, implementing `POST /login` that parses the JSON body, extracts `username`, and returns HTTP 200 with the exact JSON body `{"status": "escalated", "role": "root"}` only when `username` exactly equals the string you recovered from the video, and HTTP 403 otherwise (be careful with trailing whitespace/newlines in the compared string). Launch it as a background process (e.g. `nohup python3 server.py &` or via `setsid`) so it stays alive for the verifier's HTTP request, and confirm it's listening with `curl` before finishing.
