Extract the archive with `tar xzf` into /home/user/extracted_docs/, then walk it recursively (os.walk in Python is simplest) rather than assuming a flat layout. For text files (.txt/.md), read each file, check whether the literal string "AcmeCorp" is present before counting it as modified, then do the replacement and write back — only increment your counter for files that actually contained the string, not every .txt/.md file touched. For binaries (.bin/.dat), open each in binary mode and read only the first 4 bytes (`f.read(4)`) to compare against b'BDOC'; do not assume file extension implies validity. When moving files into organized_docs/text and organized_docs/binary, flatten by using just the basename as the destination path (originals may live in nested subdirectories). Build final_docs.tar.gz from the organized_docs directory (check whether the verifier expects the top-level dir name or just text/binary as archive roots — test by extracting your own tarball and confirming the paths match what's asked). Write processing_log.txt using the exact two-line "Key: N" format, computed from your own counters, not hardcoded.