Two-stage extraction-then-compute task. First, run OCR (e.g. `tesseract`, or a Python OCR/vision library) on `/app/api_spec.png` to recover the actual minimum version string and the scoring formula text — read the OCR output yourself rather than assuming values, since both are needed downstream and must not be hardcoded from guesswork. Second, parse `/app/responses.jsonl` line by line, compare each record's `version` field against the recovered minimum using proper semantic-version comparison (split into major.minor.patch integers, or use a semver library — do not do plain string comparison, and treat pre-release tags like `-alpha` as less than the corresponding release version). For records passing the filter, base64-decode `payload`, parse the JSON to get A/B/C, and evaluate the recovered formula (build a small safe expression evaluator, e.g. substitute values and use Python's `eval` on a sanitized expression, or parse it manually) rather than hardcoding the arithmetic. Average the scores across valid records and write only that float to `/home/user/result.txt`. Sanity-check by manually recomputing one record's score by hand against your script's output.
