Write a Python script that: loads the JSON, iterates `chunks` in list order, and for each does `zlib.decompress(base64.b64decode(chunk))`, concatenating the resulting bytes in order to rebuild the tar archive exactly (do not decompress the concatenation of raw compressed bytes — decompress each chunk individually first, since each chunk was compressed independently). Then open the reconstructed bytes with `tarfile.open(fileobj=io.BytesIO(...))` and iterate members manually rather than calling `extractall()` unguarded — for each member, resolve its target path with `os.path.normpath(os.path.join(dest, member.name))` and verify (e.g. via `os.path.commonpath` or a prefix check) that the resolved absolute path stays strictly inside `/home/user/project/`; skip (do not extract) any member whose name contains `..` traversal or is an absolute path that escapes the target directory. Only extract members that pass this check, track their final extracted paths as you go, then sort them alphabetically and write one per line to `/home/user/extracted_files.txt`. Test explicitly that the malicious paths are excluded and the legitimate files exist with correct content afterward.
