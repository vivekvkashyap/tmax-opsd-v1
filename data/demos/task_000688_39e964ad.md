Handle the three steps in dependency order since later steps rely on earlier ones. Step 1: open `/app/text-sampler-0.5.0/setup.py`, find the impossible pinned dependency (an unavailable exact numpy version) and relax it to a normal installable constraint (e.g. just `numpy`), then `pip install /app/text-sampler-0.5.0` and confirm the `text-sampler` CLI is on PATH. Step 2: write `/home/user/filter_data.sh` as a strict validator — check line 1 equals `SCHEMA_VERSION=1.0` exactly, scan the whole file for non-ASCII/control bytes outside normal whitespace (e.g. `grep -P '[^\x09\x0A\x0D\x20-\x7E]'` or use `file`/`iconv` to detect encoding issues), and reject any line beginning with `[INJECT]`; exit 0 only if all three checks pass, exit 1 otherwise — validate it yourself against both `/home/user/corpora/clean/` and `/home/user/corpora/evil/` before trusting it, since the hidden holdout set will probe edge cases in each rule independently. Step 3: write `/home/user/pipeline.sh` that iterates every file in `<input_dir>`, runs it through your filter script, and for files that pass, invokes `text-sampler --file <file> --n 5 > <output_dir>/<filename>.sample`, skipping failures silently. Make both scripts executable and dry-run the pipeline against the example corpora to confirm correct behavior.
