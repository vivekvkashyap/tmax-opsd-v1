Set up the Go module first (`go mod init report` then `go get github.com/mattn/go-sqlite3`) in `/home/user/`, since the sqlite3 driver requires cgo — make sure a C compiler is available or the build will fail silently at `go build`. In `generate_report.go`, parse `-region` and `-limit` with the `flag` package, open the DB via `database/sql` with the `sqlite3` driver, and write ONE query rather than doing joins/ranking in Go: join `customers` to a per-customer `SUM(amount)` subquery (or aggregate directly) filtered by `region = ?` (bound parameter, not string-interpolated, to avoid injection), wrap it with `RANK() OVER (ORDER BY total_spend DESC)` for rank and `SUM(total_spend) OVER (ORDER BY total_spend DESC ROWS UNBOUNDED PRECEDING)` for the running total, then apply `LIMIT ?` (bound) at the outer level using the `-limit` flag value. Iterate the `*sql.Rows` result, write each row plus the exact header `name,total_spend,rank,running_total` to `/home/user/report.csv` using `encoding/csv`. Test by running the program with a chosen region/limit and inspecting the CSV before considering the task done.
