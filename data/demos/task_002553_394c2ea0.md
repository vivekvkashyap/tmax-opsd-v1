Tackle this task stage by stage, verifying each before moving on. First decode the DTMF PIN from `/app/telemetry.wav`: either write a small Python script using `scipy`/`numpy` to do a Goertzel or FFT-based analysis of short audio windows against the standard DTMF frequency pairs (697/770/852/941 Hz rows x 1209/1336/1477 Hz columns) and map detected tone pairs to digits, or install and run `multimon-ng -a DTMF -t wav /app/telemetry.wav`; confirm the decoded digit sequence looks like a plausible PIN before hardcoding it anywhere. Second, build the directory tree and symlink with `ln -s /home/user/telemetry_app/releases/v1 /home/user/telemetry_app/current` (relative vs absolute target — verify with `readlink`), and write status.json with the exact required content. Third, write server.py using `http.server` for static files on 9080 and a raw `socket`/`threading` TCP listener on 9081 comparing the stripped received line against the extracted PIN; launch it with `nohup python3 server.py &` and confirm both ports are listening (`ss -ltnp`) before proceeding. Fourth, write the expect script referencing the extracted PIN and test it manually (`expect health_check.exp; echo $?`). Finally install the crontab with `crontab -l | { cat; echo "*/5 * * * * /home/user/telemetry_app/health_check.exp >> /home/user/telemetry_app/logs/health.log 2>&1"; } | crontab -`.
