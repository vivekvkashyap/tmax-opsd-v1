Work this as a debugging pipeline with distinct, verifiable stages rather than jumping straight to a code fix. Bring the stack up with `start_all.sh` and confirm baseline behavior (`curl localhost:9000/`, `echo STATUS | nc localhost 9002`). Reproduce the leak by watching proxy fd counts (`ls /proc/<pid>/fd | wc -l` or `lsof -p <pid>`) before/after a request that aborts mid-connection (e.g. `curl -m 1`, or raw `nc` that connects then disconnects); iterate until one command reliably leaks exactly one fd, and save it to `minimal_repro.sh`. For git, run `git bisect start`, mark an early good commit and HEAD as bad, and at each step rebuild and rerun your fd-count check as the pass/fail oracle; write the resulting hash to `bad_commit.txt`. Fix the C++ by ensuring every error/disconnect branch in the connection loop (recv() returning 0 or negative) explicitly closes the socket, not just the success path. Rebuild with `make`, restart, and re-run the leak check to confirm fd count stays flat under repeated aborted connections.
