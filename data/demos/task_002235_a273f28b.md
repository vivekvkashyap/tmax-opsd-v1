Treat this as three independent, separately-verifiable stages and validate each before moving to the next. (1) Video/OCR: use `ffmpeg -vf select='between(n,120,150)'` (or similar) to extract that frame range to images, then run OCR (tesseract or an OpenCV preprocessing pass — threshold/upscale for a flashing overlay that may be low-contrast or brief) to recover the base64 ciphertext text; don't guess it, actually extract and print it to confirm it looks like valid base64 before proceeding. (2) Decryption: complete `/home/user/decryptor.py` using `cryptography.hazmat` with AES-256-CBC, the key/IV already given in that file's comments, base64-decoding first, then decrypting and stripping PKCS7 padding — print the plaintext token and sanity-check it looks like a real token string, since a padding error usually means key/IV/mode mismatch. (3) Server: build a stdlib `http.server` (or Flask) app bound exactly to `127.0.0.1:8080`, checking `Authorization: Bearer <token>` against the recovered plaintext token (401 on mismatch/missing), parsing the JSON body, replacing `victim_email` with the literal string `[REDACTED]`, computing `hashlib.sha256(xss_payload.encode()).hexdigest()`, and returning the exact JSON shape specified. Keep the server running in foreground/background so the verifier can hit it repeatedly, and log activity to `/home/user/server.log`.