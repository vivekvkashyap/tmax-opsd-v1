Recover the source first: `strings /home/user/disk_image.bin` (or grep with `-a` for binary-safe text search) to locate the block between the literal markers `// AUTH_MODULE_START` and `// AUTH_MODULE_END`, then extract exactly that span (e.g. `sed -n`/`awk` between the two marker lines, or a small Python script scanning byte offsets) into `/home/user/auth.cpp` — verify it looks like valid, complete C++ before moving on. Next, determine the exact symbol `auth_service` needs by inspecting the binary's undefined/required symbols, e.g. `nm -D /home/user/auth_service | grep verify_token` or `readelf -s`/`objdump -T`, and demangle with `c++filt` to see the expected namespace and signature (the recovered source likely uses a different, wrong namespace than what the binary actually calls) — edit `auth.cpp` so its namespace/function signature matches exactly, then compile as a shared object: `g++ -shared -fPIC -o /home/user/libauth.so auth.cpp`. Confirm linkage by checking the compiled `.so`'s exported symbol matches the mangled name the binary expects. For `reproduce.sh`, loop running `LD_LIBRARY_PATH=/home/user /home/user/auth_service`, check `$?` each iteration, and once it's non-zero append `CRASHED` to `/home/user/status.log` and exit the loop/script — run it yourself to confirm it actually catches a crash (the bug is probabilistic, so the loop must retry enough times).