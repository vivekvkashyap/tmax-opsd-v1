Read `malware.c` closely to extract the exact LCG recurrence (multiplier, increment, mask) and how each output byte is derived from the evolving state — you need to reimplement this exact generator in your decrypt tool, not just approximate it. Since the seed space is only 16 bits (0-65535), write `decrypt.c` to brute-force every candidate seed: for each seed, run the LCG forward and XOR-decrypt just enough leading bytes of `exfil.enc` to compare against the known plaintext prefix `CUSTOMER_DATA_START`; when a seed produces a match, use that same seed to decrypt the entire file and stop. This known-plaintext + brute-force approach is what makes a weak 16-bit stream cipher breakable. After decrypting, verify integrity by computing SHA256 of your recovered file and diffing it against the hash in `checksum.sha256` — do not proceed to redaction until this matches, since a subtly wrong seed can still produce plausible-looking early bytes. For redaction, scan the recovered text with a regex matching `DDDD-DDDD-DDDD-DDDD` and replace the first three groups with `XXXX-XXXX-XXXX-`, preserving the last four digits and all surrounding text exactly, then write the result to the redacted output file.
