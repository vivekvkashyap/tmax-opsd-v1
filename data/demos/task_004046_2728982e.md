Diagnose both bugs in the vendored package before rewriting anything. For `compute_norm.sh`: notice it forks background subshells that each append a partial sum to a shared file, then `wait`s and sums the file — the nondeterminism comes from concurrent appends interleaving in unpredictable order. Fix it by making the chunk sums accumulate in a fixed, deterministic order: either run the chunks sequentially, or have each background job write its partial sum to its own uniquely-named temp file (e.g. indexed by loop iteration) and then sum those files in a fixed numeric order afterward — order of *summation*, not just avoiding a race, is what makes floating point reproducible. For `serve.sh`: read through the `nc`-based request loop carefully; a raw TCP listener that pipes to a script must read the full HTTP request (or at least request line + headers) before deciding on a response, and must emit a well-formed `HTTP/1.1 200 OK` status line, headers (`Content-Type: application/json`), a blank line, then the JSON body in the correct order — check header parsing isn't dropping the `X-Auth` value due to reading only one line. Test manually with `curl -H "X-Auth: secret-token-123" http://127.0.0.1:8080/features` and without the header (expect a 403/drop) before leaving the service running in the background. Compute the scale factor and preview from `/home/user/raw_input.h5`'s `/data/batch_1` dataset using the now-deterministic summation so results are stable across repeated runs.
