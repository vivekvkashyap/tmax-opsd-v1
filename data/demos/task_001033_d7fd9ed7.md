Treat `/app/legacy_indexer` as a black-box oracle for reverse engineering, since it's stripped — pipe a range of crafted stdin lines through it (empty string, pure alphanumerics, strings with punctuation/slashes/dots in various positions, leading/trailing symbols, consecutive symbols, mixed-case) and diff the outputs character-by-character to infer the transformation rule: check whether case is normalized, whether certain character classes are filtered or substituted, how runs of "special" characters are handled versus single occurrences, whether leading/trailing artifacts get trimmed, and whether there's a constant wrapper (prefix/suffix) around the transformed core. Build up your hypothesis incrementally — start with the simplest inputs and add complexity only once each simpler case matches exactly. Once your hypothesis reproduces the oracle's output on every test line you throw at it, implement it in `/home/user/indexer.cpp` using standard C++ streaming I/O (`std::getline` in a loop with per-character classification via `<cctype>`), compile to `/home/user/indexer`, `chmod +x` it, and keep testing against the oracle on adversarial edge cases (empty lines, all-symbol lines, single-char lines) before considering it done, since the fuzz equivalence check requires byte-exact output on 1000 random lines.
