Work through the chain sequentially, verifying each step's output before moving to the next. Run `tesseract /app/master_pass.png stdout` (try `--psm 7` or similar single-line mode if default OCR is noisy) and inspect the raw text carefully — trim whitespace/newlines since OCR often appends trailing characters that will silently break the openssl passphrase. Use the trimmed extracted string as `-pass pass:<extracted_password>` in `openssl enc -d -aes-256-cbc -pbkdf2 -in /app/payloads.tar.gz.enc -out /tmp/payloads.tar.gz`, then `tar -xzf` it into `/home/user/payloads/`; if decryption fails with a bad magic number error, that's a signal the OCR text is wrong and needs re-extraction/cleanup. For the Go validator, don't try to build a general security scanner — write targeted checks against the concrete classes described: SQL injection markers (e.g. `' OR`, `--`, quote+operator patterns), HTML/script tags (`<script`, `<`/`>` with tag-like content), and shell/command metacharacters (`;`, `|`, `` ` ``, `$(`). Read the whole file, run each detector, and exit 1 on any match, exit 0 otherwise. Test your compiled `/home/user/validator` binary directly against the `clean/` and `evil/` files you extracted before considering it done.