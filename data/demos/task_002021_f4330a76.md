Split this into three pieces and verify each before wiring them together. (1) Write `/home/user/process_math.sh` to normalize `equation_text`: translate full-width Unicode digits to ASCII and the Unicode multiplication sign to `*` (e.g. via `sed`/awk `gsub` mapping each codepoint), and strip whitespace — test each rule against the actual UTF-8 bytes, since naive `tr` byte-ranges mishandle multi-byte characters. Hash each normalized string with `sha256sum`, dedupe by keeping only the first occurrence per hash, and emit `<hash>\t<normalized>` to `unique_equations.tsv`. (2) In `/app/bashttpd-0.1/bashttpd`, find where the request URI is sanitized — a common bug strips `?`, `=`, `-` via a restrictive `tr -dc` charset; widen or drop that filter so query strings pass through. (3) Write `/home/user/server.sh` as the handler: parse the `hash` query param, look it up in the TSV, and return a proper `HTTP/1.1 200`/`404` with the equation as body. Start it on `127.0.0.1:8080` in the background and test with `curl` yourself.