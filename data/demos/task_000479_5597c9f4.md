Build this migration script incrementally, verifying each artifact before moving to the next. Parse `db.json` with the standard `json` module, `server.ini` with `configparser` (note: configparser returns a nested section->key structure and all values as strings — match that shape under the `server` root key), and `metrics.xml` with `xmltodict` or `xml.etree` (decide how nested tags become dict values, e.g. plain text content for leaf elements). Assemble `{"db": ..., "server": ..., "metrics": ...}` and dump with `yaml.safe_dump` to `/home/user/backup/consolidated.yaml`, creating the `/home/user/backup/` directory first. Compute SHA-256 of each of the three original files plus the just-written YAML using `hashlib.sha256(open(f,'rb').read()).hexdigest()` — compute the YAML's hash only after it's written to disk, not before, since the manifest must match the actual on-disk bytes. Write manifest lines as `<filename> <hexdigest>`, sorted alphabetically by filename. Finally, build the tarball with Python's `tarfile` module (mode `w:gz`) adding both the `app_configs` and `backup` directories with their relative arcnames so extraction reproduces the directory structure. Run the script and confirm `/home/user/config_backup.tar.gz` exists and extracts cleanly.