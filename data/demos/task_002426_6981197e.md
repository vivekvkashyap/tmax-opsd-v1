Write `/home/user/etl_dedup.py` as a clear multi-stage pipeline. First, ingest: walk `/home/user/raw_logs/`, parsing both `.csv` (via `csv.DictReader`) and `.json` (list of dict objects) files into a common in-memory list of records. Second, normalize each record: cast `id` to int; for `time`, detect whether it's already a numeric epoch or an ISO 8601 string (try `int()` first, fall back to `datetime.fromisoformat`/`strptime` handling the trailing `Z`, then convert to epoch seconds via UTC); for `text`, lowercase it and strip everything except `[a-z0-9 ]` with a regex (`re.sub(r'[^a-z0-9 ]', '', text)`), then collapse repeated spaces with another regex and `.strip()`. Third, deduplicate: use a dict keyed by `(id, time)`, keeping only the first (or last) occurrence encountered. Fourth, sort remaining records by `(time, id)` ascending, and write out via `pandas.DataFrame` with explicit dtypes (`id`/`time` as int64, `text` as string) to `/home/user/clean_logs.parquet` using `pyarrow`. Run the script and spot-check the resulting parquet with `pd.read_parquet` to confirm row count, column names/order, and a couple of normalized values before finishing.