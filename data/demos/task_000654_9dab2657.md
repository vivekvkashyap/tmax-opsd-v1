Two independent deliverables — fix the file first, it's simple: open `/home/user/polybuild/src/app.py`, collect its top-level `import X` / `from X import Y` statements, and reorder them by module name in strict ascending alphabetical order (keep the rest of the file's logic unchanged). For the server, use only `http.server` (subclass `BaseHTTPRequestHandler`, run via `HTTPServer` on `('127.0.0.1', 8080)`) and start it in the background. In `do_GET`, parse the path/query with `urllib.parse.urlparse` and `parse_qs` to get `target` and `crc`; base64-decode `target` to raw bytes, then decode those bytes as `iso-8859-1` to get the file path string — but compute the CRC check on the *raw decoded bytes* (before the iso-8859-1 string decode) using `zlib.crc32`, and compare against the integer `crc` param, returning 400 on mismatch. On match, wrap the file-read-and-parse step with `tracemalloc.start()` / `tracemalloc.get_traced_memory()` / `tracemalloc.stop()` to get peak bytes, parse the file with `ast.parse` and walk top-level `Import`/`ImportFrom` nodes (module name is `alias.name` or `node.module`) to check they're in strictly ascending order, returning 406 if not, else 200 with the exact JSON schema `{"status": "pass", "peak_memory_bytes": <int>}`. Test both the pass and 400 cases against your own fixed `app.py` before finishing.