Write process.py so each CSV file is handled by one worker function, then dispatch across files using `concurrent.futures.ProcessPoolExecutor` (real OS processes, not `ThreadPoolExecutor`) mapping the worker over the list of file paths in `/home/user/sensor_data/`, and merge each worker's returned (sensor_id, feature-dict) result into the final summary dict in the parent process. Inside the per-file worker: load with pandas (treating empty strings/'NaN' as NaN via `na_values`), apply `temperature.interpolate(method='linear')` first, then `.bfill()` then `.ffill()` to patch any still-missing edges (get this order right — interpolate, then bfill, then ffill), and fill `vibration` NaNs with `vibration.median()` computed per-file after interpolation isn't needed there (median fill only, no interpolation) — compute the median before filling so NaNs don't skew it (pandas `.median()` already ignores NaN by default, which is correct). Then compute `max_temperature` (post-imputation max), `mean_vibration` (post-imputation mean, rounded to 2 dp), and the mean temperature threshold (`mean_temp + 2.0`) to count `temp_spikes` as rows strictly greater than that threshold. Write the merged dict to summary.json with `json.dump`, and verify by re-deriving one file's numbers by hand from the CSV before trusting the pipeline.