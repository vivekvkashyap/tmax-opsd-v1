Discover the dependency graph empirically rather than guessing: run `ldd /home/user/deploy/math_engine` and `readelf -d <each .so>` (looking at `NEEDED` entries) to see which local libraries each binary directly links against, then build the adjacency list in Python by recursively following only entries whose filename exists inside `/home/user/deploy/` (filter out libc, ld-linux, etc. by checking the file exists locally rather than by name-matching a blocklist). Write this to `deps.json` before or after patching — either is fine as long as it reflects real `NEEDED` entries. For the ABI fix, write `/home/user/deploy/magic.s` implementing `calculate_magic_constant` as a globally-visible symbol (`.global calculate_magic_constant`) that just does `mov eax, 42` then `ret`, per the System V ABI's return-in-eax convention; assemble and link it into a shared object with `gcc -shared -fPIC -o libmagic.so magic.s` (or `as`+`ld`). Identify which intermediate library actually calls the old symbol by grepping `readelf -d`/`nm -D` output for `NEEDED liblegacy.so`, then use `patchelf --replace-needed liblegacy.so libmagic.so <that_lib>.so` to retarget only that dependency. Finally write `run_deployment.sh` exporting `LD_LIBRARY_PATH=$(pwd)` and running `./math_engine > result.txt`, and confirm the printed result matches the expected arithmetic chain before declaring success.
