First recover the spec from the image: install OCR tooling (`sudo apt-get install tesseract-ocr`) and run `tesseract /app/memo.png stdout`, then read off the exact output prefix string, the list of exactly four tracked key names, and the special clamping rule for one numeric key — use these values verbatim rather than assuming plausible ones. In the C program, process stdin line by line, split on the first `:` allowing surrounding whitespace on both key and value; lowercase the key before comparing against the memo's tracked-key list (case-insensitive matching, but store/output lowercase). Implement a proper URL-decoder (convert `%XX` hex escapes to the corresponding byte, and treat unrecognized `%` sequences literally) applied to the value before storing it. Maintain a small fixed-size state table (one slot per tracked key) that gets overwritten on each repeat occurrence so only the last value survives; apply the memo's special validation/clamp (e.g. negative numeric values clamp to 0) after decoding. At EOF, print only keys actually seen, sorted alphabetically, each as `PREFIX-key=value`. Compile to `/home/user/config_tracker` and test it against crafted streams covering mixed-case keys, encoded chars, duplicates, and negative values before trusting it — the verifier fuzzes heavily against a hidden oracle.