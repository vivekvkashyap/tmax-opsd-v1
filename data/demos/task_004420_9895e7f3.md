Treat this as three independent repairs plus a deploy step — verify each in isolation. For the memory dump, don't eyeball the binary; scan it programmatically (e.g. `strings /app/memdump.raw | grep -E 'MEM_SEC_[A-Za-z0-9]{6}'` or a Python regex over the raw bytes) to reliably locate the 14-char token regardless of its offset, and write exactly that token to `/home/user/build_token.txt`. For the convergence bug, read `processor.py`'s loop carefully — an off-by-one boundary (e.g. `<=` where it should be `<`, or vice versa) causes an out-of-bounds index into the matrix; run the existing tests first to see the exact failure/traceback before editing, then fix only the comparison operator/bound, not the algorithm's logic. For the OCR fixture, run `tesseract /app/reference.png stdout` (or via pytesseract) to recover the embedded string programmatically, then substitute that recovered string into the assertion in `test_processor.py` — don't guess the text. Run `pytest /home/user/build/test_processor.py` to confirm both fixes pass before touching deploy. Finally start `/home/user/build/deploy.py` in the background, and confirm `GET http://0.0.0.0:8080/health` (or 127.0.0.1) returns the JSON with your extracted token, matching the format `{"status": "running", "token": "<extracted_memory_token>"}`.