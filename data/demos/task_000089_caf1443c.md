Work in two clearly separated phases. Phase 1 — fix the vendored dependency: `grep` for `#include` lines in `/app/cJSON-1.7.15/cJSON.c` to spot the misspelled header (compile once with `gcc -c cJSON.c` to see the exact error if unsure), correct the typo, then build the static lib with `gcc -c cJSON.c -o cJSON.o && ar rcs libcjson.a cJSON.o`. Phase 2 — write `analyzer.c` implementing the spec literally and in order: read one line via `fgets`, strip the trailing newline before any comparisons, find the first `|` with `strchr` (not `strstr`, since payload JSON may itself contain `|`-like bytes — actually just be careful to search from the start once), and handle the "no pipe" case first. Parse header JSON with cJSON, check for an `"alg"` key that is a string exactly equal to `"none"` (use `cJSON_IsString` + `strcmp`, not a loose truthy check) before touching the payload. Parse payload JSON, require `"user"` to be a string and `"pin_hash"` to be a number (integer), rejecting missing/wrong-typed fields with exit code 2. Brute-force the PIN by looping p from 0 to 9999 and testing `(p ^ 0x5A5A) == pin_hash`, printing zero-padded `%04d`. Match the oracle's exact stdout wording and exit codes for every branch — since correctness is judged by fuzz equivalence against a hidden oracle, test your binary against varied malformed/edge-case inputs (empty string, no pipe, bad alg, missing fields, huge random bytes) yourself before considering it done. Compile linking `libcjson.a` statically: `gcc analyzer.c libcjson.a -I/app/cJSON-1.7.15 -o /home/user/analyzer`.