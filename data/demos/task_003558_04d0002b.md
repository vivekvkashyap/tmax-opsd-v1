Fix each of the three subsystems independently and verify each in isolation before moving to the next. (1) Nginx: inspect `/home/user/nginx/nginx.conf`'s `location /api/metrics` block — it's missing (or has an incorrect) `proxy_pass` directive; add `proxy_pass http://127.0.0.1:8081;` inside it, restart nginx, and confirm with `curl http://localhost:8080/api/metrics` returns 200. (2) Cron/backup: cron jobs run with a minimal environment and the script's cwd is not what you'd expect interactively — check `/home/user/scripts/backup.sh` for relative paths (e.g. `cp data/metrics.db metrics.db.bak` without a `cd`); rewrite it using absolute paths for both source and destination so it works regardless of cron's working directory, then wait for a cron tick (or manually confirm the crontab entry) and check the backup file lands at the exact target path. (3) Normalizer: rather than guessing the output format, black-box test `/app/oracle_normalizer` with a wide range of hand-crafted inputs (valid, missing fields, malformed floats, garbage strings) piped via stdin, diff its stdout against your hypothesis, and only write `/home/user/normalizer.py` once you've confirmed the parsing rules (timestamp/metric/value/tags regex, and the exact error-string fallback) match the oracle's behavior on edge cases, not just the happy path.
