Treat this as three independent subsystems to fix/build in sequence. (1) Routing: edit nginx.conf to add a `location /api/ { proxy_pass http://127.0.0.1:5000; }` block (or equivalent) inside the server block listening on 8080, then restart nginx with the local config file (`nginx -c /home/user/nginx/nginx.conf -s reload` or kill+relaunch); also change app.py's DB_PATH constant from prod.db to backup.db — grep for the literal path string to find it. (2) Export: inspect the `dependencies` table schema first (`sqlite3 backup.db ".schema dependencies"`) to learn actual column names, then write a recursive CTE (`WITH RECURSIVE tree AS (SELECT ... WHERE parent_name = 'root_node' UNION ALL SELECT ... JOIN tree ...)`) that walks the DAG from root_node outward, and dump parent/child pairs to exported_graph.json as a list of {"parent":..., "child":...} dicts. (3) Cycle detector: implement standard DFS with a recursion-stack/visiting-set per graph (white/gray/black coloring or a `visiting` set that's removed on backtrack) — a plain "visited" set alone will mis-detect DAGs with shared ancestors as cyclic, so distinguish "currently on this path" from "fully explored". Print `<filename>: CLEAN` or `<filename>: EVIL` per file processed.