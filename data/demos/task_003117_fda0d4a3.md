This is a deterministic pandas pipeline with several strict, order-sensitive requirements — work through each requirement precisely rather than approximating.

1. Load `/home/user/transactions.csv` and filter rows using all three validity rules simultaneously (logical AND): `amount >= 0`, `date <= '2023-12-31'` (parse as dates, don't string-compare naively unless format guarantees lexicographic order works, which it does here since format is YYYY-MM-DD — but be careful with malformed lengths), and `ssn` matching exactly 9 digits (e.g. a regex `^\d{9}$`, rejecting shorter/longer or non-numeric SSNs).
2. For the rolling average: group by `user_id`, sort each group by `date` then `transaction_id`, and compute a rolling window of size 3 with `min_periods=1` over `amount` (pandas `.rolling(window=3, min_periods=1).mean()`), then round to exactly 2 decimals.
3. Mask SSN by replacing the first 5 characters with `X`, keeping the last 4 digits, and rename the column to `ssn_masked`.
4. Assemble the final frame with columns in the exact order `transaction_id, user_id, ssn_masked, date, amount, rolling_avg_amount`, sort by `user_id`, then `date`, then `transaction_id`, and write to `/home/user/processed_transactions.csv` with the header, matching values exactly (including float formatting) since verification does an exact frame comparison.
