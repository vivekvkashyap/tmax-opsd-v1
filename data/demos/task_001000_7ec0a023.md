First recover the exact query template text from the image: run `tesseract /app/cypher_template.png stdout` (try `--psm 6` if line breaks get mangled) and transcribe the MATCH/SET/RETURN structure precisely, including punctuation, quoting style, and variable placement — this template's exact wording is what your C program must reproduce byte-for-byte, so don't paraphrase it. In query_builder.c, read stdin line by line with fgets, parse the 4 CSV fields with sscanf (comma-delimited, no library), skip rows where FromAccount equals ToAccount, and determine ordering with `strcmp(from, to) < 0` to decide which account becomes n1 (lexicographically smaller) vs n2 — critically, track which alias (n1 or n2) corresponds to FromAccount vs ToAccount so the SET clause's deduction/addition target the correct alias, not just the correct id string. Print the MATCH line, SET line, and RETURN line exactly per the extracted template with printf, one query block per valid CSV row, with no extra blank lines or trailing whitespace beyond what the template dictates. Compile with plain gcc and standard libc only, then self-test with edge cases (identical accounts, ties in ordering, various amounts) to sanity-check exact output formatting before trusting the fuzz-equivalence check.