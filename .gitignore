# Python
__pycache__/
*.py[cod]
.python-version
.pytest_cache/
*.egg-info/

# Virtual env / build artifacts
.venv/
dist/

# Prime Intellect local state (contains owner metadata)
.prime/

# Run outputs (local absolute paths + full task traces)
outputs/

# Hint-generation working files (batch slices, regen lists)
.hintgen/

# Large / derivable upstream data (296MB; regenerated by build.py).
# NOTE: data/demos/ IS committed; data/tasks.parquet is NOT (see bottom).
data/raw/

# Env / secrets
.env
*.env

# Not committed: build.py regenerates it; contains synthetic secret-shaped
# dataset strings that trip GitHub secret-scanning. File is kept locally.
data/tasks.parquet

# Derived from data/demos/*.md by scripts/hintgen/assemble_demos.py
data/demos.parquet
data/hinted_task_ids.txt

# Local validation-run findings/runbook (kept locally, not pushed)
first_test/
